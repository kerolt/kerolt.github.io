<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Kubernetes on Kerolt's Note</title><link>https://kerolt.github.io/categories/kubernetes/</link><description>Recent content in Kubernetes on Kerolt's Note</description><generator>Hugo -- gohugo.io</generator><language>zh-cn</language><lastBuildDate>Sun, 02 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://kerolt.github.io/categories/kubernetes/index.xml" rel="self" type="application/rss+xml"/><item><title>ConfigMap、Secret 与安全配置注入</title><link>https://kerolt.github.io/p/configmapsecret-%E4%B8%8E%E5%AE%89%E5%85%A8%E9%85%8D%E7%BD%AE%E6%B3%A8%E5%85%A5/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/configmapsecret-%E4%B8%8E%E5%AE%89%E5%85%A8%E9%85%8D%E7%BD%AE%E6%B3%A8%E5%85%A5/</guid><description>&lt;h2 id="配置和秘密看起来都像键值对但待遇必须不同"&gt;配置和秘密看起来都像键值对，但待遇必须不同
&lt;/h2&gt;&lt;p&gt;Kubernetes 用 ConfigMap 保存普通配置，用 Secret 保存敏感数据。二者都能以环境变量或文件挂入 Pod，但安全语义完全不同。&lt;/p&gt;
&lt;p&gt;本项目遵循一条清晰边界：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;可公开、可进 Git 的平台参数与脚本 → ConfigMap
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;密码、API key、代理订阅与节点凭据 → Secret
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;不要因为 Secret 的 YAML 里常出现 base64，就把 base64 当成加密。base64 只是编码，任何拿到值的人都能还原。&lt;/p&gt;
&lt;h2 id="项目中有三类配置来源"&gt;项目中有三类配置来源
&lt;/h2&gt;&lt;h3 id="operator-configuration"&gt;Operator Configuration
&lt;/h3&gt;&lt;p&gt;位于 &lt;code&gt;subpool-system&lt;/code&gt; 的 ConfigMap，保存所有 Pool 共享的非敏感平台参数：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;baseDomain&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;Shared Envoy Gateway 的位置和 selector；&lt;/li&gt;
&lt;li&gt;Shared Mihomo Service、端口和 selector；&lt;/li&gt;
&lt;li&gt;Namespace 附加 labels；&lt;/li&gt;
&lt;li&gt;StorageClass；&lt;/li&gt;
&lt;li&gt;单 Pool 最大 CPU、内存和存储包络。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它不保存某个 Pool 的镜像和资源申请，也不保存代理节点和任何密码。&lt;/p&gt;
&lt;h3 id="pool-bootstrap-configmap"&gt;Pool bootstrap ConfigMap
&lt;/h3&gt;&lt;p&gt;Operator 为每个 Pool 生成一个 ConfigMap，其中包含 &lt;code&gt;initialize.sh&lt;/code&gt;。脚本负责在 PVC 第一次使用时创建 CPA 配置。&lt;/p&gt;
&lt;p&gt;脚本本身没有秘密，可以作为受管资源和代码进行审计。执行时需要的秘密从 Secret 文件读取。&lt;/p&gt;
&lt;h3 id="secret"&gt;Secret
&lt;/h3&gt;&lt;p&gt;项目中有两类 Secret：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每 Pool 的 &lt;code&gt;subpool-credentials&lt;/code&gt;：由 Operator 使用 &lt;code&gt;crypto/rand&lt;/code&gt; 首次生成；&lt;/li&gt;
&lt;li&gt;Shared Mihomo 的 &lt;code&gt;mihomo-gateway-providers&lt;/code&gt;：由外部安全流程提供，包含 &lt;code&gt;proxies.yaml&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;前者属于单个 SubPool 生命周期，后者是共享基础设施，不能由某个 SubPool 删除。&lt;/p&gt;
&lt;h2 id="为什么-operator-configuration-使用-immutable-configmap"&gt;为什么 Operator Configuration 使用 immutable ConfigMap
&lt;/h2&gt;&lt;p&gt;配置声明：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;immutable&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="kc"&gt;true&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;不可变配置的优点是：正在运行的 Operator 不会在没有发布动作的情况下突然改变平台行为。&lt;/p&gt;
&lt;p&gt;例如 StorageClass、Gateway selector 或资源上限若被原地修改，可能导致不同时间创建的 Pool 使用不同基线，也可能让控制器在运行中产生难以追踪的漂移。&lt;/p&gt;
&lt;p&gt;正确的变更流程是：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;创建新的版本化 ConfigMap，例如 &lt;code&gt;subpool-operator-config-v3&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;修改 Deployment 的 &lt;code&gt;--operator-config-name&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;触发并观察受控滚动更新；&lt;/li&gt;
&lt;li&gt;验证新 Operator 使用新配置；&lt;/li&gt;
&lt;li&gt;在确认无引用后，再按变更流程处理旧版本。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这类似发布应用版本：配置也应该有身份、有审计、有回滚路径。&lt;/p&gt;
&lt;h2 id="secret-以文件方式投影"&gt;Secret 以文件方式投影
&lt;/h2&gt;&lt;p&gt;runtime Pod 把每 Pool Secret 挂到：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;/run/secrets/cpa_client_key
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;/run/secrets/cpa_management_key
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;/run/secrets/cpamp_admin_key
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Volume 的 &lt;code&gt;items&lt;/code&gt; 只列出需要的 key，而不是无差别暴露 Secret 全部内容。这叫最小数据暴露。&lt;/p&gt;
&lt;p&gt;初始化脚本读取 CPA 所需 key；cpa-manager 则通过文件路径环境变量读取管理 key，例如：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;CPA_MANAGER_ADMIN_KEY_FILE=/run/secrets/cpamp_admin_key
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;CPA_MANAGEMENT_KEY_FILE=/run/secrets/cpa_management_key
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这里环境变量里只有文件路径，没有 secret value。&lt;/p&gt;
&lt;h2 id="为什么不把-secret-value-直接写进环境变量"&gt;为什么不把 Secret value 直接写进环境变量
&lt;/h2&gt;&lt;p&gt;环境变量使用简单，但敏感值更容易出现在：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;进程诊断信息；&lt;/li&gt;
&lt;li&gt;错误日志；&lt;/li&gt;
&lt;li&gt;调试转储；&lt;/li&gt;
&lt;li&gt;应用打印全部环境变量的输出；&lt;/li&gt;
&lt;li&gt;某些监控或支持工具中。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;文件挂载也不是绝对安全，但更容易控制路径、权限和读取时机。应用若支持 &lt;code&gt;*_FILE&lt;/code&gt; 形式，本项目优先使用它。&lt;/p&gt;
&lt;h2 id="secret-生命周期与所有权校验"&gt;Secret 生命周期与所有权校验
&lt;/h2&gt;&lt;p&gt;Operator 只在 &lt;code&gt;subpool-credentials&lt;/code&gt; 不存在时生成凭据。已存在时，它不会自动覆盖或轮换，而是检查：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Secret 是否由当前 SubPool 控制；&lt;/li&gt;
&lt;li&gt;必需 key 是否都存在；&lt;/li&gt;
&lt;li&gt;每个 key 的值是否非空。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;所有权不匹配时报告 &lt;code&gt;CredentialConflict&lt;/code&gt;，形状不符合时报告 &lt;code&gt;CredentialInvalid&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这避免了一个危险行为：Operator 因临时读取失败或资源漂移，重新生成 key，导致已经写入 PVC 的 CPA 配置与新 Secret 不一致。&lt;/p&gt;
&lt;p&gt;轮换凭据需要专门协议：先让生产者和消费者同时接受新旧值，再切换并清理旧值。当前 v1 没有实现这套协议，所以“不要自动轮换”是更安全的选择。&lt;/p&gt;
&lt;h2 id="shared-mihomo-secret-为什么不进-git"&gt;Shared Mihomo Secret 为什么不进 Git
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;mihomo-gateway-providers&lt;/code&gt; 包含上游代理定义，可能涉及订阅 URL、密码和节点凭据。仓库只声明 Mihomo Deployment 会引用这个 Secret，不包含 Secret resource 或真实值。&lt;/p&gt;
&lt;p&gt;安全流程可以这样创建或更新：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway create secret generic mihomo-gateway-providers &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --from-file&lt;span class="o"&gt;=&lt;/span&gt;proxies.yaml&lt;span class="o"&gt;=&lt;/span&gt;/secure/path/proxies.yaml &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --dry-run&lt;span class="o"&gt;=&lt;/span&gt;client -o yaml &lt;span class="p"&gt;|&lt;/span&gt; kubectl apply -f -
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这里依然要注意本地 shell 历史、CI 日志和命令输出。不要执行会把完整 Secret YAML 打印到共享日志的诊断命令。&lt;/p&gt;
&lt;h2 id="读取-secret-的权限就是读取明文的权限"&gt;读取 Secret 的权限就是读取明文的权限
&lt;/h2&gt;&lt;p&gt;拥有下面权限的人，通常就能取得 Secret value：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;对 Secret 的 &lt;code&gt;get&lt;/code&gt;、&lt;code&gt;list&lt;/code&gt; 或 &lt;code&gt;watch&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;对能挂载该 Secret 的 Pod 进行创建或修改；&lt;/li&gt;
&lt;li&gt;对现有 Pod 执行 &lt;code&gt;exec&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;访问运行节点或底层数据存储。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此仅仅“把数据放进 Secret”还不够，还要限制 RBAC、Pod 创建权限、审计和 etcd 静态加密。&lt;/p&gt;
&lt;p&gt;项目的 runtime ServiceAccount 禁止自动挂载 API token，减少容器被攻破后直接调用 API Server 的能力。&lt;/p&gt;
&lt;h2 id="configmap-和-secret-更新后pod-会怎样"&gt;ConfigMap 和 Secret 更新后，Pod 会怎样
&lt;/h2&gt;&lt;p&gt;以 Volume 挂载时，kubelet通常会在一段时间后更新投影文件，但存在重要例外和应用行为差异：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;使用 &lt;code&gt;subPath&lt;/code&gt; 挂载单文件时通常不会自动收到更新；&lt;/li&gt;
&lt;li&gt;应用可能只在启动时读取配置；&lt;/li&gt;
&lt;li&gt;immutable ConfigMap 根本不能原地更新；&lt;/li&gt;
&lt;li&gt;Secret 内容变更并不保证应用自动重载。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此不能只问“对象更新了吗”，还要问“应用何时重新读取”。本项目的 CPA 初始配置写入 PVC 后由应用使用，Operator Configuration 则通过版本化发布和 Operator 重启切换。&lt;/p&gt;
&lt;h2 id="不要把秘密带进这些地方"&gt;不要把秘密带进这些地方
&lt;/h2&gt;&lt;p&gt;本项目明确禁止敏感值进入：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;SubPool.spec&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SubPool.status&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;ConfigMap；&lt;/li&gt;
&lt;li&gt;Git；&lt;/li&gt;
&lt;li&gt;Kubernetes Event；&lt;/li&gt;
&lt;li&gt;日志和 metrics label；&lt;/li&gt;
&lt;li&gt;工单与聊天记录；&lt;/li&gt;
&lt;li&gt;长期保留的 shell history。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可以记录 Secret 的名称、Namespace、必需 key 名、对象是否存在和脱敏摘要，但不要记录 value。&lt;/p&gt;
&lt;h2 id="安全读取-cpa-manager-key"&gt;安全读取 cpa-manager key
&lt;/h2&gt;&lt;p&gt;运行手册提供的命令会把凭据打印到当前终端：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get secret subpool-credentials &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -o &lt;span class="nv"&gt;jsonpath&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s1"&gt;&amp;#39;{.data.cpamp_admin_key}&amp;#39;&lt;/span&gt; &lt;span class="p"&gt;|&lt;/span&gt; base64 --decode
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;执行它意味着你已经决定让明文出现在屏幕和当前进程管道中。不要把输出重定向到普通日志，不要截图发到公开工单，使用完后按组织流程妥善清理。&lt;/p&gt;
&lt;h2 id="诊断时看结构不看-value"&gt;诊断时看结构，不看 value
&lt;/h2&gt;&lt;p&gt;检查 Secret 是否存在：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get secret subpool-credentials
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;检查 key 名称时，优先使用只输出键名或元数据的方式，并避免任何 &lt;code&gt;.data.*&lt;/code&gt; value。对于本项目，Conditions 已能报告 &lt;code&gt;CredentialConflict&lt;/code&gt; 与 &lt;code&gt;CredentialInvalid&lt;/code&gt;，通常不需要在普通排障中读取实际秘密。&lt;/p&gt;
&lt;p&gt;配置管理的核心不是“把值塞进 Pod”，而是明确谁生产、谁读取、何时更新、如何审计、泄露后如何处置。ConfigMap 和 Secret 只是承载这些规则的 Kubernetes 对象。&lt;/p&gt;</description></item><item><title>CRD、Operator、Reconcile 与 Finalizer</title><link>https://kerolt.github.io/p/crdoperatorreconcile-%E4%B8%8E-finalizer/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/crdoperatorreconcile-%E4%B8%8E-finalizer/</guid><description>&lt;h2 id="operator-的本质把运维知识写成控制循环"&gt;Operator 的本质：把运维知识写成控制循环
&lt;/h2&gt;&lt;p&gt;普通 YAML 能描述一组静态对象，但很难表达这些规则：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;一个业务对象自动派生一个 Namespace；&lt;/li&gt;
&lt;li&gt;先校验资源预算，再创建任何持久资源；&lt;/li&gt;
&lt;li&gt;Secret 缺失时随机生成，存在时绝不覆盖；&lt;/li&gt;
&lt;li&gt;Gateway 模式才创建 HTTPRoute；&lt;/li&gt;
&lt;li&gt;suspend 时缩容但保留 PVC；&lt;/li&gt;
&lt;li&gt;删除时等待 Namespace 真正清理完；&lt;/li&gt;
&lt;li&gt;持续把依赖状态汇总成 Conditions。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Operator 把这类领域运维知识编码进 controller。使用者面对的是 &lt;code&gt;SubPool&lt;/code&gt;，底层 Kubernetes 积木由 controller 组织。&lt;/p&gt;
&lt;h2 id="crd给-kubernetes-扩展一种新对象"&gt;CRD：给 Kubernetes 扩展一种新对象
&lt;/h2&gt;&lt;p&gt;CustomResourceDefinition 告诉 API Server：集群现在支持一种新资源。&lt;/p&gt;
&lt;p&gt;本项目安装：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;plural: subpools
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;group: subrelay.example.com
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;version: v1alpha1
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kind: SubPool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;scope: Cluster
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;shortName: sp
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;安装 CRD 后，才能执行：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get subpools
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -f examples/subpool-internal.yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;CRD 只定义 API 和校验规则，不会自己创建 StatefulSet。真正的自动化逻辑来自 Operator controller。&lt;/p&gt;
&lt;h2 id="go-类型如何变成-kubernetes-api"&gt;Go 类型如何变成 Kubernetes API
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;api/v1alpha1/subpool_types.go&lt;/code&gt; 定义：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;span class="lnt"&gt;13
&lt;/span&gt;&lt;span class="lnt"&gt;14
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;SubPool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ ObjectMeta
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ Spec
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ ├─ Exposure
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ ├─ Storage
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ ├─ CPA
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ ├─ CPAManager
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ └─ Suspend
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ Status
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ ObservedGeneration
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ Namespace
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ CPAService
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ Hostname
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ Conditions
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Kubebuilder markers 描述 scope、short name、status subresource、打印列和 CEL 校验。生成的 CRD YAML被放在 &lt;code&gt;config/crd/bases/&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;Go 类型是 controller 编译时使用的强类型模型，CRD schema 是 API Server 接收对象时使用的运行时契约。修改其中一边却忘记重新生成另一边，会造成代码和集群 API 不一致。&lt;/p&gt;
&lt;h2 id="静态校验与动态校验的边界"&gt;静态校验与动态校验的边界
&lt;/h2&gt;&lt;p&gt;CRD schema 适合检查：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;exposure mode 枚举；&lt;/li&gt;
&lt;li&gt;必填字段；&lt;/li&gt;
&lt;li&gt;名称格式；&lt;/li&gt;
&lt;li&gt;storage.size 不可变；&lt;/li&gt;
&lt;li&gt;resources 必须包含 CPU 和内存。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Operator 适合检查：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;配置的 ConfigMap 能否读取与解析；&lt;/li&gt;
&lt;li&gt;两个容器的合计资源是否超过平台包络；&lt;/li&gt;
&lt;li&gt;派生 Namespace 是否已被别人占用；&lt;/li&gt;
&lt;li&gt;Secret 是否属于当前 SubPool；&lt;/li&gt;
&lt;li&gt;Shared Mihomo 有没有 Ready Endpoint；&lt;/li&gt;
&lt;li&gt;HTTPRoute 和 Gateway status 是否就绪。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;静态规则尽早拒绝，动态规则通过 Condition 提供可诊断状态。&lt;/p&gt;
&lt;h2 id="controller-managerscheme-与-client"&gt;Controller Manager、Scheme 与 Client
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;main.go&lt;/code&gt; 创建 controller-runtime Manager，并把这些 API 注册到 Scheme：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Kubernetes core API；&lt;/li&gt;
&lt;li&gt;Gateway API v1；&lt;/li&gt;
&lt;li&gt;SubPool v1alpha1。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Scheme 让 client 知道 Go 类型与 Group/Version/Kind 的对应关系。没有注册类型，controller 无法正确编码或解码对象。&lt;/p&gt;
&lt;p&gt;Manager 统一负责 client/cache、controller 生命周期、metrics、health endpoints、leader election 和信号处理。&lt;/p&gt;
&lt;h2 id="watch-不是每秒全量扫描"&gt;Watch 不是每秒全量扫描
&lt;/h2&gt;&lt;p&gt;Controller 通常通过 list/watch 与本地 cache 感知对象变化。SubPool 创建、spec 更新或受管对象变化时，事件进入 work queue，触发 reconcile。&lt;/p&gt;
&lt;p&gt;本项目声明 &lt;code&gt;Owns&lt;/code&gt; Namespace、Secret、ConfigMap、ServiceAccount、Service、StatefulSet、ResourceQuota、NetworkPolicy 和 PDB。它们的 owner reference 让 controller-runtime 能把子资源事件映射回 SubPool。&lt;/p&gt;
&lt;p&gt;共享 Gateway 和 Mihomo EndpointSlice 不归任何 SubPool 所有，不能通过 &lt;code&gt;Owns&lt;/code&gt; 关联。因此 controller 在活动状态下默认每 30 秒 requeue，周期性复核这些共享依赖。&lt;/p&gt;
&lt;p&gt;这不是用轮询创建资源，而是为无法直接归属到单个 SubPool 的共享状态提供保守复查。&lt;/p&gt;
&lt;h2 id="predicate-为什么过滤-status-only-更新"&gt;predicate 为什么过滤 status-only 更新
&lt;/h2&gt;&lt;p&gt;Controller 更新 SubPool status 本身也会产生对象更新事件。如果每次 status 更新都再次触发 reconcile，可能形成无意义循环。&lt;/p&gt;
&lt;p&gt;项目的 predicate 只在以下根对象变化时继续：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;新建或删除事件；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;metadata.generation&lt;/code&gt; 变化，也就是 spec 更新；&lt;/li&gt;
&lt;li&gt;首次出现 deletionTimestamp。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;单纯 status 更新不会触发新的根 reconcile。这能降低 API Server 和 work queue 噪声。&lt;/p&gt;
&lt;h2 id="一次-reconcile-的固定顺序"&gt;一次 reconcile 的固定顺序
&lt;/h2&gt;&lt;p&gt;活动 SubPool 的流程可以概括为：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;1. 读取并校验 immutable Operator Configuration
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;2. 校验 Gateway 模式配置是否完整
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;3. 校验 SubPool ID、镜像、资源、存储与资源包络
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;4. 构造全部期望对象
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;5. 创建或确认 Namespace 所有权
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;6. 创建或确认 Credential Secret
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;7. 清理本 Pool 所有的旧版 LimitRange
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;8. 创建或修正其余受管对象
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;9. 根据 Internal/Gateway/suspend 删除或保留 HTTPRoute
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;10. 读取实际状态并更新 Conditions
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;先校验再创建很重要。无效请求不会留下 Namespace、PVC 或 Secret 等持久半成品。&lt;/p&gt;
&lt;h2 id="reconcile-必须幂等"&gt;Reconcile 必须幂等
&lt;/h2&gt;&lt;p&gt;幂等表示：对同样的期望和实际状态重复执行，最终结果不变，也不会每次制造新资源。&lt;/p&gt;
&lt;p&gt;项目的 &lt;code&gt;ensureManagedObject&lt;/code&gt; 使用这种模式：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Get 当前对象
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ 不存在 → Create
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ 已存在
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ owner 不匹配 → 报冲突，不接管
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ 已等于期望 → 什么也不做
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ 有受控字段漂移 → Update
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这比“每次先删再建”安全得多。Service 的 ClusterIP 等 API Server 分配字段必须保留，不能拿一份新对象整体覆盖。&lt;/p&gt;
&lt;h2 id="只管理明确拥有的字段"&gt;只管理明确拥有的字段
&lt;/h2&gt;&lt;p&gt;Controller 同步稳定 labels 和 owner references，并按 Kind 更新特定 spec。&lt;/p&gt;
&lt;p&gt;以 Service 为例，它只同步：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;type；&lt;/li&gt;
&lt;li&gt;ports；&lt;/li&gt;
&lt;li&gt;selector。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;ClusterIP、IP family 等由 API Server 分配或默认的字段被保留。&lt;/p&gt;
&lt;p&gt;Metadata 中的额外 labels 也被保留，只覆盖 Operator 自己期望的键。这让其他合法系统可以添加自己的 label，而不会被每轮 reconcile 清掉。&lt;/p&gt;
&lt;p&gt;“SubPool 是唯一期望来源”不意味着粗暴覆盖对象所有字节，而是要明确 controller 对哪些字段拥有写入权。&lt;/p&gt;
&lt;h2 id="所有权冲突为什么必须阻塞"&gt;所有权冲突为什么必须阻塞
&lt;/h2&gt;&lt;p&gt;若同名对象已存在但不由当前 SubPool 控制，项目不会自动加 owner reference。否则可能出现：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;别人创建了 subpool-demo-pool Namespace
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Operator 只因名字相同就接管
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 用户删除 demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Operator 删除别人的 Namespace 和数据
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;因此 Namespace、Secret 和其他受管资源都会检查 &lt;code&gt;metav1.IsControlledBy&lt;/code&gt;。Namespace 还额外检查 managed label 和 Pool label。&lt;/p&gt;
&lt;p&gt;冲突会写入稳定 reason，例如 &lt;code&gt;NamespaceConflict&lt;/code&gt;、&lt;code&gt;CredentialConflict&lt;/code&gt; 或 &lt;code&gt;ManagedResourceConflict&lt;/code&gt;。&lt;/p&gt;
&lt;h2 id="finalizer删除前的待办清单"&gt;Finalizer：删除前的待办清单
&lt;/h2&gt;&lt;p&gt;SubPool 使用：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subrelay.example.com/finalizer
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;正常活动对象先加入 finalizer。用户执行 delete 后，API Server 设置 deletionTimestamp，但在 finalizer 清空前保留对象。&lt;/p&gt;
&lt;p&gt;Operator 删除路径：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;看到 deletionTimestamp
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 计算派生 Namespace
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Namespace 不存在：移除 finalizer
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Namespace 存在且属于当前 SubPool：请求删除 Namespace
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 等 Namespace 及内部资源完成删除
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Namespace 消失后移除 finalizer
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → SubPool 最终从 API 消失
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;如果同名 Namespace 不属于当前 SubPool，Operator 不删除它，只移除自己的 finalizer。这保持所有权边界。&lt;/p&gt;
&lt;h2 id="为什么不要手工移除-finalizer"&gt;为什么不要手工移除 finalizer
&lt;/h2&gt;&lt;p&gt;直接移除 finalizer 会让 SubPool 消失，但它负责清理的 Namespace 可能继续存在，形成孤儿资源、PVC 和 Secret。&lt;/p&gt;
&lt;p&gt;若删除卡住，应检查：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;DeletionBlocked&lt;/code&gt; 的 reason 和 message；&lt;/li&gt;
&lt;li&gt;Namespace 的 finalizers；&lt;/li&gt;
&lt;li&gt;PVC/PV 与 CSI 状态；&lt;/li&gt;
&lt;li&gt;第三方 controller 是否还在运行；&lt;/li&gt;
&lt;li&gt;API 删除是否收到 Forbidden 或其他错误。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;修复负责收尾的依赖，比强行撕掉“待办清单”更安全。&lt;/p&gt;
&lt;h2 id="suspend-是期望状态不是删除流程"&gt;suspend 是期望状态，不是删除流程
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;spec.suspend=true&lt;/code&gt; 时：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;StatefulSet replicas 改为 0；&lt;/li&gt;
&lt;li&gt;HTTPRoute 删除；&lt;/li&gt;
&lt;li&gt;Namespace、PVC 和 Secret 保留；&lt;/li&gt;
&lt;li&gt;Conditions 明确使用 &lt;code&gt;Suspended&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;ProxyGatewayReady 设为 Unknown，因为暂停期间不需要评估代理。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这说明 Operator API 不只有 CRUD。一个业务资源可以拥有清晰的生命周期状态，而不需要通过手工改底层对象实现暂停。&lt;/p&gt;
&lt;h2 id="status-更新也要避免噪声"&gt;Status 更新也要避免噪声
&lt;/h2&gt;&lt;p&gt;Condition 只有在 status、reason、message 或 observedGeneration 变化时才替换，并保留未变化条件的 lastTransitionTime。&lt;/p&gt;
&lt;p&gt;如果每 30 秒 reconcile 都刷新时间，会让监控以为状态持续变化，也产生大量无意义 API 写入。稳定的 transition time 才能回答“这个问题从什么时候开始”。&lt;/p&gt;
&lt;h2 id="阅读-operator-代码的推荐路线"&gt;阅读 Operator 代码的推荐路线
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;span class="lnt"&gt;13
&lt;/span&gt;&lt;span class="lnt"&gt;14
&lt;/span&gt;&lt;span class="lnt"&gt;15
&lt;/span&gt;&lt;span class="lnt"&gt;16
&lt;/span&gt;&lt;span class="lnt"&gt;17
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;api/v1alpha1/subpool_types.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 先看 API 契约
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;controllers/subpool_controller.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 再看 Reconcile 入口、watch、finalizer
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;controllers/subpool_reconcile.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 看活动路径的顺序
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;internal/subpool/resources.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;internal/subpool/workloads.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;internal/subpool/network.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 看期望对象怎样构造
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;controllers/subpool_readiness.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;controllers/subpool_status.go
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 看现实状态怎样投影回 CR
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Operator 并不是 Kubernetes 的魔法插件。它只是一个认真遵守幂等、所有权和异步状态语义的程序：不断比较愿望与现实，并把差距一点点收敛掉。&lt;/p&gt;</description></item><item><title>Gateway API 与 HTTPRoute 公网入口</title><link>https://kerolt.github.io/p/gateway-api-%E4%B8%8E-httproute-%E5%85%AC%E7%BD%91%E5%85%A5%E5%8F%A3/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/gateway-api-%E4%B8%8E-httproute-%E5%85%AC%E7%BD%91%E5%85%A5%E5%8F%A3/</guid><description>&lt;h2 id="入站与出站先分清"&gt;入站与出站先分清
&lt;/h2&gt;&lt;p&gt;本项目同时出现两个“Gateway”：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Shared Envoy Gateway：管理外部请求如何进入 CPA；&lt;/li&gt;
&lt;li&gt;Shared Mihomo Gateway：管理 CPA 的请求如何通过代理出去。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;名字相似，方向相反：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;互联网用户 → Envoy Gateway → CPA
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;CPA → Mihomo Gateway → 互联网目标
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;本文只讲入站的 Gateway API。&lt;/p&gt;
&lt;h2 id="为什么不为每个-pool-创建-loadbalancer"&gt;为什么不为每个 Pool 创建 LoadBalancer
&lt;/h2&gt;&lt;p&gt;如果每个号池都创建一个 LoadBalancer Service，会带来：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每个 Pool 一份云负载均衡成本；&lt;/li&gt;
&lt;li&gt;大量公网 IP；&lt;/li&gt;
&lt;li&gt;DNS 和证书分散；&lt;/li&gt;
&lt;li&gt;暴露 cpa-manager 的误配置风险；&lt;/li&gt;
&lt;li&gt;入口策略难以统一审计。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;项目选择共享入口：集群只维护 Shared Envoy Gateway，各 Pool 通过自己的 hostname 和 HTTPRoute 绑定到它。&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;demo-pool.example →┐
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;team-a.example →┼→ Shared Gateway → 各自 CPA Service
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;team-b.example →┘
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这叫“共享基础设施、租户自带路由”。&lt;/p&gt;
&lt;h2 id="gateway-api-的三个核心对象"&gt;Gateway API 的三个核心对象
&lt;/h2&gt;&lt;h3 id="gatewayclass谁来实现-gateway"&gt;GatewayClass：谁来实现 Gateway
&lt;/h3&gt;&lt;p&gt;项目声明：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;apiVersion&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;gateway.networking.k8s.io/v1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;GatewayClass&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;metadata&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subpool-envoy&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;controllerName&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;gateway.envoyproxy.io/gatewayclass-controller&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;GatewayClass 是 cluster-scoped，类似 StorageClass：它说明哪种 controller 负责实现 Gateway。&lt;/p&gt;
&lt;p&gt;仓库不会安装 Envoy Gateway controller。若集群中没有对应 controller，GatewayClass 和 Gateway 即使能创建，也不会真正产生可工作的入口。&lt;/p&gt;
&lt;h3 id="gateway共享入口与-listener"&gt;Gateway：共享入口与 listener
&lt;/h3&gt;&lt;p&gt;项目在 &lt;code&gt;subpool-gateway&lt;/code&gt; Namespace 创建 &lt;code&gt;subpool-edge&lt;/code&gt;：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;gatewayClassName&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subpool-envoy&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;listeners&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;http&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;protocol&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;HTTP&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;port&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;80&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Gateway 描述一个基础设施入口，listener 描述它监听的协议、端口和允许哪些 Route 绑定。&lt;/p&gt;
&lt;p&gt;当前仓库 listener 是 HTTP 80。README 提到生产需要通配 DNS/TLS，但当前 manifest 没有直接配置 HTTPS listener 和证书，因此生产 TLS 仍属于必须补齐并验证的环境能力，不能因为有 Gateway 对象就假定 HTTPS 已完成。&lt;/p&gt;
&lt;h3 id="httproute某个-hostname-去哪个后端"&gt;HTTPRoute：某个 hostname 去哪个后端
&lt;/h3&gt;&lt;p&gt;当 SubPool 使用 Gateway 模式时，Operator 在 Pool Namespace 创建名为 &lt;code&gt;cpa&lt;/code&gt; 的 HTTPRoute：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;hostname: &amp;lt;pool-id&amp;gt;.&amp;lt;baseDomain&amp;gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;parent: subpool-gateway/subpool-edge listener http
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;backend: 同 Namespace 的 Service cpa:8317
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;HTTPRoute 属于租户 Namespace，Gateway 属于共享基础设施 Namespace。二者通过 parent reference 建立关系。&lt;/p&gt;
&lt;h2 id="跨-namespace-route-如何获得准入"&gt;跨 Namespace Route 如何获得准入
&lt;/h2&gt;&lt;p&gt;Gateway listener 配置：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;allowedRoutes&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;namespaces&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;from&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;Selector&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;selector&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;matchLabels&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;subrelay.example.com/managed&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;true&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;只有带 managed label 的 Namespace，其 HTTPRoute 才能绑定这个 listener。&lt;/p&gt;
&lt;p&gt;这些 label 由 Operator 在创建 SubPool Namespace 时添加。因此任意普通 Namespace 不能随便提交一个 Route 劫持共享入口域名。&lt;/p&gt;
&lt;p&gt;这体现了 Gateway API 的角色分工：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;基础设施管理员管理 Gateway 和 listener 策略；&lt;/li&gt;
&lt;li&gt;应用或 Operator 管理自己 Namespace 中的 HTTPRoute；&lt;/li&gt;
&lt;li&gt;Gateway status 明确告诉双方 Route 是否被接受。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="完整数据路径"&gt;完整数据路径
&lt;/h2&gt;&lt;p&gt;Gateway 模式下，外部请求大致经过：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;span class="lnt"&gt;13
&lt;/span&gt;&lt;span class="lnt"&gt;14
&lt;/span&gt;&lt;span class="lnt"&gt;15
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;客户端请求 demo-pool.subrelay.example.com
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ DNS 解析到共享入口地址
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Envoy Gateway listener
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ 根据 hostname 匹配 HTTPRoute
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpool-demo-pool/cpa HTTPRoute
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ backendRef
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpool-demo-pool/cpa Service:8317
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ selector + EndpointSlice
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runtime-0 Pod 的 CPA 容器
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;任何一段缺失，外部访问都可能失败。Gateway API 不负责自动购买域名，也不保证云安全组、LoadBalancer、DNS 和证书已经正确配置。&lt;/p&gt;
&lt;h2 id="operator-如何判断-exposureready"&gt;Operator 如何判断 ExposureReady
&lt;/h2&gt;&lt;p&gt;Gateway 模式下，Operator 分两步检查。&lt;/p&gt;
&lt;p&gt;先检查 HTTPRoute status 中目标 parent：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;parent name 必须是配置指定的 Gateway；&lt;/li&gt;
&lt;li&gt;parent Namespace 必须匹配；&lt;/li&gt;
&lt;li&gt;sectionName 必须匹配 &lt;code&gt;http&lt;/code&gt; listener；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Accepted=True&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ResolvedRefs=True&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;再检查 Shared Gateway：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Programmed=True&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;只有这些条件同时成立，&lt;code&gt;ExposureReady=True&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;Accepted&lt;/code&gt; 表示 Gateway controller 接受该 Route；&lt;code&gt;ResolvedRefs&lt;/code&gt; 表示后端等引用能成功解析；&lt;code&gt;Programmed&lt;/code&gt; 表示 Gateway 配置已经下发到数据面。它们比“对象存在”提供了更强证据。&lt;/p&gt;
&lt;h2 id="四个常见-reason"&gt;四个常见 reason
&lt;/h2&gt;&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;Reason&lt;/th&gt;
					&lt;th&gt;含义&lt;/th&gt;
					&lt;th&gt;优先检查&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;RouteMissing&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Operator 尚未看到 HTTPRoute&lt;/td&gt;
					&lt;td&gt;reconcile、权限、受管资源冲突&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;RouteNotAccepted&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Route 未被目标 listener 接受&lt;/td&gt;
					&lt;td&gt;allowedRoutes、parentRef、backendRef status&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;GatewayMissing&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;配置指定的共享 Gateway 不存在&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;deploy/gateway&lt;/code&gt; 是否安装、名称与 Namespace&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;GatewayNotProgrammed&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Gateway controller 尚未准备好数据面&lt;/td&gt;
					&lt;td&gt;Envoy Gateway controller、Gateway status 和 Event&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;排查命令：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get httproute cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get gateway subpool-edge -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get gatewayclass subpool-envoy -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;重点看 &lt;code&gt;status.parents[].conditions&lt;/code&gt; 与 &lt;code&gt;status.conditions&lt;/code&gt;，而不只是 spec。&lt;/p&gt;
&lt;h2 id="internal-切换到-gateway-会发生什么"&gt;Internal 切换到 Gateway 会发生什么
&lt;/h2&gt;&lt;p&gt;修改 &lt;code&gt;spec.exposure.mode&lt;/code&gt; 为 &lt;code&gt;Gateway&lt;/code&gt; 后，Operator 会创建 HTTPRoute，并在 status 中写入派生 hostname。&lt;/p&gt;
&lt;p&gt;从 Gateway 切回 Internal，Operator 会删除 HTTPRoute，但保留 CPA ClusterIP Service 和运行数据。&lt;/p&gt;
&lt;p&gt;暂停 Pool 时也会删除 HTTPRoute并把 StatefulSet 缩容为 0，避免一个没有后端的公共路由继续挂在共享入口上。&lt;/p&gt;
&lt;h2 id="cpa-manager-为什么没有-httproute"&gt;cpa-manager 为什么没有 HTTPRoute
&lt;/h2&gt;&lt;p&gt;项目明确把 CPA 定义为 Pool Endpoint，cpa-manager 只是管理员看板。Manager 始终保持 ClusterIP，只通过受控 port-forward 使用。&lt;/p&gt;
&lt;p&gt;这是一条安全边界：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;外部用户流量 → CPA
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;管理员临时访问 → cpa-manager
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;不能因为 Gateway 模式已经存在，就顺手给 Manager 再加一条 Route。那会扩大攻击面，并绕过项目既定的管理员访问控制。&lt;/p&gt;
&lt;h2 id="dnstls-与-gateway-对象不是一回事"&gt;DNS、TLS 与 Gateway 对象不是一回事
&lt;/h2&gt;&lt;p&gt;Gateway status 正常并不能自动证明：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;&amp;lt;pool-id&amp;gt;.&amp;lt;baseDomain&amp;gt;&lt;/code&gt; 已解析到入口 IP；&lt;/li&gt;
&lt;li&gt;通配符 DNS 已配置；&lt;/li&gt;
&lt;li&gt;HTTPS listener 已存在；&lt;/li&gt;
&lt;li&gt;TLS Secret 或证书 controller 正常；&lt;/li&gt;
&lt;li&gt;云 LoadBalancer 可从互联网访问；&lt;/li&gt;
&lt;li&gt;防火墙或安全组已开放。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些属于 Cluster Bootstrap 和环境运维。Operator 只根据已有共享 Gateway 和 Route status 判断 Kubernetes 内部的入口前提。&lt;/p&gt;
&lt;h2 id="gateway-api-相比旧式-ingress-的思维变化"&gt;Gateway API 相比旧式 Ingress 的思维变化
&lt;/h2&gt;&lt;p&gt;Ingress 常把基础设施与应用路由挤在同一种对象里，并大量依赖 controller-specific annotations。Gateway API 把职责拆成 GatewayClass、Gateway 和 Route：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;实现选择 → GatewayClass
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;共享入口 → Gateway
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;应用路由 → HTTPRoute
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这种分层非常适合本项目：平台团队控制 Shared Envoy，SubPool Operator 只创建每 Pool 的 HTTPRoute。&lt;/p&gt;
&lt;h2 id="安全的排障原则"&gt;安全的排障原则
&lt;/h2&gt;&lt;p&gt;如果 &lt;code&gt;ExposureReady=False&lt;/code&gt;，应修复 Shared Gateway、listener、allowedRoutes、DNS 或 TLS，而不是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;把 CPA Service 改成 LoadBalancer；&lt;/li&gt;
&lt;li&gt;给每个 Pool 新建公网 IP；&lt;/li&gt;
&lt;li&gt;删除 managed Namespace selector；&lt;/li&gt;
&lt;li&gt;直接暴露 cpa-manager；&lt;/li&gt;
&lt;li&gt;放开 CPA 出站绕过 Mihomo。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;绕过架构可能让一次临时排障变成长期安全债务。按照 &lt;code&gt;DNS → Gateway → Route → Service → Endpoint → Pod&lt;/code&gt; 的顺序逐段验证，才能修复真正的断点。&lt;/p&gt;</description></item><item><title>Kustomize 部署、生命周期操作与排障</title><link>https://kerolt.github.io/p/kustomize-%E9%83%A8%E7%BD%B2%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E6%93%8D%E4%BD%9C%E4%B8%8E%E6%8E%92%E9%9A%9C/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/kustomize-%E9%83%A8%E7%BD%B2%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E6%93%8D%E4%BD%9C%E4%B8%8E%E6%8E%92%E9%9A%9C/</guid><description>&lt;h2 id="先渲染再写入集群"&gt;先渲染，再写入集群
&lt;/h2&gt;&lt;p&gt;Kustomize 不是模板语言，它以 Kubernetes YAML 为基础，通过 resources、patches、images、namespace 等机制组合最终 manifests。&lt;code&gt;kubectl&lt;/code&gt; 已内置常用 Kustomize 能力：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl kustomize &amp;lt;目录&amp;gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -k &amp;lt;目录&amp;gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;前者只渲染到标准输出，不修改集群；后者把渲染结果提交给 API Server。&lt;/p&gt;
&lt;p&gt;一个稳妥习惯是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;先看最终会提交什么
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 做本地检查
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 再 apply
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 最后观察状态
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="本项目的-kustomize-分层"&gt;本项目的 Kustomize 分层
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;span class="lnt"&gt;13
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;config/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ crd/ SubPool CRD
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ rbac/ ServiceAccount、权限与绑定
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ manager/ Operator Deployment、metrics Service、PDB
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ default/ 组合上述资源，加 Namespace 与 Operator Configuration
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;deploy/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ operator/ Operator 正式入口，并覆盖镜像
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ gateway/ Shared Mihomo 与 Envoy Gateway
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;examples/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ subpool-internal.yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ mihomo/ 引用共享 Mihomo manifests 的兼容入口
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;deploy/operator&lt;/code&gt; 与 &lt;code&gt;deploy/gateway&lt;/code&gt; 是独立入口。安装 Operator 不会自动安装 Gateway，也不会创建 SubPool 或 Secret。&lt;/p&gt;
&lt;p&gt;这能防止一次看似普通的控制面安装，顺便启动真实代理或创建业务实例。&lt;/p&gt;
&lt;h2 id="resources-是组合不是复制"&gt;resources 是组合，不是复制
&lt;/h2&gt;&lt;p&gt;例如 &lt;code&gt;config/default/kustomization.yaml&lt;/code&gt; 引用：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;resources&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;namespace.yaml&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;../crd&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;../rbac&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;operator-config.yaml&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;../manager&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Kustomize 递归加载这些资源，形成一个最终对象集合。&lt;code&gt;deploy/operator&lt;/code&gt; 再引用 &lt;code&gt;config/default&lt;/code&gt;，并通过 &lt;code&gt;images&lt;/code&gt; 修改 Operator 镜像。&lt;/p&gt;
&lt;p&gt;因此修改基础文件会影响所有引用它的入口。排查最终内容时，不要只看当前目录那几行 kustomization，要渲染完整结果。&lt;/p&gt;
&lt;h2 id="images-transformer-做什么"&gt;images transformer 做什么
&lt;/h2&gt;&lt;p&gt;项目通过：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;images&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;registry.example.invalid/example-org/subpool-operator&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;newName&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;example-org/subrelay-operator&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;newTag&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;example-tag&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;把基础 Deployment 中的镜像名称和 tag 替换为环境要使用的版本。&lt;/p&gt;
&lt;p&gt;远程集群必须能从它自己的节点网络拉取该镜像。本地电脑能 &lt;code&gt;docker pull&lt;/code&gt;，不代表集群 Node 一定能拉取。&lt;/p&gt;
&lt;p&gt;生产更推荐不可变版本或 digest，并在发布前确认：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;registry 可达；&lt;/li&gt;
&lt;li&gt;私有仓库凭据已配置；&lt;/li&gt;
&lt;li&gt;CPU architecture 匹配；&lt;/li&gt;
&lt;li&gt;镜像 tag/digest 正确；&lt;/li&gt;
&lt;li&gt;imagePullPolicy 符合预期。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="dockerfile-的两阶段构建"&gt;Dockerfile 的两阶段构建
&lt;/h2&gt;&lt;p&gt;项目先在 Go 镜像中编译静态 manager，再复制到 distroless nonroot 镜像：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;golang:1.26.0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → go build /out/manager
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → gcr.io/distroless/static:nonroot
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → USER 65532:65532
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;优点是最终镜像不包含完整编译工具链和 shell，体积与攻击面更小。代价是进入容器临时执行 shell 命令并不可用，排障应依赖日志、metrics、健康端点和 Kubernetes 对象，而不是假设能 &lt;code&gt;sh&lt;/code&gt; 进去。&lt;/p&gt;
&lt;h2 id="部署前本地验证"&gt;部署前本地验证
&lt;/h2&gt;&lt;p&gt;项目提供：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nv"&gt;GOCACHE&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;/tmp/subrelay-operator-go-build-cache go &lt;span class="nb"&gt;test&lt;/span&gt; ./...
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;bash deploy/tests/render.sh
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl kustomize deploy/operator &amp;gt;/dev/null
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl kustomize deploy/gateway &amp;gt;/dev/null
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl kustomize examples/mihomo &amp;gt;/dev/null
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这些命令不向集群写资源。它们分别验证 Go 行为和 manifests 能否渲染，并检查渲染结果是否违反项目约束。&lt;/p&gt;
&lt;p&gt;本地通过不保证集群部署一定成功，因为 StorageClass、Gateway API、镜像仓库和 CNI 等依赖属于目标环境。&lt;/p&gt;
&lt;h2 id="推荐部署顺序"&gt;推荐部署顺序
&lt;/h2&gt;&lt;h3 id="第一步构建并准备-operator-镜像"&gt;第一步：构建并准备 Operator 镜像
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;docker build -t subpool-operator:latest .
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;远程集群需推送到可拉取 registry，并更新 &lt;code&gt;deploy/operator/kustomization.yaml&lt;/code&gt;。&lt;/p&gt;
&lt;h3 id="第二步渲染并安装-operator-bootstrap"&gt;第二步：渲染并安装 Operator Bootstrap
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl kustomize deploy/operator
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -k deploy/operator
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这会安装：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;subpool-system&lt;/code&gt; Namespace；&lt;/li&gt;
&lt;li&gt;SubPool CRD；&lt;/li&gt;
&lt;li&gt;RBAC；&lt;/li&gt;
&lt;li&gt;immutable Operator Configuration；&lt;/li&gt;
&lt;li&gt;Operator Deployment；&lt;/li&gt;
&lt;li&gt;metrics Service；&lt;/li&gt;
&lt;li&gt;Operator PDB。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-system get deployment,pod,service
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get crd subpools.subrelay.example.com
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-system logs deployment/subpool-operator &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --all-containers&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nb"&gt;true&lt;/span&gt; --tail&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;200&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h3 id="第三步按需安装共享-gateway"&gt;第三步：按需安装共享 Gateway
&lt;/h3&gt;&lt;p&gt;先通过安全流程创建 &lt;code&gt;mihomo-gateway-providers&lt;/code&gt; Secret，再执行：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -k deploy/gateway
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这会安装 &lt;code&gt;subpool-gateway&lt;/code&gt; Namespace、Shared Mihomo、GatewayClass 和 Gateway。它不会安装 Envoy Gateway controller，也不包含 Secret value。&lt;/p&gt;
&lt;p&gt;检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get deployment,pod,service,gateway
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get gatewayclass subpool-envoy
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;mihomo-gateway
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h3 id="第四步创建第一个-internal-pool"&gt;第四步：创建第一个 Internal Pool
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -f examples/subpool-internal.yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl describe sp demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Internal 是最适合第一次验证的模式，因为它不依赖外部 DNS、TLS 和 Gateway route。&lt;/p&gt;
&lt;h2 id="创建后的观察顺序"&gt;创建后的观察顺序
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get namespace subpool-demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; statefulset,service,pvc,pod,networkpolicy,poddisruptionbudget
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get events --sort-by&lt;span class="o"&gt;=&lt;/span&gt;.lastTimestamp
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;等待 Ready：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl &lt;span class="nb"&gt;wait&lt;/span&gt; --for&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nv"&gt;condition&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;Ready &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; subpool/demo-pool --timeout&lt;span class="o"&gt;=&lt;/span&gt;300s
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;若超时，立即转为 Conditions 驱动的排查，不要反复 apply 同一文件期待随机恢复。&lt;/p&gt;
&lt;h2 id="internal-模式的访问"&gt;Internal 模式的访问
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool port-forward service/cpa 8317:8317
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool port-forward &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; service/cpa-manager 18317:18317
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;两个命令需要分别占用一个持续运行的终端。它们只适合开发与受控管理访问。&lt;/p&gt;
&lt;h2 id="更新操作"&gt;更新操作
&lt;/h2&gt;&lt;p&gt;镜像或 CPU/内存资源应修改 SubPool spec，由 Operator 同步到 StatefulSet。生产镜像优先使用 digest。&lt;/p&gt;
&lt;p&gt;不要直接编辑受管 StatefulSet。即使暂时成功，下一轮 reconcile 也会按 SubPool 恢复。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;storage.size&lt;/code&gt; 当前不可修改。若业务需要更大容量，必须先设计受支持的数据迁移或未来扩容流程，不能通过手工改 PVC 与 CR 制造状态分叉。&lt;/p&gt;
&lt;h2 id="暂停与恢复"&gt;暂停与恢复
&lt;/h2&gt;&lt;p&gt;暂停：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl patch subpool demo-pool --type&lt;span class="o"&gt;=&lt;/span&gt;merge &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -p &lt;span class="s1"&gt;&amp;#39;{&amp;#34;spec&amp;#34;:{&amp;#34;suspend&amp;#34;:true}}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;预期结果：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;StatefulSet replicas 为 0；&lt;/li&gt;
&lt;li&gt;HTTPRoute 被删除；&lt;/li&gt;
&lt;li&gt;Namespace、PVC、Secret 保留；&lt;/li&gt;
&lt;li&gt;Ready=False，reason 为 Suspended。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;恢复：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl patch subpool demo-pool --type&lt;span class="o"&gt;=&lt;/span&gt;merge &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -p &lt;span class="s1"&gt;&amp;#39;{&amp;#34;spec&amp;#34;:{&amp;#34;suspend&amp;#34;:false}}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Operator 会重新创建运行 Pod，并继续使用旧 PVC。&lt;/p&gt;
&lt;h2 id="删除前先停一下"&gt;删除前先停一下
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl delete subpool demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;删除会销毁专属 Namespace、PVC、Credential Secret 和运行数据。当前项目不提供备份恢复。&lt;/p&gt;
&lt;p&gt;不要使用 &lt;code&gt;--force&lt;/code&gt;，不要手工删除 &lt;code&gt;subrelay.example.com/finalizer&lt;/code&gt;。如果只是临时不用，选择 suspend。&lt;/p&gt;
&lt;p&gt;删除卡住时检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get namespace subpool-demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pvc -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;关注 &lt;code&gt;DeletionBlocked&lt;/code&gt;、Namespace/PVC finalizers 以及负责相关资源的第三方 controller。&lt;/p&gt;
&lt;h2 id="conditions-驱动的排障地图"&gt;Conditions 驱动的排障地图
&lt;/h2&gt;&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;Condition/Reason&lt;/th&gt;
					&lt;th&gt;首要对象&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;OperatorConfigurationInvalid&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;subpool-system&lt;/code&gt; 中版本化 ConfigMap 与 Operator 参数&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;InvalidPoolResources&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;SubPool 的 requests/limits&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;ResourcesExceedBaseline&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;SubPool 合计申请与 &lt;code&gt;maxPerPool&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;NamespaceConflict&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;派生 Namespace labels、ownerReferences、UID&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;CredentialConflict&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;subpool-credentials&lt;/code&gt; ownerReferences&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;CredentialInvalid&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Secret 必需 key 是否存在且非空，不读取 value&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;WorkloadMissing&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;StatefulSet 是否创建&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;WorkloadsNotReady&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Pod、PVC、镜像、探针和 Event&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;ServiceMissing&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;CPA 与 cpa-manager Service&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;EndpointsUnavailable&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Shared Mihomo Pod 与 EndpointSlice&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;RouteNotAccepted&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;HTTPRoute parent conditions 与 allowedRoutes&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;GatewayNotProgrammed&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Envoy Gateway controller 和 Gateway status&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;NamespaceTerminating&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Namespace 内资源及第三方 finalizers&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2 id="三层排障法"&gt;三层排障法
&lt;/h2&gt;&lt;h3 id="第一层api-与控制器"&gt;第一层：API 与控制器
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-system logs deployment/subpool-operator &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --all-containers&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nb"&gt;true&lt;/span&gt; --tail&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;200&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;确认 CR 被接受、generation 被观察、Operator 没有 Forbidden 或 reconcile error。&lt;/p&gt;
&lt;h3 id="第二层kubernetes-资源"&gt;第二层：Kubernetes 资源
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get statefulset,pod,pvc,service
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get events --sort-by&lt;span class="o"&gt;=&lt;/span&gt;.lastTimestamp
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;确认调度、存储、镜像和探针。&lt;/p&gt;
&lt;h3 id="第三层数据路径"&gt;第三层：数据路径
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;入站：Gateway → HTTPRoute → CPA Service → Endpoint → Pod
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;出站：CPA → Mihomo Service → Endpoint → Mihomo → 上游代理
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;每次只验证相邻两个跳点，避免一句“网络不通”覆盖十种不同故障。&lt;/p&gt;
&lt;h2 id="不要用破坏架构的方式排障"&gt;不要用破坏架构的方式排障
&lt;/h2&gt;&lt;p&gt;以下动作可能让表面症状消失，却破坏项目边界：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;删除 default-deny NetworkPolicy；&lt;/li&gt;
&lt;li&gt;给 CPA 临时开直接公网出站后忘记恢复；&lt;/li&gt;
&lt;li&gt;把 CPA Service 改成每 Pool LoadBalancer；&lt;/li&gt;
&lt;li&gt;给 cpa-manager 创建公网 Route；&lt;/li&gt;
&lt;li&gt;强删 Namespace 或 finalizer；&lt;/li&gt;
&lt;li&gt;手工覆盖 Credential Secret；&lt;/li&gt;
&lt;li&gt;直接修改 Operator 管理的 StatefulSet。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;更可靠的做法是记录具体 Condition、对象、Event 和时间点，修复负责该层的依赖，再验证状态是否自然收敛。&lt;/p&gt;
&lt;h2 id="最终验收清单"&gt;最终验收清单
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Operator 两个 Pod 正常，leader election 无持续错误；&lt;/li&gt;
&lt;li&gt;SubPool CRD 已安装，&lt;code&gt;kubectl get sp&lt;/code&gt; 可用；&lt;/li&gt;
&lt;li&gt;Operator Configuration 名称与 Deployment 参数一致；&lt;/li&gt;
&lt;li&gt;Shared Mihomo 有 Ready Endpoint；&lt;/li&gt;
&lt;li&gt;SubPool 派生 Namespace 所有权正确；&lt;/li&gt;
&lt;li&gt;runtime StatefulSet、PVC、两个 Service 均存在；&lt;/li&gt;
&lt;li&gt;Pod probes Ready，重启次数无异常增长；&lt;/li&gt;
&lt;li&gt;Internal 模式 port-forward 可用，或 Gateway 模式 Route Accepted 且 Gateway Programmed；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;metadata.generation == status.observedGeneration&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ready=True&lt;/code&gt; 的含义没有被误解为账号和真实模型请求也已验证。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;部署完成不是“apply 命令退出码为 0”，而是从声明、控制器、资源到数据路径都有证据证明系统达到了它承诺的状态。&lt;/p&gt;</description></item><item><title>Namespace、Label、Selector 与资源所有权</title><link>https://kerolt.github.io/p/namespacelabelselector-%E4%B8%8E%E8%B5%84%E6%BA%90%E6%89%80%E6%9C%89%E6%9D%83/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/namespacelabelselector-%E4%B8%8E%E8%B5%84%E6%BA%90%E6%89%80%E6%9C%89%E6%9D%83/</guid><description>&lt;h2 id="kubernetes-不靠目录组织资源"&gt;Kubernetes 不靠目录组织资源
&lt;/h2&gt;&lt;p&gt;传统服务器上，我们习惯用目录表达归属：这个程序在 &lt;code&gt;/opt/app-a&lt;/code&gt;，那个程序在 &lt;code&gt;/opt/app-b&lt;/code&gt;。Kubernetes 的资源不是文件，它主要靠 Namespace、Label、Selector 和 OwnerReference 建立边界与关系。&lt;/p&gt;
&lt;p&gt;这四个概念各管一件事：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;概念&lt;/th&gt;
					&lt;th&gt;回答的问题&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;Namespace&lt;/td&gt;
					&lt;td&gt;资源位于哪个隔离空间&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Label&lt;/td&gt;
					&lt;td&gt;资源带有什么可查询身份&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Selector&lt;/td&gt;
					&lt;td&gt;我想选择哪些资源&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;OwnerReference&lt;/td&gt;
					&lt;td&gt;这个资源由谁负责生命周期&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;它们经常一起出现，但不能相互替代。&lt;/p&gt;
&lt;h2 id="namespace资源名称的房间也是策略边界"&gt;Namespace：资源名称的房间，也是策略边界
&lt;/h2&gt;&lt;p&gt;本项目为每个 &lt;code&gt;SubPool&lt;/code&gt; 派生一个专属 Namespace：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;SubPool demo-pool → Namespace subpool-demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;SubPool team-a → Namespace subpool-team-a
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这样每个号池内部都可以使用固定名称：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpool-demo-pool/cpa
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpool-team-a/cpa
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;两个 Service 都叫 &lt;code&gt;cpa&lt;/code&gt;，因为它们位于不同 Namespace，不会冲突。&lt;/p&gt;
&lt;p&gt;Namespace 还是很多策略的作用域：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;ResourceQuota 限制整个 Namespace 的资源；&lt;/li&gt;
&lt;li&gt;NetworkPolicy 通常在 Namespace 内选择 Pod；&lt;/li&gt;
&lt;li&gt;Role 和 RoleBinding 可把权限约束在 Namespace；&lt;/li&gt;
&lt;li&gt;Secret、ConfigMap、Service 和 PVC 默认只能被同 Namespace 的 Pod 直接引用。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;不过 Namespace 不是虚拟机。多个 Namespace 的 Pod 仍可能运行在同一个 Node 上，共享同一个内核。它提供的是 API、命名和策略层隔离，真正的安全还依赖 RBAC、NetworkPolicy、Pod Security、容器隔离和底层运行时。&lt;/p&gt;
&lt;h2 id="cluster-scoped-与-namespaced"&gt;Cluster-scoped 与 namespaced
&lt;/h2&gt;&lt;p&gt;Kubernetes 对象分为两类。&lt;/p&gt;
&lt;p&gt;Cluster-scoped 对象全局存在，例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Namespace；&lt;/li&gt;
&lt;li&gt;Node；&lt;/li&gt;
&lt;li&gt;ClusterRole、ClusterRoleBinding；&lt;/li&gt;
&lt;li&gt;CRD；&lt;/li&gt;
&lt;li&gt;本项目的 &lt;code&gt;SubPool&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;GatewayClass。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Namespaced 对象属于某个 Namespace，例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Pod、Deployment、StatefulSet；&lt;/li&gt;
&lt;li&gt;Service、ConfigMap、Secret；&lt;/li&gt;
&lt;li&gt;PVC、NetworkPolicy、ResourceQuota；&lt;/li&gt;
&lt;li&gt;HTTPRoute；&lt;/li&gt;
&lt;li&gt;Gateway。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可以用 API discovery 查询某个资源是否 namespaced：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl api-resources
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl api-resources --namespaced&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nb"&gt;true&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl api-resources --namespaced&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nb"&gt;false&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;本项目故意把 &lt;code&gt;SubPool&lt;/code&gt; 设计为 cluster-scoped，因为一个 SubPool 会创建并拥有一个 Namespace。若它自己先被放进某个业务 Namespace，生命周期与权限边界会更绕。&lt;/p&gt;
&lt;h2 id="label贴在对象上的机器可读标签"&gt;Label：贴在对象上的机器可读标签
&lt;/h2&gt;&lt;p&gt;Label 是简短键值对。项目使用 Kubernetes 推荐风格的应用标签：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;labels&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;app.kubernetes.io/name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subpool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;app.kubernetes.io/component&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;runtime&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;app.kubernetes.io/part-of&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subpool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;subrelay.example.com/pool&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这些标签既方便人查询，也让 Service、NetworkPolicy 和 PDB 能精确选择目标。&lt;/p&gt;
&lt;p&gt;常用查询方式：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pods --show-labels
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pods &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l app.kubernetes.io/component&lt;span class="o"&gt;=&lt;/span&gt;runtime
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get namespaces &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l subrelay.example.com/managed&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nb"&gt;true&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Label 适合低基数、用于筛选的稳定信息。大段说明、时间戳、配置内容更适合 annotation，不应塞进 label。&lt;/p&gt;
&lt;h2 id="selector用标签建立松耦合关系"&gt;Selector：用标签建立松耦合关系
&lt;/h2&gt;&lt;p&gt;Selector 是查询条件。例如 CPA Service 并不记录某个 Pod 的名字，而是声明：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;selector&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;app.kubernetes.io/name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subpool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;app.kubernetes.io/component&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;runtime&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;subrelay.example.com/pool&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;符合这些标签的 Pod 就成为它的后端。Pod 被重建、名字和 IP 改变后，只要新 Pod 仍带相同标签，Service 仍能找到它。&lt;/p&gt;
&lt;p&gt;这就是 Kubernetes 常见的松耦合方式：对象不记住易变实例，而是描述“我需要哪一类实例”。&lt;/p&gt;
&lt;p&gt;同样的 selector 还被用在：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;StatefulSet 判断哪些 Pod 属于自己；&lt;/li&gt;
&lt;li&gt;PDB 判断哪些 Pod 受中断预算保护；&lt;/li&gt;
&lt;li&gt;NetworkPolicy 判断策略作用于哪些 Pod；&lt;/li&gt;
&lt;li&gt;Mihomo NetworkPolicy 判断哪些号池 Pod 可以访问代理端口。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="selector-配错会发生什么"&gt;Selector 配错会发生什么
&lt;/h2&gt;&lt;p&gt;Selector 错误通常不会产生明显语法错误，却会造成很真实的故障：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Service 选不到 Pod，于是 EndpointSlice 为空；&lt;/li&gt;
&lt;li&gt;Service 误选了其他 Pod，流量进入错误后端；&lt;/li&gt;
&lt;li&gt;PDB 选不到 Pod，实际上没有保护任何工作负载；&lt;/li&gt;
&lt;li&gt;NetworkPolicy 选错 Pod，可能该拦的没拦、该放的没放；&lt;/li&gt;
&lt;li&gt;StatefulSet 的 selector 与 template labels 不匹配时，API Server 会拒绝对象。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;排查 Service 时，应并排检查 selector 和 Pod labels：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get service cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pods --show-labels
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;cpa
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="ownerreference谁负责这个对象的生与死"&gt;OwnerReference：谁负责这个对象的生与死
&lt;/h2&gt;&lt;p&gt;Operator 创建资源时会写入 owner reference。概念上类似：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;metadata&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;ownerReferences&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="nt"&gt;apiVersion&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subrelay.example.com/v1alpha1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;SubPool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;uid&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;...&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;controller&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="kc"&gt;true&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这里最重要的是 UID，而不只是 name。即使旧的 &lt;code&gt;demo-pool&lt;/code&gt; 被删除后又创建了同名对象，新对象的 UID 也不同，不能自然接管旧对象。&lt;/p&gt;
&lt;p&gt;OwnerReference 有两个主要用途：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;让控制器判断对象是不是自己管理的；&lt;/li&gt;
&lt;li&gt;让 Kubernetes garbage collector 在 owner 删除后清理 dependents。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本项目对所有权非常保守。若 &lt;code&gt;subpool-demo-pool&lt;/code&gt; Namespace 已存在，但 owner reference 不属于当前 SubPool，Operator 报告 &lt;code&gt;NamespaceConflict&lt;/code&gt;，不会接管它。&lt;/p&gt;
&lt;p&gt;这不是“自动化不够聪明”，而是安全边界。仅凭同名就接管资源，可能误删别人的 Namespace 和数据。&lt;/p&gt;
&lt;h2 id="为什么-namespace-也能由-subpool-拥有"&gt;为什么 Namespace 也能由 SubPool 拥有
&lt;/h2&gt;&lt;p&gt;Namespaced owner 不能跨 Namespace 随意拥有其他 Namespace 的对象，但 cluster-scoped owner 可以拥有 namespaced 对象。本项目的 &lt;code&gt;SubPool&lt;/code&gt; 是 cluster-scoped，因此可以成为派生 Namespace 的 controller owner。&lt;/p&gt;
&lt;p&gt;具体生命周期是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;SubPool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ Namespace subpool-demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ Secret
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ StatefulSet
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ Service
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ PVC
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ NetworkPolicy 等
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;删除 SubPool 时，Operator 先明确删除自己拥有的 Namespace。Namespace 的删除又触发内部 namespaced 资源清理。&lt;/p&gt;
&lt;h2 id="所有权与名字一样是两回事"&gt;所有权与“名字一样”是两回事
&lt;/h2&gt;&lt;p&gt;项目中有两个典型冲突：&lt;/p&gt;
&lt;h3 id="namespaceconflict"&gt;NamespaceConflict
&lt;/h3&gt;&lt;p&gt;派生 Namespace 已存在，但不属于当前 SubPool。Operator 停止创建，不覆盖 labels，也不在删除 SubPool 时删除它。&lt;/p&gt;
&lt;h3 id="credentialconflict"&gt;CredentialConflict
&lt;/h3&gt;&lt;p&gt;&lt;code&gt;subpool-credentials&lt;/code&gt; Secret 已存在，但不由当前 SubPool 控制。Operator 不读取并当作自己的凭据，也不覆盖其内容。&lt;/p&gt;
&lt;p&gt;正确判断逻辑是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;名称符合预期 + owner UID 符合预期 + 对象结构符合约定
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;缺一项都不应盲目接管。&lt;/p&gt;
&lt;h2 id="namespace-label-还能连接跨-namespace-策略"&gt;Namespace label 还能连接跨 Namespace 策略
&lt;/h2&gt;&lt;p&gt;本项目给派生 Namespace 添加：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;subrelay.example.com/managed&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;true&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Shared Mihomo 的 NetworkPolicy 通过 &lt;code&gt;namespaceSelector&lt;/code&gt; 只允许这些受管 Namespace 中的 CPA Pod访问 18000 端口。&lt;/p&gt;
&lt;p&gt;Shared Envoy Gateway 的 listener 也用相同 Namespace label 限制哪些 Namespace 可以绑定 HTTPRoute：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;allowedRoutes&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;namespaces&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;from&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;Selector&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;selector&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;matchLabels&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;subrelay.example.com/managed&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;true&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这是一种很漂亮的“共享基础设施准入证”：Operator 创建并标记受管 Namespace，共享网关只接受带这个标记的租户。&lt;/p&gt;
&lt;h2 id="pod-security-也通过-namespace-label-启用"&gt;Pod Security 也通过 Namespace label 启用
&lt;/h2&gt;&lt;p&gt;Operator Configuration 还会把这些标签投影到每个号池 Namespace：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;pod-security.kubernetes.io/enforce&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;restricted&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;pod-security.kubernetes.io/audit&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;restricted&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;pod-security.kubernetes.io/warn&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;restricted&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它们启用 Pod Security Admission 的 restricted 级别。若 Pod 配置违反规则，&lt;code&gt;enforce&lt;/code&gt; 会阻止创建，&lt;code&gt;audit&lt;/code&gt; 和 &lt;code&gt;warn&lt;/code&gt; 则留下审计或警告信号。&lt;/p&gt;
&lt;p&gt;因此 Namespace 不只是“分类文件夹”，还是安全策略和准入策略的重要挂载点。&lt;/p&gt;
&lt;h2 id="实用排查顺序"&gt;实用排查顺序
&lt;/h2&gt;&lt;p&gt;遇到“资源明明存在，但系统不工作”时，可以按下面检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get namespace subpool-demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get all --show-labels
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get service cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;重点回答：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Namespace 是否正确派生并带有 managed label？&lt;/li&gt;
&lt;li&gt;子资源的 owner UID 是否指向当前 SubPool？&lt;/li&gt;
&lt;li&gt;Service、PDB、NetworkPolicy 的 selector 是否与 Pod label 完全匹配？&lt;/li&gt;
&lt;li&gt;有没有同名但不属于当前 SubPool 的遗留资源？&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;理解这些关系后，Kubernetes 就不再是一堆相互独立的 YAML，而是一张由作用域、标签选择和生命周期所有权连接起来的对象图。&lt;/p&gt;</description></item><item><title>NetworkPolicy 与 Mihomo 代理数据链路</title><link>https://kerolt.github.io/p/networkpolicy-%E4%B8%8E-mihomo-%E4%BB%A3%E7%90%86%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/networkpolicy-%E4%B8%8E-mihomo-%E4%BB%A3%E7%90%86%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF/</guid><description>&lt;h2 id="默认互通很方便也很危险"&gt;默认互通很方便，也很危险
&lt;/h2&gt;&lt;p&gt;在没有 NetworkPolicy 时，多数 Kubernetes 网络实现允许 Pod 之间直接通信。对开发环境很省事，但一个 Pod 被攻破后，攻击者也更容易扫描和访问其他服务。&lt;/p&gt;
&lt;p&gt;本项目采用“默认拒绝，再按需要放行”的思路：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;先把门全部关上
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 再开放 CPA/Manager 入站端口
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 只允许 runtime 出站到 DNS 与 Shared Mihomo
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 只允许受管 Pool 的 CPA 访问 Mihomo
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Mihomo 只能访问 DNS 和公共地址
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;NetworkPolicy 描述允许的流量，不是按顺序执行的防火墙脚本。&lt;/p&gt;
&lt;h2 id="前提cni-必须真正实现-networkpolicy"&gt;前提：CNI 必须真正实现 NetworkPolicy
&lt;/h2&gt;&lt;p&gt;API Server 会接受合法的 NetworkPolicy 对象，但是否执行取决于集群网络插件。若 CNI 不支持 NetworkPolicy，策略对象可能看起来都存在，流量却没有被拦截。&lt;/p&gt;
&lt;p&gt;因此部署前要确认集群的 CNI 和策略能力。不要只用 &lt;code&gt;kubectl get networkpolicy&lt;/code&gt; 证明隔离已经生效，还应做有界连通性验证。&lt;/p&gt;
&lt;h2 id="default-deny-如何生效"&gt;default-deny 如何生效
&lt;/h2&gt;&lt;p&gt;每个号池 Namespace 有一个策略，选择带当前 Pool label 的所有 Pod，并同时声明：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;policyTypes&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;Ingress&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;Egress&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;但不提供任何 ingress 或 egress allow rule。这会让选中的 Pod 在两个方向上进入隔离状态。&lt;/p&gt;
&lt;p&gt;随后另一个 runtime NetworkPolicy 添加允许规则。NetworkPolicy 是可加和的：只要任一适用策略允许某段流量，该段流量就被允许。不同策略不会按文件顺序互相覆盖。&lt;/p&gt;
&lt;h2 id="runtime-pod-的入站规则"&gt;runtime Pod 的入站规则
&lt;/h2&gt;&lt;p&gt;项目允许 TCP 端口：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;8317 → CPA
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;18317 → cpa-manager
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;当前规则只限制端口，没有填写 &lt;code&gt;from&lt;/code&gt;，因此从 NetworkPolicy 语义看，任何能够路由到该 Pod 的来源都可以访问这两个端口。&lt;/p&gt;
&lt;p&gt;这不等于互联网能直接访问，因为 Pod 仍没有公网入口，Service 也是 ClusterIP；但集群内其他可达工作负载原则上可能连接这两个端口。&lt;/p&gt;
&lt;p&gt;如果未来需要让 cpa-manager 只接受特定管理员跳板 Pod，应继续收紧 &lt;code&gt;from&lt;/code&gt;，而不是把 ClusterIP 当作完整的身份认证边界。&lt;/p&gt;
&lt;h2 id="runtime-pod-的出站规则"&gt;runtime Pod 的出站规则
&lt;/h2&gt;&lt;p&gt;只允许两类目的地。&lt;/p&gt;
&lt;p&gt;第一类是 kube-system 中带 &lt;code&gt;k8s-app: kube-dns&lt;/code&gt; 标签的 DNS Pod，端口为 UDP/TCP 53。&lt;/p&gt;
&lt;p&gt;第二类是 Operator Configuration 指定 Namespace 和 pod selector 的 Mihomo Pod，端口为 TCP 18000。&lt;/p&gt;
&lt;p&gt;因此 CPA 不能直接访问任意公网地址，正常出站必须走：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;CPA
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → mihomo-gateway.subpool-gateway.svc:18000
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Shared Mihomo Pod
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 上游代理
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 外部目标
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这把代理策略集中到共享数据面，避免每个账号池保存代理订阅和节点凭据。&lt;/p&gt;
&lt;h2 id="为什么-dns-必须单独放行"&gt;为什么 DNS 必须单独放行
&lt;/h2&gt;&lt;p&gt;NetworkPolicy 一旦隔离 egress，DNS 查询也会被阻止。应用即使只想连接一个允许的 Service DNS 名称，也必须先解析它。&lt;/p&gt;
&lt;p&gt;本项目同时允许 UDP 53 和 TCP 53。普通 DNS 查询常用 UDP，但响应过大、重试或特定场景会使用 TCP，只开 UDP 可能产生难以复现的问题。&lt;/p&gt;
&lt;p&gt;策略假设集群 DNS Pod 位于 &lt;code&gt;kube-system&lt;/code&gt;，并带 &lt;code&gt;k8s-app: kube-dns&lt;/code&gt; 标签。不同发行版若使用不同标签，应同步修改 Operator 逻辑或平台配置，否则所有 Pool 都会出现 DNS 故障。&lt;/p&gt;
&lt;h2 id="shared-mihomo-的入站边界"&gt;Shared Mihomo 的入站边界
&lt;/h2&gt;&lt;p&gt;Mihomo NetworkPolicy 只允许：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Namespace 带 &lt;code&gt;subrelay.example.com/managed=true&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;Pod 带 CPA 组件相关 label；&lt;/li&gt;
&lt;li&gt;目标端口为 TCP 18000。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;namespaceSelector&lt;/code&gt; 与 &lt;code&gt;podSelector&lt;/code&gt; 出现在同一个 peer 中时，是“Namespace 条件 AND Pod 条件”，不是 OR。&lt;/p&gt;
&lt;p&gt;这阻止普通 Namespace 的 Pod 随意把 Shared Mihomo 当公共代理使用，也阻止受管 Namespace 中没有正确身份标签的 Pod访问它。&lt;/p&gt;
&lt;h2 id="shared-mihomo-的出站边界"&gt;Shared Mihomo 的出站边界
&lt;/h2&gt;&lt;p&gt;Mihomo 可以访问 DNS，也可以访问 &lt;code&gt;0.0.0.0/0&lt;/code&gt; 中排除私有、loopback、link-local、组播和保留地址后的公网 IPv4。&lt;/p&gt;
&lt;p&gt;这种设计意图是：允许代理连接外部节点和目标，同时降低它访问集群内网、云元数据地址和私网服务的风险。&lt;/p&gt;
&lt;p&gt;但它不是完整的出站安全产品：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;这里只展示 IPv4 规则，配置也关闭了 IPv6；&lt;/li&gt;
&lt;li&gt;公网地址仍是很大的范围；&lt;/li&gt;
&lt;li&gt;域名最终解析到什么 IP 会变化；&lt;/li&gt;
&lt;li&gt;NetworkPolicy 通常是 L3/L4 控制，不理解 HTTP 用户或 URL；&lt;/li&gt;
&lt;li&gt;CNI 对 ipBlock 与集群 NAT 的具体行为需要实测。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;若威胁模型更严格，可在专用 egress gateway、代理认证、域名策略和审计层继续加固。&lt;/p&gt;
&lt;h2 id="networkpolicy-同时检查两端"&gt;NetworkPolicy 同时检查两端
&lt;/h2&gt;&lt;p&gt;一次连接要成功，源 Pod 的 egress 和目标 Pod 的 ingress 都必须允许：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runtime egress 允许 Mihomo
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; AND
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Mihomo ingress 允许 runtime
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ↓
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;连接才成功
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;只检查其中一侧，会看到“规则明明写了却还是不通”的现象。&lt;/p&gt;
&lt;h2 id="mihomo-配置在做什么"&gt;Mihomo 配置在做什么
&lt;/h2&gt;&lt;p&gt;Shared Mihomo 从 Secret 挂载的 &lt;code&gt;proxies.yaml&lt;/code&gt; 读取节点，启用 health check，并建立一个代理组：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;type&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;load-balance&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;strategy&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;round-robin&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;然后在 18000 端口提供 HTTP proxy listener。CPA 只知道这个统一地址，不知道上游节点凭据。&lt;/p&gt;
&lt;p&gt;这种设计有三个好处：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;节点和订阅只集中保存在一个共享 Secret；&lt;/li&gt;
&lt;li&gt;Pool 无需因代理节点变化而修改 CR；&lt;/li&gt;
&lt;li&gt;Operator 的 status、日志和 Event 不接触代理秘密。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="round-robin-为什么不等于每个请求换-ip"&gt;round-robin 为什么不等于每个请求换 IP
&lt;/h2&gt;&lt;p&gt;Mihomo 的负载选择以新建出站连接为重要粒度。HTTP keep-alive、HTTP/2 多路复用或应用连接池可能让多个请求复用同一条连接，因此连续请求可能使用相同出口。&lt;/p&gt;
&lt;p&gt;另外两个 Mihomo Pod 各自维护选择状态，没有跨 Pod 共享一个全局计数器。&lt;/p&gt;
&lt;p&gt;所以项目只承诺：新连接会在健康节点集合中按策略选择。它不承诺：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每个 HTTP 请求切换 IP；&lt;/li&gt;
&lt;li&gt;所有 Gateway Pod 之间全局严格轮转；&lt;/li&gt;
&lt;li&gt;某个账号固定到某个出口；&lt;/li&gt;
&lt;li&gt;存在 primary/candidate 主备关系。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="service-ready-也不等于代理链路端到端成功"&gt;Service Ready 也不等于代理链路端到端成功
&lt;/h2&gt;&lt;p&gt;Operator 通过 EndpointSlice 判断 Mihomo 至少有一个 Ready Pod。这只能证明 Kubernetes 层有可用代理 Endpoint。&lt;/p&gt;
&lt;p&gt;以下问题仍可能存在：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;proxies.yaml&lt;/code&gt; 内容错误；&lt;/li&gt;
&lt;li&gt;健康检查目标不可达；&lt;/li&gt;
&lt;li&gt;上游代理认证失败；&lt;/li&gt;
&lt;li&gt;代理可用但目标模型服务不可用；&lt;/li&gt;
&lt;li&gt;网络策略或云安全组阻断实际公网路径。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此 &lt;code&gt;ProxyGatewayReady=True&lt;/code&gt; 是基础设施信号，不是业务请求成功率指标。&lt;/p&gt;
&lt;h2 id="排查流量时按跳点验证"&gt;排查流量时按跳点验证
&lt;/h2&gt;&lt;p&gt;不要一句“网络不通”就开始改策略。按链路分段：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;1. runtime Pod 能否解析 Mihomo Service DNS
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;2. Mihomo Service 是否有 Ready EndpointSlice
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;3. runtime egress 是否选中正确 Mihomo Pod
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;4. Mihomo ingress 是否选中正确 Pool Namespace 与 Pod
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;5. Mihomo Pod 自身是否 Ready
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;6. Mihomo egress 是否允许目标地址
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;7. 上游代理与最终业务请求是否成功
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;常用只读对象检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get networkpolicy -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get networkpolicy -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;mihomo-gateway -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway logs deployment/mihomo-gateway &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --tail&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;100&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;不要为了“先通再说”永久删除 default-deny 或放开 CPA 直连公网。那会绕过项目最核心的代理与秘密边界，并把真正故障藏起来。&lt;/p&gt;</description></item><item><title>Pod 多容器、InitContainer 与共享 Volume</title><link>https://kerolt.github.io/p/pod-%E5%A4%9A%E5%AE%B9%E5%99%A8initcontainer-%E4%B8%8E%E5%85%B1%E4%BA%AB-volume/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/pod-%E5%A4%9A%E5%AE%B9%E5%99%A8initcontainer-%E4%B8%8E%E5%85%B1%E4%BA%AB-volume/</guid><description>&lt;h2 id="pod-才是-kubernetes-的最小调度单位"&gt;Pod 才是 Kubernetes 的最小调度单位
&lt;/h2&gt;&lt;p&gt;Kubernetes 不直接把单个容器调度到 Node，而是调度 Pod。一个 Pod 可以包含一个或多个容器，这些容器会被当作一个整体：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;被调度到同一台 Node；&lt;/li&gt;
&lt;li&gt;共享同一个网络命名空间；&lt;/li&gt;
&lt;li&gt;拥有相同的 Pod IP；&lt;/li&gt;
&lt;li&gt;可以通过 Volume 共享文件；&lt;/li&gt;
&lt;li&gt;一起创建，也一起消失。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本项目每个号池只有一个 runtime Pod，但里面同时运行：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ initContainer: initialize-config
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ container: cpa
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ container: cpa-manager
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这不是三个 Pod，而是一个 Pod 中的一个初始化容器和两个长期运行容器。&lt;/p&gt;
&lt;h2 id="为什么-cpa-与-cpa-manager-放在同一个-pod"&gt;为什么 CPA 与 cpa-manager 放在同一个 Pod
&lt;/h2&gt;&lt;p&gt;cpa-manager 需要紧密访问 CPA，并与它共享持久数据。放在同一个 Pod 后，二者可以通过 loopback 通信：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa-manager → http://127.0.0.1:8317 → CPA
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这里不经过 Service、集群 DNS 或 CNI 网络。对 cpa-manager 来说，CPA 就像运行在同一台机器上的另一个本地进程。&lt;/p&gt;
&lt;p&gt;共享 Pod 的好处：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;本地通信简单，延迟低；&lt;/li&gt;
&lt;li&gt;两个容器一起调度，不会出现 Manager 在 A 节点、CPA 在 B 节点；&lt;/li&gt;
&lt;li&gt;可以自然共享同一个 RWO PVC；&lt;/li&gt;
&lt;li&gt;生命周期耦合关系与业务现实一致。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;代价也很明确：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;两者不能独立扩缩容；&lt;/li&gt;
&lt;li&gt;Pod 重建时二者一起重启；&lt;/li&gt;
&lt;li&gt;任一容器一直不 Ready，整个 Pod 就不 Ready；&lt;/li&gt;
&lt;li&gt;两个容器的资源总和决定这个 Pod 的调度需求。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此“多容器 Pod”适合强耦合进程，不应只为了少写一个 Deployment 就随便合并。&lt;/p&gt;
&lt;h2 id="共享网络不等于共享端口"&gt;共享网络不等于共享端口
&lt;/h2&gt;&lt;p&gt;同一个 Pod 的容器共享 IP 和端口空间。CPA 监听 &lt;code&gt;8317&lt;/code&gt;，cpa-manager 监听 &lt;code&gt;18317&lt;/code&gt;：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Pod IP:8317 → CPA
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Pod IP:18317 → cpa-manager
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;因为端口空间共享，两个容器不能同时监听同一个 IP 和端口。若都尝试监听 &lt;code&gt;0.0.0.0:8317&lt;/code&gt;，后启动的进程会因为端口被占用而失败。&lt;/p&gt;
&lt;p&gt;容器在 YAML 中声明 &lt;code&gt;containerPort&lt;/code&gt;，主要用于表达和命名端口，并不会像 Docker 的 &lt;code&gt;-p&lt;/code&gt; 那样自动把它暴露到集群外。真正的稳定入口由 Service 提供。&lt;/p&gt;
&lt;h2 id="initcontainer开店前先布置柜台"&gt;InitContainer：开店前先布置柜台
&lt;/h2&gt;&lt;p&gt;InitContainer 在普通容器启动前依次执行。只有所有 InitContainer 成功退出，主容器才会启动。&lt;/p&gt;
&lt;p&gt;本项目的 &lt;code&gt;initialize-config&lt;/code&gt; 负责：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在共享数据卷中创建目录；&lt;/li&gt;
&lt;li&gt;从 Secret 挂载文件读取凭据；&lt;/li&gt;
&lt;li&gt;根据 Operator Configuration 注入的 Mihomo URL 生成 CPA 配置；&lt;/li&gt;
&lt;li&gt;只在配置文件不存在时写入，避免每次重启覆盖运行数据。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;关键逻辑可以概括为：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;if&lt;/span&gt; &lt;span class="o"&gt;[&lt;/span&gt; ! -f /data/cpa/config.yaml &lt;span class="o"&gt;]&lt;/span&gt;&lt;span class="p"&gt;;&lt;/span&gt; &lt;span class="k"&gt;then&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="c1"&gt;# 读取凭据并创建配置&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;fi&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这叫幂等初始化：执行一次和重复执行多次，最终结果一致。Pod 因节点维护被重建时，PVC 上已有配置不会被覆盖。&lt;/p&gt;
&lt;p&gt;InitContainer 失败时，主容器不会启动。排查时可查看：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool logs runtime-0 &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -c initialize-config
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;不要只看 &lt;code&gt;cpa&lt;/code&gt; 容器日志；如果初始化阶段就失败，它甚至还没开始运行。&lt;/p&gt;
&lt;h2 id="volumepod-内的文件连接器"&gt;Volume：Pod 内的文件连接器
&lt;/h2&gt;&lt;p&gt;容器文件系统默认是临时的。Volume 让 Pod 把外部数据源或共享存储挂进容器。&lt;/p&gt;
&lt;p&gt;本项目 runtime Pod 使用三种 Volume：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;Volume&lt;/th&gt;
					&lt;th&gt;来源&lt;/th&gt;
					&lt;th&gt;用途&lt;/th&gt;
					&lt;th&gt;是否持久&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;data&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;StatefulSet 的 PVC&lt;/td&gt;
					&lt;td&gt;CPA 与 Manager 数据&lt;/td&gt;
					&lt;td&gt;是&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;credentials&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Secret&lt;/td&gt;
					&lt;td&gt;三个凭据文件&lt;/td&gt;
					&lt;td&gt;随 Secret 存在&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;bootstrap&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;ConfigMap&lt;/td&gt;
					&lt;td&gt;初始化脚本&lt;/td&gt;
					&lt;td&gt;随 ConfigMap 存在&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Volume 定义“数据从哪里来”，&lt;code&gt;volumeMounts&lt;/code&gt; 定义“在某个容器中挂到哪里”。同一个 Volume 可以在不同容器里挂到不同路径。&lt;/p&gt;
&lt;h2 id="一个-pvc如何避免两个容器互相踩文件"&gt;一个 PVC，如何避免两个容器互相踩文件
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;data&lt;/code&gt; PVC 在 InitContainer 中挂载到 &lt;code&gt;/data&lt;/code&gt;。主容器则使用 &lt;code&gt;subPath&lt;/code&gt; 把其中不同部分挂到各自路径：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;span class="lnt"&gt;8
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;PVC 根目录
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ cpa/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ ├─ config.yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ ├─ auths/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ └─ logs/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ cpa-manager/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─ usage.sqlite
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ data.key
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;CPA 获得：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa/config.yaml → /CLIProxyAPI/config.yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa/auths → /CLIProxyAPI/auths
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa/logs → /CLIProxyAPI/logs
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;cpa-manager 获得：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa-manager → /data
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这样既共享同一个 PVC，又保持目录边界清楚。&lt;/p&gt;
&lt;p&gt;需要注意：&lt;code&gt;subPath&lt;/code&gt; 不是权限隔离机制。容器是否能看到其他目录取决于挂载方式，但底层仍是同一个卷；真正的安全还依赖进程身份、文件权限和应用设计。&lt;/p&gt;
&lt;h2 id="secret-与-configmap-为什么挂成文件"&gt;Secret 与 ConfigMap 为什么挂成文件
&lt;/h2&gt;&lt;p&gt;本项目把凭据投影为 &lt;code&gt;/run/secrets/...&lt;/code&gt; 下的文件，只选择约定的 key：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa_client_key
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpa_management_key
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;cpamp_admin_key
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;相较于直接把敏感值写进命令参数，文件方式能减少它出现在进程参数、YAML 和日志中的机会。初始化脚本读取后把必要内容写入权限受限的持久配置。&lt;/p&gt;
&lt;p&gt;ConfigMap 中的初始化脚本则以只读、可执行模式挂到 &lt;code&gt;/bootstrap/initialize.sh&lt;/code&gt;。普通配置和敏感凭据被明确分开：脚本可以进 Git，Secret value 不可以。&lt;/p&gt;
&lt;h2 id="pod-securitycontext-与-container-securitycontext"&gt;Pod SecurityContext 与 Container SecurityContext
&lt;/h2&gt;&lt;p&gt;Pod 级配置决定共同的进程和文件身份：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runAsNonRoot: true
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runAsUser: 65532
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runAsGroup: 65532
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;fsGroup: 65532
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;seccompProfile: RuntimeDefault
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;容器级配置进一步限制：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;allowPrivilegeEscalation: false
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;capabilities.drop: [ALL]
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runAsNonRoot: true
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;fsGroup&lt;/code&gt; 很重要：PVC 挂载后，kubelet 会尽量让卷中的文件对这个组可访问，否则非 root 容器可能无法写入数据卷。&lt;/p&gt;
&lt;p&gt;安全配置不是“字段越多越安全”。它必须与镜像实际行为匹配。如果应用硬编码要求 root 或尝试写只读目录，Pod 会启动失败。正确做法是修正镜像和写路径，而不是第一时间取消安全约束。&lt;/p&gt;
&lt;h2 id="terminationgraceperiodseconds-的意义"&gt;terminationGracePeriodSeconds 的意义
&lt;/h2&gt;&lt;p&gt;项目为 runtime Pod 设置 30 秒优雅终止时间。Pod 被删除时，大致经历：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;kubelet 向容器主进程发送 &lt;code&gt;SIGTERM&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;应用有机会停止接收请求、刷新文件、关闭数据库；&lt;/li&gt;
&lt;li&gt;最多等待 30 秒；&lt;/li&gt;
&lt;li&gt;仍未退出时才会被强制终止。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;对于使用 SQLite 或持久配置的应用，优雅退出比无状态 HTTP 服务更重要。应用也必须正确处理 &lt;code&gt;SIGTERM&lt;/code&gt;，否则 YAML 中的等待时间只是空等。&lt;/p&gt;
&lt;h2 id="镜像拉取策略"&gt;镜像拉取策略
&lt;/h2&gt;&lt;p&gt;本项目按镜像字符串选择策略：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;以 &lt;code&gt;:latest&lt;/code&gt; 结尾：&lt;code&gt;Always&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;其他标签或 digest：&lt;code&gt;IfNotPresent&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;开发时 &lt;code&gt;latest&lt;/code&gt; 方便快速替换，生产更推荐不可变 digest：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;registry.example/cpa@sha256:...
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;同一个 tag 的内容可能变化，导致重建后的 Pod 与之前运行的并非同一版本。digest 把“我想运行哪个镜像”变成可审计的精确声明。&lt;/p&gt;
&lt;h2 id="如何观察多容器-pod"&gt;如何观察多容器 Pod
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool logs runtime-0 -c cpa --tail&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;100&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool logs runtime-0 -c cpa-manager --tail&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;100&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool logs runtime-0 &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -c initialize-config --previous
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;kubectl logs&lt;/code&gt; 面对多容器 Pod 时应明确 &lt;code&gt;-c&lt;/code&gt;。&lt;code&gt;--previous&lt;/code&gt; 用于查看同一个容器上一次崩溃实例的日志，对 CrashLoopBackOff 很有用。&lt;/p&gt;
&lt;p&gt;还可以查看每个容器的状态：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pod runtime-0 &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -o &lt;span class="nv"&gt;jsonpath&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s1"&gt;&amp;#39;{range .status.containerStatuses[*]}{.name}{&amp;#34;\t&amp;#34;}{.ready}{&amp;#34;\t&amp;#34;}{.restartCount}{&amp;#34;\n&amp;#34;}{end}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="一个容易误解的地方两个容器不等于高可用"&gt;一个容易误解的地方：两个容器不等于高可用
&lt;/h2&gt;&lt;p&gt;CPA 与 cpa-manager 在同一个 Pod 中，是为了协作和共享状态，不是冗余副本。这个 StatefulSet 的 replicas 是 1，PVC 又是 RWO，所以数据面仍是单实例。&lt;/p&gt;
&lt;p&gt;Operator 自己运行两个副本并通过 leader election 提供控制面接管能力，但那不会把号池 runtime 自动变成双活。控制面高可用和数据面高可用必须分开讨论。&lt;/p&gt;
&lt;p&gt;理解 Pod 的关键不是记住字段，而是看清“哪些进程必须同生共死、共享什么、通过什么通信”。本项目把 CPA 和 cpa-manager 放进同一个 Pod，正是对这种强耦合关系的明确表达。&lt;/p&gt;</description></item><item><title>Requests、Limits、ResourceQuota 与 PDB</title><link>https://kerolt.github.io/p/requestslimitsresourcequota-%E4%B8%8E-pdb/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/requestslimitsresourcequota-%E4%B8%8E-pdb/</guid><description>&lt;h2 id="资源配置不是装饰它决定能否调度和如何失效"&gt;资源配置不是装饰，它决定能否调度和如何失效
&lt;/h2&gt;&lt;p&gt;本项目要求 CPA 与 cpa-manager 都明确填写 CPU、内存的 requests 和 limits：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;resources&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;requests&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;200m&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;256Mi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;limits&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;300m&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;512Mi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这四个值既影响 Scheduler，也影响容器运行时和 Namespace 配额。它们不是“给监控看的备注”。&lt;/p&gt;
&lt;h2 id="requests我至少要预留多少"&gt;requests：我至少要预留多少
&lt;/h2&gt;&lt;p&gt;Scheduler 主要根据 requests 判断 Node 是否放得下 Pod。&lt;/p&gt;
&lt;p&gt;CPU &lt;code&gt;200m&lt;/code&gt; 表示 0.2 个 CPU core 的请求。&lt;code&gt;1000m&lt;/code&gt; 等于 1 core。&lt;/p&gt;
&lt;p&gt;内存 &lt;code&gt;256Mi&lt;/code&gt; 使用二进制单位：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;1 Mi = 1024 × 1024 bytes
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;1 Gi = 1024 Mi
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;一个 Pod 中有多个普通容器时，调度通常考虑它们 requests 的总和。本项目 runtime Pod 同时运行 CPA 与 cpa-manager，因此号池的基础计算需求是二者之和。&lt;/p&gt;
&lt;p&gt;InitContainer 按顺序运行，Kubernetes 会把普通容器总和与单个 InitContainer 的最大值进行有效资源计算。本项目初始化容器复用 CPA 的资源声明，而普通容器总和包含 CPA 加 Manager，因此有效值仍由普通容器总和主导。&lt;/p&gt;
&lt;p&gt;requests 太低的后果不是“应用只能用这么多”，而是集群可能在一台 Node 上塞入过多 Pod，压力出现时应用性能剧烈抖动或被驱逐。&lt;/p&gt;
&lt;h2 id="limits最多允许使用多少"&gt;limits：最多允许使用多少
&lt;/h2&gt;&lt;p&gt;CPU 超过 limit 时通常被 throttling，也就是被限制执行时间，不会只因为超过 CPU limit 就直接杀死容器。&lt;/p&gt;
&lt;p&gt;内存不同。容器实际内存超过 limit 时，可能被 OOM kill，然后由 kubelet 按 restart policy 重启。&lt;/p&gt;
&lt;p&gt;因此：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;CPU limit 太紧 → 延迟变高、吞吐下降、探针可能超时
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;内存 limit 太紧 → OOMKilled、容器重启、CrashLoopBackOff
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;查看上一次终止原因：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pod runtime-0 &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -o &lt;span class="nv"&gt;jsonpath&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s1"&gt;&amp;#39;{range .status.containerStatuses[*]}{.name}{&amp;#34;\t&amp;#34;}{.lastState.terminated.reason}{&amp;#34;\n&amp;#34;}{end}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="requests-与-limits-的关系"&gt;requests 与 limits 的关系
&lt;/h2&gt;&lt;p&gt;本项目校验：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;值必须大于 0；&lt;/li&gt;
&lt;li&gt;只能声明 CPU 和内存；&lt;/li&gt;
&lt;li&gt;requests 和 limits 都必须完整；&lt;/li&gt;
&lt;li&gt;limit 不能小于 request。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;例如：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;requests.cpu = 500m
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;limits.cpu = 1
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;调度时按 0.5 core 预留，运行时最多允许使用约 1 core。&lt;/p&gt;
&lt;p&gt;若 request 与 limit 相差很大，集群可以提高利用率，但资源争抢时性能更不稳定。数值应基于真实监控调整，而不是照抄示例。&lt;/p&gt;
&lt;h2 id="qos-class压力下谁更容易受影响"&gt;QoS Class：压力下谁更容易受影响
&lt;/h2&gt;&lt;p&gt;Kubernetes 根据每个容器的 requests/limits 给 Pod 分 QoS class：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Guaranteed&lt;/code&gt;：所有容器的 CPU、内存 request 都等于对应 limit；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Burstable&lt;/code&gt;：至少声明了一些 request/limit，但不满足 Guaranteed；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BestEffort&lt;/code&gt;：完全没有声明 request/limit。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;项目要求完整声明资源，但示例中 request 与 limit 不相等，因此 runtime Pod 通常是 Burstable，而不是 Guaranteed。&lt;/p&gt;
&lt;p&gt;QoS 会影响节点内存压力下的驱逐优先级，但它不是唯一因素，实际还要看使用量相对 request、优先级和节点状态。&lt;/p&gt;
&lt;h2 id="operator-的资源包络"&gt;Operator 的资源包络
&lt;/h2&gt;&lt;p&gt;Operator Configuration 定义单 Pool 最大值：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;span class="lnt"&gt;8
&lt;/span&gt;&lt;span class="lnt"&gt;9
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;resourcePolicy&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;maxPerPool&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;requests&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;3&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;4Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;limits&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;4&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;8Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;storage&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;20Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Operator 在创建 Namespace、PVC 和 Secret 等持久资源之前，先计算 CPA 与 cpa-manager 的合计申请。超出包络时报告 &lt;code&gt;ResourcesExceedBaseline&lt;/code&gt;，不创建半套资源。&lt;/p&gt;
&lt;p&gt;这个顺序很重要。若先创建 PVC 和 Secret，最后才发现 CPU 超限，集群里会留下难以解释的半成品。&lt;/p&gt;
&lt;h2 id="resourcequota给-namespace-装上总闸"&gt;ResourceQuota：给 Namespace 装上总闸
&lt;/h2&gt;&lt;p&gt;每个号池 Namespace 都会得到一个 ResourceQuota，hard limits 来自平台最大包络，而不是当前 CR 的实际申请：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;requests.cpu
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;requests.memory
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;limits.cpu
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;limits.memory
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;requests.storage
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;persistentvolumeclaims: 1
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;为什么不直接把 Quota 设成当前申请量？因为用户可能在允许范围内调整 CPU 和内存。如果 Quota 只等于旧值，更新工作负载时可能先被 Quota 阻止，控制器还要同步处理两个互相约束的对象。&lt;/p&gt;
&lt;p&gt;使用平台最大包络后：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;SubPool spec 决定当前想用多少
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Operator 校验决定申请是否合法
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;ResourceQuota 防止 Namespace 内总用量突破平台上限
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这是应用层校验和 Kubernetes 原生硬限制的双层防线。&lt;/p&gt;
&lt;h2 id="为什么没有-limitrange"&gt;为什么没有 LimitRange
&lt;/h2&gt;&lt;p&gt;LimitRange 常用于给未声明资源的容器补默认值，或限制单个容器的范围。本项目要求 SubPool 明确声明每个容器的 requests/limits，Operator 又负责校验并投影，所以不需要 LimitRange 自动补值。&lt;/p&gt;
&lt;p&gt;显式输入有两个好处：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;用户提交的 CR 就能看出真实资源意图；&lt;/li&gt;
&lt;li&gt;不会因不同 Namespace 的默认值而产生隐式差异。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;控制器还会删除属于本 Pool 的旧版 LimitRange，避免遗留策略与新契约冲突。&lt;/p&gt;
&lt;h2 id="poddisruptionbudget-在保护什么"&gt;PodDisruptionBudget 在保护什么
&lt;/h2&gt;&lt;p&gt;项目为 runtime 和 Operator、Mihomo 各配置了 PDB，常见形式是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;minAvailable&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;PDB 限制的是自愿中断中的并发不可用数，例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;管理员执行 node drain；&lt;/li&gt;
&lt;li&gt;集群自动缩容驱逐 Pod；&lt;/li&gt;
&lt;li&gt;某些受 eviction API 管理的维护操作。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它告诉执行驱逐的组件：“至少保留一个满足 selector 的可用 Pod。”&lt;/p&gt;
&lt;h2 id="pdb-不是什么"&gt;PDB 不是什么
&lt;/h2&gt;&lt;p&gt;PDB 不会：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;创建额外副本；&lt;/li&gt;
&lt;li&gt;阻止 Node 突然宕机；&lt;/li&gt;
&lt;li&gt;阻止容器崩溃；&lt;/li&gt;
&lt;li&gt;阻止用户直接删除 Pod 或控制器；&lt;/li&gt;
&lt;li&gt;修复探针或应用故障；&lt;/li&gt;
&lt;li&gt;让一个 RWO 单副本应用自动变成高可用。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;runtime 只有一个 replica，&lt;code&gt;minAvailable: 1&lt;/code&gt; 意味着 drain 可能无法驱逐它，除非使用者处理预算或接受中断。它更像“维护刹车”，不是“永不断线保险”。&lt;/p&gt;
&lt;p&gt;Operator 与 Mihomo 各有两个副本，PDB 才能在自愿中断时允许一个副本离开、同时保留另一个可用。但 Operator 还需要 leader election，Mihomo 两个副本则各自维护连接选择状态。&lt;/p&gt;
&lt;h2 id="resourcequota-与实际-node-容量是两回事"&gt;ResourceQuota 与实际 Node 容量是两回事
&lt;/h2&gt;&lt;p&gt;Quota 允许一个 Pool 请求 3 CPU，不代表集群此时一定有 Node 能提供 3 CPU。Quota 是“最多准许申请多少”，Scheduler 仍要根据实际 Node 可分配资源决定能否调度。&lt;/p&gt;
&lt;p&gt;因此 Pod Pending 时要区分：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;API 创建被 ResourceQuota 拒绝；&lt;/li&gt;
&lt;li&gt;Pod 已创建，但 Scheduler 报 Insufficient cpu/memory；&lt;/li&gt;
&lt;li&gt;PVC 或 topology 阻止调度；&lt;/li&gt;
&lt;li&gt;taint、affinity 等其他约束阻止调度。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe resourcequota
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get nodes
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="调参的正确闭环"&gt;调参的正确闭环
&lt;/h2&gt;&lt;p&gt;不要仅凭“CPU 经常到 100%”就盲目加 limit，也不要为了调度成功把 request 压到极低。更可靠的闭环是：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;观察一段有代表性的 CPU、内存和延迟；&lt;/li&gt;
&lt;li&gt;区分平均值、峰值与持续峰值；&lt;/li&gt;
&lt;li&gt;检查 CPU throttling、OOM、重启和探针超时；&lt;/li&gt;
&lt;li&gt;调整 SubPool spec；&lt;/li&gt;
&lt;li&gt;确认没有超过 Operator 包络；&lt;/li&gt;
&lt;li&gt;观察滚动更新后的新基线。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;资源管理的目标不是让数字尽可能小，而是在成本、调度稳定性和应用性能之间建立可解释的边界。&lt;/p&gt;</description></item><item><title>Service、DNS、EndpointSlice 与 port-forward</title><link>https://kerolt.github.io/p/servicednsendpointslice-%E4%B8%8E-port-forward/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/servicednsendpointslice-%E4%B8%8E-port-forward/</guid><description>&lt;h2 id="pod-ip-会变service-给它一个稳定电话号码"&gt;Pod IP 会变，Service 给它一个稳定电话号码
&lt;/h2&gt;&lt;p&gt;Pod 被重建后，名字和 IP 都可能变化。如果客户端直接记住 Pod IP，系统稍微滚动一次就断线。&lt;/p&gt;
&lt;p&gt;Service 提供稳定的虚拟 IP、DNS 名称和端口，并通过 label selector 持续找到合适的 Pod。可以把它理解成公司总机：员工座位会变，但总机号码不变。&lt;/p&gt;
&lt;p&gt;本项目每个号池创建两个 ClusterIP Service：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;Service&lt;/th&gt;
					&lt;th style="text-align: right"&gt;端口&lt;/th&gt;
					&lt;th&gt;后端&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;cpa&lt;/code&gt;&lt;/td&gt;
					&lt;td style="text-align: right"&gt;8317&lt;/td&gt;
					&lt;td&gt;runtime Pod 中的 CPA 容器&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;cpa-manager&lt;/code&gt;&lt;/td&gt;
					&lt;td style="text-align: right"&gt;18317&lt;/td&gt;
					&lt;td&gt;同一 runtime Pod 中的 cpa-manager 容器&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;两个 Service 使用相同 Pod selector，但转发到不同端口。&lt;/p&gt;
&lt;h2 id="service-的三组端口概念"&gt;Service 的三组端口概念
&lt;/h2&gt;&lt;p&gt;典型配置如下：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;type&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;ClusterIP&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;ports&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;http&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;port&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;8317&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;targetPort&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;8317&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;protocol&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;TCP&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;selector&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;app.kubernetes.io/component&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;runtime&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;subrelay.example.com/pool&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;常见字段含义：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;字段&lt;/th&gt;
					&lt;th&gt;含义&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;port&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Service 对客户端提供的端口&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;targetPort&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;后端 Pod 实际接收流量的端口&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;nodePort&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;NodePort/LoadBalancer 类型可使用的节点端口&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;本项目使用 ClusterIP，因此没有 &lt;code&gt;nodePort&lt;/code&gt;。&lt;code&gt;port&lt;/code&gt; 与 &lt;code&gt;targetPort&lt;/code&gt; 数值相同只是设计选择，并非 Kubernetes 要求。&lt;/p&gt;
&lt;h2 id="clusterip-的边界"&gt;ClusterIP 的边界
&lt;/h2&gt;&lt;p&gt;ClusterIP 一般只在集群网络内可达。它不会自动创建公网 IP，也不会开放宿主机端口。&lt;/p&gt;
&lt;p&gt;这正是 Internal 模式需要的安全边界：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;集群内组件可以通过 Service 访问；&lt;/li&gt;
&lt;li&gt;管理员可以临时 port-forward；&lt;/li&gt;
&lt;li&gt;互联网不能直接访问它；&lt;/li&gt;
&lt;li&gt;生产外部流量必须经过共享 Gateway。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;“Service 已经有 ClusterIP”不等于“我的浏览器一定能直接打开”。先确认客户端到底在集群内还是集群外。&lt;/p&gt;
&lt;h2 id="集群-dns-名称怎样组成"&gt;集群 DNS 名称怎样组成
&lt;/h2&gt;&lt;p&gt;同 Namespace 中可以用短名称访问：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;http://cpa:8317
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;跨 Namespace 常用完整名称：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&amp;lt;service&amp;gt;.&amp;lt;namespace&amp;gt;.svc:&amp;lt;port&amp;gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;本项目给 CPA 生成的 Mihomo 地址是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;http://mihomo-gateway.subpool-gateway.svc:18000
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;可进一步写成集群域名全名：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;mihomo-gateway.subpool-gateway.svc.cluster.local
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;cluster.local&lt;/code&gt; 是常见默认值，但实际集群域可能不同，所以应用配置常使用到 &lt;code&gt;.svc&lt;/code&gt; 为止的名称，让搜索域补齐剩余部分。&lt;/p&gt;
&lt;h2 id="service-如何找到-pod"&gt;Service 如何找到 Pod
&lt;/h2&gt;&lt;p&gt;Service controller 根据 selector 匹配 Pod，并维护 EndpointSlice。EndpointSlice 里记录真实后端地址、端口和 Ready 状态。&lt;/p&gt;
&lt;p&gt;链路如下：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Service selector
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;匹配带标签的 Pod
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;EndpointSlice 记录 Pod IP 和 ready condition
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;集群网络规则把 Service 流量送到后端
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Service 本身存在，并不能证明后端可用。应继续检查 EndpointSlice：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get service cpa
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;cpa
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;若 EndpointSlice 没有 Endpoint，常见原因是 selector 不匹配、Pod 未 Ready 或 Pod 根本未创建。&lt;/p&gt;
&lt;h2 id="本项目为什么检查-mihomo-endpointslice"&gt;本项目为什么检查 Mihomo EndpointSlice
&lt;/h2&gt;&lt;p&gt;Operator 的 &lt;code&gt;ProxyGatewayReady&lt;/code&gt; 不只检查 &lt;code&gt;mihomo-gateway&lt;/code&gt; Service 是否存在，还要求它至少有一个 Ready Endpoint。&lt;/p&gt;
&lt;p&gt;这是很重要的差别：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Service 存在：有一个电话号码
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Ready Endpoint 存在：至少有人能接电话
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Operator 会在 &lt;code&gt;subpool-gateway&lt;/code&gt; Namespace 中查找带有：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubernetes.io/service-name=mihomo-gateway
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;标签的 EndpointSlice，然后检查 endpoint condition。没有 Ready Endpoint 时，&lt;code&gt;ProxyGatewayReady=False&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;它仍没有深入验证每个上游代理或真实模型请求。这属于更高层业务健康，而非 Kubernetes Service 健康。&lt;/p&gt;
&lt;h2 id="pod-readiness-如何影响-service"&gt;Pod readiness 如何影响 Service
&lt;/h2&gt;&lt;p&gt;Pod readinessProbe 成功后，Pod 才通常被视为 Ready。EndpointSlice controller 会把这个结果投影到 Endpoint condition。&lt;/p&gt;
&lt;p&gt;因此一条常见故障链是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;应用启动失败
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → readinessProbe 失败
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Pod NotReady
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Endpoint 不 Ready
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Service 没有可用后端
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 上游调用失败
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;排查时不要只盯着 Service。Service 更像路由入口，真正提供响应的是后端 Pod 中的进程。&lt;/p&gt;
&lt;h2 id="port-forward-在做什么"&gt;port-forward 在做什么
&lt;/h2&gt;&lt;p&gt;开发时使用：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool port-forward service/cpa 8317:8317
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它让本机的 &lt;code&gt;127.0.0.1:8317&lt;/code&gt; 临时转发到 Service 选中的后端端口。流量通道由 &lt;code&gt;kubectl&lt;/code&gt; 与集群 API 通路维持。&lt;/p&gt;
&lt;p&gt;它的特征是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;命令运行期间才有效；&lt;/li&gt;
&lt;li&gt;终端退出或网络断开即停止；&lt;/li&gt;
&lt;li&gt;访问权限依赖 kubeconfig 和 RBAC；&lt;/li&gt;
&lt;li&gt;适合开发、诊断和 break-glass；&lt;/li&gt;
&lt;li&gt;不适合作为生产服务入口。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;port-forward&lt;/code&gt; 成功不能证明 Gateway、DNS 和 TLS 都已配置，因为它绕过了生产外部入口链路。&lt;/p&gt;
&lt;h2 id="两个-service-指向同一个-pod会不会冲突"&gt;两个 Service 指向同一个 Pod会不会冲突
&lt;/h2&gt;&lt;p&gt;不会。两个 Service 可以选中同一个 Pod，并把流量送往不同端口：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Service cpa:8317 ─┐
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ├─&amp;gt; Pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Service cpa-manager:18317 ─┘
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;因为 CPA 和 cpa-manager 共享 Pod IP，但监听不同端口，所以流量能被准确送到对应进程。&lt;/p&gt;
&lt;p&gt;NetworkPolicy 也明确允许 8317 和 18317 入站。不过允许端口不等于允许所有外部访问；是否能到达 Pod 还取决于网络路径、Service 类型和 Gateway 配置。&lt;/p&gt;
&lt;h2 id="servicegateway-与-httproute-的分工"&gt;Service、Gateway 与 HTTPRoute 的分工
&lt;/h2&gt;&lt;p&gt;Gateway 模式的完整入站链路是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;客户端
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → DNS
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Shared Envoy Gateway
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → HTTPRoute
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → cpa ClusterIP Service
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → EndpointSlice
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → runtime Pod:8317
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Service 负责稳定连接后端 Pod，HTTPRoute 负责按照 hostname 等 HTTP 规则把共享 Gateway 的流量送到某个 Service。它们不是互相替代关系。&lt;/p&gt;
&lt;h2 id="常见误区"&gt;常见误区
&lt;/h2&gt;&lt;h3 id="有-service-就一定能访问"&gt;有 Service 就一定能访问
&lt;/h3&gt;&lt;p&gt;不一定。继续检查 selector、EndpointSlice、Pod Ready 和 NetworkPolicy。&lt;/p&gt;
&lt;h3 id="containerport-就是对外开放端口"&gt;containerPort 就是对外开放端口
&lt;/h3&gt;&lt;p&gt;不是。&lt;code&gt;containerPort&lt;/code&gt; 主要是声明容器端口；集群稳定访问依赖 Service，外部访问还需要 Gateway、LoadBalancer、Ingress 或 port-forward。&lt;/p&gt;
&lt;h3 id="clusterip-可以直接从个人电脑访问"&gt;ClusterIP 可以直接从个人电脑访问
&lt;/h3&gt;&lt;p&gt;通常不可以，除非电脑本身接入了集群网络。开发场景使用 port-forward。&lt;/p&gt;
&lt;h3 id="port-forward-是轻量生产入口"&gt;port-forward 是轻量生产入口
&lt;/h3&gt;&lt;p&gt;不是。它依赖一个持续运行的客户端进程，没有正式入口应有的高可用、证书、域名和流量治理能力。&lt;/p&gt;
&lt;h2 id="有界排查清单"&gt;有界排查清单
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get service cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pod -o wide --show-labels
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get networkpolicy
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;把路径按“入口对象 → selector → EndpointSlice → Pod readiness → 应用端口”逐段验证，通常比反复重启 Pod 更快找到根因。&lt;/p&gt;</description></item><item><title>ServiceAccount、RBAC、Leader Election 与容器安全</title><link>https://kerolt.github.io/p/serviceaccountrbacleader-election-%E4%B8%8E%E5%AE%B9%E5%99%A8%E5%AE%89%E5%85%A8/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/serviceaccountrbacleader-election-%E4%B8%8E%E5%AE%B9%E5%99%A8%E5%AE%89%E5%85%A8/</guid><description>&lt;h2 id="安全不是一个开关而是一组互相补位的边界"&gt;安全不是一个开关，而是一组互相补位的边界
&lt;/h2&gt;&lt;p&gt;本项目同时使用：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;ServiceAccount 表达工作负载身份；&lt;/li&gt;
&lt;li&gt;RBAC 限制 API 权限；&lt;/li&gt;
&lt;li&gt;leader election 保证多个 Operator 副本只有一个主动协调；&lt;/li&gt;
&lt;li&gt;Pod Security Admission 限制 Pod 配置；&lt;/li&gt;
&lt;li&gt;securityContext 限制容器进程权限；&lt;/li&gt;
&lt;li&gt;NetworkPolicy 限制网络路径；&lt;/li&gt;
&lt;li&gt;Secret 隔离敏感数据。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些机制没有哪个能单独解决全部问题。RBAC 管 API，不负责网络；NetworkPolicy 管流量，不负责文件权限；非 root 容器也不能弥补过大的 API 权限。&lt;/p&gt;
&lt;h2 id="serviceaccountpod-在集群中的机器身份"&gt;ServiceAccount：Pod 在集群中的机器身份
&lt;/h2&gt;&lt;p&gt;用户通过 kubeconfig 访问 API Server，Pod 通常通过 ServiceAccount 身份访问。&lt;/p&gt;
&lt;p&gt;本项目有两类 ServiceAccount：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;ServiceAccount&lt;/th&gt;
					&lt;th&gt;Namespace&lt;/th&gt;
					&lt;th&gt;用途&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;subpool-operator&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;subpool-system&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Operator 调用 Kubernetes API&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;subpool&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;每个 Pool Namespace&lt;/td&gt;
					&lt;td&gt;runtime Pod 的身份&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;mihomo-gateway&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;subpool-gateway&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Shared Mihomo Pod 身份&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;为工作负载单独创建 ServiceAccount，比使用 Namespace 的 &lt;code&gt;default&lt;/code&gt; ServiceAccount 更容易审计，也便于精确绑定权限。&lt;/p&gt;
&lt;h2 id="为什么-runtime-禁止自动挂载-api-token"&gt;为什么 runtime 禁止自动挂载 API token
&lt;/h2&gt;&lt;p&gt;项目在 ServiceAccount 和 Pod 两处都设置：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;automountServiceAccountToken&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="kc"&gt;false&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;CPA 与 cpa-manager 不需要调用 Kubernetes API，因此没有必要把 ServiceAccount token 放进容器。&lt;/p&gt;
&lt;p&gt;如果应用被攻破，攻击者即使进入容器，也少了一份可直接拿来访问 API Server 的凭据。这是最小权限原则中非常实用的一步：不用的身份能力不要发放。&lt;/p&gt;
&lt;p&gt;Mihomo 同样禁用 token 自动挂载，因为代理进程也不需要访问 Kubernetes API。&lt;/p&gt;
&lt;h2 id="rbac-的四个问题"&gt;RBAC 的四个问题
&lt;/h2&gt;&lt;p&gt;一条 RBAC rule 可以拆成四个问题：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;谁（Subject）
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;通过什么绑定（RoleBinding / ClusterRoleBinding）
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;可以对什么资源（apiGroups + resources）
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;执行哪些动作（verbs）
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;例如 Operator 需要管理 StatefulSet：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;apiGroups&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;apps&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;resources&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;statefulsets&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;verbs&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;create&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;delete&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;get&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;list&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;patch&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;update&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;watch&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;get&lt;/code&gt; 读取单个对象，&lt;code&gt;list&lt;/code&gt; 读取集合，&lt;code&gt;watch&lt;/code&gt; 订阅变化，&lt;code&gt;create/update/patch/delete&lt;/code&gt; 才会修改状态。&lt;/p&gt;
&lt;h2 id="role-与-clusterrole-的区别"&gt;Role 与 ClusterRole 的区别
&lt;/h2&gt;&lt;p&gt;Role 只在一个 Namespace 中定义权限。ClusterRole 可以表达 cluster-scoped 资源权限，也可以被不同 Namespace 的 RoleBinding 引用。&lt;/p&gt;
&lt;p&gt;SubPool Operator 需要：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;监听 cluster-scoped &lt;code&gt;SubPool&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;创建 cluster-scoped Namespace；&lt;/li&gt;
&lt;li&gt;在任意派生 Pool Namespace 创建资源；&lt;/li&gt;
&lt;li&gt;读取共享 Gateway、EndpointSlice；&lt;/li&gt;
&lt;li&gt;更新 SubPool 的 status 和 finalizers。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此它的主要管理权限必须通过 ClusterRole 和 ClusterRoleBinding 提供。单一 Namespace 内的 Role 无法完成跨 Namespace 生命周期管理。&lt;/p&gt;
&lt;p&gt;权限大并不意味着可以省略边界。控制器代码仍严格检查 owner reference，不凭名字接管对象。RBAC 是“技术上能做什么”，所有权校验是“业务上允许对谁做”。&lt;/p&gt;
&lt;h2 id="status-与-finalizers-为什么是独立子资源权限"&gt;status 与 finalizers 为什么是独立子资源权限
&lt;/h2&gt;&lt;p&gt;RBAC 中单独出现：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpools/status
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpools/finalizers
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Kubernetes 把 status 和 finalizer 更新视为独立 API 路径。控制器需要更新现实状态和删除收尾标记，但普通业务使用者未必需要这些权限。&lt;/p&gt;
&lt;p&gt;把它们单独授权，能避免“只想允许用户编辑 spec，却顺手允许伪造 Ready 状态或移除 finalizer”。&lt;/p&gt;
&lt;h2 id="可以用-auth-can-i-验证权限"&gt;可以用 auth can-i 验证权限
&lt;/h2&gt;&lt;p&gt;查看当前身份能否执行某动作：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl auth can-i get subpools
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl auth can-i update subpools/status
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl auth can-i create namespaces
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;模拟 ServiceAccount：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl auth can-i list endpointslices &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --as&lt;span class="o"&gt;=&lt;/span&gt;system:serviceaccount:subpool-system:subpool-operator
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它适合验证 RBAC，不证明控制器业务逻辑一定正确。&lt;/p&gt;
&lt;h2 id="两个-operator-pod-为什么不会同时-reconcile"&gt;两个 Operator Pod 为什么不会同时 reconcile
&lt;/h2&gt;&lt;p&gt;Operator Deployment 设置 replicas 为 2，同时启用 leader election：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;--leader-elect=true
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;--leader-election-namespace=subpool-system
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;多个 controller-manager 实例通过 &lt;code&gt;Lease&lt;/code&gt; 对象竞争领导权。获得 Lease 的实例执行 controller 工作，其他实例保持待命；leader 退出或续租失败后，另一个实例接管。&lt;/p&gt;
&lt;p&gt;这解决的是控制面单实例故障：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Operator Pod A 是 leader → A 故障 → Lease 到期/转移 → Pod B 接管
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它不让两个实例并行处理同一控制器以提高吞吐，更不会把 runtime StatefulSet 变成双副本。&lt;/p&gt;
&lt;h2 id="leader-election-为什么单独使用-namespaced-role"&gt;leader election 为什么单独使用 namespaced Role
&lt;/h2&gt;&lt;p&gt;Lease 位于 &lt;code&gt;subpool-system&lt;/code&gt;，所以项目用该 Namespace 中的 Role 授权对 &lt;code&gt;coordination.k8s.io/leases&lt;/code&gt; 的读写，并授权创建或 patch Event。&lt;/p&gt;
&lt;p&gt;这部分不需要 cluster-wide 权限。把 leader election 权限限制在固定 Namespace，减少无关权限范围。&lt;/p&gt;
&lt;h2 id="pdb-与-leader-election-是互补关系"&gt;PDB 与 leader election 是互补关系
&lt;/h2&gt;&lt;p&gt;Operator 有两个 replica，PDB &lt;code&gt;minAvailable: 1&lt;/code&gt;。二者分工如下：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;机制&lt;/th&gt;
					&lt;th&gt;作用&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;replicas: 2&lt;/td&gt;
					&lt;td&gt;保持一个待命实例&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;leader election&lt;/td&gt;
					&lt;td&gt;确保只有一个实例主动协调&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PDB&lt;/td&gt;
					&lt;td&gt;自愿中断时尽量保留至少一个可用 Pod&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;readinessProbe&lt;/td&gt;
					&lt;td&gt;判断某个 Operator Pod 是否可用&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;缺少 leader election，两个副本可能同时写对象；缺少第二个副本，leader election 没有接管者；缺少 PDB，维护工具可能同时驱逐两个 Pod。&lt;/p&gt;
&lt;p&gt;即使三者都具备，Node 同时故障、API Server 不可用等场景仍可能中断控制面。&lt;/p&gt;
&lt;h2 id="容器为什么使用-uid-65532"&gt;容器为什么使用 UID 65532
&lt;/h2&gt;&lt;p&gt;Operator 的最终镜像基于 &lt;code&gt;distroless/static:nonroot&lt;/code&gt;，并声明：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-dockerfile" data-lang="dockerfile"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;USER&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s"&gt;65532:65532&lt;/span&gt;&lt;span class="err"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Deployment 也要求 non-root，并固定 UID/GID 65532。双重声明使镜像默认行为与 Kubernetes 运行约束一致。&lt;/p&gt;
&lt;p&gt;Operator 容器还使用：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;allowPrivilegeEscalation: false
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;capabilities.drop: ALL
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;readOnlyRootFilesystem: true
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;seccompProfile: RuntimeDefault
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这些配置分别限制提权、Linux capabilities、根文件系统写入和系统调用面。&lt;/p&gt;
&lt;p&gt;runtime 容器也以 non-root 运行、drop all capabilities，并通过 PVC 挂载提供需要写入的路径。&lt;/p&gt;
&lt;h2 id="pod-security-admission-的-namespace-级防线"&gt;Pod Security Admission 的 Namespace 级防线
&lt;/h2&gt;&lt;p&gt;每个 Pool Namespace 带 restricted 级别标签：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;pod-security.kubernetes.io/enforce=restricted
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;pod-security.kubernetes.io/audit=restricted
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;pod-security.kubernetes.io/warn=restricted
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;enforce&lt;/code&gt; 会拒绝不符合 restricted 标准的 Pod；&lt;code&gt;audit&lt;/code&gt; 记录违规；&lt;code&gt;warn&lt;/code&gt; 给请求者提示。&lt;/p&gt;
&lt;p&gt;它可以阻止常见危险配置，例如特权容器、危险 capabilities 或不满足 non-root 等要求。具体规则随 Kubernetes Pod Security Standards 定义，应在集群升级时做兼容验证。&lt;/p&gt;
&lt;h2 id="livenessreadiness-与-rbac-无关"&gt;liveness、readiness 与 RBAC 无关
&lt;/h2&gt;&lt;p&gt;Operator 的 &lt;code&gt;/healthz&lt;/code&gt; 和 &lt;code&gt;/readyz&lt;/code&gt; 只说明进程健康，不会自动验证 ClusterRole 是否包含全部业务权限。&lt;/p&gt;
&lt;p&gt;一个 Operator Pod 可以 Ready，但 reconcile 时不断收到 Forbidden。因此故障时要同时看：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-system get pods
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-system logs deployment/subpool-operator &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --all-containers&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nb"&gt;true&lt;/span&gt; --tail&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;200&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl auth can-i ...
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="安全设计中的几个诚实边界"&gt;安全设计中的几个诚实边界
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;ClusterIP 不等于只有受认证用户能访问；它只是集群内入口类型。&lt;/li&gt;
&lt;li&gt;Secret 不等于加密保险箱；访问 Secret 的 RBAC 必须严格。&lt;/li&gt;
&lt;li&gt;non-root 不等于容器没有漏洞；仍要维护镜像和依赖。&lt;/li&gt;
&lt;li&gt;NetworkPolicy 不限制 Kubernetes API 权限；RBAC 也不限制普通 TCP 流量。&lt;/li&gt;
&lt;li&gt;leader election 不等于业务数据高可用。&lt;/li&gt;
&lt;li&gt;PDB 不阻止所有故障。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;成熟的安全不是给系统贴上“secure”标签，而是每层都明确允许什么、拒绝什么，以及这层机制覆盖不到什么。&lt;/p&gt;</description></item><item><title>StatefulSet、PVC 与 RWO 持久化存储</title><link>https://kerolt.github.io/p/statefulsetpvc-%E4%B8%8E-rwo-%E6%8C%81%E4%B9%85%E5%8C%96%E5%AD%98%E5%82%A8/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/statefulsetpvc-%E4%B8%8E-rwo-%E6%8C%81%E4%B9%85%E5%8C%96%E5%AD%98%E5%82%A8/</guid><description>&lt;h2 id="容器会消失数据不能跟着失忆"&gt;容器会消失，数据不能跟着失忆
&lt;/h2&gt;&lt;p&gt;Pod 是可替换资源。镜像更新、节点维护、健康检查失败或控制器重建，都可能让旧 Pod 消失并创建新 Pod。若数据只写在容器可写层，旧 Pod 一删，数据也没了。&lt;/p&gt;
&lt;p&gt;本项目需要保存：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;CPA 配置；&lt;/li&gt;
&lt;li&gt;账号认证数据；&lt;/li&gt;
&lt;li&gt;CPA 日志；&lt;/li&gt;
&lt;li&gt;cpa-manager 的 SQLite 数据库；&lt;/li&gt;
&lt;li&gt;cpa-manager 的数据密钥。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此每个 SubPool 都需要独立的持久卷。Operator 通过 StatefulSet 的 &lt;code&gt;volumeClaimTemplates&lt;/code&gt; 为它创建一个 PVC。&lt;/p&gt;
&lt;h2 id="四个容易混淆的角色"&gt;四个容易混淆的角色
&lt;/h2&gt;&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;对象&lt;/th&gt;
					&lt;th&gt;可以理解为&lt;/th&gt;
					&lt;th&gt;由谁关心&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;Volume&lt;/td&gt;
					&lt;td&gt;Pod 中的挂载定义&lt;/td&gt;
					&lt;td&gt;Pod&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PVC&lt;/td&gt;
					&lt;td&gt;应用提交的存储申请&lt;/td&gt;
					&lt;td&gt;应用与控制器&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PV&lt;/td&gt;
					&lt;td&gt;集群中实际可分配的存储资源&lt;/td&gt;
					&lt;td&gt;Kubernetes 存储控制面&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;StorageClass&lt;/td&gt;
					&lt;td&gt;动态创建存储的规格与驱动&lt;/td&gt;
					&lt;td&gt;集群管理员&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;关系大致是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;StatefulSet
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ volumeClaimTemplates
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ PVC: 我要 20Gi、RWO、指定 StorageClass
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ StorageClass / CSI provisioner
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ PV: 实际云盘或其他存储
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └─ 挂载进 runtime-0 Pod
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;PVC 不等于磁盘本身，它更像一张存储申请单。PV 才代表已经供 Kubernetes 使用的一块存储。&lt;/p&gt;
&lt;h2 id="为什么本项目使用-statefulset"&gt;为什么本项目使用 StatefulSet
&lt;/h2&gt;&lt;p&gt;Deployment 更适合可替换的无状态副本；StatefulSet 为 Pod 和存储提供稳定身份。&lt;/p&gt;
&lt;p&gt;本项目生成的 StatefulSet 名为 &lt;code&gt;runtime&lt;/code&gt;，第一个 Pod 通常叫：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;由 volume claim template 创建的 PVC 名称通常带模板名和 Pod 名，例如：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;data-runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Pod 重建后名字仍然是 &lt;code&gt;runtime-0&lt;/code&gt;，并继续使用原来的 PVC。对于依赖本地 SQLite、配置文件和账号目录的工作负载，这种稳定身份比 Deployment 更合适。&lt;/p&gt;
&lt;p&gt;不过 StatefulSet 并不自动等于“有状态高可用”。本项目只有一个 replica，同一个 RWO PVC 也只服务这个 runtime Pod。&lt;/p&gt;
&lt;h2 id="volumeclaimtemplates-做了什么"&gt;volumeClaimTemplates 做了什么
&lt;/h2&gt;&lt;p&gt;项目中的核心声明可以抽象为：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;volumeClaimTemplates&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="nt"&gt;metadata&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;data&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;accessModes&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;- &lt;span class="l"&gt;ReadWriteOnce&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;storageClassName&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;standard-rwo&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;resources&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;requests&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;storage&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;20Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它不是引用一个预先写死的 PVC，而是告诉 StatefulSet controller：“给每个 Pod 按这个模板创建自己的 PVC。”&lt;/p&gt;
&lt;p&gt;本项目 replicas 固定为 1，所以每个 Pool 正常只有一个由模板生成的 PVC。ResourceQuota 也固定最多允许一个 PVC，进一步阻止意外扩张。&lt;/p&gt;
&lt;h2 id="rwo-的真实含义"&gt;RWO 的真实含义
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;ReadWriteOnce&lt;/code&gt; 常被误读成“只能被一个 Pod 使用”。它更准确的含义是：卷可以被单个 Node 以读写方式挂载。&lt;/p&gt;
&lt;p&gt;这意味着：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;同一 Node 上的多个 Pod 是否能同时访问，要看具体驱动和挂载方式；&lt;/li&gt;
&lt;li&gt;不应把 RWO 当作应用级分布式锁；&lt;/li&gt;
&lt;li&gt;Pod 跨 Node 重调度时，旧挂载需要先解除，新 Node 才能挂载；&lt;/li&gt;
&lt;li&gt;云盘 attach/detach 可能让恢复比无状态 Pod 慢。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本项目用一个 Pod 承载两个容器，正好适配 RWO：两个容器通过同一 Pod 挂载同一个卷，不需要跨 Node 多写。&lt;/p&gt;
&lt;h2 id="storageclass-是平台能力不是应用细节"&gt;StorageClass 是平台能力，不是应用细节
&lt;/h2&gt;&lt;p&gt;当前 Operator Configuration 指定：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;storage&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;className&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;standard-rwo&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这表示 SubPool 只声明容量，平台统一决定使用哪种存储。换一个集群时，管理员必须确认目标集群存在该 StorageClass：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get storageclass
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get storageclass standard-rwo -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;如果 StorageClass 不存在或 CSI provisioner 异常，PVC 会一直 Pending，Pod 也无法正常启动。&lt;/p&gt;
&lt;p&gt;典型检查：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pvc
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pvc data-runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get events --sort-by&lt;span class="o"&gt;=&lt;/span&gt;.lastTimestamp
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Event 中常能看到 provision、topology、quota 或 attach 失败原因。&lt;/p&gt;
&lt;h2 id="容量为什么创建后不可修改"&gt;容量为什么创建后不可修改
&lt;/h2&gt;&lt;p&gt;Kubernetes 的 PVC 本身在满足 StorageClass 与 CSI 条件时可能支持扩容，但本项目把 &lt;code&gt;spec.storage.size&lt;/code&gt; 定义为不可变。这是项目 API 契约，而不是一句“Kubernetes 永远不能扩容”。&lt;/p&gt;
&lt;p&gt;原因在于当前 Operator 直接把容量放进 StatefulSet 的 &lt;code&gt;volumeClaimTemplates&lt;/code&gt;。对已存在 StatefulSet，这部分字段受更新限制。若 CR 允许修改而底层模板不能可靠同步，就会出现：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;SubPool spec 写 40Gi
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;现有 PVC 仍是 20Gi
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;StatefulSet 更新又被 API Server 拒绝
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;与其提供一个表面可改、实际行为不一致的字段，v1 选择在 CRD 层直接禁止修改。&lt;/p&gt;
&lt;p&gt;未来若要支持扩容，需要明确设计：校验 StorageClass 能力、直接扩 PVC、处理文件系统扩容、记录进度和失败恢复，而不是只删除一条 CEL 校验。&lt;/p&gt;
&lt;h2 id="暂停为什么能保留数据"&gt;暂停为什么能保留数据
&lt;/h2&gt;&lt;p&gt;设置：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl patch subpool demo-pool --type&lt;span class="o"&gt;=&lt;/span&gt;merge &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -p &lt;span class="s1"&gt;&amp;#39;{&amp;#34;spec&amp;#34;:{&amp;#34;suspend&amp;#34;:true}}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Operator 会把 StatefulSet replicas 调成 0，但保留：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;SubPool；&lt;/li&gt;
&lt;li&gt;派生 Namespace；&lt;/li&gt;
&lt;li&gt;PVC；&lt;/li&gt;
&lt;li&gt;Credential Secret；&lt;/li&gt;
&lt;li&gt;其他基础配置。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;恢复时再把 replicas 调回 1，新 Pod 重新挂载旧 PVC，从上次状态继续运行。&lt;/p&gt;
&lt;p&gt;暂停适合临时停机、节省计算资源或等待外部依赖修复。它不释放持久存储费用。&lt;/p&gt;
&lt;h2 id="删除为什么是高风险操作"&gt;删除为什么是高风险操作
&lt;/h2&gt;&lt;p&gt;删除 &lt;code&gt;SubPool&lt;/code&gt; 会删除它拥有的 Namespace，Namespace 中的 PVC 也随之删除。PVC 删除后，底层 PV 如何处理取决于 StorageClass 的 reclaim policy：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Delete&lt;/code&gt;：通常连底层存储一起删除；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Retain&lt;/code&gt;：PV 和底层存储可能保留，但需要人工回收。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;项目的产品语义明确把删除视为永久丢失号池运行数据。不要因为某个 Pod 卡住就删除 SubPool“试试看”。&lt;/p&gt;
&lt;p&gt;删除前至少确认：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pvc
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get pv
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;当前 v1 不负责备份恢复，因此重要数据必须另有备份流程。&lt;/p&gt;
&lt;h2 id="pdb-不能保护磁盘也不能制造副本"&gt;PDB 不能保护磁盘，也不能制造副本
&lt;/h2&gt;&lt;p&gt;runtime StatefulSet 有一个 &lt;code&gt;minAvailable: 1&lt;/code&gt; 的 PodDisruptionBudget。它只限制某些自愿中断，例如节点 drain 时的 eviction。&lt;/p&gt;
&lt;p&gt;它不能防止：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;容器进程崩溃；&lt;/li&gt;
&lt;li&gt;Node 突然宕机；&lt;/li&gt;
&lt;li&gt;磁盘故障；&lt;/li&gt;
&lt;li&gt;用户删除 StatefulSet 或 Namespace；&lt;/li&gt;
&lt;li&gt;只有一个副本时的滚动更新中断；&lt;/li&gt;
&lt;li&gt;数据损坏。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本项目单副本加 RWO 的设计重点是隔离和持久化，不是数据面无中断高可用。&lt;/p&gt;
&lt;h2 id="常见状态与判断"&gt;常见状态与判断
&lt;/h2&gt;&lt;h3 id="pvc-pending"&gt;PVC Pending
&lt;/h3&gt;&lt;p&gt;优先检查 StorageClass、CSI provisioner、容量配额和拓扑限制。&lt;/p&gt;
&lt;h3 id="pod-pending-且提示-unbound-pvc"&gt;Pod Pending 且提示 unbound PVC
&lt;/h3&gt;&lt;p&gt;Pod 在等 PVC 绑定。不要先查应用日志，因为容器还没有运行。&lt;/p&gt;
&lt;h3 id="multi-attach-error"&gt;Multi-Attach error
&lt;/h3&gt;&lt;p&gt;通常是卷仍挂在旧 Node，或新旧 Pod 在不同 Node 争用 RWO 卷。检查 Pod 调度位置和 VolumeAttachment，等待安全 detach，不要强行绕过。&lt;/p&gt;
&lt;h3 id="pod-运行但-permission-denied"&gt;Pod 运行但 Permission denied
&lt;/h3&gt;&lt;p&gt;检查镜像运行 UID、&lt;code&gt;fsGroup&lt;/code&gt;、卷中文件的历史权限以及 CSI 驱动行为。本项目使用 UID/GID 65532，并设置 &lt;code&gt;FSGroupChangeOnRootMismatch&lt;/code&gt;。&lt;/p&gt;
&lt;h2 id="实用观察命令"&gt;实用观察命令
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get statefulset runtime
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pod runtime-0 -o wide
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pvc -o wide
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pvc data-runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get storageclass
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;排查存储时沿着“Pod → PVC → StorageClass/PV → CSI”逐层向下，不要把所有 Pending 都归咎于应用镜像。&lt;/p&gt;</description></item><item><title>SubRelay Operator 项目全景与学习路线</title><link>https://kerolt.github.io/p/subrelay-operator-%E9%A1%B9%E7%9B%AE%E5%85%A8%E6%99%AF%E4%B8%8E%E5%AD%A6%E4%B9%A0%E8%B7%AF%E7%BA%BF/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/subrelay-operator-%E9%A1%B9%E7%9B%AE%E5%85%A8%E6%99%AF%E4%B8%8E%E5%AD%A6%E4%B9%A0%E8%B7%AF%E7%BA%BF/</guid><description>&lt;h2 id="先把项目当成一座自动化公寓"&gt;先把项目当成一座自动化公寓
&lt;/h2&gt;&lt;p&gt;第一次打开这个项目，很容易被 CRD、StatefulSet、NetworkPolicy、Gateway API 和一堆 Go 代码吓住。其实它做的事情可以先用一句话概括：&lt;/p&gt;

 &lt;blockquote&gt;
 &lt;p&gt;用户提交一张名为 &lt;code&gt;SubPool&lt;/code&gt; 的“入住申请”，Operator 自动建出一套隔离的 CPA 运行环境，并持续保证它与申请内容一致。&lt;/p&gt;

 &lt;/blockquote&gt;
&lt;p&gt;可以把 Kubernetes 集群想成一座公寓楼：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;项目概念&lt;/th&gt;
					&lt;th&gt;公寓类比&lt;/th&gt;
					&lt;th&gt;实际职责&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;SubPool&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;入住申请单&lt;/td&gt;
					&lt;td&gt;描述一个号池想要的镜像、资源、存储和访问方式&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Operator&lt;/td&gt;
					&lt;td&gt;物业管家&lt;/td&gt;
					&lt;td&gt;读取申请，创建、修正、暂停或删除资源&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Namespace&lt;/td&gt;
					&lt;td&gt;独立房间&lt;/td&gt;
					&lt;td&gt;隔离不同号池的资源&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;StatefulSet&lt;/td&gt;
					&lt;td&gt;固定工位&lt;/td&gt;
					&lt;td&gt;运行 CPA 与 cpa-manager 两个容器&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PVC&lt;/td&gt;
					&lt;td&gt;带锁储物柜&lt;/td&gt;
					&lt;td&gt;保存账号配置、日志和管理数据&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Service&lt;/td&gt;
					&lt;td&gt;稳定房间号码&lt;/td&gt;
					&lt;td&gt;为会变化的 Pod 提供稳定访问地址&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;NetworkPolicy&lt;/td&gt;
					&lt;td&gt;门禁规则&lt;/td&gt;
					&lt;td&gt;限制谁能进入、Pod 能去哪里&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Shared Mihomo&lt;/td&gt;
					&lt;td&gt;共用出站大厅&lt;/td&gt;
					&lt;td&gt;为所有号池提供代理出口&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Envoy Gateway&lt;/td&gt;
					&lt;td&gt;共用前台&lt;/td&gt;
					&lt;td&gt;在 Gateway 模式下接收外部 HTTP 流量&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;这个类比不是 Kubernetes 的正式定义，但它能帮我们抓住项目最重要的设计：一个号池不是一个孤零零的 Pod，而是一组彼此配合、由 Operator 统一管理的对象。&lt;/p&gt;
&lt;h2 id="一张图看懂完整链路"&gt;一张图看懂完整链路
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;span class="lnt"&gt;13
&lt;/span&gt;&lt;span class="lnt"&gt;14
&lt;/span&gt;&lt;span class="lnt"&gt;15
&lt;/span&gt;&lt;span class="lnt"&gt;16
&lt;/span&gt;&lt;span class="lnt"&gt;17
&lt;/span&gt;&lt;span class="lnt"&gt;18
&lt;/span&gt;&lt;span class="lnt"&gt;19
&lt;/span&gt;&lt;span class="lnt"&gt;20
&lt;/span&gt;&lt;span class="lnt"&gt;21
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; Kubernetes API
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; 用户提交 SubPool Custom Resource
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; SubRelay Operator
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; 持续执行 reconcile
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ┌───────────────┴────────────────┐
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ▼ ▼
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; subpool-&amp;lt;pool-id&amp;gt; Namespace 读取共享基础设施状态
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ┌────────┼────────┐ ┌────────┴────────┐
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ │ │ │ │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; StatefulSet Service PVC Shared Mihomo Shared Envoy
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ Gateway Gateway
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; ┌───┴──────────┐ ▲ │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ CPA │──── HTTP Proxy ──────┘ │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; │ cpa-manager │ │
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; └──────────────┘◀──── HTTPRoute（仅 Gateway 模式）────┘
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这里有两条不同方向的数据流：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;入站流量：用户访问 CPA。开发时通常通过 &lt;code&gt;kubectl port-forward&lt;/code&gt;；生产的 Gateway 模式通过 Envoy Gateway 和 &lt;code&gt;HTTPRoute&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;出站流量：CPA 访问外部服务时，只允许走 Shared Mihomo Gateway，再由 Mihomo 选择健康的上游代理。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;不要把二者混为一谈。Envoy 解决“外面怎么进来”，Mihomo 解决“里面怎么出去”。&lt;/p&gt;
&lt;h2 id="三层结构声明控制和数据"&gt;三层结构：声明、控制和数据
&lt;/h2&gt;&lt;h3 id="声明层subpool-cr"&gt;声明层：SubPool CR
&lt;/h3&gt;&lt;p&gt;用户不直接创建十几种底层资源，而是提交一个 &lt;code&gt;SubPool&lt;/code&gt;：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;span class="lnt"&gt;13
&lt;/span&gt;&lt;span class="lnt"&gt;14
&lt;/span&gt;&lt;span class="lnt"&gt;15
&lt;/span&gt;&lt;span class="lnt"&gt;16
&lt;/span&gt;&lt;span class="lnt"&gt;17
&lt;/span&gt;&lt;span class="lnt"&gt;18
&lt;/span&gt;&lt;span class="lnt"&gt;19
&lt;/span&gt;&lt;span class="lnt"&gt;20
&lt;/span&gt;&lt;span class="lnt"&gt;21
&lt;/span&gt;&lt;span class="lnt"&gt;22
&lt;/span&gt;&lt;span class="lnt"&gt;23
&lt;/span&gt;&lt;span class="lnt"&gt;24
&lt;/span&gt;&lt;span class="lnt"&gt;25
&lt;/span&gt;&lt;span class="lnt"&gt;26
&lt;/span&gt;&lt;span class="lnt"&gt;27
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;apiVersion&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subrelay.example.com/v1alpha1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;SubPool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;metadata&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;exposure&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;mode&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;Internal&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;storage&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;size&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;20Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpa&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;image&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;registry.example.invalid/subrelay/cpa:vX.Y.Z&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;resources&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;requests&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;2200m&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;3Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;limits&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;3200m&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;4Gi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpaManager&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;image&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;registry.example.invalid/subrelay/cpa-manager:vX.Y.Z&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;resources&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;requests&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;200m&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;256Mi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;limits&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;cpu&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;300m&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;memory&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;512Mi&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这是项目对使用者提供的业务 API。使用者只表达“我要什么”，不必自己拼装 Namespace、PVC、Service 和 NetworkPolicy。&lt;/p&gt;
&lt;h3 id="控制层operator"&gt;控制层：Operator
&lt;/h3&gt;&lt;p&gt;Operator 是一个运行在 &lt;code&gt;subpool-system&lt;/code&gt; Namespace 中的 Deployment。它监听 &lt;code&gt;SubPool&lt;/code&gt; 的变化，然后不断执行控制循环：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;读取当前状态 → 计算期望状态 → 创建或修正资源 → 更新 status → 等待下一次事件
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这套循环叫 &lt;code&gt;reconcile&lt;/code&gt;。它不是只执行一次的安装脚本；只要资源被误删或配置发生漂移，Operator 还会尝试把它恢复到期望状态。&lt;/p&gt;
&lt;h3 id="数据层cpacpa-manager-与网关"&gt;数据层：CPA、cpa-manager 与网关
&lt;/h3&gt;&lt;p&gt;每个号池的 CPA 和 cpa-manager 运行在同一个 Pod 中，共享网络和 RWO 数据卷。Shared Mihomo 与 Envoy Gateway 是集群级共享基础设施，不归任何单个 &lt;code&gt;SubPool&lt;/code&gt; 所有。&lt;/p&gt;
&lt;p&gt;这个边界很关键：删除一个 &lt;code&gt;SubPool&lt;/code&gt; 可以删除它自己的 Namespace，但不能顺手删除共享网关，否则一个号池的生命周期操作会影响所有号池。&lt;/p&gt;
&lt;h2 id="创建一个-subpool-后会发生什么"&gt;创建一个 SubPool 后会发生什么
&lt;/h2&gt;&lt;p&gt;以 &lt;code&gt;demo-pool&lt;/code&gt; 为例，Operator 会派生 Namespace：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subpool-demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;随后创建或维护这些资源：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;资源&lt;/th&gt;
					&lt;th&gt;用途&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;Namespace&lt;/td&gt;
					&lt;td&gt;形成号池隔离边界&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;ServiceAccount&lt;/td&gt;
					&lt;td&gt;给运行时 Pod 提供身份，同时禁止自动挂载 token&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Secret&lt;/td&gt;
					&lt;td&gt;保存随机生成的 CPA 和管理端凭据&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;ConfigMap&lt;/td&gt;
					&lt;td&gt;保存幂等初始化脚本&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;StatefulSet&lt;/td&gt;
					&lt;td&gt;在一个 Pod 中运行 CPA 与 cpa-manager&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PVC&lt;/td&gt;
					&lt;td&gt;保存两套程序的持久数据&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;两个 Service&lt;/td&gt;
					&lt;td&gt;分别提供 CPA 和 cpa-manager 的稳定入口&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;ResourceQuota&lt;/td&gt;
					&lt;td&gt;限制整个号池 Namespace 的资源上限&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PodDisruptionBudget&lt;/td&gt;
					&lt;td&gt;限制自愿中断期间的可用副本数&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;NetworkPolicy&lt;/td&gt;
					&lt;td&gt;默认拒绝，并只开放必要流量&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;HTTPRoute&lt;/td&gt;
					&lt;td&gt;仅 Gateway 模式创建，将域名流量转给 CPA&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Operator 还会把检查结果写入 &lt;code&gt;SubPool.status.conditions&lt;/code&gt;，让使用者知道究竟卡在工作负载、代理网关还是外部入口。&lt;/p&gt;
&lt;h2 id="两种访问模式"&gt;两种访问模式
&lt;/h2&gt;&lt;h3 id="internal适合开发和管理"&gt;Internal：适合开发和管理
&lt;/h3&gt;&lt;p&gt;&lt;code&gt;Internal&lt;/code&gt; 只创建 ClusterIP Service，不创建公网入口。访问方式是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool port-forward service/cpa 8317:8317
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool port-forward service/cpa-manager 18317:18317
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它的优点是简单、安全、几乎不依赖集群外部设施。缺点是终端一断开，转发就停止，因此不适合作为生产入口。&lt;/p&gt;
&lt;h3 id="gateway适合统一的外部入口"&gt;Gateway：适合统一的外部入口
&lt;/h3&gt;&lt;p&gt;&lt;code&gt;Gateway&lt;/code&gt; 模式会额外创建 &lt;code&gt;HTTPRoute&lt;/code&gt;，使用 &lt;code&gt;&amp;lt;pool-id&amp;gt;.&amp;lt;baseDomain&amp;gt;&lt;/code&gt; 这样的域名连接 Shared Envoy Gateway。&lt;/p&gt;
&lt;p&gt;Operator 不会为每个号池创建独立 LoadBalancer、DNS 记录或 TLS 证书。共享入口只维护一套，单个号池只维护自己的路由。这既节约成本，也让证书、域名和入口安全策略集中管理。&lt;/p&gt;
&lt;h2 id="ready-到底表示什么"&gt;Ready 到底表示什么
&lt;/h2&gt;&lt;p&gt;本项目的 &lt;code&gt;Ready=True&lt;/code&gt; 表示“空号池平台已经准备好”，具体包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;StatefulSet 至少有一个 Ready replica；&lt;/li&gt;
&lt;li&gt;CPA 与 cpa-manager Service 存在；&lt;/li&gt;
&lt;li&gt;Shared Mihomo Service 有 Ready Endpoint；&lt;/li&gt;
&lt;li&gt;当前暴露模式的前提已满足；&lt;/li&gt;
&lt;li&gt;Operator 已观察到当前这一版 spec。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它不表示以下业务事实：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;已导入账号；&lt;/li&gt;
&lt;li&gt;OAuth 一定有效；&lt;/li&gt;
&lt;li&gt;Mihomo 的每个上游节点都可用；&lt;/li&gt;
&lt;li&gt;真实模型请求已经成功；&lt;/li&gt;
&lt;li&gt;每个请求都会切换出口 IP。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;基础设施 Ready 和业务 Ready 是两个层次。把边界写清楚，比做一个“看起来什么都检查了、实际上没人知道含义”的健康状态更可靠。&lt;/p&gt;
&lt;h2 id="生命周期不是只有创建"&gt;生命周期不是只有创建
&lt;/h2&gt;&lt;p&gt;一个 &lt;code&gt;SubPool&lt;/code&gt; 主要有四种操作：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;操作&lt;/th&gt;
					&lt;th&gt;发生什么&lt;/th&gt;
					&lt;th&gt;数据是否保留&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;创建&lt;/td&gt;
					&lt;td&gt;建 Namespace 和全部运行资源&lt;/td&gt;
					&lt;td&gt;新建数据&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;更新&lt;/td&gt;
					&lt;td&gt;镜像和计算资源在策略范围内收敛&lt;/td&gt;
					&lt;td&gt;保留&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;暂停&lt;/td&gt;
					&lt;td&gt;StatefulSet 缩容为 0，删除 HTTPRoute&lt;/td&gt;
					&lt;td&gt;保留 Namespace、PVC 和 Secret&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;删除&lt;/td&gt;
					&lt;td&gt;删除专属 Namespace，等待清理完成&lt;/td&gt;
					&lt;td&gt;永久删除&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;暂停和删除的区别非常大。只是临时停用时应设置 &lt;code&gt;spec.suspend: true&lt;/code&gt;，不要删除 CR。&lt;/p&gt;
&lt;h2 id="推荐阅读顺序"&gt;推荐阅读顺序
&lt;/h2&gt;&lt;p&gt;这套笔记按依赖关系排列：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;先读 Kubernetes 对象、Namespace、Label 和 Selector，建立“对象如何互相找到”的直觉。&lt;/li&gt;
&lt;li&gt;再读 Pod、多容器、StatefulSet、PVC、Service，理解号池的运行实体。&lt;/li&gt;
&lt;li&gt;接着读 ConfigMap、Secret、健康探针、资源管理和 NetworkPolicy，理解稳定性与安全基线。&lt;/li&gt;
&lt;li&gt;然后读 Gateway API、RBAC 和 leader election，理解共享入口及控制面高可用。&lt;/li&gt;
&lt;li&gt;最后读 CRD、reconcile、finalizer 与 Kustomize，把所有积木拼回完整 Operator。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id="读代码时从哪里下手"&gt;读代码时从哪里下手
&lt;/h2&gt;&lt;p&gt;项目中的关键入口如下：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;路径&lt;/th&gt;
					&lt;th&gt;建议关注点&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;examples/subpool-internal.yaml&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;用户最小输入是什么&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;api/v1alpha1/subpool_types.go&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;CRD 的 Go 类型和校验规则&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;controllers/subpool_controller.go&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;reconcile 入口、watch 和 finalizer&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;controllers/subpool_reconcile.go&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;创建资源的顺序与阻塞条件&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;internal/subpool/resources.go&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;一个 SubPool 会生成哪些对象&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;internal/subpool/workloads.go&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;StatefulSet、容器、探针和存储细节&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;internal/subpool/network.go&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;NetworkPolicy 与 HTTPRoute&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;config/default/operator-config.yaml&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;全局平台配置和资源包络&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;deploy/gateway/&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Shared Mihomo 与 Envoy Gateway&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;学习 Kubernetes 最舒服的方式不是背完所有 Kind，而是每次追踪一个具体问题：这个对象是谁创建的、被谁选择、依赖谁、坏了看哪里、删除时谁负责收尾。这个项目正好把这些问题串成了一条完整链路。&lt;/p&gt;</description></item><item><title>从 YAML 开始理解 Kubernetes 对象</title><link>https://kerolt.github.io/p/%E4%BB%8E-yaml-%E5%BC%80%E5%A7%8B%E7%90%86%E8%A7%A3-kubernetes-%E5%AF%B9%E8%B1%A1/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/%E4%BB%8E-yaml-%E5%BC%80%E5%A7%8B%E7%90%86%E8%A7%A3-kubernetes-%E5%AF%B9%E8%B1%A1/</guid><description>&lt;h2 id="yaml-不是-kubernetesyaml-只是申请表"&gt;YAML 不是 Kubernetes，YAML 只是申请表
&lt;/h2&gt;&lt;p&gt;初学 Kubernetes 时，我们每天都在写 YAML，很容易产生一种错觉：Kubernetes 就是一堆 YAML。&lt;/p&gt;
&lt;p&gt;更准确的理解是：YAML 只是把一个 API 对象序列化成文本。真正重要的是 API Server 中保存的对象，以及控制器围绕这些对象持续运行的控制循环。&lt;/p&gt;
&lt;p&gt;下面这个文件不是一段“创建脚本”，而是一份期望状态声明：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;span class="lnt"&gt;7
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;apiVersion&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subrelay.example.com/v1alpha1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;SubPool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;metadata&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;spec&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;exposure&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;mode&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;Internal&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它表达的是：“集群里应该存在一个名为 &lt;code&gt;demo-pool&lt;/code&gt; 的 &lt;code&gt;SubPool&lt;/code&gt;，它的暴露模式应该是 &lt;code&gt;Internal&lt;/code&gt;。”&lt;/p&gt;
&lt;h2 id="每个对象都有四个核心部分"&gt;每个对象都有四个核心部分
&lt;/h2&gt;&lt;h3 id="apiversion去哪个-api-组找规则"&gt;apiVersion：去哪个 API 组找规则
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;apiVersion&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;subrelay.example.com/v1alpha1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它由 API group 和 version 组成：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;API group：&lt;code&gt;subrelay.example.com&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;version：&lt;code&gt;v1alpha1&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Kubernetes 内置核心资源常用 &lt;code&gt;apiVersion: v1&lt;/code&gt;，例如 Namespace、Service、ConfigMap 和 Secret。其他资源会带 API group：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;资源&lt;/th&gt;
					&lt;th&gt;apiVersion&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;Deployment、StatefulSet&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;apps/v1&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;NetworkPolicy&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;networking.k8s.io/v1&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;PodDisruptionBudget&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;policy/v1&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;HTTPRoute、Gateway&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;gateway.networking.k8s.io/v1&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;SubPool&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;subrelay.example.com/v1alpha1&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;code&gt;apiVersion&lt;/code&gt; 不是镜像版本，也不是 Kubernetes 集群版本。它决定 API Server 用哪套 schema 解析和校验对象。&lt;/p&gt;
&lt;h3 id="kind这张申请表是什么类型"&gt;kind：这张申请表是什么类型
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;SubPool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;&lt;code&gt;kind&lt;/code&gt; 决定对象的类别。不同 Kind 有不同的 &lt;code&gt;spec&lt;/code&gt; 结构。例如 Service 的 &lt;code&gt;spec&lt;/code&gt; 里有端口和 selector，StatefulSet 的 &lt;code&gt;spec&lt;/code&gt; 里有 Pod template 和 volume claim template。&lt;/p&gt;
&lt;h3 id="metadata对象的身份证"&gt;metadata：对象的身份证
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;metadata&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;demo-pool&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;常见 metadata 包括：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;字段&lt;/th&gt;
					&lt;th&gt;作用&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;name&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;对象在作用域内的名称&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;namespace&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;对象属于哪个 Namespace&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;labels&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;供查询、选择和策略匹配使用的键值对&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;annotations&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;给工具或控制器使用的非标识性信息&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;ownerReferences&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;表示资源归谁管理&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;generation&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;spec 被修改后递增的代数&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;resourceVersion&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;API Server 用于并发控制的版本&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;finalizers&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;删除前必须完成的收尾任务&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;本项目的 &lt;code&gt;SubPool&lt;/code&gt; 是 cluster-scoped，所以它没有 &lt;code&gt;metadata.namespace&lt;/code&gt;。即使执行 &lt;code&gt;kubectl -n test get subpool&lt;/code&gt;，&lt;code&gt;-n test&lt;/code&gt; 也不会让它突然变成 Namespace 内资源。&lt;/p&gt;
&lt;h3 id="spec-与-status愿望和现实"&gt;spec 与 status：愿望和现实
&lt;/h3&gt;&lt;p&gt;&lt;code&gt;spec&lt;/code&gt; 是用户或上层控制器表达的期望状态，&lt;code&gt;status&lt;/code&gt; 是控制器观察到的现实状态。&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;spec：我希望有一套 Internal 模式、20Gi 存储的号池
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;status：它已经派生出 subpool-demo-pool，目前代理 Endpoint 尚未就绪
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;在本项目里，&lt;code&gt;spec&lt;/code&gt; 包含镜像、CPU、内存、存储、暴露模式和暂停开关；&lt;code&gt;status&lt;/code&gt; 包含 Namespace、CPA Service、hostname、&lt;code&gt;observedGeneration&lt;/code&gt; 和 Conditions。&lt;/p&gt;
&lt;p&gt;不要手工编辑 status。它属于控制器，不是用户配置区。&lt;/p&gt;
&lt;h2 id="kubectl-apply-实际做了什么"&gt;kubectl apply 实际做了什么
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -f examples/subpool-internal.yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这条命令大致经历：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;kubectl&lt;/code&gt; 读取 YAML。&lt;/li&gt;
&lt;li&gt;根据当前 kubeconfig 把请求发给 API Server。&lt;/li&gt;
&lt;li&gt;API Server 做认证、授权、schema 校验和 admission。&lt;/li&gt;
&lt;li&gt;对象被持久化。&lt;/li&gt;
&lt;li&gt;监听这个对象的 controller 收到事件。&lt;/li&gt;
&lt;li&gt;controller 开始 reconcile。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;kubectl&lt;/code&gt; 并没有亲自创建 StatefulSet 或 PVC。那些资源是 SubRelay Operator 看到 &lt;code&gt;SubPool&lt;/code&gt; 后创建的。&lt;/p&gt;
&lt;p&gt;这也是排障时必须分层的原因：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;kubectl apply&lt;/code&gt; 失败：通常还没通过 API Server 校验或授权。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SubPool&lt;/code&gt; 已存在但没有派生资源：看 Operator 日志和 Conditions。&lt;/li&gt;
&lt;li&gt;StatefulSet 已创建但 Pod 不 Ready：看 Pod、Event、探针、镜像和存储。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="声明式管理与命令式操作"&gt;声明式管理与命令式操作
&lt;/h2&gt;&lt;p&gt;Kubernetes 同时支持两种操作方式。&lt;/p&gt;
&lt;p&gt;命令式操作描述“执行什么动作”：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl create namespace demo
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl scale statefulset runtime --replicas&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="m"&gt;0&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;声明式操作描述“最终应该是什么样”：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl apply -f object.yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;Operator 项目更依赖声明式思维。用户修改 &lt;code&gt;SubPool.spec&lt;/code&gt;，Operator 再把底层资源收敛到新状态。手工修改 Operator 管理的 StatefulSet，可能暂时生效，但下一轮 reconcile 会把它改回来。&lt;/p&gt;
&lt;p&gt;可以把这叫作“方向盘归 Operator”：底层对象的期望状态来源不是临时的 &lt;code&gt;kubectl edit&lt;/code&gt;，而是 &lt;code&gt;SubPool&lt;/code&gt; 和 Operator Configuration。&lt;/p&gt;
&lt;h2 id="createapplypatchedit-的区别"&gt;create、apply、patch、edit 的区别
&lt;/h2&gt;&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;命令&lt;/th&gt;
					&lt;th&gt;适合场景&lt;/th&gt;
					&lt;th&gt;注意事项&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;kubectl create&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;明确只创建一次&lt;/td&gt;
					&lt;td&gt;对象已存在时失败&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;kubectl apply&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;持续声明式维护&lt;/td&gt;
					&lt;td&gt;适合 Git 中的 manifests&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;kubectl patch&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;修改少数字段&lt;/td&gt;
					&lt;td&gt;适合暂停、恢复等小操作&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;kubectl edit&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;临时交互修改&lt;/td&gt;
					&lt;td&gt;容易留下不可复现的配置&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;kubectl replace&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;用完整对象替换&lt;/td&gt;
					&lt;td&gt;对资源版本和不可变字段更敏感&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;本项目暂停号池使用 merge patch：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl patch subpool demo-pool --type&lt;span class="o"&gt;=&lt;/span&gt;merge &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -p &lt;span class="s1"&gt;&amp;#39;{&amp;#34;spec&amp;#34;:{&amp;#34;suspend&amp;#34;:true}}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这只修改 &lt;code&gt;spec.suspend&lt;/code&gt;，其他字段保持不变。&lt;/p&gt;
&lt;h2 id="服务端校验为什么重要"&gt;服务端校验为什么重要
&lt;/h2&gt;&lt;p&gt;&lt;code&gt;SubPool&lt;/code&gt; 的 CRD 给 API Server 安装了一份 schema。例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;exposure.mode&lt;/code&gt; 只能是 &lt;code&gt;Internal&lt;/code&gt; 或 &lt;code&gt;Gateway&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;metadata.name&lt;/code&gt; 最长 40 个字符，并且必须是 DNS label；&lt;/li&gt;
&lt;li&gt;CPU 和内存的 request/limit 必须存在；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;storage.size&lt;/code&gt; 创建后不可修改。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这类静态错误应尽量在对象进入系统前被拒绝。动态依赖则不能只靠 schema，例如 Shared Mihomo 是否有 Ready Endpoint，需要 Operator 运行时检查并写入 Condition。&lt;/p&gt;
&lt;p&gt;因此项目用了两层防线：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;CRD OpenAPI/CEL：检查格式和不可变规则
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Operator reconcile：检查资源预算、共享网关和所有权等动态条件
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="名称作用域与完整身份"&gt;名称、作用域与完整身份
&lt;/h2&gt;&lt;p&gt;Namespaced 对象的完整身份通常是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;API Group + Kind + Namespace + Name
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;例如：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;v1 / Service / subpool-demo-pool / cpa
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;不同 Namespace 中可以同时存在名为 &lt;code&gt;cpa&lt;/code&gt; 的 Service。因此每个 SubPool 都可以使用固定资源名，而不会互相冲突。&lt;/p&gt;
&lt;p&gt;Cluster-scoped 对象没有 Namespace，例如：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;subrelay.example.com/v1alpha1 / SubPool / demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这也是 &lt;code&gt;SubPool&lt;/code&gt; 名称必须全局唯一的原因。&lt;/p&gt;
&lt;h2 id="读取对象时先看哪里"&gt;读取对象时先看哪里
&lt;/h2&gt;&lt;p&gt;初学者常对着完整 YAML 从第一行看到最后一行，很快被 &lt;code&gt;managedFields&lt;/code&gt; 淹没。更有效的顺序是：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl describe sp demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;阅读时优先关注：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;metadata.name&lt;/code&gt;、&lt;code&gt;namespace&lt;/code&gt;、labels 和 owner references；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;spec&lt;/code&gt; 是否等于你的期望；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;status.conditions&lt;/code&gt; 的 &lt;code&gt;type&lt;/code&gt;、&lt;code&gt;status&lt;/code&gt;、&lt;code&gt;reason&lt;/code&gt;、&lt;code&gt;message&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;metadata.generation&lt;/code&gt; 是否已经被 &lt;code&gt;status.observedGeneration&lt;/code&gt; 观察；&lt;/li&gt;
&lt;li&gt;最后才看工具自动维护的内部字段。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;项目中可用更紧凑的命令看 Conditions：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -o &lt;span class="nv"&gt;jsonpath&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s1"&gt;&amp;#39;{range .status.conditions[*]}{.type}={.status}{&amp;#34;\t&amp;#34;}{.reason}{&amp;#34;\n&amp;#34;}{end}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="删除也是一次状态转换"&gt;删除也是一次状态转换
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl delete subpool demo-pool
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;API Server 通常不会立刻把带 finalizer 的对象物理删除，而是先设置 &lt;code&gt;deletionTimestamp&lt;/code&gt;。Operator 看到它后删除专属 Namespace，等待子资源清理完毕，最后移除 finalizer。&lt;/p&gt;
&lt;p&gt;因此“执行了 delete”与“对象已经消失”不是同一件事。对象停在 &lt;code&gt;Terminating&lt;/code&gt; 时，应检查 finalizer 和负责收尾的 controller，而不是直接 &lt;code&gt;--force&lt;/code&gt;。&lt;/p&gt;
&lt;h2 id="本项目中的对象阅读练习"&gt;本项目中的对象阅读练习
&lt;/h2&gt;&lt;p&gt;可以按下面顺序观察一个号池：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get namespace subpool-demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get statefulset runtime -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get service cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pvc
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;每看一个对象，都回答五个问题：它是谁、在哪里、谁创建它、它选择谁、它当前是否达到期望。只要这五个问题能回答清楚，大部分 Kubernetes YAML 就不再神秘。&lt;/p&gt;</description></item><item><title>健康探针、Conditions 与真正的 Ready</title><link>https://kerolt.github.io/p/%E5%81%A5%E5%BA%B7%E6%8E%A2%E9%92%88conditions-%E4%B8%8E%E7%9C%9F%E6%AD%A3%E7%9A%84-ready/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><guid>https://kerolt.github.io/p/%E5%81%A5%E5%BA%B7%E6%8E%A2%E9%92%88conditions-%E4%B8%8E%E7%9C%9F%E6%AD%A3%E7%9A%84-ready/</guid><description>&lt;h2 id="进程还在与系统可用不是同一件事"&gt;“进程还在”与“系统可用”不是同一件事
&lt;/h2&gt;&lt;p&gt;一个容器进程存在，只能说明它还没有退出。它可能仍在加载数据、已经死锁、依赖的网关不可用，甚至根本无法处理请求。&lt;/p&gt;
&lt;p&gt;本项目用了两层健康表达：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Pod probes：kubelet 判断单个容器是否启动、存活、能否接流量；&lt;/li&gt;
&lt;li&gt;SubPool Conditions：Operator 判断整套空号池平台的依赖是否满足。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;两层都叫“健康”，但观察范围完全不同。&lt;/p&gt;
&lt;h2 id="三种-probe-分别问什么"&gt;三种 Probe 分别问什么
&lt;/h2&gt;&lt;h3 id="startupprobe你启动完了吗"&gt;startupProbe：你启动完了吗
&lt;/h3&gt;&lt;p&gt;慢启动应用可能需要初始化文件、打开数据库或加载大量配置。startupProbe 在成功前，会保护应用不被 livenessProbe 过早判死。&lt;/p&gt;
&lt;p&gt;本项目 CPA 和 cpa-manager 都允许最多约 150 秒启动：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;failureThreshold: 30
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;periodSeconds: 5
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;粗略上限是 &lt;code&gt;30 × 5 = 150&lt;/code&gt; 秒，实际还会受到 timeout 和执行时间影响。&lt;/p&gt;
&lt;h3 id="readinessprobe现在可以接流量吗"&gt;readinessProbe：现在可以接流量吗
&lt;/h3&gt;&lt;p&gt;readinessProbe 失败时，容器不一定被重启，但 Pod 会变成 NotReady，通常也不会作为 Service 的 Ready Endpoint 接收流量。&lt;/p&gt;
&lt;p&gt;它适合表达暂时不可服务的状态，例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;应用还没完成初始化；&lt;/li&gt;
&lt;li&gt;本地依赖暂时不可用；&lt;/li&gt;
&lt;li&gt;应用正在优雅排空请求。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="livenessprobe你是不是已经坏到需要重启"&gt;livenessProbe：你是不是已经坏到需要重启
&lt;/h3&gt;&lt;p&gt;livenessProbe 持续失败时，kubelet 会重启容器。它应检测无法自行恢复的故障，而不是把所有外部依赖都塞进去。&lt;/p&gt;
&lt;p&gt;如果 livenessProbe 强依赖互联网或共享网关，一个短暂外部故障可能让整个集群中的 Pod 同时重启，造成故障放大。&lt;/p&gt;
&lt;h2 id="本项目的探针设计"&gt;本项目的探针设计
&lt;/h2&gt;&lt;p&gt;CPA 使用 HTTP GET &lt;code&gt;/&lt;/code&gt;，端口 8317；cpa-manager 使用 HTTP GET &lt;code&gt;/health&lt;/code&gt;，端口 18317。&lt;/p&gt;
&lt;p&gt;Mihomo Gateway 使用 TCP socket 探测 18000 端口。TCP 探针只证明进程正在监听端口，不能证明每个代理节点可用或访问外部网站一定成功。&lt;/p&gt;
&lt;p&gt;Operator 自己暴露：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;/healthz&lt;/code&gt;：进程健康；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/readyz&lt;/code&gt;：controller manager 准备状态。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;探针越靠近应用语义，结论越强，但也越可能被外部依赖干扰。设计时要明确“这个探针失败后，Kubernetes 会采取什么动作”。&lt;/p&gt;
&lt;h2 id="probe-失败后谁做什么"&gt;Probe 失败后谁做什么
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;span class="lnt"&gt;11
&lt;/span&gt;&lt;span class="lnt"&gt;12
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;startupProbe 失败
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 主容器继续等待或最终被重启
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → readiness/liveness 暂不接管
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;readinessProbe 失败
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → Pod NotReady
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → EndpointSlice 标记后端不可用
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → 通常不再接收 Service 新流量
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;livenessProbe 失败
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → kubelet 重启该容器
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; → restartCount 增加
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;在多容器 Pod 中，一个容器重启不一定重建整个 Pod；但任何一个普通容器不 Ready，Pod 整体通常不会 Ready。&lt;/p&gt;
&lt;h2 id="subpool-conditions-是系统级状态"&gt;SubPool Conditions 是系统级状态
&lt;/h2&gt;&lt;p&gt;本项目固定维护这些 Conditions：&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;Condition&lt;/th&gt;
					&lt;th&gt;True 表示什么&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;InfrastructureReady&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;StatefulSet 有 Ready replica，两个内部 Service 存在&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;ProxyGatewayReady&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Shared Mihomo Service 至少有一个 Ready Endpoint&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;ExposureReady&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;Internal 边界成立，或 Gateway Route/Gateway 已就绪&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;PlatformReady&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;上面三类依赖同时满足&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;Ready&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;当前 generation 的空号池平台已就绪&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;DeletionBlocked&lt;/code&gt;&lt;/td&gt;
					&lt;td&gt;删除正被 Namespace 或 finalizer 阻塞&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;依赖关系可以画成：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;InfrastructureReady ─┐
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;ProxyGatewayReady ───┼─&amp;gt; PlatformReady ─&amp;gt; Ready
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;ExposureReady ───────┘
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;这样比只有一个模糊的 &lt;code&gt;Ready=False&lt;/code&gt; 更容易排查。&lt;/p&gt;
&lt;h2 id="condition-的四个核心字段"&gt;Condition 的四个核心字段
&lt;/h2&gt;&lt;p&gt;一个标准 Condition 常包含：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;type&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;ProxyGatewayReady&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;status&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;False&amp;#34;&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;reason&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;EndpointsUnavailable&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;message&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;Shared Mihomo Gateway has no ready Endpoint&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;阅读顺序：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;type&lt;/code&gt;：在检查哪个维度；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;status&lt;/code&gt;：True、False 或 Unknown；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;reason&lt;/code&gt;：机器可读、相对稳定的原因码；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;message&lt;/code&gt;：给人看的上下文说明。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;自动化应优先依赖 &lt;code&gt;type/status/reason&lt;/code&gt;，不要用字符串包含关系解析 message。&lt;/p&gt;
&lt;h2 id="observedgeneration-防止读到过期的好消息"&gt;observedGeneration 防止读到过期的好消息
&lt;/h2&gt;&lt;p&gt;每次修改 SubPool spec，&lt;code&gt;metadata.generation&lt;/code&gt; 都会递增。Operator 完成本轮状态计算后，把这代数写入：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;status&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;observedGeneration&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;3&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;如果出现：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;metadata.generation = 4
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;status.observedGeneration = 3
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;说明 status 仍对应旧 spec。即使 Ready 还是 True，也不能直接当作新配置已生效。&lt;/p&gt;
&lt;p&gt;这是异步控制系统的重要常识：状态有时间差，必须知道控制器观察的是哪一代期望。&lt;/p&gt;
&lt;h2 id="internal-模式为什么-exposureready-可以为-true"&gt;Internal 模式为什么 ExposureReady 可以为 True
&lt;/h2&gt;&lt;p&gt;Internal 模式不要求外部 Gateway。它的 ExposureReady 表示设计中的访问边界已经成立：CPA 是 ClusterIP，管理员可通过受控 port-forward 访问。&lt;/p&gt;
&lt;p&gt;这不是说 CPA 已经公网可达。Condition 的含义必须结合产品契约，而不能凭名字猜。&lt;/p&gt;
&lt;p&gt;Gateway 模式则要求：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;HTTPRoute 存在；&lt;/li&gt;
&lt;li&gt;Route 被指定 Gateway listener Accepted；&lt;/li&gt;
&lt;li&gt;Route 的引用已成功解析；&lt;/li&gt;
&lt;li&gt;Shared Gateway 的 &lt;code&gt;Programmed=True&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;所以同一个 Condition type 在两种模式下有不同的检查路径，但都回答“所选择的暴露方式是否准备好”。&lt;/p&gt;
&lt;h2 id="readytrue-明确不检查什么"&gt;Ready=True 明确不检查什么
&lt;/h2&gt;&lt;p&gt;本项目把 &lt;code&gt;Ready&lt;/code&gt; 限定为“空号池平台 Ready”，不承诺：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;账号已经导入；&lt;/li&gt;
&lt;li&gt;OAuth token 有效；&lt;/li&gt;
&lt;li&gt;真实模型请求成功；&lt;/li&gt;
&lt;li&gt;所有上游代理节点健康；&lt;/li&gt;
&lt;li&gt;每个 HTTP 请求切换一次 IP。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这是负责任的状态设计。若控制器没有证据验证某件事，就不应让 Ready 暗示它已经验证。&lt;/p&gt;
&lt;p&gt;未来可以新增业务级健康对象或指标，但不能悄悄改变现有 Ready 的语义，否则旧自动化会被误导。&lt;/p&gt;
&lt;h2 id="kubectl-wait-在等什么"&gt;kubectl wait 在等什么
&lt;/h2&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl &lt;span class="nb"&gt;wait&lt;/span&gt; --for&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="nv"&gt;condition&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;Ready &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; subpool/demo-pool --timeout&lt;span class="o"&gt;=&lt;/span&gt;300s
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;它等待名为 &lt;code&gt;Ready&lt;/code&gt; 的 Condition 变为 True。超时不代表 kubectl 出错，只表示在指定时间内条件没有满足。&lt;/p&gt;
&lt;p&gt;超时后不要立刻重复 wait，应读取 Conditions：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get sp demo-pool &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -o &lt;span class="nv"&gt;jsonpath&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s1"&gt;&amp;#39;{range .status.conditions[*]}{.type}={.status}{&amp;#34;\t&amp;#34;}{.reason}{&amp;#34;\t&amp;#34;}{.message}{&amp;#34;\n&amp;#34;}{end}&amp;#39;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;然后沿失败维度继续检查。&lt;/p&gt;
&lt;h2 id="从-condition-到对象的排查路径"&gt;从 Condition 到对象的排查路径
&lt;/h2&gt;&lt;h3 id="infrastructurereadyfalse"&gt;InfrastructureReady=False
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get statefulset,pod,service,pvc
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool describe pod runtime-0
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h3 id="proxygatewayreadyfalse"&gt;ProxyGatewayReady=False
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get deployment,service,pod
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get endpointslice &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -l kubernetes.io/service-name&lt;span class="o"&gt;=&lt;/span&gt;mihomo-gateway
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h3 id="exposurereadyfalse-且为-gateway-模式"&gt;ExposureReady=False 且为 Gateway 模式
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get httproute cpa -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-gateway get gateway subpool-edge -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h3 id="deletionblockedtrue"&gt;DeletionBlocked=True
&lt;/h3&gt;&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl get namespace subpool-demo-pool -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;kubectl -n subpool-demo-pool get pvc -o yaml
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h2 id="常见探针误区"&gt;常见探针误区
&lt;/h2&gt;&lt;h3 id="livenessprobe-做得越深越好"&gt;livenessProbe 做得越深越好
&lt;/h3&gt;&lt;p&gt;错误。它失败会触发重启，检查外部依赖过深容易制造重启风暴。&lt;/p&gt;
&lt;h3 id="readinessprobe-失败就一定要重启"&gt;readinessProbe 失败就一定要重启
&lt;/h3&gt;&lt;p&gt;不一定。暂时不能接流量与进程不可恢复是两个问题。&lt;/p&gt;
&lt;h3 id="tcp-probe-成功等于业务正常"&gt;TCP probe 成功等于业务正常
&lt;/h3&gt;&lt;p&gt;不等于。它只证明端口可建立 TCP 连接。&lt;/p&gt;
&lt;h3 id="pod-ready-等于整个平台-ready"&gt;Pod Ready 等于整个平台 Ready
&lt;/h3&gt;&lt;p&gt;不等于。Pod 可能 Ready，但 Shared Mihomo 或 Gateway 尚未就绪。&lt;/p&gt;
&lt;h3 id="readyfalse-都是故障"&gt;Ready=False 都是故障
&lt;/h3&gt;&lt;p&gt;不一定。新建中的资源、显式 suspend 或等待外部基础设施时，False 可能是诚实的过渡状态。&lt;/p&gt;
&lt;p&gt;健康检查的价值不在于把所有东西染成绿色，而在于让每一盏灯都有准确、稳定、可行动的含义。&lt;/p&gt;</description></item></channel></rss>