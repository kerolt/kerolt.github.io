<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C++中lambda与priority_queue一起使用</title>
    <url>/posts/3096ce14/</url>
    <content><![CDATA[<p>想写这篇博客的原因是在刷力扣的 <a href="https://leetcode.cn/problems/top-k-frequent-elements/">347. 前 K 个高频元素</a> 一题时，需要使用到优先队列<code>priority_queue</code>，其定义如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;</span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">T</span>,</span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">Container</span> = std::vector&lt;T&gt;,</span><br><span class="line">    <span class="keyword">class</span> Compare = std::less&lt;<span class="keyword">typename</span> Container::value_type&gt;</span><br><span class="line">&gt; <span class="keyword">class</span> priority_queue;</span><br></pre></td></tr></table></figure>
<p>第三个参数是一个可以自定义的比较类型，其必须满足<a href="https://zh.cppreference.com/w/cpp/named_req/BinaryPredicate">二元谓词</a>，通常可以使用如下两种方法：</p>
<ol>
<li>使用自定义的函数对象</li>
<li>lambda表达式</li>
<li>使用<code>std::greater</code>或<code>std::less</code>（这里就不介绍这种方法了）</li>
</ol>
<span id="more"></span>
<p>以题 <a href="https://leetcode.cn/problems/top-k-frequent-elements/">347. 前 K 个高频元素</a> 为例，我们要建立一个小根堆，那么代码如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 方法一</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> PII = pair&lt;<span class="type">int</span>, <span class="type">int</span>&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 比较类，重载了括号运算符</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Comp</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(PII&amp; p1, PII&amp; p2)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> p<span class="number">1.</span>second &gt; p<span class="number">2.</span>second;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">priority_queue&lt;PII, vector&lt;PII&gt;, Comp&gt; pq;</span><br></pre></td></tr></table></figure>
<p>当然，另一种方法就是使用lambda表达式，如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 方法二</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> PII = pair&lt;<span class="type">int</span>, <span class="type">int</span>&gt;;</span><br><span class="line"><span class="keyword">auto</span> comp = [](PII&amp; p1, PII&amp; p2) &#123;</span><br><span class="line">    <span class="keyword">return</span> p<span class="number">1.</span>second &gt; p<span class="number">2.</span>second;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 注意这里需要使用decltype</span></span><br><span class="line">priority_queue&lt;PII, vector&lt;PII&gt;, <span class="keyword">decltype</span>(comp)&gt; pq;</span><br></pre></td></tr></table></figure>
<p>但值得注意的是，方法二需要在<strong>C++20</strong>下才可使用。这是因为priority_queue的第三个模板形参需要的二元谓词要求<a href="https://zh.cppreference.com/w/cpp/named_req/CopyConstructible">可复制构造</a>。</p>
<p>lambda表达式即构造闭包（能够捕获作用域中的变量的无名函数对象）。而在C<ins>20之前，闭包类型非可默认构造，闭包类型没有默认构造函数。C</ins>20及之后，如果<strong>没有指定捕获</strong>，那么闭包类型拥有预置的默认构造函数。</p>
<p><img src="/images/2024-01-22-16-14.png" alt=""></p>
<p>而在目前，力扣中C<ins>编译器使用的是clang17，支持C</ins>20，故使用lambda表达式是没有问题的。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
      </tags>
  </entry>
  <entry>
    <title>C++中如何使用lambda实现递归</title>
    <url>/posts/63513b30/</url>
    <content><![CDATA[<p>在用C++刷leetcode时，我希望把一个递归函数像js、python那样写在运行函数内部，那么可以使用<code>function</code>和<code>lambda表达式</code>来实现。但如果这个递归函数的参数比较多，那么function的模板参数同样需要写很多，能不能用<code>auto</code>来实现得简单一点呢？</p>
<span id="more"></span>
<p>在C++中，使用lambda表达式实现递归时，由于lambda本身没有显式的类型名，需要通过一些技巧来实现递归调用。使用<code>auto&amp;&amp;</code>作为参数类型是其中一种常见的做法：</p>
<p><strong>Lambda表达式的类型推断</strong>：C++中的lambda表达式没有类型名，意味着无法直接在lambda内部调用自身。如果直接将lambda表达式定义为递归函数，会遇到无法识别的编译错误。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> factorial = [](<span class="keyword">auto</span>&amp;&amp; self, <span class="type">int</span> n) -&gt; <span class="type">int</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">return</span> n * <span class="built_in">self</span>(self, n - <span class="number">1</span>); <span class="comment">// 递归调用lambda</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">factorial</span>(factorial, <span class="number">5</span>) &lt;&lt; std::endl; <span class="comment">// 输出120</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里不像js、python直接定义就好了，还需要多写一个通用引用（什么是通用引用，可参考这篇<a href="https://juejin.cn/post/7236239609571328058">博客</a>），具体为什么我也暂时不清楚，只知道和<code>Y组合子</code>的知识点有关。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
      </tags>
  </entry>
  <entry>
    <title>enable_shared_from_this的作用</title>
    <url>/posts/5f62bd9/</url>
    <content><![CDATA[<p>C++ 中，<code>std::enable_shared_from_this</code>类模板和<code>shared_from_this</code>成员函数主要用于在一个类的成员函数中安全地获取指向自身的<code>std::shared_ptr</code>。它们的作用更多是为了确保资源正确管理。</p>
<span id="more"></span>
<p>当一个对象被多个<code>std::shared_ptr</code>管理时，如果在对象内部的成员函数中直接创建新的<code>std::shared_ptr</code>指向自身（也就是<code>this</code>），可能会导致多个独立的引用计数，从而无法正确管理对象的生命周期。而使用<code>shared_from_this</code>可以确保所有指向该对象的<code>std::shared_ptr</code>共享同一个引用计数，从而正确管理对象的生命周期。</p>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> : <span class="keyword">public</span> std::enable_shared_from_this&lt;MyClass&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        std::shared_ptr&lt;MyClass&gt; ptr = <span class="built_in">shared_from_this</span>();</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Use count: &quot;</span> &lt;&lt; ptr.<span class="built_in">use_count</span>() &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::shared_ptr&lt;MyClass&gt; obj = std::<span class="built_in">make_shared</span>&lt;MyClass&gt;();</span><br><span class="line">    obj-&gt;<span class="built_in">print</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>print</code>函数中使用<code>shared_from_this</code>获取指向自身的<code>std::shared_ptr</code>，确保了与外部创建的<code>std::shared_ptr</code>共享同一个引用计数。</p>
<p>如果在对象内部的成员函数中直接返回一个指向自身的普通指针，当外部的<code>std::shared_ptr</code>被销毁后，这个普通指针就会变成悬空指针。而使用<code>shared_from_this</code>可以避免这种情况，因为它返回的<code>std::shared_ptr</code>会在引用计数为零时自动释放对象所占用的内存。例如这里不使用<code>enable_shared_from_this</code>和<code>shared_from_this</code>，则对象会多次释放，导致程序出错。</p>
<p>代码及其执行结果可参见：<a href="https://godbolt.org/z/7rr5zMW63">这里</a></p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
      </tags>
  </entry>
  <entry>
    <title>muduo网络库的安装</title>
    <url>/posts/dd35cbb6/</url>
    <content><![CDATA[<h2 id="下载源码">下载源码<a class="anchor" href="#下载源码"></a></h2>
<p>链接：<a href="https://github.com/chenshuo/muduo/releases/tag/v2.0.2">https://github.com/chenshuo/muduo/releases/tag/v2.0.2</a></p>
<span id="more"></span>
<h2 id="编译安装">编译安装<a class="anchor" href="#编译安装"></a></h2>
<p>解压后，在项目根目录中更改 <code>CMakeLists.txt</code> 文件</p>
<p><img src="/images/2024-01-19-10-50.png" alt=""></p>
<p>如图，将 <code>option</code> 属性注释掉（这是muduo的例子的编译选项，如果开启将增加编译时间）</p>
<p>之后，执行</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./build.sh</span><br><span class="line">./build.sh install</span><br></pre></td></tr></table></figure>
<p>如果没有出错的话，在与muduo-v2.0.2同目录下将生成一个 <code>build</code> 目录，其中有</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">release-install-cpp11</span><br><span class="line">    ├── include</span><br><span class="line">    │   └── muduo</span><br><span class="line">    │       ├── base</span><br><span class="line">    │       └── net</span><br><span class="line">    │           ├── http</span><br><span class="line">    │           └── inspect</span><br><span class="line">    └── lib</span><br></pre></td></tr></table></figure>
<p>将include目录和lib目录下的内容复制到系统路径下：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mv</span> include/muduo /usr/local/include</span><br><span class="line"><span class="built_in">mv</span> lib/* /usr/local/lib</span><br></pre></td></tr></table></figure>
<p>ok，现在就可以使用muduo库了</p>
<h2 id="测试使用">测试使用<a class="anchor" href="#测试使用"></a></h2>
<p>测试代码可以参考博客
https://www.cnblogs.com/conefirst/articles/15224039.html</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
      </tags>
  </entry>
  <entry>
    <title>【协程】C++20协程初体验</title>
    <url>/posts/f663291a/</url>
    <content><![CDATA[<h2 id="为什么我们需要协程？">为什么我们需要协程？<a class="anchor" href="#为什么我们需要协程？"></a></h2>
<p>为什么我们有了线程还需要协程呢？（其实这个问题不应该这么问，协程的出现在线程之前）在一个进程中虽然我们可以创建多个线程，但是在一个进程中能创建的线程数量是有限制的，并且线程的调度仍然受操作系统控制，也就是说线程何时抢占、何时被抢占对于开发者来说都是透明的，并且在调度的过程中还可能涉及到用户态和内核态的切换开销。</p>
<span id="more"></span>
<p>当我们需要去处理一个非常耗时的IO操作时（假设使用的阻塞IO），为了不阻塞当前线程，我们可能会想到新建一个线程去执行这个操作，但是线程的创建和调度也是需要消耗资源的，我们希望有更加轻便的方法，例如：在当前线程中，一个任务遇到阻塞IO时，不要傻傻的停在这里，而是暂停这个任务，转而去执行其他任务，直到IO完成再恢复之前的任务来运行。</p>
<p><img src="../../images/Pasted%20image%2020240816220159.png" alt=""></p>
<p>这里看起来是不是像两个函数之间的调用和被调用关系？确实有点像，但区别可大了，在任务1中我们并没有去显式地调用任务2！</p>
<p>协程的最本质的解释是“<strong>可以挂起和恢复的函数</strong>”。例如上图中我们遇到耗时的IO操作了，我们就可以<strong>主动</strong>将当前运行的函数挂起（suspend），让其等待（await）IO操作，让线程去运行其他的函数，直到IO操作完成后再恢复（resume）。上下文切换的时机是靠调用方（写代码的开发人员）自身去控制的，这样协程的调度掌握在我们自己手中，相比与线程减小了系统切换上下文和其他资源的开销。因此协程在需要处理大量I/O操作或者并发任务的情况下提高程序的性能和可维护性。</p>
<h2 id="C-20带来的协程">C++20带来的协程<a class="anchor" href="#C-20带来的协程"></a></h2>
<p>C<ins>20带来的协程并不像python或lua中的那么易用，相反，C</ins>给我们提供的是更为底层的操作（不过C++23已经有<code>std::generator</code>这种更高级的抽象，之后也会有更多丰富的用法）。</p>
<p>C++协程中有很重要的三个概念：</p>
<ul>
<li><code>Promise</code></li>
<li><code>Awaitable</code></li>
<li><code>Coroutine Handle</code></li>
</ul>
<h2 id="Promise">Promise<a class="anchor" href="#Promise"></a></h2>
<p><code>promise_type</code> 是每个协程函数的幕后执行对象。它主要负责以下几个任务：</p>
<ul>
<li><strong>创建和初始化协程</strong>：当协程开始执行时，编译器会通过 <code>promise_type</code> 创建一个对象，并调用其 <code>get_return_object()</code> 方法来获取协程的返回对象。</li>
<li><strong>处理协程的暂停和恢复</strong>：协程在暂停时会调用 <code>yield_value()</code> 或 <code>await_suspend()</code> 等方法来处理协程的状态，并决定何时恢复。</li>
<li><strong>处理协程的结束</strong>：当协程执行结束时，<code>return_void()</code> 或 <code>return_value()</code> 会被调用，来处理协程的返回结果。</li>
</ul>
<p>以下是 <code>promise_type</code> 中一些常见的方法：</p>
<ul>
<li><code>get_return_object()</code>：用于创建和返回协程的返回对象，一般是协程返回类型的实例。</li>
<li><code>initial_suspend()</code>：返回一个 <code>std::suspend_always</code> 或 <code>std::suspend_never</code>，决定协程在启动时是否立即暂停。</li>
<li><code>final_suspend()</code>：返回一个 <code>std::suspend_always</code> 或 <code>std::suspend_never</code>，决定协程在结束时是否暂停，以允许调用方执行清理操作。</li>
<li><code>return_void()</code> 或 <code>return_value(T value)</code>：用于在协程完成时返回结果。<code>return_void()</code> 用于没有返回值的协程，而 <code>return_value(T)</code> 则用于有返回值的协程。</li>
<li><code>yield_value(T value)</code>：用于生成值并让协程暂停，等待下一次恢复时继续执行。</li>
</ul>
<h2 id="Awaitable">Awaitable<a class="anchor" href="#Awaitable"></a></h2>
<p><code>Awaitable</code> 是一个可以与 <code>co_await</code> 表达式一起使用的对象或类型。<code>Awaitable</code> 对象<strong>必须提供一组特定的方法</strong>，使协程可以暂停执行，并在某个条件满足时继续执行。</p>
<p><code>co_await</code> 是 C++ 协程中的一种<strong>操作符</strong>，用于暂停协程并等待某个条件的满足。当协程遇到 <code>co_await</code> 时，它会暂停，并返回控制权给调用者。协程可以通过调用 <code>co_await some_awaitable</code> 来等待 <code>some_awaitable</code> 完成。</p>
<p>一个 <code>Awaitable</code> 对象需要提供以下三个方法中的一个或多个：</p>
<ul>
<li><code>operator co_await</code>：返回一个 <code>Awaitable</code> 对象。<code>Awaitable</code> 对象是实际实现等待逻辑的对象。</li>
<li><code>await_ready()</code>：这是 <code>Awaitable</code> 对象上的方法。它返回一个 <code>bool</code>，用于指示是否需要等待。如果返回 <code>true</code>，协程将不会暂停。</li>
<li><code>await_suspend(std::coroutine_handle&lt;&gt;)</code>：这是 <code>Awaitable</code> 对象上的方法。它接受一个 <code>std::coroutine_handle&lt;&gt;</code> 参数，并在协程暂停时调用。这个方法决定协程何时恢复执行。</li>
<li><code>await_resume()</code>：这是 <code>Awaitable</code> 对象上的方法。它在协程恢复时调用，并返回 <code>co_await</code> 表达式的结果。</li>
</ul>
<p>C++中提供了两个简单的Awaitable：<a href="https://zh.cppreference.com/w/cpp/coroutine/suspend_never"><code>std::suspend_never</code></a>和<a href="https://zh.cppreference.com/w/cpp/coroutine/suspend_always"><code>std::suspend_always</code></a>。</p>
<h2 id="Coroutine-Handle">Coroutine Handle<a class="anchor" href="#Coroutine-Handle"></a></h2>
<p><code>std::coroutine_handle</code> 是 C++20 协程库中一个核心的工具类，用于表示和操作协程。它是一个模板类，通常用来指向协程的状态信息。它可以通过协程的 <code>promise_type</code> 访问和控制协程的状态。每个协程在创建时，都会生成一个 <code>std::coroutine_handle</code>，用于管理协程的生命周期。</p>
<p><code>std::coroutine_handle</code> 提供了一系列方法来控制协程的执行，包括以下几个主要功能：</p>
<ul>
<li><strong>创建和获取句柄</strong>：
<ul>
<li><code>std::coroutine_handle&lt;&gt;::from_address(void* ptr)</code>：通过指针获取一个句柄。</li>
<li><code>std::coroutine_handle&lt;promise_type&gt;::from_promise(promise_type&amp; promise)</code>：通过 <code>promise_type</code> 对象创建一个句柄。</li>
</ul>
</li>
<li><strong>控制协程的执行</strong>：
<ul>
<li><code>void resume()</code>：恢复协程的执行。</li>
<li><code>void destroy()</code>：销毁协程并释放其占用的资源。</li>
<li><code>void operator()()</code>：等效于 <code>resume()</code>，恢复协程的执行。</li>
<li><code>void* address()</code>：返回协程句柄的地址，用于低级操作。</li>
</ul>
</li>
<li><strong>检查协程的状态</strong>：
<ul>
<li><code>bool done()</code>：检查协程是否已经完成执行。</li>
</ul>
</li>
<li><strong>访问 <code>promise_type</code></strong>：
<ul>
<li><code>promise_type&amp; promise()</code>：获取与当前协程关联的 <code>promise_type</code> 对象，允许访问协程内部状态。</li>
</ul>
</li>
</ul>
<h2 id="简单示例">简单示例<a class="anchor" href="#简单示例"></a></h2>
<p>这里有一个使用C++20 coroutine来实现挂起和恢复函数的例子。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;coroutine&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std::chrono_literals;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Result</span> &#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">Promise</span> &#123;</span><br><span class="line">        <span class="function">Result <span class="title">get_return_object</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> std::coroutine_handle&lt;Promise&gt;::<span class="built_in">from_promise</span>(*<span class="keyword">this</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function">std::suspend_never <span class="title">initial_suspend</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> &#123;&#125;;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function">std::suspend_always <span class="title">final_suspend</span><span class="params">()</span> <span class="keyword">noexcept</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> &#123;&#125;;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="type">void</span> <span class="title">unhandled_exception</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">using</span> promise_type = Promise;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Result</span>(std::coroutine_handle&lt;Promise&gt; h)</span><br><span class="line">        : <span class="built_in">handle</span>(h) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    std::coroutine_handle&lt;Promise&gt; handle;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">Result <span class="title">hello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Hello &quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    <span class="keyword">co_await</span> std::suspend_always&#123;&#125;; <span class="comment">// 挂起hello</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;world!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;hello one\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Result <span class="title">hello2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;你好 &quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;hello two two\n&quot;</span>;</span><br><span class="line">    <span class="keyword">co_await</span> std::suspend_always&#123;&#125;; <span class="comment">// 挂起hello2</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;世界!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Result coro = <span class="built_in">hello</span>();</span><br><span class="line">    Result coro2 = <span class="built_in">hello2</span>();</span><br><span class="line">    coro.handle.<span class="built_in">resume</span>();  <span class="comment">// 恢复hello</span></span><br><span class="line">    coro<span class="number">2.</span>handle.<span class="built_in">resume</span>(); <span class="comment">// 恢复hello2</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在main函数中，一开始我们启动了协程<code>hello</code>，输出了“Hello”后被挂起；之后启动了协程<code>hello2</code>，其输出“你好\nhello two two\n”后也被挂起。紧接着我们通过coroutine_handle来依次恢复这两个协程，最终输出结果为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Hello </span><br><span class="line">你好 </span><br><span class="line">hello two two</span><br><span class="line">world!</span><br><span class="line">hello one</span><br><span class="line">世界!</span><br></pre></td></tr></table></figure>
<h2 id="References">References<a class="anchor" href="#References"></a></h2>
<ul>
<li>https://zplutor.github.io/2022/03/25/cpp-coroutine-beginner/</li>
<li>https://www.bluepuni.com/archives/stackless-coroutine-and-asio-coroutine</li>
<li>https://zhuanlan.zhihu.com/p/355100152?utm_psn=1808059511308697600</li>
<li>https://itnext.io/c-20-coroutines-complete-guide-7c3fc08db89d</li>
<li>https://jasonkayzk.github.io/2022/06/03/%E6%B5%85%E8%B0%88%E5%8D%8F%E7%A8%8B/</li>
<li>https://lewissbaker.github.io/2017/11/17/understanding-operator-co-await</li>
<li>https://juejin.cn/post/6844903715099377672</li>
</ul>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>【协程】C++是如何通过SFINAE来检查promise_type的</title>
    <url>/posts/21a47b3b/</url>
    <content><![CDATA[<p>我们知道在C++20的协程中，自己实现的Coroutine中必须包含一个<code>Promise</code>，并且这个<code>Promise</code>必须要实现：</p>
<ul>
<li><code>get_return_object()</code></li>
<li><code>initial_suspend()</code></li>
<li><code>final_suspend()</code></li>
<li><code>unhandled_exception()</code></li>
</ul>
<p>少了其中任何一个，编译器都会报错。那这是怎么实现的呢？如果是像java那样是一个接口而没有对应的实现从而报错还能理解，但是我们的代码中的<code>Promise</code>完完全全是我们自己写的，也没有使用继承，编译器你怎么知道我在实现协程时少了什么东西呢？</p>
<p>答案就是：<strong>SFINAE</strong>（Substitution Failure Is Not An Error，替换失败不是错误）。</p>
<span id="more"></span>
<p>SFINAE 主要应用于函数模板的重载解析过程中。当编译器尝试选择一个合适的函数模板重载版本时，如果某个模板参数的替换导致编译错误，编译器不会立即报错，而是继续尝试其他可能的重载版本。</p>
<p>在 C++20 协程中，编译器会根据协程函数的定义和 <code>promise_type</code> 的实现来生成协程的执行代码。<code>promise_type</code> 是一个用户定义的类型，它必须提供一些特定的函数，如 <code>get_return_object</code>、<code>initial_suspend</code>、<code>final_suspend</code>、<code>unhandled_exception</code> 和 <code>return_value</code> 等。这些函数定义了协程的行为和状态，编译器会在编译期检查这些函数的存在性和正确性，以确保协程能够正确地执行。</p>
<p>编译器使用 SFINAE 机制来检查 <code>promise_type</code> 中是否包含特定的函数。具体来说，就是编译器会在协程的实现代码中使用表达式 SFINAE，尝试调用这些函数，并根据调用的结果来确定函数的存在性。</p>
<p>我们来写个简单的例子，只检查<code>get_return_object</code>函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * check(int) 是一个重载的模板函数，用于检查U类型是否有get_return_object()函数。</span></span><br><span class="line"><span class="comment"> * decltype(std::declval&lt;U&gt;().get_return_object(), std::true_type&#123;&#125;) 会在U具有get_return_object()时返回std::true_type。</span></span><br><span class="line"><span class="comment"> * 如果U没有get_return_object()，则会匹配check(...)重载，返回std::false_type。</span></span><br><span class="line"><span class="comment"> * value成员是一个常量布尔值，指示T是否具有get_return_object()函数。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">has_get_return_object</span> &#123;</span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">typename</span> U&gt;</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="keyword">auto</span> <span class="title">check</span><span class="params">(<span class="type">int</span>)</span> -&gt; <span class="title">decltype</span><span class="params">(std::declval&lt;U&gt;().get_return_object(), std::true_type&#123;&#125;)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">typename</span>&gt;</span><br><span class="line">    <span class="function"><span class="type">static</span> std::false_type <span class="title">check</span><span class="params">(...)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">bool</span> value = <span class="keyword">decltype</span>(<span class="built_in">check</span>&lt;T&gt;(<span class="number">0</span>))::value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">check_promise_type</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">static_assert</span>(has_get_return_object&lt;T&gt;::value, <span class="string">&quot;Promise type must have a get_return_object() method.&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyCoroutine</span> &#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">promise_type</span> &#123;</span><br><span class="line">        <span class="function">MyCoroutine <span class="title">get_return_object</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> &#123;&#125;;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">check_promise_type</span>&lt;MyCoroutine::promise_type&gt;();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当我们去掉<code>Mycoroutine::promise_type</code>中的<code>get_return_object</code>函数时，<code>check_promise_type</code>函数中就会出发断言失败，从而编译不通过。</p>
<p>这也就是在将一个函数变为协程时编译器是如何检查的一个大概思路了。不得不感叹，C++真是博大精深。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
        <tag>Template</tag>
      </tags>
  </entry>
  <entry>
    <title>纠正一下对cpp移动语义的错误理解</title>
    <url>/posts/2a4bdcb8/</url>
    <content><![CDATA[<p>之前对于移动语义的理解就是使用std::move将一个对象所占有的资源的所有权转移给另一个对象，但是只要使用std::move就足够了吗？这显然是错误的。</p>
<span id="more"></span>
<p>看一下<code>std::move</code>的源码（g++12.2）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* @brief Convert a value to an rvalue.</span></span><br><span class="line"><span class="comment">* @param __t A thing of arbitrary type.</span></span><br><span class="line"><span class="comment">* @return The parameter cast to an rvalue-reference to allow moving it.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line">_GLIBCXX_NODISCARD</span><br><span class="line"><span class="keyword">constexpr</span> <span class="keyword">typename</span> std::remove_reference&lt;_Tp&gt;::<span class="function">type&amp;&amp;</span></span><br><span class="line"><span class="function"><span class="title">move</span><span class="params">(_Tp&amp;&amp; <span class="type">__t</span>)</span> <span class="keyword">noexcept</span></span></span><br><span class="line"><span class="function"></span>&#123; <span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;<span class="keyword">typename</span> std::remove_reference&lt;_Tp&gt;::type&amp;&amp;&gt;(<span class="type">__t</span>); &#125;</span><br></pre></td></tr></table></figure>
<p>其实<code>move</code>的实现并没有很复杂，粗略一点的理解就是将一个左值强制转换为右值。</p>
<p>std::move 并不会真正地移动对象，真正的移动操作是在移动构造函数、移动赋值函数等完成的，std::move 只是将参数转换为右值引用而已。</p>
<p>写一个简单的例子如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fmt/core.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="built_in">A</span>(std::string str)</span><br><span class="line">        : <span class="built_in">data</span>(str) &#123;&#125;</span><br><span class="line">    <span class="built_in">A</span>(<span class="type">const</span> A&amp;) &#123;</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;copy&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">A</span>(A&amp;&amp;) &#123;</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;move&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    std::string data = <span class="string">&quot;default&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">A <span class="title">a</span><span class="params">(<span class="string">&quot;hello&quot;</span>)</span></span>;</span><br><span class="line">    <span class="function">A <span class="title">a2</span><span class="params">(std::move(a))</span></span>;</span><br><span class="line">    fmt::<span class="built_in">print</span>(<span class="string">&quot;a: &#123;&#125;, a2: &#123;&#125;\n&quot;</span>, a.data, a<span class="number">2.</span>data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看样子我们使用std::move后a2的data数据应该为“hello”，但是运行结果为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">move</span><br><span class="line">a: hello, a2: default</span><br></pre></td></tr></table></figure>
<p>虽然使用move匹配到了A的移动构造函数，但是在上文提到过，std::move仅仅只是一个强制转换，并没有实现真正的移动！但要是我们不写A中的移动构造函数或是将其设置成<code>default</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="built_in">A</span>(std::string str)</span><br><span class="line">        : <span class="built_in">data</span>(str) &#123;&#125;</span><br><span class="line">    <span class="built_in">A</span>(<span class="type">const</span> A&amp;) &#123;</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;copy&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">A</span>(A&amp;&amp;) = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line">    std::string data = <span class="string">&quot;default&quot;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这样，运行结果为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a: , a2: hello</span><br></pre></td></tr></table></figure>
<p>这是因为当我们不显式指定移动构造函数（或是拷贝构造函数、移动or拷贝运算符）编译器会自动生成，貌似也一并实现了数据的移动（？这我也还不清楚）</p>
<p>我们通常使用std::move能够实现标准库中一些资源的转移，是因为标准库中已经实现了这些资源类的移动构造函数or移动赋值运算符。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
      </tags>
  </entry>
  <entry>
    <title>避免cout线程不安全的一个做法</title>
    <url>/posts/68d3d49b/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p><code>std::cout</code>重载了<code>&lt;&lt;</code>运算符，这使得写一些很短的代码时很方便。但是如果在多线程的条件下，cout并不是线程安全的。</p>
<span id="more"></span>
<h2 id="举例">举例<a class="anchor" href="#举例"></a></h2>
<p>举个例子，我们创建5个线程</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;msg1&quot;</span></span><br><span class="line">              &lt;&lt; <span class="string">&quot; msg2&quot;</span></span><br><span class="line">              &lt;&lt; <span class="string">&quot; msg3&quot;</span></span><br><span class="line">              &lt;&lt; <span class="string">&quot; thread_id = &quot;</span> &lt;&lt; std::this_thread::<span class="built_in">get_id</span>() &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::thread threads[<span class="number">5</span>];</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">        threads[i] = std::<span class="built_in">thread</span>(Test);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">        threads[i].<span class="built_in">join</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际上，看样子好像控制台应该输出5行内容，但是运行结果可能是这样的</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">msg1 msg2 msg3 thread_id = msg1 msg2 msg3 thread_id = 139926598575808139926606968512</span><br><span class="line"></span><br><span class="line">msg1 msg2 msg3 thread_id = 139926590183104</span><br><span class="line">msg1 msg2 msg3 thread_id = 139926455965376</span><br><span class="line">msg1 msg2 msg3 thread_id = 139926581790400</span><br></pre></td></tr></table></figure>
<p>这是因为cout在使用时<strong>可能</strong>会存在线程之间的打印信息<strong>乱串</strong>的问题，看一下编译器眼中我们这段程序中的cout是什么样的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::<span class="keyword">operator</span>&lt;&lt;(std::<span class="keyword">operator</span>&lt;&lt;(std::<span class="keyword">operator</span>&lt;&lt;(std::<span class="keyword">operator</span>&lt;&lt;(std::<span class="keyword">operator</span>&lt;&lt;(std::cout, <span class="string">&quot;msg1&quot;</span>), <span class="string">&quot; msg2&quot;</span>), <span class="string">&quot; msg3&quot;</span>), <span class="string">&quot; thread_id = &quot;</span>), std::this_thread::<span class="built_in">get_id</span>()).<span class="keyword">operator</span>&lt;&lt;(std::endl);</span><br></pre></td></tr></table></figure>
<p>可以看到，这不是通过单个 <code>std::operator&lt;&lt;</code> 调用完成的，也就是说这个操作<strong>并不是原子的</strong></p>
<h2 id="解决方法">解决方法<a class="anchor" href="#解决方法"></a></h2>
<ol>
<li>使用<code>std::format</code>（C++20）</li>
<li>使用第三方库，如<code>folly</code>，<code>fmtlib</code>等</li>
<li>使用stringstream</li>
<li>…</li>
</ol>
<p>这里使用stringstream做个演示。将Test函数修改如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::stringstream ss;</span><br><span class="line">ss &lt;&lt; <span class="string">&quot;msg1&quot;</span></span><br><span class="line">   &lt;&lt; <span class="string">&quot; msg2&quot;</span></span><br><span class="line">   &lt;&lt; <span class="string">&quot; msg3&quot;</span></span><br><span class="line">   &lt;&lt; <span class="string">&quot; thread_id = &quot;</span> &lt;&lt; std::this_thread::<span class="built_in">get_id</span>() &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; ss.<span class="built_in">str</span>();</span><br></pre></td></tr></table></figure>
<p>这样，控制台的输出就不会乱串了。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue3封装el-upload</title>
    <url>/posts/be7ff390/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p>在Vue3项目中，如果我们想上传图片一般可以利用element-ui中的<code>el-upload</code>，为了避免代码的重复，我们可以自己封装一个图片上传组件。</p>
<p>其中，主要实现思想为前端利用el-upload组件选择上传的图片，并利用其<code>http-request</code>属性来自定义函数来实现文件上传请求：该请求函数使用七牛云的对象存储，在通过后端得到的上传凭证token后来实现文件上传。</p>
<span id="more"></span>
<h2 id="后端代码">后端代码<a class="anchor" href="#后端代码"></a></h2>
<blockquote>
<p>使用express框架，获取七牛云上传凭证并响应给前端</p>
</blockquote>
<h3 id="项目结构">项目结构<a class="anchor" href="#项目结构"></a></h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> - routes</span><br><span class="line">     |- token.js</span><br><span class="line">     |- index.js</span><br><span class="line"> - app.js</span><br><span class="line"> - config.js</span><br><span class="line"> - package.json</span><br></pre></td></tr></table></figure>
<h3 id="安装七牛云的SDK：">安装七牛云的SDK：<a class="anchor" href="#安装七牛云的SDK："></a></h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> npm i qiniu</span><br></pre></td></tr></table></figure>
<h3 id="获取上传凭证">获取上传凭证<a class="anchor" href="#获取上传凭证"></a></h3>
<p>编写获取上传凭证的相关代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> /* config.js */</span><br><span class="line"> ​</span><br><span class="line"> const qiniu = require(&#x27;qiniu&#x27;)</span><br><span class="line"> ​</span><br><span class="line"> // 创建上传凭证</span><br><span class="line"> const accessKey = &#x27;*****&#x27; // 这里填写七牛云的accessKey</span><br><span class="line"> const secretKey = &#x27;*****&#x27;// 这里填写七牛云的secretKey</span><br><span class="line"> const mac = new qiniu.auth.digest.Mac(accessKey, secretKey)</span><br><span class="line"> const options = &#123;</span><br><span class="line">   scope: &#x27;*****&#x27;, // 这里填写七牛云空间名称</span><br><span class="line">   expires: 60 * 60 * 24 * 7 // 这里是凭证的有效时间，默认是一小时</span><br><span class="line"> &#125;</span><br><span class="line"> const putPolicy = new qiniu.rs.PutPolicy(options)</span><br><span class="line"> const uploadToken = putPolicy.uploadToken(mac)</span><br><span class="line"> ​</span><br><span class="line"> module.exports = &#123;</span><br><span class="line">   uploadToken</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<h3 id="配置路由">配置路由<a class="anchor" href="#配置路由"></a></h3>
<p>token.js</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> const tokenRouter = require(&#x27;express&#x27;).Router()</span><br><span class="line"> const qnconfig = require(&#x27;../config&#x27;) // 引入七牛云配置</span><br><span class="line"> ​</span><br><span class="line"> tokenRouter.get(&#x27;/qiniu&#x27;, (req, res, next) =&gt; &#123;</span><br><span class="line">   res.status(200).send(qnconfig.uploadToken)</span><br><span class="line"> &#125;)</span><br><span class="line"> ​</span><br><span class="line"> module.exports = tokenRouter</span><br></pre></td></tr></table></figure>
<p>index.js</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> const token = require(&#x27;./token&#x27;)</span><br><span class="line"> ​</span><br><span class="line"> module.exports = routes = (app) =&gt; &#123;</span><br><span class="line">   app.use(&#x27;/token&#x27;, token) // 可以通过/token/qiniu的方式获取上传凭证</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<h3 id="项目启动">项目启动<a class="anchor" href="#项目启动"></a></h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> const express = require(&#x27;express&#x27;)</span><br><span class="line"> const bodyparse = require(&#x27;body-parser&#x27;)</span><br><span class="line"> const routers = require(&#x27;./route&#x27;)</span><br><span class="line"> ​</span><br><span class="line"> // 创建服务</span><br><span class="line"> const app = express()</span><br><span class="line"> // 解析数据</span><br><span class="line"> app.use(bodyparse.json())</span><br><span class="line"> ​</span><br><span class="line"> // 路由</span><br><span class="line"> routes(app)</span><br><span class="line"> ​</span><br><span class="line"> // 监听3000端口</span><br><span class="line"> app.listen(3000, () =&gt; &#123;</span><br><span class="line">   console.log(&#x27;this server are running on localhost:3000!&#x27;)</span><br><span class="line"> &#125;)</span><br></pre></td></tr></table></figure>
<p>使用命令<code>node app.js</code>启动项目，这时访问<code>http://localhost:3000/token/qiniu</code>即可获取上传凭证了。</p>
<h2 id="前端代码">前端代码<a class="anchor" href="#前端代码"></a></h2>
<h3 id="配置跨域">配置跨域<a class="anchor" href="#配置跨域"></a></h3>
<p>由于前后端项目运行在不同的端口，因此需要解决跨域问题，这里在vite.config.js中解决如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> server: &#123;</span><br><span class="line">   proxy: &#123;</span><br><span class="line">     &#x27;/api&#x27;: &#123;</span><br><span class="line">       target: &#x27;http://localhost:3000&#x27;,</span><br><span class="line">       changeOrigin: true,</span><br><span class="line">       rewrite: (path) =&gt; path.replace(/^/api/, &#x27;&#x27;)</span><br><span class="line">     &#125;</span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<h3 id="父组件使用">父组件使用<a class="anchor" href="#父组件使用"></a></h3>
<p>我们希望子组件上传图片得到一串url后父组件能接受到，并且在展示上传图片时其尺寸应能指定或者有默认值。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> &lt;template&gt;</span><br><span class="line">   &lt;Upload :url=&quot;imageUrl&quot; @upload=&quot;changeUrl&quot; /&gt;</span><br><span class="line"> &lt;/template&gt;</span><br><span class="line"> ​</span><br><span class="line"> &lt;script setup&gt;</span><br><span class="line"> import Upload from &#x27;@/components/Upload.vue&#x27;</span><br><span class="line"> import &#123; ref &#125; from &#x27;vue&#x27;</span><br><span class="line">     </span><br><span class="line"> const imageUrl = ref(&#x27;&#x27;)</span><br><span class="line"> ​</span><br><span class="line"> const changeUrl = (url) =&gt; &#123;</span><br><span class="line">     imageUrl.value = url</span><br><span class="line"> &#125;</span><br><span class="line"> &lt;/script&gt;</span><br></pre></td></tr></table></figure>
<h3 id="封装组件Upload-vue">封装组件Upload.vue<a class="anchor" href="#封装组件Upload-vue"></a></h3>
<blockquote>
<p>这里只是简单使用axios，没有对其进行封装。</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;!- action=&quot;https://upload-z2.qiniup.com&quot;：每个地区访问域名不同，具体可通过 https://developer.qiniu.com/kodo/1671/region-endpoint-fq 查看 -&gt;	</span><br><span class="line">  &lt;el-upload</span><br><span class="line">    class=&quot;avatar-uploader&quot;</span><br><span class="line">    action=&quot;https://upload-z2.qiniup.com&quot;</span><br><span class="line">    :show-file-list=&quot;false&quot;</span><br><span class="line">    :http-request=&quot;up2qiniu&quot;</span><br><span class="line">    :before-upload=&quot;beforeUpload&quot;</span><br><span class="line">  &gt;</span><br><span class="line">    &lt;img</span><br><span class="line">      v-if=&quot;props.url&quot;</span><br><span class="line">      :src=&quot;props.url&quot;</span><br><span class="line">      class=&quot;avatar&quot;</span><br><span class="line">      :style=&quot;&#x27;width: &#x27; + props.width + &#x27;px;&#x27; + &#x27;height: &#x27; + props.height + &#x27;px;&#x27;&quot;</span><br><span class="line">    /&gt;</span><br><span class="line">    &lt;el-icon</span><br><span class="line">      v-else</span><br><span class="line">      class=&quot;avatar-uploader-icon&quot;</span><br><span class="line">      :style=&quot;&#x27;width: &#x27; + props.width + &#x27;px;&#x27; + &#x27;height: &#x27; + props.height + &#x27;px;&#x27;&quot;</span><br><span class="line">      &gt;&lt;Plus</span><br><span class="line">    /&gt;&lt;/el-icon&gt;</span><br><span class="line">  &lt;/el-upload&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref &#125; from &#x27;vue&#x27;</span><br><span class="line">import &#123; getQiniuToken &#125; from &#x27;../api/token&#x27;</span><br><span class="line">import axios from &#x27;axios&#x27;</span><br><span class="line">import &#123; ElMessage &#125; from &#x27;element-plus&#x27;</span><br><span class="line"></span><br><span class="line">const qiniuaddr = &#x27;rlr92qkze.hn-bkt.clouddn.com&#x27; // 这里是七牛云存储对象中的CDN域名</span><br><span class="line">const imageUrl = ref(&#x27;&#x27;)</span><br><span class="line">// 父组件传值时，须有图片的url；其次可选择图片的宽高（默认都为180）</span><br><span class="line">const props = defineProps(&#123;</span><br><span class="line">  url: String,</span><br><span class="line">  width: &#123;</span><br><span class="line">    type: Number,</span><br><span class="line">    default: 180</span><br><span class="line">  &#125;,</span><br><span class="line">  height: &#123;</span><br><span class="line">    type: Number,</span><br><span class="line">    default: 180</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line">const emit = defineEmits([&#x27;upload&#x27;])</span><br><span class="line"></span><br><span class="line">const beforeUpload = (rawFile) =&gt; &#123;</span><br><span class="line">  if (rawFile.type !== &#x27;image/jpg&#x27; &amp;&amp; rawFile.type !== &#x27;image/png&#x27;) &#123;</span><br><span class="line">    ElMessage.error(&#x27;图片格式应该是png或jpg&#x27;)</span><br><span class="line">    return false</span><br><span class="line">  &#125; else if (rawFile.size / 1024 / 1024 &gt; 2) &#123;</span><br><span class="line">    ElMessage.error(&#x27;图片大小应该小于2MB&#x27;)</span><br><span class="line">    return false</span><br><span class="line">  &#125;</span><br><span class="line">  return true</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 上传图片至七牛云</span><br><span class="line"> * @param &#123;*&#125; req</span><br><span class="line"> */</span><br><span class="line">const up2qiniu = (req) =&gt; &#123;</span><br><span class="line">  const config = &#123;</span><br><span class="line">    headers: &#123; &#x27;Content-Type&#x27;: &#x27;multipart/form-data&#x27; &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  const fileType = req.file.type === &#x27;image/png&#x27; ? &#x27;png&#x27; : &#x27;jpg&#x27;</span><br><span class="line">  // 重命名要上传的文件</span><br><span class="line">  const keyname = &#x27;blog&#x27; + new Date().getTime() + &#x27;.&#x27; + fileType</span><br><span class="line">  axios.get(&#x27;/api/token/qiniu&#x27;).then(res =&gt; &#123;</span><br><span class="line">      const formdata = new FormData()</span><br><span class="line">      formdata.append(&#x27;file&#x27;, req.file)</span><br><span class="line">      formdata.append(&#x27;token&#x27;, res.data)</span><br><span class="line">      formdata.append(&#x27;key&#x27;, keyname)</span><br><span class="line">      // 获取到凭证之后再将文件上传到七牛云空间</span><br><span class="line">      axios.post(&#x27;https://upload-z2.qiniup.com&#x27;, formdata, config).then((res) =&gt; &#123;</span><br><span class="line">        imageUrl.value = &#x27;http://&#x27; + qiniuaddr + &#x27;/&#x27; + res.data.key</span><br><span class="line">        emit(&#x27;upload&#x27;, imageUrl.value) // 向父组件传递图片的url</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;)  </span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;style lang=&quot;scss&quot; scoped&gt;</span><br><span class="line">.avatar-uploader .avatar &#123;</span><br><span class="line">  width: 360px;</span><br><span class="line">  height: 180px;</span><br><span class="line">  display: block;</span><br><span class="line">&#125;</span><br><span class="line">.avatar-uploader :deep(.el-upload) &#123;</span><br><span class="line">  border: 1px dashed var(--el-border-color);</span><br><span class="line">  border-radius: 6px;</span><br><span class="line">  cursor: pointer;</span><br><span class="line">  position: relative;</span><br><span class="line">  overflow: hidden;</span><br><span class="line">  transition: var(--el-transition-duration-fast);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.avatar-uploader :deep(.el-upload:hover) &#123;</span><br><span class="line">  border-color: var(--el-color-primary);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.el-icon.avatar-uploader-icon &#123;</span><br><span class="line">  font-size: 28px;</span><br><span class="line">  color: #8c939d;</span><br><span class="line">  width: 360px;</span><br><span class="line">  height: 180px;</span><br><span class="line">  text-align: center;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Vue</tag>
      </tags>
  </entry>
  <entry>
    <title>写一个简单的浏览器脚本来屏蔽CSDN</title>
    <url>/posts/e322e7b4/</url>
    <content><![CDATA[<p>在使用手机浏览器搜索时为了免于CSDN垃圾信息的影响，我们可以使用Bing（其他搜索引擎也有类似的功能，但是国内不借助其他方法的情况下体验感最好的也只有Bing了）的“<code>-site:*.csdn.net</code>”来屏蔽CSDN，但是每次输入搜索内容后还要加这么一个字符串真是太麻烦了，因此可以写一个浏览器脚本来完成这一操作。</p>
<span id="more"></span>
<p>在PC上可以直接使用油猴中其他大佬写的脚本，而我在手机上使用Via浏览器时不一定能用（可能可以，不过我还没试～）这些脚本，同时我需要的功能比较简单，故写一个Via浏览器的脚本。</p>
<p>其实主要就是一个立即执行函数，其中对于输入框注册了<code>keydown</code>的事件，当按下回车键时可以在输入框的文本后添加<code>-site:*.csdn.net</code>。需要注意的是需要使用<code>preventDefault()</code>方法来防止默认的表单提交动作：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ==UserScript==</span></span><br><span class="line"><span class="comment">// @name         Bing CSDN Filter</span></span><br><span class="line"><span class="comment">// @namespace    https://viayoo.com/</span></span><br><span class="line"><span class="comment">// @version      0.0</span></span><br><span class="line"><span class="comment">// @description  自动在Bing搜索框输入内容后添加-csdn，屏蔽CSDN内容</span></span><br><span class="line"><span class="comment">// @author       kerolt</span></span><br><span class="line"><span class="comment">// @match        https://*.bing.com/*</span></span><br><span class="line"><span class="comment">// @grant        none</span></span><br><span class="line"><span class="comment">// ==/UserScript==</span></span><br><span class="line"></span><br><span class="line">(<span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="string">&#x27;use strict&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取搜索框中的textarea元素</span></span><br><span class="line">    <span class="keyword">let</span> searchInput = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;textarea[name=&quot;q&quot;]&#x27;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (searchInput) &#123;</span><br><span class="line">        <span class="comment">// 监听搜索框中的按键事件</span></span><br><span class="line">        searchInput.<span class="title function_">addEventListener</span>(<span class="string">&#x27;keydown&#x27;</span>, <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">            <span class="comment">// 检查是否按下回车键</span></span><br><span class="line">            <span class="keyword">if</span> (event.<span class="property">key</span> === <span class="string">&#x27;Enter&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// 如果输入中没有-site:*.csdn.net，自动添加</span></span><br><span class="line">                <span class="keyword">if</span> (!searchInput.<span class="property">value</span>.<span class="title function_">includes</span>(<span class="string">&#x27;-site:*.csdn.net&#x27;</span>)) &#123;</span><br><span class="line">                    searchInput.<span class="property">value</span> += <span class="string">&#x27; -site:*.csdn.net&#x27;</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 提交表单，进行搜索</span></span><br><span class="line">                event.<span class="title function_">preventDefault</span>(); <span class="comment">// 防止默认的表单提交动作</span></span><br><span class="line">                <span class="keyword">let</span> searchForm = searchInput.<span class="title function_">closest</span>(<span class="string">&#x27;form&#x27;</span>);</span><br><span class="line">                <span class="keyword">if</span> (searchForm) &#123;</span><br><span class="line">                    searchForm.<span class="title function_">submit</span>(); <span class="comment">// 手动提交表单</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Browser</tag>
      </tags>
  </entry>
  <entry>
    <title>函数防抖</title>
    <url>/posts/9221d57e/</url>
    <content><![CDATA[<p>最近的练手项目web-terminal中（也就是一个网页终端，可执行一些命令），在按下键盘后会显示可能匹配的命令列表（假设对应的函数是<code>setHintList</code>），这不仅是按下字母按键会触发，按下删除键、tab键都会触发。那就不得不考虑一个问题，如果我们手速太快，那么<code>setHintList</code>就会频繁触发，但我们只需要响应用户最后一次输入的命令即可，虽然在这个小项目中没啥问题，但是由此可以引出一些对于以后大项目的考虑：如何减小这种多次频繁执行函数带来的性能开销问题？那就是函数防抖~</p>
<span id="more"></span>
<p>函数防抖是一种优化技术，用来限制某个函数在一定时间内被调用的频率。当事件被触发后，它会等待一段时间，如果在这段时间内再次被触发，那么它会重新开始等待。</p>
<p>下面是一种实现方式：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">buildDebounce</span>(<span class="params"><span class="attr">fn</span>: (...arg: <span class="built_in">any</span>[]) =&gt; <span class="built_in">any</span>, <span class="attr">duration</span>: <span class="built_in">number</span> = <span class="number">300</span></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> timer = -<span class="number">1</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params"><span class="attr">this</span>: <span class="built_in">unknown</span>, ...<span class="attr">args</span>: <span class="built_in">any</span>[]</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (timer &gt; -<span class="number">1</span>) &#123;</span><br><span class="line">      <span class="built_in">clearTimeout</span>(timer);</span><br><span class="line">    &#125;</span><br><span class="line">    timer = <span class="variable language_">window</span>.<span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      fn.<span class="title function_">bind</span>(<span class="variable language_">this</span>)(...args);</span><br><span class="line">      timer = -<span class="number">1</span>;</span><br><span class="line">    &#125;, duration);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>buildDebounce</code>接受一个函数<code>fn</code>和一个可选的时间间隔<code>duration</code>（默认为 300 毫秒）作为参数，并返回一个新的函数。</p>
<ol>
<li>内部使用了一个变量<code>timer</code>来跟踪定时器的引用。初始值为 -1，表示没有定时器在运行。</li>
<li>返回的函数在被调用时，首先检查<code>timer</code>是否大于 -1。如果是，说明之前已经有一个定时器在运行，此时会调用<code>clearTimeout</code>清除这个定时器，以取消之前可能正在等待执行的函数调用。</li>
<li>然后，使用<code>window.setTimeout</code>创建一个新的定时器，在<code>duration</code>毫秒后执行传入的函数<code>fn</code>，并通过<code>bind</code>方法确保函数在正确的上下文中执行。</li>
<li>当定时器执行完函数后，将<code>timer</code>重置为 -1，表示没有定时器在运行。</li>
</ol>
<p>在<code>(this: unknown, ...args: any[])</code>中，<code>this</code>的值是在调用由<code>buildDebounce</code>返回的函数时，根据调用的上下文确定的。也就是当这个返回的函数被调用时，通过保留传入的 <code>this</code> 值，可以确保在最终执行被包裹的函数 <code>fn</code> 时，<code>fn</code> 能够在正确的上下文中执行。</p>
<p><code>buildDebounce</code>的使用如下：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">printMessage</span>(<span class="params">message</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(message);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> debouncedPrint = <span class="title function_">buildDebounce</span>(printMessage);</span><br><span class="line"></span><br><span class="line"><span class="title function_">debouncedPrint</span>(<span class="string">&#x27;Hello&#x27;</span>);</span><br><span class="line"><span class="title function_">debouncedPrint</span>(<span class="string">&#x27;World&#x27;</span>);</span><br><span class="line"><span class="comment">// 如果在 300 毫秒内连续调用 debouncedPrint，只有最后一次调用会在 300 毫秒后执行打印操作。</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>FrontEnd</tag>
      </tags>
  </entry>
  <entry>
    <title>用uni-cli创建项目后使用eslint</title>
    <url>/posts/4976678f/</url>
    <content><![CDATA[<p>记录一下如何配置eslint（其实是怕下一次又被折磨）。</p>
<span id="more"></span>
<h2 id="uni-cli创建项目">uni-cli创建项目<a class="anchor" href="#uni-cli创建项目"></a></h2>
<p>由于我是在linux上使用uniapp，无法使用HBuilderX，故采用uni-cli来创建项目并使用vscode进行开发。</p>
<p>uni-cli创建项目很简单，一行命令即可，这里采用vue3/vite版：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npx degit dcloudio/uni-preset-vue#vite my-vue3-project</span><br></pre></td></tr></table></figure>
<p>这里只用来创建项目，发布运行什么的具体可以看<a href="https://zh.uniapp.dcloud.io/worktile/CLI.html">官网</a>。</p>
<h2 id="使用eslint">使用eslint<a class="anchor" href="#使用eslint"></a></h2>
<p>eslint的配置文件和配置方式我感觉有好多种，所以在配置的时候折腾了挺久，最好还是按照官网最新文档来：</p>
<ul>
<li><strong>eslint</strong>：https://eslint.org/docs/latest/use/getting-started</li>
<li><strong>eslint-plugin-vue</strong>：https://eslint.vuejs.org/</li>
</ul>
<h3 id="安装eslint">安装eslint<a class="anchor" href="#安装eslint"></a></h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm init @eslint/config@latest</span><br></pre></td></tr></table></figure>
<h3 id="配置">配置<a class="anchor" href="#配置"></a></h3>
<p>安装完后，一般项目的根目录下就会出现<code>eslint.config.mjs</code>文件（eslint 9.4.0版本，采用vue框架），主体如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pluginVue <span class="keyword">from</span> <span class="string">&quot;eslint-plugin-vue&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> [</span><br><span class="line">  ......</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">rules</span>: &#123;</span><br><span class="line">    </span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">];</span><br></pre></td></tr></table></figure>
<p>现在我们就要来按照自己的要求来配置，最好是按照官方的文档来定制：<a href="https://eslint.vuejs.org/">eslint-plugin-vue</a>，一般使用的规则会写在<code>rules</code>对象中。</p>
<h3 id="举个🌰">举个🌰<a class="anchor" href="#举个🌰"></a></h3>
<p>比如我希望在代码中使用双引号，并且语句使用分号结尾，那么<code>eslint.config.mjs</code>文件就可以这样写：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pluginVue <span class="keyword">from</span> <span class="string">&quot;eslint-plugin-vue&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> [</span><br><span class="line">  ...pluginVue.<span class="property">configs</span>[<span class="string">&quot;flat/recommended&quot;</span>],</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">rules</span>: &#123;</span><br><span class="line">      <span class="attr">quotes</span>: [<span class="string">&quot;error&quot;</span>, <span class="string">&quot;double&quot;</span>],</span><br><span class="line">      <span class="attr">semi</span>: [<span class="string">&quot;error&quot;</span>]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">];</span><br></pre></td></tr></table></figure>
<p>如果在代码中碰到一些eslint的报错，例如</p>
<p><img src="../../images/Pasted%20image%2020240608133029.png" alt=""></p>
<p>那么可以在报错信息中给出的链接网页中查找</p>
<p><img src="../../images/Pasted%20image%2020240608133229.png" alt=""></p>
<p>在<code>eslint.config.mjs</code>中的rules中加入<code>&quot;vue/multi-word-component-names&quot;: 0</code>后，报错就消失了。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Uniapp</tag>
      </tags>
  </entry>
  <entry>
    <title>记一次项目构建部署的总结</title>
    <url>/posts/81b0e9e7/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p>暑假里跟着鱼皮的<code>yuindex</code>项目写了个web终端的小玩具，完成的终端命令不多，但是大体上成型了。由于我打算做一个纯前端的项目，并没有写后端api，这样也方便我最终直接部署到<strong>Github Pages</strong>上（这样就不用花钱了hh）。</p>
<p>项目使用vite构建，并使用了ts。在构建部署时遇到了一些问题，由于是第一次将前端项目发布到github pages上，这里记录下。</p>
<span id="more"></span>
<h2 id="生成了额外的源映射文件">生成了额外的源映射文件<a class="anchor" href="#生成了额外的源映射文件"></a></h2>
<p>例如在执行了<code>pnpm run build</code>后，虽然生成了dist文件夹和其中的内容，但是源文件中的<code>.ts</code>文件会额外生成<code>.js</code>和<code>.js.map</code>文件，<code>.vue</code>文件会额外生成<code>.vue.js</code>和<code>.vue.js.map</code>文件：</p>
<p><img src="../../images/Pasted%20image%2020240830160408.png" alt=""></p>
<p>很明显这些额外生成的文件我们在发布部署项目时用不上，而且也无需提交到git上。摸索一番后，知道了这是TypeScript编译器生成的，我们需要修改项目的<code>tsconfig.json</code>文件。</p>
<p>对于<code>.map</code>文件：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;compilerOptions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;sourceMap&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>对于ts生成的对应的js文件：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;compilerOptions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;allowJs&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span> <span class="comment">// 禁止编译JS文件</span></span><br><span class="line">    <span class="attr">&quot;noEmit&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span> <span class="comment">// 仅进行类型检查，而不输出任何文件</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<h2 id="如何部署到Github-Pages">如何部署到Github Pages<a class="anchor" href="#如何部署到Github-Pages"></a></h2>
<p>在解决完前面的问题后，我们就可以将dist目录提交到github上了（记住要在.gitignore文件中排除dist目录），这里我们可以使用：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">git subtree push --prefix dist origin gh-pages</span><br></pre></td></tr></table></figure>
<p>这会将项目的子目录dist推送到远程仓库的gh-pages分支，如果远程仓库没有gh-pages分支，Git 会自动创建它并推送内容。</p>
<p><img src="../../images/Pasted%20image%2020240830162448.png" alt=""></p>
<p>接着在Github仓库的Settings中设置部署的位置：</p>
<p><img src="../../images/Pasted%20image%2020240830162659.png" alt=""></p>
<h2 id="Github-Pages项目路径错误">Github Pages项目路径错误<a class="anchor" href="#Github-Pages项目路径错误"></a></h2>
<p>由于<code>kerolt.github.io</code>用作了我的博客，这个项目只能作为子项来部署，也就是通过<code>kerolt.github.io/web-terminal</code>来访问。完成前面的步骤后，我虽然能访问，但是其中的内容无法显示，其原因为无法正确获取对应的文件。</p>
<p>这是因为相当于是在嵌套的公共路径下部署项目，在vite中需指定 <a href="https://cn.vitejs.dev/config/shared-options.html#base"><code>base</code> 配置项</a>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vite.config.ts</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(&#123;</span><br><span class="line">  <span class="attr">base</span>: <span class="string">&quot;/web-terminal/&quot;</span>,</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<hr>
<p>至此，项目已经可以通过<a href="https://kerolt.github.io/web-terminal/">https://kerolt.github.io/web-terminal/</a>来访问。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Vite</tag>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>API开放平台</title>
    <url>/posts/c40430e5/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p>通过一个项目来学习一下如何设计一个多服务的系统。同时也能学习Spring Cloud Gateway、Dubbo、API签名等一些知识。</p>
<h2 id="项目架构">项目架构<a class="anchor" href="#项目架构"></a></h2>
<p><img src="../../images/Screenshot_20240321_190000.png" alt=""></p>
<span id="more"></span>
<h2 id="用户前台">用户前台<a class="anchor" href="#用户前台"></a></h2>
<h3 id="普通用户">普通用户<a class="anchor" href="#普通用户"></a></h3>
<p>例如管理员发布了一个新的接口A，如果一位开发者并没有注册为该平台的用户，那么即便这位开发者拿到了这个接口地址，也是不可以使用接口A的。</p>
<p>当他注册为平台用户后，后台会给他生成<strong>AccessKey</strong>和<strong>SecretKey</strong>（后文简称为ak和sk）用于API签名认证，这时，用户带着这两个key，就可通过前台提供的在线测试或者客户端SDK来调用想要的接口了。</p>
<h3 id="管理员">管理员<a class="anchor" href="#管理员"></a></h3>
<p>管理员职责就是管理接口，包括接口的上线、下线、添加等</p>
<h2 id="客户端SDK（api-client-sdk）">客户端SDK（api-client-sdk）<a class="anchor" href="#客户端SDK（api-client-sdk）"></a></h2>
<p>若管理员将接口下线，则请求无效</p>
<p>通过<strong>AccessKey</strong>和<strong>SecretKey</strong>来请求<code>api-interface</code>接口服务。其中ak和sk只有用户在平台注册账号后才会分。当然，添加网关后，应该在<code>api-interface</code>前使用添加一层网关服务来避免api接口的直接暴露</p>
<p>sdk中的请求方法应该和api-interface中提供的接口一一对应，也就是两边应该同步修改</p>
<h2 id="网关（api-gateway）">网关（api-gateway）<a class="anchor" href="#网关（api-gateway）"></a></h2>
<p>使用Spring Cloud Gateway来完成一下功能</p>
<ol>
<li><strong>统一鉴权认证</strong>：应用 API 签名认证算法校验用户请求的合法性。根据请求拿到用户信息后从数据库中获取ak、sk进行比较，如果相同则代表用户请求合法</li>
<li><strong>公共业务逻辑</strong>：对每个接口的调用进行集中的统计 。这类似于Spring MVC中的拦截器和Spring中的AOP</li>
<li><strong>路由转发</strong>：前端发送请求到 API 网关，通过网关转发到实际的 API 接口 。这样就避免了直接将完整的接口地址暴露出来</li>
<li><strong>流量染色</strong>：给经过网关的请求加上特定的请求头参数，便于让实际的 API 服务确定请求来源及合法性</li>
</ol>
<h2 id="接口服务（api-interface）">接口服务（api-interface）<a class="anchor" href="#接口服务（api-interface）"></a></h2>
<p>真正实现和提供接口的地方，可以只编写controller层</p>
<h2 id="抽象接口（api-common）">抽象接口（api-common）<a class="anchor" href="#抽象接口（api-common）"></a></h2>
<p>在网关层由于要进行API鉴权，这避免不料要查询数据库，但是网关服务项目并没有使用MyBatis-Plus，为了避免写重复的代码，利用Dubbo分布式改造，使得网关层可以通过RPC来调用后端服务的方法</p>
<h2 id="服务后台（api-backend）">服务后台（api-backend）<a class="anchor" href="#服务后台（api-backend）"></a></h2>
<p>最核心的业务层服务！</p>
<ul>
<li>用户注册时会自动生成其ak和sk</li>
<li>接口信息的管理，即普通的增删改查，发布、下线接口</li>
<li>接口调用统计（没错，网关是通过rpc调用这里的服务）</li>
<li>当前台用户要进行在线测试时，我们会使用到用户的ak和sk，只有这样才能够比较安全的调用接口；而再通过客户端SDK，就能比较方便地去请求接口</li>
</ul>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>CMake中使用find_pakage来使用MySQL</title>
    <url>/posts/50cb0d54/</url>
    <content><![CDATA[<p>一般来说，如果我们在C++程序中要使用mysql的库，最简单的就是</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">g++ server.cpp -o server -lmysqlclient</span><br></pre></td></tr></table></figure>
<p>但要是在大一点的项目中，在数不清的源文件下使用g++命令来完成，怕是不太现实。</p>
<span id="more"></span>
<p>通常使用的工具为CMake，用其来构建项目，但它又没有包管理功能，对于我们想要使用的库，需要在 <code>CMakeLists.txt</code> 中引入。</p>
<p>引用的方式我这里选择 <code>find_pakage</code> （其概述和使用方法这里就不过多赘述了）</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span>(CMAKE_MODULE_PATH /usr/share/cmake/Modules)</span><br><span class="line"></span><br><span class="line"><span class="keyword">find_package</span>(MySQL REQUIRED)</span><br><span class="line"></span><br><span class="line"><span class="keyword">add_executable</span>(server <span class="variable">$&#123;src_list&#125;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(MYSQL_FOUND)</span><br><span class="line">    <span class="keyword">target_link_libraries</span>(server <span class="variable">$&#123;MYSQL_LIBRARIES&#125;</span>)</span><br><span class="line"><span class="keyword">else</span>(MYSQL_FOUND)</span><br><span class="line">    <span class="keyword">message</span>(FATAL_ERROR <span class="string">&quot;MySQL library not found&quot;</span>)</span><br><span class="line"><span class="keyword">endif</span>(CURL_FOUND)</span><br></pre></td></tr></table></figure>
<p>对上面的代码做一下解释：</p>
<ol>
<li>首先设置一下cmake module的路径，其实就是存放<code>.cmake</code>文件的位置。有时候看一些项目，会发现其根目录下会有一个<code>cmake</code>目录，诶，没错，就和这个一样</li>
<li>使用find_package来引入依赖库</li>
<li>添加可执行程序server</li>
<li>如果使用find_package找到了MySQL库，则将其链接到server上，否则终止构建</li>
</ol>
<p>值得注意的是，在CMAKE_MODULE_PATH目录下，必须有<code>Find&lt;LibaryName&gt;.cmake</code> 模块，在本例中，即为 <code>FindMySQL.cmake</code>，该文件可在网上找到。</p>
]]></content>
      <categories>
        <category>学习记录</category>
      </categories>
      <tags>
        <tag>CMake</tag>
      </tags>
  </entry>
  <entry>
    <title>VMware17.5.2启动踩坑</title>
    <url>/posts/4be0a7a8/</url>
    <content><![CDATA[<p>最近听说VMware17.5.2个人版可以免费使用了，故在Linux下安装用用，顺便记录一下踩的坑。</p>
<span id="more"></span>
<p>我是想在Linux上想用Uniapp，用wine的体验不是很好，故打算用虚拟机跑Windows。安装好VMware17.5.2后，配置好windows镜像后，点击启动，却报了这样的错：</p>
<blockquote>
<p>Could not open /dev/vmmon: ???. Please make sure that the kernel module `vmmon’ is loaded.</p>
</blockquote>
<p>搜了一下发现是原因为<strong>VMware无法访问其必要的内核模块vmmon</strong>：</p>
<p>我首先是尝试了手动启用VMware模块，然后执行命令安装缺失的模块</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> /etc/init.d/vmware start</span><br><span class="line"></span><br><span class="line"><span class="built_in">sudo</span> vmware-modconfig --console --install-all</span><br></pre></td></tr></table></figure>
<p>但是还是无效，在查看别人的<a href="https://segmentfault.com/a/1190000042268631">博客</a>后，我手动去编译安装缺失的<code>vmmon</code>和<code>vmnet</code>模块：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/mkubecek/vmware-host-modules</span><br><span class="line"><span class="built_in">cd</span> vmware-host-modules</span><br><span class="line">git checkout workstation-17.5.1</span><br><span class="line"></span><br><span class="line"><span class="built_in">sudo</span> make</span><br><span class="line"><span class="built_in">sudo</span> make install</span><br></pre></td></tr></table></figure>
<p>执行完成后，这两个模块将会安装到<code>/lib/modules/6.1.0-18-amd64/misc</code>下</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">kerolt  /usr/lib/modules/6.1.0-18-amd64/misc     </span><br><span class="line">$ <span class="built_in">ls</span> -l  </span><br><span class="line">总计 7164  </span><br><span class="line">-rw-r--r-- 1 root root 3996784  5月27日 18:48 vmmon.ko  </span><br><span class="line">-rw-r--r-- 1 root root 3337384  5月27日 18:48 vmnet.ko</span><br></pre></td></tr></table></figure>
<p>本以为到现在已经结束，结果再次启动VMware，还是不信，于是我想着可能是内核模块没有加载，采用如下命令查看：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">lsmod | grep vmmon</span><br></pre></td></tr></table></figure>
<p>不出意外，没有输出，手动加载模块：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> modprobe vmmon</span><br><span class="line"><span class="comment"># modprobe: ERROR: could not insert &#x27;vmmon&#x27;: Key was rejected by service</span></span><br></pre></td></tr></table></figure>
<p>根据该错误询问ChatGPT，其给出的答复为：<strong>启用了安全启动（Secure Boot），导致系统拒绝加载未签名或未正确签名的内核模块</strong>。</p>
<p>重启计算机，进入 BIOS/UEFI 设置，将<code>Secure Boot</code> 设置为 <code>Disabled</code>，之后再次启动VMware时就没有问题了～</p>
]]></content>
      <categories>
        <category>学习记录</category>
      </categories>
  </entry>
  <entry>
    <title>【CMake】CMake使用笔记</title>
    <url>/posts/aa749fff/</url>
    <content><![CDATA[<p>记录学习CMake中碰到的一些问题和笔记</p>
<p>ps：CMake相比xmake来说确实太繁琐，但还是得学习🍭</p>
<span id="more"></span>
<h2 id="现代CMake的命令行构建">现代CMake的命令行构建<a class="anchor" href="#现代CMake的命令行构建"></a></h2>
<p>在“古代”CMake中，我们想要构建项目要这样：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> build</span><br><span class="line"><span class="built_in">cd</span> build</span><br><span class="line">cmake ..</span><br><span class="line">make</span><br></pre></td></tr></table></figure>
<p>太啰嗦了，最后一步可能还用的不是make命令。不过在现代CMake中，提供了更为方便的 -B 和 --build，如下：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">cmake -B build <span class="comment"># 用于生成构建目录。-B 参数后面跟的是一个目录名，这里指定为 build</span></span><br><span class="line">cmake --build build <span class="comment"># 用于在已经生成的构建目录中构建项目</span></span><br></pre></td></tr></table></figure>
<p>这样省去了创建build目录等繁琐的操作，还统一了不同平台上的构建命令。</p>
<p>如果碰到了有关build缓存的相关问题，可以使用rm删除build目录，或者使用<code>cmake --build build --clean-first</code>来在构建之前先清理构建目录。</p>
<h2 id="添加源文件的几种方法">添加源文件的几种方法<a class="anchor" href="#添加源文件的几种方法"></a></h2>
<p>假设当前项目文件目录如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── CMakeLists.txt</span><br><span class="line">├── include</span><br><span class="line">│   └── add.h</span><br><span class="line">└── src</span><br><span class="line">    ├── add.cpp</span><br><span class="line">    └── main.cpp</span><br></pre></td></tr></table></figure>
<p>即当前项目的源文件为src目录下的cpp文件，在顶层的<code>CMakeLists.txt</code>中添加源文件目标：</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方法一</span></span><br><span class="line"><span class="keyword">file</span>(GLOB SOURCE_FILES <span class="string">&quot;$&#123;CMAKE_CURRENT_SOURCE_DIR&#125;/src/*.cpp&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方法二</span></span><br><span class="line"><span class="keyword">file</span>(GLOB_RECURSE SOURCE_FILES CONFIGURE_DEPENDS <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方法三</span></span><br><span class="line"><span class="keyword">aux_source_directory</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/src SOURCE_FILES)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加下面这一句即用上述源文件生成可执行文件app（也可使用add_library生成库文件等）</span></span><br><span class="line"><span class="keyword">add_executable</span>(app <span class="variable">$&#123;SOURCE_FILES&#125;</span>)</span><br></pre></td></tr></table></figure>
<p>可以看到，方法一和方法二虽然都是使用<code>file</code>指令，但内容却稍有区别：</p>
<ol>
<li>都是按照通配符批量匹配文件，<code>GLOB</code>和<code>GLOB_RECURSE</code>的区别在于后者允许*递归目录去匹配</li>
<li>对于选项<code>CONFIGURE_DEPENDS</code>，如果不添加，则在src/下添加新文件，由于cmake缓存的原因，<code>SOURCE_FILES</code>变量并不会更新，需要重新执行<code>cmake -B build</code></li>
</ol>
<h2 id="设置可执行文件的输出位置">设置可执行文件的输出位置<a class="anchor" href="#设置可执行文件的输出位置"></a></h2>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span>(EXECUTABLE_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/bin)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>学习记录</category>
      </categories>
      <tags>
        <tag>CMake</tag>
      </tags>
  </entry>
  <entry>
    <title>解决X11下窗口拖动撕裂的一个方法</title>
    <url>/posts/5b18479/</url>
    <content><![CDATA[<blockquote>
<p>Debian系Linux安装N卡驱动参考这篇<a href="https://www.if-not-true-then-false.com/2021/debian-ubuntu-linux-mint-nvidia-guide/">博客</a></p>
</blockquote>
<p>最近Debian终于是装好nvidia的驱动了，但是只能用在X11上，并且相比于不用n卡驱动时的Wayland，X11下的窗口拖动和视频播放会有比较明显的撕裂感，以下是解决的一个方法。</p>
<span id="more"></span>
<p>修改<code>/etc/X11/xorg.conf</code> 文件，在屏幕相关的区域内加上：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Option &quot;TripleBuffer&quot; &quot;true&quot;</span><br><span class="line">Option &quot;ForceFullCompositionPipeline&quot; &quot;on&quot;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>Option &quot;TripleBuffer&quot; &quot;true&quot;</code> 启用三重缓冲（Triple Buffering）来减少屏幕撕裂，它通过增加一个额外的缓冲区来同步视频输出和显示器的刷新率。</li>
<li><code>Option &quot;ForceFullCompositionPipeline&quot; &quot;on&quot;</code> 用于强制使用完整的合成管线（Full Composition Pipeline）。启用这个选项可以提高性能，在某些游戏和应用程序中，它可以帮助减少延迟和提高帧率。</li>
</ul>
]]></content>
      <categories>
        <category>学习记录</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux开放端口号</title>
    <url>/posts/2945410c/</url>
    <content><![CDATA[<p>假设使用vite启动项目后，希望在移动设备上通过ip和端口号来访问项目，通常需要在Linux上开放端口号。</p>
<span id="more"></span>
<p>这里通过<code>firewalld</code>来完成，可能需要自己安装一下：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install firewalld</span><br></pre></td></tr></table></figure>
<p>可通过systemctl来检测其是否工作：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">systemctl status firewalld</span><br></pre></td></tr></table></figure>
<p>添加端口想要的端口，返回 success 代表成功（<code>–permanent</code>表示永久生效，没有此参数重启后失效）。例如vite的默认端口为5173：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --add-port=5173/tcp --permanent</span><br></pre></td></tr></table></figure>
<p>之后执行<code>firewall-cmd --reload</code>，返回 success 代表成功。</p>
<p>查询端口号：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --query-port=5173/tcp</span><br></pre></td></tr></table></figure>
<p>关闭端口号：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --remove-port=5173/tcp --permanent</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab1 utilities</title>
    <url>/posts/50c5c1b7/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p>该Lab通过实现几个命令来熟悉 xv6 及其系统调用</p>
<ul>
<li>sleep</li>
<li>pingpong</li>
<li>primes</li>
<li>find</li>
<li>xargs</li>
</ul>
<p>官方实验指导：<a href="https://pdos.csail.mit.edu/6.S081/2021/labs/util.html">https://pdos.csail.mit.edu/6.S081/2021/labs/util.html</a></p>
<p>个人代码实现仓库：<a href="https://github.com/kerolt/xv6-labs-2023/">https://github.com/kerolt/xv6-labs-2023</a></p>
<span id="more"></span>
<h2 id="环境搭建">环境搭建<a class="anchor" href="#环境搭建"></a></h2>
<p>使用<strong>docker</strong>创建ubuntu20.04容器，后执行：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">apt install git build-essential gdb-multiarch qemu-system-misc gcc-riscv64-linux-gnu binutils-riscv64-linux-gnu </span><br></pre></td></tr></table></figure>
<p>之后测试一下：</p>
<ul>
<li>首先下载xv6源码：<code>git clone https://github.com/mit-pdos/xv6-riscv.git</code></li>
<li>运行：<code>make qemu</code>，如果结果如下，说明成功，按下<code>ctrl + a</code>和<code>x</code>退出qemu</li>
</ul>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ... lots of output ...</span></span><br><span class="line">init: starting sh</span><br><span class="line">$</span><br></pre></td></tr></table></figure>
<h2 id="测试">测试<a class="anchor" href="#测试"></a></h2>
<p>对于完成的程序，如果想要<strong>测试</strong>，则在<code>Makefile</code>中的<strong>UPROGS</strong>中添加：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="variable">$U</span>/_&lt;xxx&gt;\</span><br></pre></td></tr></table></figure>
<p>其中的xxx即为程序的名称，如sleep,则为<code>$U/_sleep\</code>。</p>
<p>之后，可使用如下方法进行测试：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./grade-lab-util xxx</span><br><span class="line"></span><br><span class="line"><span class="comment"># or make GRADEFLAGS=xxx grade</span></span><br></pre></td></tr></table></figure>
<h2 id="sleep-easy">sleep (easy)<a class="anchor" href="#sleep-easy"></a></h2>
<p>练前开胃菜，使用sleep系统调用。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/stat.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: sleep &lt;seconds&gt;\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> time = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    sleep(time);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="pingpong-easy">pingpong (easy)<a class="anchor" href="#pingpong-easy"></a></h2>
<p>编写一个程序，使用 UNIX 系统调用在两个进程之间通过一对管道 &quot;乒乓 &quot;传送一个字节，每个管道一个方向。</p>
<p>该程序需要注意的就是对于两个管道的操作，何时关，关哪个？读写顺序又如何？</p>
<ul>
<li>对于父进程，应该先写再读</li>
<li>对于子进程，应该先读再写</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/stat.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> R 0</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> W 1</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> p2c[<span class="number">2</span>], c2p[<span class="number">2</span>];</span><br><span class="line">    pipe(p2c);</span><br><span class="line">    pipe(c2p);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> pid = fork();</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123; <span class="comment">// chile: read from the parent</span></span><br><span class="line">        <span class="type">char</span> buf[<span class="number">32</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">        close(c2p[R]); <span class="comment">// 不用从子进程读</span></span><br><span class="line">        close(p2c[W]); <span class="comment">// 不用从父进程写</span></span><br><span class="line"></span><br><span class="line">        read(p2c[R], buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">        close(p2c[R]);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d: received ping\n&quot;</span>, getpid());</span><br><span class="line">        write(c2p[W], <span class="string">&quot;pong&quot;</span>, <span class="number">4</span>);</span><br><span class="line">        close(c2p[W]);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">// parent: read from the child</span></span><br><span class="line">        <span class="type">char</span> buf[<span class="number">32</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">        close(p2c[R]); <span class="comment">// 不用从父进程读</span></span><br><span class="line">        close(c2p[W]); <span class="comment">// 不用从子进程写</span></span><br><span class="line"></span><br><span class="line">        write(p2c[W], <span class="string">&quot;ping&quot;</span>, <span class="number">4</span>);</span><br><span class="line">        close(p2c[W]);</span><br><span class="line">        read(c2p[R], buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d: received pong\n&quot;</span>, getpid());</span><br><span class="line">        close(c2p[R]);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="primes-（moderate-hard）">primes （moderate/hard）<a class="anchor" href="#primes-（moderate-hard）"></a></h2>
<p>使用管道编写并发版质数筛。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/stat.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">Filter</span><span class="params">(<span class="type">int</span> pipe_fd[<span class="number">2</span>])</span> &#123;</span><br><span class="line">    close(pipe_fd[<span class="number">1</span>]);</span><br><span class="line">    <span class="type">int</span> prime;</span><br><span class="line">    read(pipe_fd[<span class="number">0</span>], &amp;prime, <span class="number">4</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;prime %d\n&quot;</span>, prime);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> num;</span><br><span class="line">    <span class="keyword">if</span> (read(pipe_fd[<span class="number">0</span>], &amp;num, <span class="number">4</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> new_pipe_fd[<span class="number">2</span>];</span><br><span class="line">    pipe(new_pipe_fd);</span><br><span class="line">    <span class="type">int</span> pid = fork();</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Fork error!\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">        Filter(new_pipe_fd);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        close(new_pipe_fd[<span class="number">0</span>]);</span><br><span class="line">        <span class="keyword">if</span> (num % prime != <span class="number">0</span>) &#123;</span><br><span class="line">            write(new_pipe_fd[<span class="number">1</span>], &amp;num, <span class="number">4</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (read(pipe_fd[<span class="number">0</span>], &amp;num, <span class="number">4</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (num % prime != <span class="number">0</span>) &#123;</span><br><span class="line">                write(new_pipe_fd[<span class="number">1</span>], &amp;num, <span class="number">4</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        close(new_pipe_fd[<span class="number">1</span>]);</span><br><span class="line">        close(pipe_fd[<span class="number">0</span>]);</span><br><span class="line">        wait(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> pipe_fd[<span class="number">2</span>];</span><br><span class="line">    pipe(pipe_fd);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> pid = fork();</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Fork error!\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">        Filter(pipe_fd);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        close(pipe_fd[<span class="number">0</span>]);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= <span class="number">35</span>; i++) &#123;</span><br><span class="line">            write(pipe_fd[<span class="number">1</span>], &amp;i, <span class="number">4</span>); <span class="comment">// 一个int为4 byte</span></span><br><span class="line">        &#125;</span><br><span class="line">        close(pipe_fd[<span class="number">1</span>]);</span><br><span class="line">        wait(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="find-moderate">find (moderate)<a class="anchor" href="#find-moderate"></a></h2>
<p>实现Unix下的find命令，利用递归处理，要注意关闭文件描述符的时机。</p>
<p>最关键的是要理解目录（文件夹）也是一种文件，其目录项就是这个文件的内容，所以我们可以通过read系统调用来读取目录项，进而当读取的内容的类型是一个目录时，即可递归调用Find。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/stat.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;user/user.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/fs.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span>* <span class="title function_">GetName</span><span class="params">(<span class="type">char</span>* path)</span> &#123;</span><br><span class="line">    <span class="type">char</span>* p;</span><br><span class="line">    <span class="keyword">for</span> (p = path + <span class="built_in">strlen</span>(path); p &gt;= path &amp;&amp; *p != <span class="string">&#x27;/&#x27;</span>; p--) &#123;&#125;</span><br><span class="line">    p++;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">Find</span><span class="params">(<span class="type">char</span>* dir, <span class="type">char</span>* name)</span> &#123;</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">512</span>];</span><br><span class="line">    <span class="type">char</span>* p;</span><br><span class="line">    <span class="type">int</span> fd = open(dir, <span class="number">0</span>);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">dirent</span> <span class="title">de</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">stat</span> <span class="title">st</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Error: open\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (fstat(fd, &amp;st) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Error: stat\n&quot;</span>);</span><br><span class="line">        close(fd);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (st.type != T_DIR) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;the current the file is not dictionary\n&quot;</span>, dir);</span><br><span class="line">        close(fd);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">strcpy</span>(buf, dir);</span><br><span class="line">    p = buf + <span class="built_in">strlen</span>(buf);</span><br><span class="line">    *p++ = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (read(fd, &amp;de, <span class="keyword">sizeof</span>(de)) == <span class="keyword">sizeof</span>(de)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (de.inum == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">strcmp</span>(de.name, <span class="string">&quot;.&quot;</span>) == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">strcmp</span>(de.name, <span class="string">&quot;..&quot;</span>) == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">char</span>* cur = p;</span><br><span class="line">        memmove(cur, de.name, DIRSIZ);</span><br><span class="line">        cur[DIRSIZ] = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (stat(buf, &amp;st) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;Error: stat\n&quot;</span>);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">switch</span> (st.type) &#123;</span><br><span class="line">            <span class="keyword">case</span> T_FILE:</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strcmp</span>(GetName(buf), name) == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, buf);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> T_DIR:</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strlen</span>(dir) + <span class="number">1</span> + DIRSIZ + <span class="number">1</span> &gt; <span class="keyword">sizeof</span>(buf)) &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;Error: path too long\n&quot;</span>);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                Find(buf, name);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(fd);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: find &lt;dir&gt; &lt;file_name&gt;\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    Find(argv[<span class="number">1</span>], argv[<span class="number">2</span>]);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="xargs-moderate">xargs (moderate)<a class="anchor" href="#xargs-moderate"></a></h2>
<p>简单实现Unix上的xargs命令。简单介绍xargs的用法，就是将标准输入作为xargs的参数。更多的介绍，可以看阮一峰老师的博客：<a href="https://ruanyifeng.com/blog/2019/08/xargs-tutorial.html">https://ruanyifeng.com/blog/2019/08/xargs-tutorial.html</a></p>
<p>该命令的可使用fork和exec来实现。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/stat.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/param.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc &lt; <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: xargs &lt;params&gt;\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span>* child_argv[MAXARG];</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">512</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">    <span class="type">int</span> index = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; argc; i++) &#123;</span><br><span class="line">        child_argv[index++] = argv[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    sleep(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从标准输入中读取命令到buf中</span></span><br><span class="line">    <span class="comment">// 若执行 echo 1 2 3，则标准输入为 1 2 3</span></span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    <span class="keyword">while</span> ((n = read(<span class="number">0</span>, buf, <span class="keyword">sizeof</span>(buf))) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="type">char</span>* p = buf;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (buf[i] != <span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            <span class="keyword">if</span> (fork() == <span class="number">0</span>) &#123;</span><br><span class="line">                buf[i] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">                <span class="comment">// 例如：echo 1 | xargs echo 2</span></span><br><span class="line">                <span class="comment">// 在xargs中，标准输入为1，buf中内容为&quot;1\n&quot;，child_argv为[&quot;echo&quot;, &quot;2&quot;]，index为2</span></span><br><span class="line">                <span class="comment">// 程序执行到此处时，buf中内容变为了&quot;1\0&quot;，child_argv变为了[&quot;echo&quot;, &quot;2&quot;, &quot;1&quot;]</span></span><br><span class="line">                child_argv[index] = p;</span><br><span class="line">                <span class="comment">// exec的第一个参数是要执行的可执行文件的路径</span></span><br><span class="line">                <span class="comment">// 第二个参数是作为新命令的参数数组，数组的第一项为新命令的名称</span></span><br><span class="line">                exec(child_argv[<span class="number">0</span>], child_argv);</span><br><span class="line">                <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">	            <span class="comment">// 在父进程中，跳过buf中的\n后，是一条新命令的开始</span></span><br><span class="line">                p = &amp;buf[i + <span class="number">1</span>];</span><br><span class="line">                wait(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab2 system calls</title>
    <url>/posts/18ad9abb/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p>这个lab开始我们就正式进入了xv6的世界了，这一次我们可以了解到内核中系统调用的注册和运行原理，这可以说是之后lab的一个基石。</p>
<span id="more"></span>
<h2 id="trace">trace<a class="anchor" href="#trace"></a></h2>
<p>首先，我们要清楚这个实验的目的是什么：</p>
<blockquote>
<p>In this assignment you will add a system call tracing feature that may help you when debugging later labs. You’ll create a new trace system call that will control tracing. It should take one argument, an integer “mask”, whose bits specify which system calls to trace. For example, to trace the fork system call, a program calls trace(1 &lt;&lt; SYS_fork), where SYS_fork is a syscall number from kernel/syscall.h. You have to modify the xv6 kernel to print out a line when each system call is about to return, if the system call’s number is set in the mask. The line should contain the process id, the name of the system call and the return value; you don’t need to print the system call arguments. The trace system call should enable tracing for the process that calls it and any children that it subsequently forks, but should not affect other processes.</p>
</blockquote>
<p>译：在Xv6的trace命令中，它应该有一个参数，一个整数“掩码”，其位指定要跟踪的系统调用。例如，要跟踪fork系统调用，程序调用<code>trace(1&lt;&lt;SYS_fork)</code>，其中SYS_fork是<code>kernel/syscall.h</code>中的系统调用编号。如果系统调用的编号在掩码中设置，则必须修改xv6内核，以便在每个系统调用即将返回时打印出一行。该行应包含<strong>进程id</strong>、<strong>系统调用的名称</strong>和<strong>返回值</strong>；您不需要打印系统调用参数。跟踪系统调用应启用对调用它的进程及其随后分叉的任何<strong>子进程</strong>的跟踪，但不应影响其他进程。</p>
<p>注意，在该实验的初始阶段，xv6已经为我们提供了trace命令的用户态实现，但是其底层的系统调用需要我们自己实现。</p>
<h3 id="mask是什么？">mask是什么？<a class="anchor" href="#mask是什么？"></a></h3>
<p>在使用trace命令时用到的掩码，是用来跟踪之后使用的命令用到了哪些系统调用。例如实验中给出的例子：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ trace 32 grep hello README</span><br><span class="line">3: syscall <span class="built_in">read</span> -&gt; 1023</span><br><span class="line">3: syscall <span class="built_in">read</span> -&gt; 966</span><br><span class="line">3: syscall <span class="built_in">read</span> -&gt; 70</span><br><span class="line">3: syscall <span class="built_in">read</span> -&gt; 0</span><br></pre></td></tr></table></figure>
<p>这个32就是掩码，其跟踪到了grep命令中使用到了read系统调用（为什么是read？马上就说到了）。在xv6的<code>kernel/syscall.h</code>中有所有系统调用的编号：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// System call numbers</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_fork    1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_exit    2</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_wait    3</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_pipe    4</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_read    5</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_kill    6</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_exec    7</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_fstat   8</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_chdir   9</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_dup    10</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_getpid 11</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_sbrk   12</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_sleep  13</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_uptime 14</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_open   15</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_write  16</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_mknod  17</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_unlink 18</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_link   19</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_mkdir  20</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_close  21</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 添加</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYS_trace  22 </span></span><br></pre></td></tr></table></figure>
<p>将这个mask以二进制的形式来看待更加容易理解，如果传入的mask是32，那么其二进制为100000，这个1出现的位置是第5位（最低位按0计数），也就是去找编号为5的系统调用，也就是<code>SYS_read</code>。</p>
<p>xv6内核提供给用户态的接口为trace，但是我们需要自己<strong>在xv6的用户头文件中添加函数的声明</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// user/user.h</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">trace</span><span class="params">(<span class="type">int</span>)</span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure>
<p>这个trace底层其实调用的应该是sys_trace（这个函数名不是固定的，但是源码中其他的系统调用的命名都为sys_*，故trace对应的系统调用写成sys_trace更加合理）。sys_trace需要做的是将用户传入的mask再传给当前进程及其子进程。</p>
<p>我们这里<strong>将系统调用sys_trace的编号设置为22</strong>。</p>
<h3 id="进程及其子进程如何获取mask？">进程及其子进程如何获取mask？<a class="anchor" href="#进程及其子进程如何获取mask？"></a></h3>
<p>在xv6 book的4.3节中，有这么一段话：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">syscall (kernel/syscall.c:132) retrieves the system call number from the saved a7 in the trapframe and uses it to index into syscalls. For the first system call, a7 contains SYS_exec (kernel/syscall.h:8),  resulting in a call to the system call implementation function sys_exec.</span><br><span class="line"></span><br><span class="line">When sys_exec returns, syscall records its return value in p-&gt;trapframe-&gt;a0. This will cause the original user-space call to exec() to return that value, since the C calling convention on RISC-V places return values in a0. System calls conventionally return negative numbers to indicate errors, and zero or positive numbers for success. If the system call number is invalid, syscall prints an error and returns −1.</span><br></pre></td></tr></table></figure>
<p>即系统调用的编号会保存在进程的trapframe中，根据a7寄存器即可获得，系统调用的返回值可通过a0寄存器获得。欸！这两个值可不就是实验实现中需要的吗！那么理所当然，实验中需要打印的语句应该就在这个函数中添加。</p>
<p>让我们来看看xv6中进程的数据结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/proc.h</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">spinlock</span> <span class="title">lock</span>;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// p-&gt;lock must be held when using these:</span></span><br><span class="line">  <span class="class"><span class="keyword">enum</span> <span class="title">procstate</span> <span class="title">state</span>;</span>        <span class="comment">// Process state</span></span><br><span class="line">  <span class="type">void</span> *chan;                  <span class="comment">// If non-zero, sleeping on chan</span></span><br><span class="line">  <span class="type">int</span> killed;                  <span class="comment">// If non-zero, have been killed</span></span><br><span class="line">  <span class="type">int</span> xstate;                  <span class="comment">// Exit status to be returned to parent&#x27;s wait</span></span><br><span class="line">  <span class="type">int</span> pid;                     <span class="comment">// Process ID</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// wait_lock must be held when using this:</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">parent</span>;</span>         <span class="comment">// Parent process</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// these are private to the process, so p-&gt;lock need not be held.</span></span><br><span class="line">  uint64 kstack;               <span class="comment">// Virtual address of kernel stack</span></span><br><span class="line">  uint64 sz;                   <span class="comment">// Size of process memory (bytes)</span></span><br><span class="line">  <span class="type">pagetable_t</span> pagetable;       <span class="comment">// User page table</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">trapframe</span>;</span> <span class="comment">// data page for trampoline.S</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">context</span> <span class="title">context</span>;</span>      <span class="comment">// swtch() here to run process</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">ofile</span>[<span class="title">NOFILE</span>];</span>  <span class="comment">// Open files</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">cwd</span>;</span>           <span class="comment">// Current directory</span></span><br><span class="line">  <span class="type">char</span> name[<span class="number">16</span>];               <span class="comment">// Process name (debugging)</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 添加</span></span><br><span class="line">  <span class="type">int</span> trace_mask;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到诸如进程名、pid、上下文等信息都是保存在这个数据结构中，那么我们可以<strong>在其中加上一个成员变量 <code>trace_mask</code> 用于保存当前进程所对应trace命令中的掩码mask</strong>。</p>
<p>xv6已经实现了用户态的trace命令，其位于 <code>user/trace.c</code> 中：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/param.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;kernel/stat.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">int</span> i;</span><br><span class="line">  <span class="type">char</span> *nargv[MAXARG];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(argc &lt; <span class="number">3</span> || (argv[<span class="number">1</span>][<span class="number">0</span>] &lt; <span class="string">&#x27;0&#x27;</span> || argv[<span class="number">1</span>][<span class="number">0</span>] &gt; <span class="string">&#x27;9&#x27;</span>))&#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">&quot;Usage: %s mask command\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (trace(atoi(argv[<span class="number">1</span>])) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">&quot;%s: trace failed\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">for</span>(i = <span class="number">2</span>; i &lt; argc &amp;&amp; i &lt; MAXARG; i++)&#123;</span><br><span class="line">    nargv[i<span class="number">-2</span>] = argv[i];</span><br><span class="line">  &#125;</span><br><span class="line">  exec(nargv[<span class="number">0</span>], nargv);</span><br><span class="line">  <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>对于这样的一条命令 <code>trace 32 grep hello README</code> ，假设开启的进程名为p，那么32将会传给<code>p.trace_mask</code>，之后的grep操作将使用<code>exec</code><strong>创建子进程</strong>（假设进程名为son）执行，那么在创建子进程后应该有<code>son.trace_mask = p.trace_mask</code>，只有这样，grep操作所用到的系统调用才能被跟踪到。</p>
<p>在使用trace命令时，其后的mask参数会存到a0寄存器中，为了从其中拿到mask，可以使用<code>argint()</code>函数，其源码为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/syscall.h</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Fetch the nth 32-bit system call argument.</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">argint</span><span class="params">(<span class="type">int</span> n, <span class="type">int</span> *ip)</span> &#123;</span><br><span class="line">  *ip = argraw(n);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> uint64 <span class="title function_">argraw</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line">  <span class="keyword">switch</span> (n) &#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">    <span class="keyword">return</span> p-&gt;trapframe-&gt;a0;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">    <span class="keyword">return</span> p-&gt;trapframe-&gt;a1;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">    <span class="keyword">return</span> p-&gt;trapframe-&gt;a2;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">    <span class="keyword">return</span> p-&gt;trapframe-&gt;a3;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">4</span>:</span><br><span class="line">    <span class="keyword">return</span> p-&gt;trapframe-&gt;a4;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">5</span>:</span><br><span class="line">    <span class="keyword">return</span> p-&gt;trapframe-&gt;a5;</span><br><span class="line">  &#125;</span><br><span class="line">  panic(<span class="string">&quot;argraw&quot;</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>argint()</code>内调用了<code>argraw()</code>，在查看以上源码后，由于只传入了一个参数，故应该将0传入argraw中。<strong>在<code>kernel/sysproc.c</code>中实现<code>sys_trace()</code></strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/sysproc.c</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">uint64 <span class="title function_">sys_trace</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> mask;</span><br><span class="line">  argint(<span class="number">0</span>, &amp;mask);</span><br><span class="line">  <span class="keyword">if</span> (mask &lt; <span class="number">0</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line">  p-&gt;trace_mask = mask;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样当trace使用了底层的sys_trace时，就可以把mask参数传递给当前进程。但是只传递给当前进程还不够，还要传给当前进程的子进程。在Linux，我们创建子进程的函数为<code>fork()</code>，在xv6中也同样如此，fork内部先获取当前进程的proc结构体，然后新创建一个proc结构体代表子进程，并将父进程中的值拷贝过去，故<strong>传递给子进程的mask也在其中拷贝</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/proc.c</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fork</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// copy mask from father process to son process</span></span><br><span class="line">  <span class="comment">// np为子进程，p为父进程</span></span><br><span class="line">  np-&gt;trace_mask = p-&gt;trace_mask;</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>要注意一个小细节，<strong>当进程结构体被释放时（进程结束或者为进程分配proc结构体），其mask也该重置</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/proc.c</span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">freeproc</span><span class="params">(<span class="keyword">struct</span> proc *p)</span> &#123;</span><br><span class="line">  <span class="comment">// ... ...</span></span><br><span class="line">  p-&gt;trace_mask = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>完成好以上内容后，就可以<strong>实现sys_trace</strong>了：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/sysproc.c</span></span><br><span class="line"></span><br><span class="line">uint64 <span class="title function_">sys_trace</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> mask;</span><br><span class="line">  argint(<span class="number">0</span>, &amp;mask);</span><br><span class="line">  <span class="keyword">if</span> (mask &lt; <span class="number">0</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line">  p-&gt;trace_mask = mask;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样，当前进程就可以获取了到mask，当其创建子进程时，子进程也可获取到mask~</p>
<h3 id="如何跟踪系统调用？">如何跟踪系统调用？<a class="anchor" href="#如何跟踪系统调用？"></a></h3>
<p>刚刚我们说了mask的作用，还有进程及其子进程如何获取mask，那么我们又应该如何跟踪系统调用呢？</p>
<p>xv6中所用的系统调用都是在 <code>kernel/syscall.c</code> 中的 <code>syscall</code> 函数中调用的，为了能在syscall.c中调用sys_trace，需要在其中<strong>添加extern声明</strong>（其定义在刚刚已经实现，位于kernel/sysproc.c）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> uint64 <span class="title function_">sys_trace</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p>同时需要<strong>在syscalls数组中添加sys_trace的编号</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="title function_">uint64</span> <span class="params">(*syscalls[])</span><span class="params">(<span class="type">void</span>)</span> = &#123;</span><br><span class="line"><span class="comment">// ... ...</span></span><br><span class="line">[SYS_trace]   sys_trace,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里实际上就是 [22] = sys_trace</span></span><br><span class="line"><span class="comment">// 使用了gcc的一个拓展</span></span><br></pre></td></tr></table></figure>
<p>并按照顺序<strong>添加各个系统调用的名字</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> *syscall_names[] = &#123;</span><br><span class="line">  <span class="string">&quot;fork&quot;</span>, <span class="string">&quot;exit&quot;</span>, <span class="string">&quot;wait&quot;</span>, <span class="string">&quot;pipe&quot;</span>, <span class="string">&quot;read&quot;</span>, <span class="string">&quot;kill&quot;</span>,</span><br><span class="line">  <span class="string">&quot;exec&quot;</span>, <span class="string">&quot;fstat&quot;</span>, <span class="string">&quot;chdir&quot;</span>, <span class="string">&quot;dup&quot;</span>, <span class="string">&quot;getpid&quot;</span>, <span class="string">&quot;sbrk&quot;</span>,</span><br><span class="line">  <span class="string">&quot;sleep&quot;</span>, <span class="string">&quot;uptime&quot;</span>, <span class="string">&quot;open&quot;</span>, <span class="string">&quot;write&quot;</span>, <span class="string">&quot;mknod&quot;</span>, <span class="string">&quot;unlink&quot;</span>,</span><br><span class="line">  <span class="string">&quot;link&quot;</span>, <span class="string">&quot;mkdir&quot;</span>, <span class="string">&quot;close&quot;</span>, <span class="string">&quot;trace&quot;</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>在 <code>kernel/syscall.c</code> 中，xv6根据a7寄存器获取系统调用的编号，然后通过syscalls函数数组执行系统调用，那么我们的实现为：当使用的系统调用合法时，获取当前进程的mask，并通过判断<code>(mask &gt;&gt; syscall_num) &amp; 1</code>是否为1来输出跟踪信息。</p>
<p>例如sys_read系统调用的编号为5，mask为32，则<code>(32 &gt;&gt; 5) &amp; 1 = 1</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">syscall</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> num;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line"></span><br><span class="line">  num = p-&gt;trapframe-&gt;a7;</span><br><span class="line">  <span class="keyword">if</span>(num &gt; <span class="number">0</span> &amp;&amp; num &lt; NELEM(syscalls) &amp;&amp; syscalls[num]) &#123;</span><br><span class="line">    <span class="comment">// Use num to lookup the system call function for num, call it,</span></span><br><span class="line">    <span class="comment">// and store its return value in p-&gt;trapframe-&gt;a0</span></span><br><span class="line">    p-&gt;trapframe-&gt;a0 = syscalls[num]();</span><br><span class="line"></span><br><span class="line">	<span class="comment">// ===============================</span></span><br><span class="line">    <span class="type">int</span> mask = p-&gt;trace_mask;</span><br><span class="line">    <span class="keyword">if</span> ((mask &gt;&gt; num) &amp; <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;%d: syscall %s -&gt; %d\n&quot;</span>, p-&gt;pid, syscall_names[num - <span class="number">1</span>], p-&gt;trapframe-&gt;a0);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ===============================</span></span><br><span class="line"></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d %s: unknown sys call %d\n&quot;</span>,</span><br><span class="line">            p-&gt;pid, p-&gt;name, num);</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么，内核是如何通过trace找到sys_trace的呢？根据实验指导上的提示，可以知道 <code>user/usys.pl</code> 起到了一个中间人的作用：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"># user/usys.pl</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">sub entry &#123;</span><br><span class="line">    my $name = shift;</span><br><span class="line">    print &quot;.global $name\n&quot;;</span><br><span class="line">    print &quot;$&#123;name&#125;:\n&quot;;</span><br><span class="line">    print &quot; li a7, SYS_$&#123;name&#125;\n&quot;;</span><br><span class="line">    print &quot; ecall\n&quot;;</span><br><span class="line">    print &quot; ret\n&quot;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">entry(&quot;uptime&quot;);</span><br><span class="line"><span class="addition">++entry(&quot;trace&quot;); # 这是实验中需要由我们自己添加的</span></span><br></pre></td></tr></table></figure>
<p>通过其中的entry函数，可以生成对应的调用（xv6中为ecall）系统调用的汇编语句，即大致的流程为xv6在构建内核时，会将用户态trace命令对应到：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.global trace</span><br><span class="line">trace:</span><br><span class="line">    li a7, SYS_trace</span><br><span class="line">    ecall</span><br><span class="line">    ret</span><br></pre></td></tr></table></figure>
<p>这样，当前进程就可以通过a7寄存器拿到sys_trace的系统调用编号了，也就是说，<code>syscall</code> 函数可以调用 <code>sys_trace</code> 了。</p>
<p>OK，那么一个大致的框架就出来了：</p>
<p><img src="../../images/Pasted%20image%2020240510183553.png" alt=""></p>
<p>完成上面的步骤后，最后只要在<code>Makefile</code>中的<code>UPROGS</code>加上<code>$U/_trace</code>即可。</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line">UPROGS=\</span><br><span class="line">	$U/_cat\</span><br><span class="line">	$U/_echo\</span><br><span class="line">	$U/_forktest\</span><br><span class="line">	$U/_grep\</span><br><span class="line">	$U/_init\</span><br><span class="line">	$U/_kill\</span><br><span class="line">	$U/_ln\</span><br><span class="line">	$U/_ls\</span><br><span class="line">	$U/_mkdir\</span><br><span class="line">	$U/_rm\</span><br><span class="line">	$U/_sh\</span><br><span class="line">	$U/_stressfs\</span><br><span class="line">	$U/_usertests\</span><br><span class="line">	$U/_grind\</span><br><span class="line">	$U/_wc\</span><br><span class="line">	$U/_zombie\</span><br><span class="line"><span class="addition">++	$U/_trace</span></span><br></pre></td></tr></table></figure>
<p>在这个lab中，我们需要添加一个系统调用sysinfo，用于收集有关正在运行的系统的信息。</p>
<!--more-->
<p>系统调用的声明为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">sysinfo</span><span class="params">(<span class="keyword">struct</span> sysinfo*)</span>;</span><br></pre></td></tr></table></figure>
<p>这个系统调用接受一个指向结构体sysinfo的指针，其定义为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/sysinfo.h</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sysinfo</span> &#123;</span></span><br><span class="line">  uint64 freemem;   <span class="comment">// amount of free memory (bytes)</span></span><br><span class="line">  uint64 nproc;     <span class="comment">// number of process</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>内核应填写此结构的字段：freemem字段应设置为可用内存的<strong>字节数</strong>，nproc字段应设为<strong>状态未使用</strong>的进程数。</p>
<h2 id="sysinfo">sysinfo<a class="anchor" href="#sysinfo"></a></h2>
<p>在这个part中，我们需要添加一个系统调用sysinfo，用于收集有关正在运行的系统的信息。</p>
<h3 id="计算可用内存字节数">计算可用内存字节数<a class="anchor" href="#计算可用内存字节数"></a></h3>
<p>我们可以通过内核中的<code>kmem</code>来获取可用的内存块的数量：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">spinlock</span> <span class="title">lock</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">freelist</span>;</span></span><br><span class="line">&#125; kmem;</span><br></pre></td></tr></table></figure>
<p><code>kmem.freelist</code>是一个链表，保存了所有<strong>可用</strong>的内存块的地址，我们遍历这个链表即可获取可用内存块数量，又一个内存块的大小为4KB，那么<code>系统可用的内存字节数 = 可用内存块数量 * 4KB</code>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/kalloc.c</span></span><br><span class="line"></span><br><span class="line">uint64 <span class="title function_">freemem</span><span class="params">()</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span>* <span class="title">r</span>;</span></span><br><span class="line">  uint64 free_page = <span class="number">0</span>;</span><br><span class="line">  acquire(&amp;kmem.lock);</span><br><span class="line">  r = kmem.freelist;</span><br><span class="line">  <span class="keyword">while</span> (r) &#123;</span><br><span class="line">    free_page++;</span><br><span class="line">    r = r-&gt;next;</span><br><span class="line">  &#125;</span><br><span class="line">  release(&amp;kmem.lock);</span><br><span class="line">  <span class="comment">// 4K = 2^12，左移操作相当于对2的乘法</span></span><br><span class="line">  <span class="keyword">return</span> (free_page &lt;&lt; <span class="number">12</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="计算状态为未为使用进程数">计算状态为未为使用进程数<a class="anchor" href="#计算状态为未为使用进程数"></a></h3>
<p>内核中有一个全局数组，其中每一项为系统中的进程，xv6中设置最多进程数为64个：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> <span class="title">proc</span>[<span class="title">NPROC</span>];</span></span><br></pre></td></tr></table></figure>
<p>在表示进程的结构体中，有一个成员表示这个进程的状态：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">procstate</span> &#123;</span> UNUSED, USED, SLEEPING, RUNNABLE, RUNNING, ZOMBIE &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Per-process state</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> &#123;</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="comment">// p-&gt;lock must be held when using these:</span></span><br><span class="line">  <span class="class"><span class="keyword">enum</span> <span class="title">procstate</span> <span class="title">state</span>;</span> </span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以遍历proc数组，找到所有<code>state != UNUSED</code>的进程的数量（这里一定要看清楚，是<strong>状态为未使用</strong>的进程数，而不是未使用的进程数）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/proc.c</span></span><br><span class="line"></span><br><span class="line">uint64 <span class="title function_">nproc</span><span class="params">()</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span>* <span class="title">p</span>;</span></span><br><span class="line">  uint64 not_unused = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (p = proc; p &lt; &amp;proc[NPROC]; p++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (p-&gt;state != UNUSED) &#123;</span><br><span class="line">      not_unused++;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> not_unused;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="完成系统调用">完成系统调用<a class="anchor" href="#完成系统调用"></a></h3>
<blockquote>
<p>如何获取用户态传递过来的参数和注册系统调用可以参考这篇<a href="https://kerolt.github.io/posts/18ad9abb/#%E5%A6%82%E4%BD%95%E8%B7%9F%E8%B8%AA%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8%EF%BC%9F">博客</a>，这里就不赘述了</p>
</blockquote>
<p>我们创建了<code>struct sysinfo</code>结构体变量<code>info</code>后，使用刚刚的<code>freemem</code>和<code>nproc</code>函数来为结构体变量赋值，之后通过<code>copyout</code>函数将内核态中的<code>info</code>拷贝给用户态的<code>struct sysinfo</code>结构体变量。</p>
<p>这里的实现原理是：用户态下我们使用系统调用传递了一个<code>struct sysinfo</code>指针，其实就是传递了一个内存地址<code>addr</code>；内核态下我们将<code>info</code>中的数据原封不动地搬一份到<code>addr</code>处。这样当用户态访问<code>addr</code>处的内存时就可以获取到想要的数据了。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">uint64 <span class="title function_">sys_sysinfo</span><span class="params">()</span> &#123;</span><br><span class="line">  uint64 addr;</span><br><span class="line">  argaddr(<span class="number">0</span>, &amp;addr);</span><br><span class="line">  <span class="keyword">if</span> (addr &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span>* <span class="title">p</span> =</span> myproc();</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sysinfo</span> <span class="title">info</span>;</span></span><br><span class="line"></span><br><span class="line">  info.freemem = freemem();</span><br><span class="line">  info.nproc = nproc();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (copyout(p-&gt;pagetable, addr, (<span class="type">char</span>*)&amp;info, <span class="keyword">sizeof</span>(info))) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab3 page tables（上）</title>
    <url>/posts/9a98c9ba/</url>
    <content><![CDATA[<h2 id="前言">前言<a class="anchor" href="#前言"></a></h2>
<p>页表是最常用的机制，操作系统通过它为每个进程提供自己的私有地址空间和内存。页表决定了内存地址的含义，以及可以访问物理内存的哪些部分。在本文中，记录了<code>Lab: page tables</code>的前两个实验：加速系统调用和打印页表。</p>
<span id="more"></span>
<h2 id="Speed-up-system-calls-easy">Speed up system calls (easy)<a class="anchor" href="#Speed-up-system-calls-easy"></a></h2>
<p>通常我们在需要执行系统调用时，在操作系统中会发生从用户态到内核态的切换，这是因为这些核心的操作只能交给内核去完成。在这个实验中，xv6要求我们通过在用户空间和内核之间<strong>共享只读区域中的数据</strong>来加快某些系统调用。</p>
<p>由于现在只是入门如何将映射添加至页表中，这个实验只需要为xv6中的<code>getpid()</code>系统调用进行优化。</p>
<p>在xv6的实验指导书中：</p>
<blockquote>
<p>创建每个进程时，在USYSCALL（memlayout.h中定义的虚拟地址）映射一个只读页。在该页面的开头，存储一个usyscall结构体（也在memlayout.h中定义），并对其进行初始化以存储当前进程的PID。</p>
</blockquote>
<p>既然如此，进程结构体中也应当有一个usyscall结构体：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/proc.h</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> &#123;</span></span><br><span class="line">	...</span><br><span class="line">	<span class="type">pagetable_t</span> pagetable;       <span class="comment">// User page table</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">trapframe</span>;</span> <span class="comment">// data page for trampoline.S</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">usyscall</span> *<span class="title">usyscall</span>;</span>   <span class="comment">// data page for USYSCALL</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">context</span> <span class="title">context</span>;</span>      <span class="comment">// swtch() here to run process</span></span><br><span class="line">	...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这样就可以通过<code>p-&gt;usyscall</code>来获取了。</p>
<p>在<code>memlayout.h</code>中我们可以看到用户态空间内存布局：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Address zero first:</span><br><span class="line">  text</span><br><span class="line">  original data and bss</span><br><span class="line">  fixed-size stack</span><br><span class="line">  expandable heap</span><br><span class="line">  ...</span><br><span class="line">  USYSCALL (shared with kernel)</span><br><span class="line">  TRAPFRAME (p-&gt;trapframe, used by the trampoline)</span><br><span class="line">  TRAMPOLINE (the same page as in the kernel)</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">MAXVA-&gt; -------------------------------------</span><br><span class="line">        | TRAMPOLINE (与内核相同的页面)        |</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | TRAPFRAME (p-&gt;trapframe, 由跳板使用)|</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | USYSCALL (与内核共享)               |</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | ...                               |</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | 可扩展堆                           |</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | 固定大小的栈                        |</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | 原始数据和BSS                       |</span><br><span class="line">        -------------------------------------</span><br><span class="line">        | text                              |</span><br><span class="line">0 -&gt;    -------------------------------------</span><br></pre></td></tr></table></figure>
<p>我们需要做的就是仿照<code>TRAPFRAME</code>将<code>USYSCALL</code>也做一层映射。</p>
<p>在<code>allocproc()</code>为进程分配物理页时，使用<code>kalloc()</code>对usyscall的分配（kalloc每次从空闲页表中取出一个项，其大小为4KB）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Allocate a usyscall page.</span></span><br><span class="line"><span class="keyword">if</span>((p-&gt;usyscall = (<span class="keyword">struct</span> usyscall *)kalloc()) == <span class="number">0</span>)&#123;</span><br><span class="line">	freeproc(p);</span><br><span class="line">	release(&amp;p-&gt;lock);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在<code>proc_pagetable()</code>函数中，其为指定进程创建用户页表，不含用户内存，但有trampoline 和 trapframe页。以下代码是对trampoline 和 trapframe进行映射。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// map the trampoline code (for system call return)</span></span><br><span class="line"><span class="comment">// at the highest user virtual address.</span></span><br><span class="line"><span class="comment">// only the supervisor uses it, on the way</span></span><br><span class="line"><span class="comment">// to/from user space, so not PTE_U.</span></span><br><span class="line"><span class="keyword">if</span>(mappages(pagetable, TRAMPOLINE, PGSIZE,</span><br><span class="line">		  (uint64)trampoline, PTE_R | PTE_X) &lt; <span class="number">0</span>)&#123;</span><br><span class="line">	uvmfree(pagetable, <span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// map the trapframe page just below the trampoline page, for</span></span><br><span class="line"><span class="comment">// trampoline.S.</span></span><br><span class="line"><span class="keyword">if</span>(mappages(pagetable, TRAPFRAME, PGSIZE,</span><br><span class="line">		  (uint64)(p-&gt;trapframe), PTE_R | PTE_W) &lt; <span class="number">0</span>)&#123;</span><br><span class="line">	uvmunmap(pagetable, TRAMPOLINE, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">	uvmfree(pagetable, <span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如此，我们照猫画虎，也可以写出usyscall的映射。需要注意的是，该页是read-only的，并且允许用户态访问，因此其权限应该为<code>PTE_R</code>和<code>PTE_U</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// map the usyscall page</span></span><br><span class="line"><span class="keyword">if</span>(mappages(pagetable, USYSCALL, PGSIZE,</span><br><span class="line">		   (uint64)(p-&gt;usyscall), PTE_R | PTE_U) &lt; <span class="number">0</span>)&#123;</span><br><span class="line">	uvmunmap(pagetable, USYSCALL, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">	uvmunmap(pagetable, TRAMPOLINE, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">	uvmfree(pagetable, <span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在结束进程时，即<code>freeproc</code>函数中，也需要对usyscall的空间进行释放：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(p-&gt;usyscall)</span><br><span class="line">	kfree((<span class="type">void</span>*)p-&gt;usyscall);</span><br><span class="line">p-&gt;usyscall = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>
<p>同时还应当在<code>proc_freepagetable</code>函数中解除之前对usyscall的映射：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">uvmunmap(pagetable, USYSCALL, <span class="number">1</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<h2 id="Print-a-page-table-easy">Print a page table (easy)<a class="anchor" href="#Print-a-page-table-easy"></a></h2>
<p>这个实验要求我们将页表打印出来。在实验开始前，让我们先看看xv6中的页表。</p>
<p><img src="../../images/Pasted%20image%2020240623170452.png" alt=""></p>
<p>xv6中的页表为三级页表，在VA转换为PA的过程中，处理单元会通过<code>satp</code>寄存器找到当前进程的页表基地址，然后取出VA中的L2部分找到一级页表的项，一级页表中的项（PTE）保存二级页表的地址，再通过L1可获取二级页表中的项，依次类推即可将VA转换为PA。</p>
<p>这样看来，想要打印页表，有点类似于DFS算法，需要使用递归。按照实验指导书所说，我们可以从<code>freewalk</code>函数中获取灵感，查看其源码可以知道如何去遍历页表项。那么按照要求所实现打印页表就比较容易了：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">print_pgtbl</span><span class="params">(<span class="type">pagetable_t</span> pagetable, <span class="type">int</span> depth)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (depth &gt; <span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">512</span>; i++)&#123;</span><br><span class="line">    <span class="type">pte_t</span> pte = pagetable[i];</span><br><span class="line">    <span class="keyword">if</span> (pte &amp; PTE_V) &#123;</span><br><span class="line">      <span class="keyword">if</span> (depth == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;..&quot;</span>);</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (depth == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;.. ..&quot;</span>);</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (depth == <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;.. .. ..&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      uint64 child = PTE2PA(pte);</span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;%d: pte %p pa %p\n&quot;</span>, i, pte, PTE2PA(pte));</span><br><span class="line">      print_pgtbl((<span class="type">pagetable_t</span>)child, depth + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">vmprint</span><span class="params">(<span class="type">pagetable_t</span> pagetable)</span> &#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;page table %p\n&quot;</span>, pagetable);</span><br><span class="line">  print_pgtbl(pagetable, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由于打印页表这个操作是进程号为1的<code>init</code>进程做的，所以不要忘记在<code>kernel/exec.c</code>的<code>exec</code>函数中添加：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (p-&gt;pid == <span class="number">1</span>) &#123;</span><br><span class="line">  vmprint(p-&gt;pagetable);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>并且在<code>kernel/defs.h</code>中添加<code>vmprint</code>的函数声明：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">vmprint</span><span class="params">(<span class="type">pagetable_t</span>)</span>;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab3 page tables（下）</title>
    <url>/posts/2224aedf/</url>
    <content><![CDATA[<p>本关的任务为“Detect which pages have been accessed”，需要实现一个新的系统调用<code>pgaccess</code>，它指出访问了哪些页面被访问了（读、写等）。系统调用需要三个参数：</p>
<ol>
<li>第一个用户页面的起始虚拟地址</li>
<li>需要检查页数</li>
<li>一个存储每一页是否被访问的掩码</li>
</ol>
<blockquote>
<p>该lab的所有代码：<a href="https://github.com/kerolt/xv6-labs-2023/commit/87373230877d27d7f60c39696688863210c41133">Github</a></p>
</blockquote>
<span id="more"></span>
<p>测试该系统调用的函数位于<code>user/pgtbltest.c:pgaccess_test()</code>中：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">pgaccess_test</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">char</span> *buf;</span><br><span class="line">  <span class="type">unsigned</span> <span class="type">int</span> abits;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;pgaccess_test starting\n&quot;</span>);</span><br><span class="line">  testname = <span class="string">&quot;pgaccess_test&quot;</span>;</span><br><span class="line">  buf = <span class="built_in">malloc</span>(<span class="number">32</span> * PGSIZE);</span><br><span class="line">  <span class="keyword">if</span> (pgaccess(buf, <span class="number">32</span>, &amp;abits) &lt; <span class="number">0</span>)</span><br><span class="line">    err(<span class="string">&quot;pgaccess failed&quot;</span>);</span><br><span class="line">  buf[PGSIZE * <span class="number">1</span>] += <span class="number">1</span>;</span><br><span class="line">  buf[PGSIZE * <span class="number">2</span>] += <span class="number">1</span>;</span><br><span class="line">  buf[PGSIZE * <span class="number">30</span>] += <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (pgaccess(buf, <span class="number">32</span>, &amp;abits) &lt; <span class="number">0</span>)</span><br><span class="line">    err(<span class="string">&quot;pgaccess failed&quot;</span>);</span><br><span class="line">  <span class="keyword">if</span> (abits != ((<span class="number">1</span> &lt;&lt; <span class="number">1</span>) | (<span class="number">1</span> &lt;&lt; <span class="number">2</span>) | (<span class="number">1</span> &lt;&lt; <span class="number">30</span>)))</span><br><span class="line">    err(<span class="string">&quot;incorrect access bits set&quot;</span>);</span><br><span class="line">  <span class="built_in">free</span>(buf);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;pgaccess_test: OK\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>分析下源码可以知道，测试程序分配了32个页，并且使用（or 访问）了这分配的32个页的第1、2、30页，之后程序调用<code>pgaccess</code>来检测abits的第1、2、30位是否为1，即判断该系统调用是否实现了“<strong>检测已经访问的页</strong>”这个功能。</p>
<p>向内核添加系统调用的方法在lab2中已经了解过了，不过这里xv6已经帮我们添加好了，我们只需要实现系统调用<code>kernel/sysproc.c:sys_pgaccess()</code>即可。</p>
<p><img src="../../images/Pasted%20image%2020240627164548.png" alt=""></p>
<p>在xv6 book中可以知道一个PTE的每位构成如上图，其中<code>0 - 9</code>位是一些标志位，第6位为<code>Accessed</code>，也就是访问位，需要在内核中添加这个标志：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/riscv.h</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PTE_A (1L &lt;&lt; 6)</span></span><br></pre></td></tr></table></figure>
<p>而risc-v处理器会利用硬件将已访问的页的<code>PTE_A</code>正确设置。</p>
<p>实现<code>sys_pgaccess</code>的步骤大致可分为：</p>
<ol>
<li>接受用户态传递的三个参数：
<ol>
<li>第一个用户页面的起始虚拟地址（指针）</li>
<li>需要检查页数（int）</li>
<li>一个存储每一页是否被访问的掩码（指针）</li>
</ol>
</li>
<li>遍历“需要检查页数”，并每次检查遍历的页是否已访问。获取每个页对应的PTE将使用<code>walk</code>函数来获取，而检查将使用<code>PTE_A</code>来判断；如果当前页的<code>PTE_A</code>为1，则说明该页被访问过，利用<strong>位运算</strong>（是的，位运算在该lab里立大功）来存储信息，即第几页被访问了，并且不要忘记了实验指导中的提示，“Be sure to clear PTE_A after checking if it is set. Otherwise, it won’t be possible to determine if the page was accessed since the last time pgaccess() was called”，检测完后应该将<code>PTE_A</code>标记位清除。</li>
<li>将存储的信息利用<code>copyout</code>函数从内核态传递给用户态，届时用户态可通过第三个参数获取。</li>
</ol>
<p>具体实现如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">sys_pgaccess</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="comment">// lab pgtbl: your code here.</span></span><br><span class="line">  uint64 uvm_pgaddr;</span><br><span class="line">  <span class="type">int</span> page_counts;</span><br><span class="line">  uint64 abits;</span><br><span class="line">  argaddr(<span class="number">0</span>, &amp;uvm_pgaddr);</span><br><span class="line">  argint(<span class="number">1</span>, &amp;page_counts);</span><br><span class="line">  argaddr(<span class="number">2</span>, &amp;abits);</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> result = <span class="number">0</span>;</span><br><span class="line">  <span class="type">pagetable_t</span> page_table = myproc()-&gt;pagetable;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; page_counts; i++) &#123;</span><br><span class="line">    <span class="type">pte_t</span> *pte = walk(page_table, uvm_pgaddr, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (((*pte) &amp; (PTE_A)) != <span class="number">0</span>) &#123;</span><br><span class="line">      result |= (<span class="number">1</span> &lt;&lt; i);</span><br><span class="line">      *pte &amp;= (~PTE_A);</span><br><span class="line">    &#125;</span><br><span class="line">    uvm_pgaddr += PGSIZE;</span><br><span class="line">  &#125;</span><br><span class="line">  copyout(page_table, abits, (<span class="type">char</span>*)&amp;result, <span class="keyword">sizeof</span>(result));</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab4 trap alarm</title>
    <url>/posts/7450901e/</url>
    <content><![CDATA[<p>Alarm综合了该lab中前几个练习的知识点：系统调用、中断、寄存器等，我们需要对trap机制有比较好的认识才能理解。Alarm的任务是需要我们完成一个定时器的实现：<code>sigalarm(interval, handler)</code>，当调用<code>sigalarm(n, fn)</code>时，内核会每n个时间间隔（tick）执行fn函数。</p>
<blockquote>
<p>该实验的代码实现见：<a href="https://github.com/kerolt/xv6-labs-2023/commit/0f70ef7d68ab1273262b8d43a2deaf7345a87698">仓库commit</a></p>
</blockquote>
<span id="more"></span>
<h2 id="如何理解Alarm">如何理解Alarm<a class="anchor" href="#如何理解Alarm"></a></h2>
<p>“内核会每n个时间间隔执行fn函数”，如何理解这“每n个时间间隔”呢？在计算机中有一个“时钟周期”的概念，而我们这里所说的时间间隔就是xv6中设置的每次发生时钟中断所间隔的始终周期。在xv6内核初始化时会执行<code>timerinit()</code>函数，其中有：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ask the CLINT for a timer interrupt.</span></span><br><span class="line"><span class="type">int</span> interval = <span class="number">1000000</span>; <span class="comment">// cycles; about 1/10th second in qemu.</span></span><br><span class="line">*(uint64*)CLINT_MTIMECMP(id) = *(uint64*)CLINT_MTIME + interval;</span><br></pre></td></tr></table></figure>
<p>这里就是设置了xv6会每经过interval个时钟周期（在qemu中大概为0.1秒）进行一次时钟中断，这个中断是<strong>硬件自动执行的</strong>（在没看源码之前，一直没搞懂xv6是什么时候、怎么进行的时钟中断）。下文将以时钟中断的间隔tick作为基本单位。</p>
<p>所以简单来说，Alarm需要我们在内核中进行计数，每当经过n个tick的时候，需要去执行fn函数。</p>
<h2 id="test0-3所做的事">test0~3所做的事<a class="anchor" href="#test0-3所做的事"></a></h2>
<ul>
<li><code>test0</code>：用于测试我们的sigalarm是否起作用了；</li>
<li><code>test1</code>：用于测试内核是否多次调用处理函数，需要确保中断发生时跳转的地址为处理函数所在的地址，还有中断时需要保存好之前寄存器中的值；</li>
<li><code>test2</code>：用于测试内核不允许重入sysalarm系统调用，即若某个进程正在执行处理函数，那么内核就不应该再次调用它；</li>
<li><code>test3</code>：用于测试sys_sigreturn系统调用能否正确返回寄存器a0的值。</li>
</ul>
<h2 id="实现">实现<a class="anchor" href="#实现"></a></h2>
<h3 id="注册系统调用">注册系统调用<a class="anchor" href="#注册系统调用"></a></h3>
<p>首先的注册系统调用的步骤这里就不展开了，具体可参考之前的lab。</p>
<h3 id="struc-proc结构体">struc proc结构体<a class="anchor" href="#struc-proc结构体"></a></h3>
<p>为了完成定时执行某个函数，我们需要在<code>struct proc</code>结构体中加入一些成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> &#123;</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="type">int</span> is_handling;                   <span class="comment">// 用于判断当前进程是否正在执行处理函数</span></span><br><span class="line">  <span class="type">int</span> tick_interval;                 <span class="comment">// 定时器间隔，由系统sigalarm的第一个参数传入</span></span><br><span class="line">  <span class="type">int</span> tick_counter;                  <span class="comment">// 定时器计数器，每次tick进行+1</span></span><br><span class="line">  uint64 tick_handler;               <span class="comment">// 间隔到了后执行的处理函数</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">saved_trapframe</span>;</span> <span class="comment">// 保存寄存器</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>值的注意的是处理函数的类型我们设置为了<code>uint64</code>，为什么不是一个函数指针呢？其实都差不多，在之后设置跳转处理函数的时候，就是通过地址来跳转，而地址在xv6中就是用的uint64来表示。故这里的设置即是处理函数所在的起始地址。</p>
<h3 id="实现sys-sigalarm">实现sys_sigalarm<a class="anchor" href="#实现sys-sigalarm"></a></h3>
<p>在<code>sysproc.c</code>中实现<code>sys_sigalarm()</code>函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">uint64 <span class="title function_">sys_sigalarm</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line">  argint(<span class="number">0</span>, &amp;(p-&gt;tick_interval));</span><br><span class="line">  argaddr(<span class="number">1</span>, &amp;(p-&gt;tick_handler));</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该函数要做的事情很简单，只需要接收从用户态传来的两个参数，并将其赋值给当前进程的<code>tick_interval</code>和<code>tick_handler</code>。</p>
<h3 id="进程初始化与结束销毁">进程初始化与结束销毁<a class="anchor" href="#进程初始化与结束销毁"></a></h3>
<p>接着需要在进程创建和销毁时对这些变量进行相应的初始化和清零：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">struct</span> proc*</span><br><span class="line"><span class="title function_">allocproc</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  p-&gt;tick_interval = <span class="number">0</span>;</span><br><span class="line">  p-&gt;tick_counter = <span class="number">0</span>;</span><br><span class="line">  p-&gt;tick_handler = <span class="number">0</span>;</span><br><span class="line">  p-&gt;is_handling = <span class="number">0</span>;</span><br><span class="line">  ...</span><br><span class="line">  <span class="comment">// Allocate a saved_trapframe page.</span></span><br><span class="line">  <span class="keyword">if</span>((p-&gt;saved_trapframe = (<span class="keyword">struct</span> trapframe *)kalloc()) == <span class="number">0</span>)&#123;</span><br><span class="line">    freeproc(p);</span><br><span class="line">    release(&amp;p-&gt;lock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">freeproc</span><span class="params">(<span class="keyword">struct</span> proc *p)</span></span><br><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="keyword">if</span>(p-&gt;saved_trapframe)</span><br><span class="line">    kfree((<span class="type">void</span>*)p-&gt;saved_trapframe);</span><br><span class="line">  p-&gt;saved_trapframe = <span class="number">0</span>;</span><br><span class="line">  ...</span><br><span class="line">  p-&gt;tick_counter = <span class="number">0</span>;</span><br><span class="line">  p-&gt;tick_interval = <span class="number">0</span>;</span><br><span class="line">  p-&gt;tick_handler = <span class="number">0</span>;</span><br><span class="line">  p-&gt;is_handling = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="补全usertrap">补全usertrap<a class="anchor" href="#补全usertrap"></a></h3>
<p>接下来就是需要实现在usertrap中处理时钟中断，在实验指导书中提示我们在<code>if(which_dev == 2) ...</code>中处理时钟中断。这里我的处理逻辑为，当时钟中断发生时：</p>
<ol>
<li>当前进程的tick计数器++</li>
<li>判断进程设置的定时器间隔是否不为0、当前计数器是否已经经过了interval个间隔、且当前进程未执行处理函数，如果其中一项不满足，则不进行第三步</li>
<li>将当前进程的trapframe的内容（即寄存器的值）保存到saved_trapframe（用于恢复现场），将<code>SEPC</code>寄存器的值设置为处理函数的地址，这样中断结束返回时就会去执行处理函数了，最后设置当前进程“正在执行处理函数”</li>
</ol>
<p>代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">usertrap</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="comment">// give up the CPU if this is a timer interrupt.</span></span><br><span class="line">  <span class="keyword">if</span> (which_dev == <span class="number">2</span>) &#123;</span><br><span class="line">    p-&gt;tick_counter++;</span><br><span class="line">    <span class="keyword">if</span> (p-&gt;tick_interval &amp;&amp; p-&gt;tick_counter % p-&gt;tick_interval == <span class="number">0</span> &amp;&amp; p-&gt;is_handling == <span class="number">0</span>) &#123;</span><br><span class="line">      memmove(p-&gt;saved_trapframe, p-&gt;trapframe, PGSIZE);</span><br><span class="line">      p-&gt;trapframe-&gt;epc = p-&gt;tick_handler;</span><br><span class="line">      p-&gt;is_handling = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    yield();</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br></pre></td></tr></table></figure>
<blockquote>
<p>在第二步中不知道是xv6的bug还是我有地方没理解好，如果不先检查<code>tick_interval != 0</code>，则可能在执行<code>p-&gt;tick_counter % p-&gt;tick_interval</code>会有问题，因为取模运算符<code>%</code>在分母为0时是未定义的，但这么运行时xv6并没有任何错误。</p>
</blockquote>
<h3 id="返回，恢复现场">返回，恢复现场<a class="anchor" href="#返回，恢复现场"></a></h3>
<p>在处理函数执行的最后，将会执行<code>sigreturn</code>系统调用进行返回并恢复现场，这时我们就可以将之前存放在saved_trapframe中的值拷贝回trapframe中，并设置当前进程“未执行处理函数”。实验指导书中还提示我们最终返回的结果为a0寄存器中的值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">uint64 <span class="title function_">sys_sigreturn</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line">  memmove(p-&gt;trapframe, p-&gt;saved_trapframe, PGSIZE); </span><br><span class="line">  p-&gt;is_handling = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">return</span> p-&gt;trapframe-&gt;a0; <span class="comment">// return this for alarm test3</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
        <tag>xv6</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab4 trap backtrace</title>
    <url>/posts/657551f/</url>
    <content><![CDATA[<p>backtrace这个lab非常有意思，虽然实现的代码量不多，但是能让我们更好地理解栈、栈帧、指针、gdb的一些知识。</p>
<blockquote>
<p>该实验的代码实现见：<a href="https://github.com/kerolt/xv6-labs-2023/commit/d1dba8ae4a1a71604e8bb6df5238f8cac1771683">仓库commit</a></p>
</blockquote>
<span id="more"></span>
<p>首先先解答一下【RISC-V assembly】中的一些问题：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Q: Which registers contain arguments to functions? For example, which register holds 13 in main&#x27;s call to printf?</span><br><span class="line">A: a0-a7, a2保存了13</span><br><span class="line"></span><br><span class="line">Q: Where is the call to function f in the assembly code for main? Where is the call to g? (Hint: the compiler may inline functions.)</span><br><span class="line">A: 函数f和g被内联优化了</span><br><span class="line"></span><br><span class="line">Q: At what address is the function printf located?</span><br><span class="line">A: 0x000000000000064a</span><br><span class="line"></span><br><span class="line">Q: What value is in the register ra just after the jalr to printf in main?</span><br><span class="line">A: jalr指令的后一条指令的地址，也是当前pc寄存器中的地址</span><br><span class="line"></span><br><span class="line">Q: Run the following code.</span><br><span class="line">	 unsigned int i = 0x00646c72;</span><br><span class="line">	 printf(&quot;H%x Wo%s&quot;, 57616, &amp;i);</span><br><span class="line">  What is the output? Here&#x27;s an ASCII table that maps bytes to characters.</span><br><span class="line">  The output depends on that fact that the RISC-V is little-endian. If the RISC-V were instead big-endian what would you set i to in order to yield the same output? Would you need to change 57616 to a different value?</span><br><span class="line">  Here&#x27;s a description of little- and big-endian and a more whimsical description.</span><br><span class="line">---</span><br><span class="line">A: output: He110 World 若risc-v为大端序，则i应该设置成0x726c6400；57616不需要变，因为无论是大端序还是小端序，其十六进制都为E110</span><br><span class="line"></span><br><span class="line">Q: In the following code, what is going to be printed after &#x27;y=&#x27;? (note: the answer is not a specific value.) Why does this happen?</span><br><span class="line"> 	printf(&quot;x=%d y=%d&quot;, 3);</span><br><span class="line">---</span><br><span class="line">A: x=3 y=1403684968 y的值是一个随机值，因为本该传入printf的第三个参数并没有传入，而其对应的寄存器为a2，故y会使用a2中残存的值</span><br></pre></td></tr></table></figure>
<p>这里的几个问题不是很难，涉及到了一些汇编、寄存器的知识，在接下来学习backtrace的时候将会有详细讨论。</p>
<p>backtrace需要我们做的事情可以概括为：<strong>在发生错误的点之上的堆栈上的函数调用列表，并在每个堆栈帧中打印保存的返回地址</strong>。</p>
<p>什么意思呢？就是例如在gdb调试中使用bt查看函数调用栈时，需要我们打印途中红色框中的地址。</p>
<p><img src="../../images/Pasted%20image%2020240630155213.png" alt=""></p>
<p>该lab让我们在kernel/printf.c中实现一个backtrace函数。在sys_sleep中插入对此函数的调用，然后运行bttest这个测试程序将调用sleep（也就是会执行sys_sleep）。</p>
<p>首先，让我们来看看xv6中栈的结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">                 .</span><br><span class="line">                 .</span><br><span class="line">    +-&gt;          .</span><br><span class="line">    |   +-----------------+   |</span><br><span class="line">    |   | return address  |   |</span><br><span class="line">    |   |   previous fp ------+</span><br><span class="line">    |   | saved registers |</span><br><span class="line">    |   | local variables |</span><br><span class="line">    |   |       ...       | &lt;-+</span><br><span class="line">    |   +-----------------+   |</span><br><span class="line">    |   | return address  |   |</span><br><span class="line">    +------ previous fp   |   |</span><br><span class="line">        | saved registers |   |</span><br><span class="line">        | local variables |   |</span><br><span class="line">    +-&gt; |       ...       |   |</span><br><span class="line">    |   +-----------------+   |</span><br><span class="line">    |   | return address  |   |</span><br><span class="line">    |   |   previous fp ------+</span><br><span class="line">    |   | saved registers |</span><br><span class="line">    |   | local variables |</span><br><span class="line">    |   |       ...       | &lt;-+</span><br><span class="line">    |   +-----------------+   |</span><br><span class="line">    |   | return address  |   |</span><br><span class="line">    +------ previous fp   |   |</span><br><span class="line">        | saved registers |   |</span><br><span class="line">        | local variables |   |</span><br><span class="line">$fp --&gt; |       ...       |   |</span><br><span class="line">        +-----------------+   |</span><br><span class="line">        | return address  |   |</span><br><span class="line">        |   previous fp ------+</span><br><span class="line">        | saved registers |</span><br><span class="line">$sp --&gt; | local variables |</span><br><span class="line">        +-----------------+</span><br></pre></td></tr></table></figure>
<p>栈是由高地址向低地址增长的，risc-v中<code>sp</code>寄存器代表“stack pointer”，即栈顶指针，<code>fp</code>寄存器代表“frame pointer”，为当前栈帧的指针。</p>
<p>假设有一个这样的程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">g</span><span class="params">()</span> &#123;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;g()\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">f</span><span class="params">()</span> &#123;</span><br><span class="line">	g();</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;f()\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">	f();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>假设程序中的main函数里，函数f调用了函数g，那么在函数调用栈中从高地址到低地址三个函数的顺序为：main、f、g；当g函数执行完成后，其栈帧将会从栈中弹出，并且通过栈帧中的数据回到调用自身的下一条指令，即f中g的调用发生在第8行，当g执行完毕后，应该继续执行第9行的指令，这也就是“return address”。</p>
<p>我们可以通过内联汇编获取当前栈帧的的指针：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kernel/riscv.h</span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> uint64</span><br><span class="line"><span class="title function_">r_fp</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">  uint64 x;</span><br><span class="line">  <span class="keyword">asm</span> <span class="title function_">volatile</span><span class="params">(<span class="string">&quot;mv %0, s0&quot;</span> : <span class="string">&quot;=r&quot;</span> (x) )</span>;</span><br><span class="line">  <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后不断遍历栈中的栈帧，打印其return address，直到遍历到最后一个栈帧。xv6在给栈分配内存时确保了每一个栈帧都在同一页中，这样的话可以通过<code>PGROUNDDOWN(fp)</code>宏来判断fp是否超出栈空间：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PGROUNDDOWN(a) (((a)) &amp; ~(PGSIZE - 1))</span></span><br></pre></td></tr></table></figure>
<p>xv6中页的大小为4096B，故<code>PGROUNDDOWN(a)</code>可以获取a地址所在的页号，或者说这一页的最高地址，只要我们的fp不等于它，就说明我们还没有遍历到栈底。</p>
<p><code>fp - 8</code>获取到<code>return address</code>的地址，<code>fp - 16</code>获取到当前栈帧的前一个栈帧的地址。由于xv6中获取的地址是用<code>uint64</code>来表示的，那么可将其强转为<code>uint64*</code>来<strong>将一个值解释为内存地址</strong>，之后便可以解引用这个地址获取其中的值了。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">backtrace</span><span class="params">()</span> &#123;</span><br><span class="line">  uint64 fp = r_fp();</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;backtrace:\n&quot;</span>);</span><br><span class="line">  <span class="keyword">while</span> (fp != PGROUNDDOWN(fp)) &#123;</span><br><span class="line">    uint64 *return_addr = (uint64 *)(fp - <span class="number">8</span>);</span><br><span class="line">    fp = *(uint64 *)(fp - <span class="number">16</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%p\n&quot;</span>, *return_addr);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当然，也不要忘记在<code>kernel/def.h</code>中声明<code>backtrace</code>，还有在<code>sys_sleep</code>中调用<code>backtrace</code>。</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
        <tag>xv6</tag>
      </tags>
  </entry>
  <entry>
    <title>【CMU15-445 Fall2023】Project0 C++ Primer 小结</title>
    <url>/posts/e3fb54d0/</url>
    <content><![CDATA[<blockquote>
<p>该系列博客只是为了记录自己在写Lab时的思路，按照课程要求不会在Github和博客中公开源代码。欢迎与我一起讨论交流！</p>
</blockquote>
<p>project0只在task4中浅浅涉及了一点BusTub的内容，其他都是检测我们对于C<ins>的一个掌握，主要涉及智能指针和C</ins>的常用特性（dynamic_cast、std::move、并发与锁等）。</p>
<span id="more"></span>
<h2 id="Task1-Copy-On-Write-Trie">Task1 - Copy-On-Write Trie<a class="anchor" href="#Task1-Copy-On-Write-Trie"></a></h2>
<blockquote>
<p>如果做过力扣上的<a href="https://leetcode.cn/problems/implement-trie-prefix-tree/">208. 实现 Trie (前缀树)</a>，实现Get和Put这两个操作会更容易。虽然这个task要求我们要使用COW，但是总体的思路是差不多的。</p>
</blockquote>
<h3 id="Get">Get<a class="anchor" href="#Get"></a></h3>
<p>这个比较简单，顺序遍历key去找节点的子节点就行。值得注意的是有值节点的类型为<code>TrieNodeWithValue</code>，其继承自<code>TrieNode</code>。当遍历玩key并找到节点后，使用<code>dynamic_cast</code>将其转换为<code>TrieNodeWithValue*</code>，如果这个节点不是一个有值的节点，则dynamic_cast会转换失败返回nullptr，否则会返回转换后的指针，通过这个指针获取最终的值即可。</p>
<h3 id="Put">Put<a class="anchor" href="#Put"></a></h3>
<p>Put的基本思路就是遍历key的同时创建相应的节点，难点在于如何实现“copy on write”。</p>
<p>看<a href="https://15445.courses.cs.cmu.edu/fall2023/project0/">官网</a>的描述，我们要做的就是在需要修改Trie树（例如进行Put操作或者Remove操作）时，需要返回一颗新的Trie树，也就是我们当前的操作不能影响之前的Trie树的结构，这就需要使用到<code>TrieNode::Clone()</code>函数来拷贝一份需要修改的节点，这样才不会影响之前Trie树中的节点。同时，在这个过程中，我们需要尽可能地使用已有节点，举个🌰：</p>
<p><img src="../../images/Pasted%20image%2020241006211602.png" alt=""></p>
<p>只需记住：</p>
<ol>
<li>本次的插入或删除操作不会影响上次的Trie树的结构，例如上图如果我使用root来访问还是原来的结构，而用new root来访问就可以访问key值为“ad”的节点的值；</li>
<li>尽可能利用已有的节点，上图我们为了不影响之前的Trie树，我们必须拷贝根节点下“a”路径的子节点，插入一个新的节点也需要创建，但是值为“233”和“C++”的节点我们可以利用。</li>
</ol>
<h3 id="Remove">Remove<a class="anchor" href="#Remove"></a></h3>
<p>与Put不同，我使用递归来进行删除，这是因为Put可以顺序遍历key值并不断向下添加节点，而Remove需要从要删除的节点不断向上进行删除。</p>
<p>删除不合法的情况有：</p>
<ol>
<li>碰到空节点</li>
<li>无法再继续往下查找key（key不存在）</li>
</ol>
<p>这时按照任务要求应该返回原来的Trie树。</p>
<h2 id="Task2-Concurrent-Key-Value-Store">Task2 - Concurrent Key-Value Store<a class="anchor" href="#Task2-Concurrent-Key-Value-Store"></a></h2>
<h3 id="Get-2">Get<a class="anchor" href="#Get-2"></a></h3>
<p>加锁获取<code>root_</code>，然后调用之前再Task1中实现的Get操作获取key对应的value，如果value不为nullptr，则将root和value封装为ValueGuard。</p>
<h3 id="Put、Remove">Put、Remove<a class="anchor" href="#Put、Remove"></a></h3>
<p>这两个操作的逻辑相同。由于之前我们实现Trie的三个操作时用到了COW，因此每次Put、Remove时返回的都是一个新的Trie，那么我们在Task2中的操作中要用全程获取写锁（ensure there is only one writer at a time），然后使用Put or Remove获取新的Trie，接着获取root_lock_，最后用获取的新的Trie更新root_。</p>
<h2 id="Task3-Debugging">Task3 - Debugging<a class="anchor" href="#Task3-Debugging"></a></h2>
<p>这个没什么太多需要说明的。选择Clion或VSCode配置好环境打断点Debug就行，当然使用print大法也是可以的haha。注意这个task虽然有给你单元测试文件，但是运行肯定是不通过，因为问题答案并不在源码中，我们只能在gradescope才能检测自己做的是否正确。</p>
<p>（我所做的project所属课程是fall2023版本，如果是spring2023版本，task3在本地测试和gradescope上的测试可能会有区别，是随机数的问题，相关老师有在Discord上<a href="https://discord.com/channels/724929902075445281/1063892636504698880/1075438762559017020">说明</a>）</p>
<h2 id="Task4-SQL-String-Functions">Task4 - SQL String Functions<a class="anchor" href="#Task4-SQL-String-Functions"></a></h2>
<p>这个task需要我们为BusTub实现两个简单的函数：lower和upper。实现并不难，找到需要修改的位置，添加相关处理逻辑和异常操作。</p>
<p>完成这个task我认为需要对<code>string_expression.h</code>这个文件的内容有一定理解，要明白如何判断获取的操作是lower还是upper，还有在<code>plan_func_call.cpp</code>中处理非法操作。总体来说不难。</p>
<h2 id="提交">提交<a class="anchor" href="#提交"></a></h2>
<p>本地跑了测试代码还不够，课程有为我们这些非CMU的学生准备检测平台gradescope，如何加入课程可以看<a href="https://15445.courses.cs.cmu.edu/fall2023/faq.html#q7">这里</a>。</p>
<p>在提交之前，需要使用进行<code>clang-tidy</code>检测，并通过<code>python3 gradescope_sign.py</code>生成签名，这样才能通过平台的预检测。</p>
<p>这里贴一个通关的截图hh：</p>
<p><img src="../../images/14bdf06120fc3933aee455ca0b8fb021.png" alt=""></p>
<h2 id="总结">总结<a class="anchor" href="#总结"></a></h2>
<p>这个project0我也是做了两三天（还是太菜了），在其中我更好地巩固了C++中的一些知识，例如智能指针、移动赋值、dynamic_cast。与之前做Xv6的Lab不同，这次的CMU15-445在网上基本没有关于实现的源代码，这就让我无法直接通过代码来学习了。当然这是课程的要求，希望我们能一起构建一个良好的学习氛围，鼓励我们独立思考完成，与他人交流而不是直接要代码，我觉得这是一个很锻炼自己的过程，网上大多是一些思路的介绍（我写的这篇博客也是自己实现的简单思路，希望没有违反课程的要求），我们在没思路时参考一下这些博客，然后再通过自己来完成代码，这比直接看别人写好的代码来说更能够提升自己！希望我能把接下来的几个project都完成，尽量不烂尾！</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Database</tag>
      </tags>
  </entry>
  <entry>
    <title>【CMU15-445 Fall2023】Project1 Buffer Pool 小结</title>
    <url>/posts/1a77524/</url>
    <content><![CDATA[<blockquote>
<p>该系列博客只是为了记录自己在写Lab时的思路，按照课程要求不会在Github和博客中公开源代码。欢迎与我一起讨论交流！</p>
</blockquote>
<p>这个Project需要我们实现一个缓存池，减少对于磁盘的频繁IO。开始慢慢上强度了，细节拉满！</p>
<span id="more"></span>
<h2 id="Task1-LRU-K-Replacement-Policy">Task1 - LRU-K Replacement Policy<a class="anchor" href="#Task1-LRU-K-Replacement-Policy"></a></h2>
<p>什么是LRU算法？LRU是Least Recently Used的缩写，即最近最少使用，是一种常用的页面置换算法，选择最近<strong>最久未使用</strong>的页面予以淘汰。该算法赋予每个页面一个访问字段，用来记录一个页面自上次被访问以来所经历的时间 t，当须淘汰一个页面时，选择现有页面中其 t 值最大的，即最近最少使用的页面予以淘汰。</p>
<p>LRU-K是一种增强型的LRU，其主要思想是利用历史访问模式来进行页面置换决策，具体过程如下：</p>
<ol>
<li><strong>访问记录</strong>：每个页面维护一个访问时间戳列表，记录其最近的k次访问时间。这个列表用于计算向后k距离。</li>
<li><strong>向后k距离</strong>：当一个页面被访问时，算法会更新其时间戳列表，并计算向后k距离。向后k距离是当前时间戳与列表中第k个时间戳之间的差值。如果列表中没有k个时间戳，该页面的向后k距离被赋值为正无穷。</li>
<li><strong>页面替换</strong>：
<ul>
<li>在需要替换页面时，算法会遍历所有页面，找到具有最大向后k距离的页面进行替换。</li>
<li>如果存在多个页面的向后k距离为正无穷，算法将选择这些页面中最早的访问时间戳进行替换。</li>
</ul>
</li>
<li><strong>优点</strong>：LRU-K相较于传统的LRU算法更为智能，它能更好地适应不同的访问模式，减少不必要的页面置换。通过考虑历史访问，它能够识别出哪些页面可能会被频繁使用，从而提高缓存的命中率。</li>
</ol>
<p>在BusTub的实现中，LRU-K有着每个frame id与<code>LRUKNode</code>的映射。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">LRUKNode</span> &#123;</span><br><span class="line"> <span class="keyword">private</span>:</span><br><span class="line">  [[maybe_unused]] std::list&lt;<span class="type">size_t</span>&gt; history_;</span><br><span class="line">  [[maybe_unused]] <span class="type">size_t</span> k_;</span><br><span class="line">  [[maybe_unused]] <span class="type">frame_id_t</span> fid_;</span><br><span class="line">  [[maybe_unused]] <span class="type">bool</span> is_evictable_&#123;<span class="literal">false</span>&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>一个node记录了一个frame的id，还有这个frame在最近使用k次的时间戳。</p>
<ul>
<li><code>Evict(frame_id_t* frame_id)</code> : 与所有其他可删除的frame相比，删除具有最大向后k距离的frame。将frame id存储在输出参数中并返回True。如果没有可替换的frame，则返回False。具有少于k个访问记录的frame被给定“无穷”作为其向后k距离。如果多个帧具有inf向后k距离，则根据LRU驱逐具有最早时间戳的帧。成功删除帧应减小替换器的大小并删除帧的访问历史记录。</li>
<li><code>RecordAccess(frame_id_t frame_id)</code> : 每次调用RecordAccess，内置的时间戳就要+1，并且根据frame_id是否在LRU缓存中来决定是更新对应的node还是添加一个新node。
<ul>
<li><code>Add</code>：新建一个node，并且初始化，再把node插入缓存</li>
<li><code>Update</code>：通过frame id找到对应的node，并修改node的历史访问时间戳，更新它在缓存中的访问位置</li>
</ul>
</li>
<li><code>Remove(frame_id_t frame_id)</code> : 从LRU缓存中删除frame id以及对应的node。要注意删除后对当前“可替换的frame数量”-1。</li>
<li><code>SetEvictable(frame_id_t frame_id, bool set_evictable)</code> : 控制frame是否可逐出。<strong>它还控制着LRUKReplacer的size</strong>。</li>
<li><code>Size()</code> : 返回LRUKReplacer中当前可替换的frame数量。</li>
</ul>
<p>总的来说，把这个task当成力扣上的一道数据结构设计题来完成就好了。</p>
<h2 id="Task2-Disk-Scheduler">Task2 - Disk Scheduler<a class="anchor" href="#Task2-Disk-Scheduler"></a></h2>
<p>比较简单，实现两个函数的功能：</p>
<ul>
<li><code>Schedule(DiskRequest r)</code>：调度<code>DiskManager</code>执行的请求。<code>DiskRequest</code>结构体指定请求是否为读/写，数据应写入/从何处写入，以及操作的页面ID。<code>DiskRequest</code>还包括一个<code>std::promise</code>，一旦请求被处理，其值应设置为true。</li>
<li><code>StartWorkerThread()</code>：启动处理请求的后台工作线程。在DiskScheduler构造函数中创建工作线程并调用此方法。此方法负责获取排队的请求并将其分派给<code>DiskManager</code>。记住设置<code>DiskRequest</code>回调的值，以向请求发出者发出请求已完成的信号。在调用DiskScheduler的析构函数之前，这不应该返回。</li>
</ul>
<h2 id="Task3-Buffer-Pool-Manager">Task3 - Buffer Pool Manager<a class="anchor" href="#Task3-Buffer-Pool-Manager"></a></h2>
<p>BufferPoolManager内部采用一个原始数组来存放Page的指针。初始时，每个page都在free list中。</p>
<p>同一个块，在内存中称作“帧（frame）”，在硬盘中称作“页（page）”。缓冲池中会存储<code>pool_size</code>个Page，这些个Page包含硬盘上的数据和一些元信息。</p>
<p><code>BufferPoolManager</code>中的<code>page_table_</code>采用<code>page_id</code>作为键，<code>frame_id</code>作为值。如果有某个frame被使用了，那么<code>page_table_</code>中就会插入相应的键值对。<code>BufferPoolManager</code>中的<code>pages_</code>的下标即代表了frame id，我们通过page id可以找到对应的frame id，再通过frame id就可以找到在<code>pages_</code>中的Page了。</p>
<blockquote>
<p>下图来源：https://www.qtmuniao.com/2021/02/10/cmu15445-project1-buffer-pool/</p>
</blockquote>
<p><img src="https://i.loli.net/2021/02/19/7QSY5EtFwKxuiVN.png" alt=""></p>
<p>我们可以从freelist或者replacer中找到frame（优先从freelist中找）。如果free list中有空闲的frame，则使用它；否则从replacer中选出需要换出的frame（replacer的size与buffer pool的size相同）。</p>
<p>要实现的每个函数在代码头文件中都有较为详细的实现过程，需要注意很多细节。</p>
<p>需要注意:</p>
<ul>
<li>每次执行<code>FetchPage</code>时相当于要使用到某一个页面，既然如此，也要在LRU-K缓存中更新历史访问序列（通过调用<code>RecordAccess</code>）；</li>
<li>需要新的frame时，优先从free list中找，其次通过replacer的<code>Evict</code>来获取；</li>
<li>对于页面的<code>pin_count_</code>，只有当某个函数返回的是<code>Page*</code>时，说明这个页是需要使用的，此时其<code>pin_count_ + 1</code>；而只有在<code>Unpin</code>中，才会对<code>pin_count_ - 1</code>；</li>
<li>如果需要替换或新建或删除某个页面时，其脏位位true，则要及时写回磁盘；</li>
</ul>
<h2 id="错误记录">错误记录<a class="anchor" href="#错误记录"></a></h2>
<h3 id="错误点1：没有保证LRU-K中的原子性">错误点1：没有保证LRU-K中的原子性<a class="anchor" href="#错误点1：没有保证LRU-K中的原子性"></a></h3>
<p>对于当前可替换的frame数量的操作应该是原子性的，可以用<code>atomic_size_t</code>来取代<code>size_t</code>，或者使用锁。</p>
<h3 id="错误点2：最大向前K距离的计算错误">错误点2：最大向前K距离的计算错误<a class="anchor" href="#错误点2：最大向前K距离的计算错误"></a></h3>
<blockquote>
<p>The LRU-K algorithm evicts a frame whose backward k-distance is maximum of all frames in the replacer. Backward k-distance is computed as the difference in time between current timestamp and the timestamp of kth previous access. A frame with fewer than k historical accesses is given +inf as its backward k-distance. <strong>When multiple frames have +inf backward k-distance, the replacer evicts the frame with the earliest overall timestamp (i.e., the frame whose least-recent recorded access is the overall least recent access, overall, out of all frames).</strong></p>
</blockquote>
<p>主要是对这段话的理解不到位，之前是这样：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 计算最大向后K距离</span></span><br><span class="line"><span class="type">int</span> k_dist =</span><br><span class="line">  node.history_.<span class="built_in">size</span>() &lt; k_ ? std::numeric_limits&lt;<span class="type">int</span>&gt;::<span class="built_in">max</span>() : (current_timestamp_ - node.history_[k_ - <span class="number">1</span>]);</span><br><span class="line"><span class="keyword">if</span> (k_dist &gt; max_k_dist) &#123;</span><br><span class="line">  evict_frame_id = fid;</span><br><span class="line">  max_k_dist = k_dist;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (k_dist == max_k_dist &amp;&amp; node.history_.<span class="built_in">back</span>() &lt; node_store_[evict_frame_id].history_.<span class="built_in">back</span>()) &#123;</span><br><span class="line">  evict_frame_id = fid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但实际应该为：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 计算最大向后K距离</span></span><br><span class="line"><span class="type">int</span> k_dist =</span><br><span class="line">  node.history_.<span class="built_in">size</span>() &lt; k_ ? std::numeric_limits&lt;<span class="type">int</span>&gt;::<span class="built_in">max</span>() : (current_timestamp_ - node.history_[k_ - <span class="number">1</span>]);</span><br><span class="line"><span class="keyword">if</span> (k_dist &gt; max_k_dist) &#123;</span><br><span class="line">  evict_frame_id = fid;</span><br><span class="line">  max_k_dist = k_dist;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (k_dist == max_k_dist &amp;&amp; node.history_.<span class="built_in">back</span>() &lt; node_store_[evict_frame_id].history_.<span class="built_in">back</span>()) &#123;</span><br><span class="line">  evict_frame_id = fid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当有多个距离为无穷大的frame时，应该选择其历史记录中具有最小时间戳的那个frame！</p>
<h3 id="错误3：FetchPage找到直接返回时没有pin一下">错误3：FetchPage找到直接返回时没有pin一下<a class="anchor" href="#错误3：FetchPage找到直接返回时没有pin一下"></a></h3>
<p>这里卡了几个测试是因为<code>FetchPage</code>在<code>page_id</code>在缓冲池中时会直接返回，但是我没有在这种情况中对这个page的<code>pin_count_</code>进行+1操作。</p>
<h3 id="错误4：Unpin中的is-dirty参数的设置">错误4：Unpin中的is_dirty参数的设置<a class="anchor" href="#错误4：Unpin中的is-dirty参数的设置"></a></h3>
<p><img src="../../images/Pasted%20image%2020241012090909.png" alt=""></p>
<p>在<code>Unpin</code>中，不能直接将<code>page.is_dirty_</code>设置成参数is_dirty，而是应该用<strong>或</strong>操作：<code>page.is_dirty_ |= is_dirty;</code>。如果不这样做，那么当原先<code>page.is_dirty_</code>为true时，如果我们通过<code>Unpin</code>设置了false，其is_dirty_就变为了false，但是这个页面仍然是脏页面。</p>
<h3 id="错误5：FetchPage没有RecordAccess">错误5：FetchPage没有RecordAccess<a class="anchor" href="#错误5：FetchPage没有RecordAccess"></a></h3>
<p><img src="../../images/Pasted%20image%2020241012131909.png" alt=""></p>
<p>在<code>FetchPage</code>中，如果缓冲池中有 page id 直接返回时，<code>replacer_</code>也应该执行<code>RecordAccess</code>。因为这时相当于使用了Page，当然要记录更新。</p>
<h2 id="最终提交">最终提交<a class="anchor" href="#最终提交"></a></h2>
<p><img src="../../images/Pasted%20image%2020241012132459.png" alt=""></p>
<p>然后Leaderboard的排名有点低了，因为之前都是使用的大锁，希望之后能优化一下。</p>
<p><img src="../../images/Pasted%20image%2020241012132602.png" alt=""></p>
<h2 id="小结">小结<a class="anchor" href="#小结"></a></h2>
<p>这个Project差不多搞了四五天，主体代码用了两天左右，然后就是漫长的修Bug。由于从这个Project开始，就不会在本地给出完整的测试集了，所以在评测平台上也是提交了很多次来检测。通过Discord中的频道，也是找到了一些解决Bug的办法，很多时候是一些细节处自己没有考虑到（错误记录）。好在最后也基本上是自己独立完成的，有点小小的成就感！</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Database</tag>
      </tags>
  </entry>
  <entry>
    <title>【CMU15-445 Fall2023】Project2 Extendible Hash Index 小结</title>
    <url>/posts/677be937/</url>
    <content><![CDATA[<blockquote>
<p>该系列博客只是为了记录自己在写Lab时的思路，按照课程要求不会在Github和博客中公开源代码。欢迎与我一起讨论交流！</p>
</blockquote>
<p>太菜了，从没打过这么艰难的仗QAQ。由于课程的要求不能公开源代码，所以网上的资源会少很多，平台上的测试案例比较全面，有的还比较刁钻，需要考虑到可拓展哈希的实现细节。在自认为写完了后，提交了近40来次总有几个测试集过不了，还好没有崩溃，在看了几篇博客的方法后，加上自己画图理解，最后终于过了😭。不过回头写博客的时候再去看代码，也没有特别的复杂，还是得明白其中的算法逻辑是如何实现的。</p>
<span id="more"></span>
<p><img src="../../images/Pasted%20image%2020241112101912.png" alt=""></p>
<h2 id="Task1-Read-Write-Page-Guards">Task1 - Read/Write Page Guards<a class="anchor" href="#Task1-Read-Write-Page-Guards"></a></h2>
<p>简单来说，就是为Page实现一个RAII来自动管理资源。因为在<code>BufferPoolManager::Unpin</code>中，每次调用这个函数，都会让对应的page的<code>pin_count_ - 1</code>，当这个值为0时，这个page就可以被回收，或者说被替换了。但如果我们忘记去手动调用，该页面将永远不会被逐出缓冲池。由于缓冲池以更少的帧数运行，磁盘内外的页面交换将更多。不仅性能受到影响，而且很难检测到错误。</p>
<p>主要需要考虑如何编写<strong>移动构造</strong>、<strong>移动赋值</strong>的逻辑。移动了一个对象后，原来的对象的资源应该转移到了新对象上，那么原来的对象无法再访问资源（将原来对象的资源重置nullptr或清空）。</p>
<p>还有一个<code>Drop()</code>的接口，这是提供给使用者的释放资源的api，在实现虚构函数时可以直接调用它。Drop的实现就是调用Unpin，然后置空资源。需要注意的是，在进行<strong>移动赋值</strong>时，一开始也要Drop一下，考虑这样一种情况：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">move</span>(basic_page_guard);</span><br><span class="line">p = std::<span class="built_in">move</span>(basic_page_guard2);</span><br></pre></td></tr></table></figure>
<p>这个时候同一个变量p接管了两个page，那么应该在第二个移动赋值时先drop掉第一个，因为第一个page不再使用了，自然要Unpin。</p>
<p>还有就是在三个page guard类重载移动赋值时，如果需要移动的对象和自身是同一个，那么直接返回自己就好：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> BasicPageGuard::<span class="keyword">operator</span>=(BasicPageGuard &amp;&amp;that) <span class="keyword">noexcept</span> -&gt; BasicPageGuard &amp; &#123;</span><br><span class="line">  <span class="keyword">if</span> (&amp;that == <span class="keyword">this</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ... 其他操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在<code>ReadPageGuard</code>和<code>WritePageGuard</code>的<code>Drop()</code>中，还需要考虑释放管理的page的锁。对应的，锁的获取发生在<code>FetchPageWrite()</code>和<code>FetchPageWrite()</code>中。</p>
<h2 id="Task2-Extendible-Hash-Table-Pages">Task2 - Extendible Hash Table Pages<a class="anchor" href="#Task2-Extendible-Hash-Table-Pages"></a></h2>
<p>为什么我们需要可扩展哈希？</p>
<blockquote>
<p>下图来源：https://www.bilibili.com/video/BV1Qt421w7JT</p>
</blockquote>
<p><img src="../../images/Pasted%20image%2020241111143541.png" alt=""></p>
<p><img src="https://15445.courses.cs.cmu.edu/fall2023/project2/img/extendible-htable-structure.svg" alt=""></p>
<p>在bustub的设计中，Header Page，Directory Page和Bucket Page都是无法直接构造出来的，<strong>即不能通过构造函数创建</strong>，只能通过各自的PageGuard中的<code>As()</code>或者<code>AsMut()</code>函数来转换。</p>
<h3 id="Header">Header<a class="anchor" href="#Header"></a></h3>
<p>header page中有一个<code>max_depth_</code>的成员变量，<code>1 &lt;&lt; max_depth_</code>即为header page中能存放的目录的索引的数量。当我们有值需要放入哈希表时，获取hash(key)的二进制<strong>最高max_depth_位</strong>作为索引，再从header中对应位置去找到directory。对应的<code>ExtendibleHTableHeaderPage</code>中的功能实现并不难。</p>
<h3 id="Directory">Directory<a class="anchor" href="#Directory"></a></h3>
<p>directory中有两个depth：</p>
<ul>
<li>Global Depth：若global depth为n，那么这个Directory就有<code>2^n</code>个entry（相当于指向2^n个bucket）</li>
<li>Local Depth：若local depth为n，则在这个对应的bucket下，每个元素的key的最后n位都相同</li>
</ul>
<p>类似header中获取下一级页的索引，directory获取hash(key)的二进制<strong>最低global_depth_位</strong>作为索引。那local depth的作用是什么呢？</p>
<p>这就要说到可拓展哈希中的插入和删除操作了。简单来说，在可拓展哈希表中，目录directory的大小是可以变化的（只要不超过最大容量限制）。目录中可能有多个entry映射到同一个bucket。当需要插入时，如果这个bucket还没有满时，可以直接插入；否则，需要将这个bucket分裂成两个bucket（或者说，将这个bucket中的一部分移动到另一个bucket中），并且<strong>这个bucket对应的local depth + 1，这样相比之前就多了一位二进制位去识别bucket</strong>。具体的用法可以看后面的Task3部分的笔记。</p>
<p>下面讲几个稍微难懂的函数：</p>
<h4 id="GetGlobalDepthMask">GetGlobalDepthMask<a class="anchor" href="#GetGlobalDepthMask"></a></h4>
<p>这个函数的作用是获取<code>global_depth_</code>个二进制1，举个例子，如果global depth是2，那么说明这个目录当前的容量为<code>2^2=4</code>，索引为0~3，用两位二进制就能表示。</p>
<p>主要用于和hash(key)进行<code>&amp;</code>操作，获取在directory中对应的索引位置。例如，假设key=3，hash(key)=101（二进制），global depth还是2，那么检查hash(key)的最低两位即为01，那么在directory就是第1个entry。</p>
<h4 id="CanShrink">CanShrink<a class="anchor" href="#CanShrink"></a></h4>
<p><code>CanShrink()</code> 的核心功能是检测是否可以减少全局深度（global depth），从而缩小哈希表的大小。它基于以下原则：</p>
<p>在可扩展哈希表中，每个桶（bucket）都有自己的局部深度（local depth），而整个哈希表有一个全局深度（global depth）。如果所有桶的局部深度都小于当前的全局深度，<strong>说明哈希表的某些位（超过局部深度的那些位）并没有被实际使用</strong>，因而可以安全地减少全局深度。</p>
<h4 id="GetSplitImageIndex">GetSplitImageIndex<a class="anchor" href="#GetSplitImageIndex"></a></h4>
<p><code>GetSplitImageIndex()</code> 是在可扩展哈希表中用于桶（bucket）拆分时确定拆分后的另一个桶的索引。</p>
<p>在可扩展哈希表中，当一个桶装满时，目录容量会翻倍，这个桶会拆分成两个桶（但是除了需要拆分的桶，其他目录还是指向原来的桶）。</p>
<ul>
<li>每个桶都有一个局部深度（local depth），表示这个桶在哈希表中使用了多少位哈希值来定位数据。桶拆分时，局部深度会增加。</li>
<li>拆分后的桶与当前桶具有相同的局部深度值，只是在第一位上有所不同。例如，如果当前桶的局部深度为 2，那么拆分后的桶与其前后 2 位相同，只有第 1 位不同。</li>
</ul>
<p>例如，当前桶的索引为 <code>01</code>，局部深度为 3。</p>
<ol>
<li>计算翻转位的值：<code>1 &lt;&lt; (local_depth - 1) = 1 &lt;&lt; (3 - 1) = 1 &lt;&lt; 2 = 100</code> （即二进制的 <code>0100</code>）。</li>
<li>按位异或：<code>bucket_index ⊕ 100 = 001 ⊕ 100 = 101</code>（即二进制的 <code>101</code>，也就是十进制的 <code>5</code>）。</li>
</ol>
<p>因此，拆分后的桶的索引是 <code>101</code>（5 in decimal）。</p>
<h4 id="GetLocalDepthMask">GetLocalDepthMask<a class="anchor" href="#GetLocalDepthMask"></a></h4>
<p>这个函数主要是用在bucket的分裂和合并中，用于判断需要操作的bucket中的项在更新后应该属于directory下哪个entry对应的bucket（有点绕）。</p>
<p>和<code>GetGlobalDepthMask</code>一样，当前directory的entry映射的bucket的local_depth_是多少，其mask的二进制就是多少个1。</p>
<h3 id="Bucket">Bucket<a class="anchor" href="#Bucket"></a></h3>
<p>Bucket存储着多个键值对，没有使用标准库的map，而是使用std::pair数组（如果都用标准库了要你实现啥哈希表hh）。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MappingType std::pair<span class="string">&lt;KeyType, ValueType&gt;</span></span></span><br><span class="line"><span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">uint64_t</span> HTABLE_BUCKET_PAGE_METADATA_SIZE = <span class="built_in">sizeof</span>(<span class="type">uint32_t</span>) * <span class="number">2</span>;</span><br><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="keyword">auto</span> <span class="title">HTableBucketArraySize</span><span class="params">(<span class="type">uint64_t</span> mapping_type_size)</span> -&gt; <span class="type">uint64_t</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (BUSTUB_PAGE_SIZE - HTABLE_BUCKET_PAGE_METADATA_SIZE) / mapping_type_size;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ExtendibleHTableBucketPage</span> &#123;</span><br><span class="line">  ...</span><br><span class="line"> <span class="keyword">private</span>:</span><br><span class="line">  <span class="type">uint32_t</span> size_;</span><br><span class="line">  <span class="type">uint32_t</span> max_size_;</span><br><span class="line">  MappingType array_[<span class="built_in">HTableBucketArraySize</span>(<span class="built_in">sizeof</span>(MappingType))];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>任务就是在这个bucket中增删查对应的key and value，由于内部使用的是定长数组，所以最简单的方法就是顺序操作。</p>
<p>但是需要注意的是，在bucket page的Insert操作中，注释上给的提示是：“当插入成功时返回true，插入失败或者键已经存在时返回false”。但是如果你按着“先判断bucket是否满了，再遍历bucket中的键值对数组，查找有没有key相同的，最后再插入”这样的逻辑去写，那当你提交时怎么测试都有几个测试集过不了，当时我想得脑子都要炸了也想不清怎么回事。</p>
<p>在看了一篇博客后，我按照他的逻辑~~“先遍历数组，如果有键值相同的，<strong>更新它而不是返回false</strong>，然后再判断是否已经满了，没满就再插入”~~去写，最后对了。（这里为啥用了删除线，是因为我的代码是按照这个逻辑来写的，但是在我写这篇博客时感觉不对劲，假如这个键已经存在，且此时bucket没有满，那更新之后又插入了一次，数据有重复）</p>
<h2 id="Task3-Extendible-Hashing-Implementation">Task3 - Extendible Hashing Implementation<a class="anchor" href="#Task3-Extendible-Hashing-Implementation"></a></h2>
<p>细节！细节！还是TMD细节！</p>
<p>前两个task比较容易写，这个task其实本质上还是一个数据结构的设计实现问题，也就是哈希表如何插入数据和删除数据，但是细节需要注意太多了！如何考虑插入后的分裂，还有删除时的合并。从第一次提交到全部通过一共用了7天（哭）。</p>
<h3 id="Insert">Insert<a class="anchor" href="#Insert"></a></h3>
<p>Insert操作需要注意的就是插入失败后的分裂问题，小细节在于下面逻辑步骤中的<code>3</code>，<code>8.5</code>和<code>9</code>（加粗表示）:</p>
<ol>
<li>检查需要插入的键是否已经存在，如果存在就返回false，否则继续第二步</li>
<li>对key使用hash算法</li>
<li>从header通过hash(key)找到对应的directory page，如果directory不存在，那么创建新的directory page，再创建新的bucket page后执行插入；否则继续第四步。<strong>需要注意，如果判断了directory存在后，需要对header page guard进行<code>Drop()</code>操作，因为平台的测试集中有比较刁钻的情况，其buffer manager pool的大小只有3，如果不把header page释放，那么到时候需要进行分裂时无法再获取一个新的page！</strong></li>
<li>从directory通过hash(key)找到对应的bucket page，如果page不存在，那么创建新的bucket page再插入；否则继续第五步</li>
<li>向bucket page中插入键值对，如果插入成功，返回true；否则继续第六步</li>
<li>（循环开始）</li>
<li>检查当前directory是否已经满了，比如说directory的max_depth_是2，那么说明最多只能有四个bucket page，若此时已经有四个bucket了，那么已经满了，无法继续分裂桶；没满继续第八步</li>
<li>分裂桶
<ol>
<li>先创建新的bucket page作为当前bucket的镜像桶</li>
<li>如果global depth等于当前bucket的local depth，说明需要扩充directory的大小，同时调整扩充后的directory中的entry与bucket的映射</li>
<li>在directory中设置镜像桶，增加原桶和镜像桶的local depth，此时获取原桶的local depth对应的mask</li>
<li>通过mask将原桶拆成两个桶，比如原桶的local depth为2，对应的mask的二进制为$(11)_2$，通过<code>&amp;</code>操作获取桶中每个键值对的hash(key)的最低两位，如果与<code>bucket_index &amp; mask</code>相同，那么就说明这个键值对应该留在原桶，反之应该移至镜像桶</li>
<li><strong>分裂成两个桶后，在使用mask判断需要插入的键值对应该插入那个桶</strong>，记录插入是否成功</li>
</ol>
</li>
<li><strong>考虑这样一种情况，需要插入的bucket满了后要进行分裂，但是有可能分裂后原来的键值对还是在一个bucket中，那这个时候插入就失败了，所以需要继续从第七步执行，直到directory满了或者插入成功</strong></li>
<li>（循环结束）</li>
<li>return true</li>
</ol>
<h3 id="Remove">Remove<a class="anchor" href="#Remove"></a></h3>
<p>由于课程要求不能公开代码，加上网上关于可拓展哈希的操作多只有Insert，而Remove很少有介绍说明的，所以不得不去思考很多情况，加上自己画图去理解，当然还是去看了几篇大佬的博客我才慢慢写出了解决方案。</p>
<p>实验指导中说了对于Remove的合并需要进行递归的处理，但其实我们用循环去处理就行，至于为什么要递归去处理，下面的步骤<code>9</code>和步骤<code>13</code>中有加粗解释：</p>
<ol>
<li>对key使用hash算法</li>
<li>从header通过hash(key)找到对应的directory page，如果directory不存在，return false；否则继续第3步。<strong>类似Insert，这里也需要Drop header page</strong></li>
<li>从directory通过hash(key)找到对应的bucket page，如果page不存在，那return false；否则继续第4步</li>
<li>删除bucket中的key对应的键值对，如果删除失败，返回false；否则继续第5步</li>
<li>如果删除成功后bucket不为空，说明不需要合并，返回true；否则继续第6步</li>
<li>（循环开始）</li>
<li>获取当前bucket的镜像bucket，如果二者的local depth不相同，那么结束循环；否则继续第8步</li>
<li>当前桶和镜像桶可以合并，将当前桶的映射更新到镜像桶，并且local depth都 -1，删除当前桶的bucket page</li>
<li>删除了bucket_index对应的page后，虽然调整了其对应的local depth和page id，<strong>但是还应该遍历所有的可成为镜像桶的index，如果它们的local depth相同，也应该合并</strong>，记录需要合并的的bucket的page id
<ol>
<li>在遍历时不需要一个一个去遍历directory的entry，而是可以跳着遍历：只有index的后<code>local_depth - 1</code>位相同的才可能需要合并</li>
</ol>
</li>
<li>如果需要合并的的bucket的page id数量为0，说明已经没有可以合并的bucket了，不应该继续合并，可以结束循环了；否则继续第11步</li>
<li>因为又合并了，所以原桶和其镜像桶对应的 local depth 又要 -1</li>
<li>删除需要合并的的bucket的page</li>
<li><strong>从第7步继续，因为合并后的bucket在其local depth - 1后可能会碰到和其local depth相同的bucket，且其中还有空的bucket，这就需要不断去合并（这就是实验中说的递归合并）</strong></li>
<li>（循环结束）</li>
<li>缩减目录大小，直到无法缩小</li>
<li>return true</li>
</ol>
<h2 id="Reference">Reference<a class="anchor" href="#Reference"></a></h2>
<ul>
<li>https://www.cnblogs.com/wevolf/p/18302985</li>
<li>https://zhuanlan.zhihu.com/p/622221722</li>
<li>https://zhuanlan.zhihu.com/p/701875021</li>
</ul>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Database</tag>
      </tags>
  </entry>
  <entry>
    <title>259. 关闭分部的可行集合数目</title>
    <url>/posts/21251a7/</url>
    <content><![CDATA[<blockquote>
<p>难度：Hard</p>
<p>标签：位运算；图论；Floyd算法</p>
<p>链接： https://leetcode.cn/problems/number-of-possible-sets-of-closing-branches/</p>
</blockquote>
<span id="more"></span>
<p>题目中给出的n范围为<code>1 &lt;= n &lt;= 10</code>，那么说明最多的10个分部的情况下，可以选择关闭的可行情况有<code>2^10</code>种，这个数据量并不大，可以用暴力枚举做出来。</p>
<p>但是如何知道选择了哪些顶点呢？这里有用到位运算这个很巧妙的方法，一个int型数据有32位，我们最多只要用其中的10位即可表示所有的情况，同时还能用每一位来表示是否选择了某一个顶点（分部）。</p>
<p><img src="../../images/Pasted%20image%2020240717111233.png" alt=""></p>
<p>利用Floyd算法可以求解出一个图中任意两个顶点之间的最小距离。当我们选择一个可能的集合时，判断这个集合中有的分部之间的最短距离是否会大于题目要求的最远距离<code>maxDistance</code>，如果有大于的，说明有分部之间最短的距离都无法满足要求。</p>
<p>需要注意的是，每一种情况我们只需要处理在集合中包含的分部。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">numberOfSets</span><span class="params">(<span class="type">int</span> n, <span class="type">int</span> maxDistance, vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&amp; roads)</span> </span>&#123;</span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">g</span>(n, <span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;(n, INT_MAX / <span class="number">2</span>));</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            g[i][i] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; r : roads) &#123;</span><br><span class="line">            <span class="type">int</span> x = r[<span class="number">0</span>], y = r[<span class="number">1</span>], w = r[<span class="number">2</span>];</span><br><span class="line">            g[x][y] = <span class="built_in">min</span>(g[x][y], w);</span><br><span class="line">            g[y][x] = <span class="built_in">min</span>(g[y][x], w);</span><br><span class="line">        &#125;</span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">f</span>(n); <span class="comment">// 用f来存储每一种情况对应的图</span></span><br><span class="line">        <span class="keyword">auto</span> check = [&amp;](<span class="type">int</span> set) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">	            <span class="comment">// 只要每种情况对应集合中的分部构成的图</span></span><br><span class="line">                <span class="keyword">if</span> ((set &gt;&gt; i) &amp; <span class="number">1</span>) &#123;</span><br><span class="line">                    f[i] = g[i];</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Floyd算法，处理f中分部之间的最短距离，需要根据set的值来选择进行计算</span></span><br><span class="line">			<span class="comment">// 因为在之前我们只选择了集合中有的分部建图</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> k = <span class="number">0</span>; k &lt; n; k++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (((set &gt;&gt; k) &amp; <span class="number">1</span>) == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (((set &gt;&gt; i) &amp; <span class="number">1</span>) == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">                    <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; n; j++) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (((set &gt;&gt; j) &amp; <span class="number">1</span>) == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">                        f[i][j] = <span class="built_in">min</span>(f[i][j], f[i][k] + f[k][j]);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// 检查图中各个分部的最短距离</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (((set &gt;&gt; i) &amp; <span class="number">1</span>) == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; n; j++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (((set &gt;&gt; j) &amp; <span class="number">1</span>) &amp;&amp; f[i][j] &gt; maxDistance) &#123;</span><br><span class="line">                        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;;</span><br><span class="line">        </span><br><span class="line">        <span class="type">int</span> res = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 暴力枚举所有情况</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; (<span class="number">1</span> &lt;&lt; n); i++) &#123;</span><br><span class="line">            res += <span class="built_in">check</span>(i); <span class="comment">// true和false分别隐式转换为1和0</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>Floyd</tag>
        <tag>Bitwise-Operation</tag>
      </tags>
  </entry>
  <entry>
    <title>30. 串联所有单词的子串</title>
    <url>/posts/831a363d/</url>
    <content><![CDATA[<blockquote>
<p>难度：Hard</p>
<p>标签：哈希表、字符串、滑动窗口</p>
<p>链接：https://leetcode.cn/problems/substring-with-concatenation-of-all-words/description/</p>
</blockquote>
<span id="more"></span>
<p>在B站上看见一个up的视频，感觉说的很清晰，言简意赅：<a href="https://www.bilibili.com/video/BV1Tg4y1z7jR/?share_source=copy_web&amp;vd_source=158b66457253e14c958ac7e040b420c9">【五分钟力扣 Leetcode 第30题 串联所有单词的子串除 Python入门算法刷题 极简解法 23行代码 97%】</a> 。</p>
<p>看完视频后，我觉得关键点在于，可以通过<strong>比较两个哈希表是否“相等”</strong> 来判断子串是否满足要求。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">findSubstring</span><span class="params">(string s, vector&lt;string&gt;&amp; words)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> words_num = words.<span class="built_in">size</span>();</span><br><span class="line">        <span class="type">int</span> uni_len = words[<span class="number">0</span>].<span class="built_in">size</span>();</span><br><span class="line">        <span class="type">int</span> n = s.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; res;</span><br><span class="line">        unordered_map&lt;string, <span class="type">int</span>&gt; dict;</span><br><span class="line">        <span class="comment">// 记录每个单词的出现次数</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; w : words) &#123;</span><br><span class="line">            dict[w]++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 外循环遍历的次数为单个单词的长度</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; uni_len; i++) &#123;</span><br><span class="line">            <span class="type">int</span> start = i; <span class="comment">// start为子串开始的索引</span></span><br><span class="line">            unordered_map&lt;string, <span class="type">int</span>&gt; cache; <span class="comment">// cache用于记录内循环中，位于dict中的单词的出现次数</span></span><br><span class="line">            <span class="comment">// 内循环，每次增加一个单词长度（即以一个单词长度为最小单位）</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = i; j &lt; n; j += uni_len) &#123;</span><br><span class="line">                string sub = s.<span class="built_in">substr</span>(j, uni_len);</span><br><span class="line">                <span class="comment">// 判断当前截取的字符串是否存在于dict中</span></span><br><span class="line">                <span class="keyword">if</span> (dict.<span class="built_in">find</span>(sub) != dict.<span class="built_in">end</span>()) &#123;</span><br><span class="line">                    cache[sub]++;</span><br><span class="line">                    <span class="comment">// 当sub在cache中出现次数大于dict中，说明当前start不可能为子串的开始索引（因为单词数目都不相等了），不断移动start位置（移动单元为uni_len），直到cache[sub] &lt;= dict[sub]</span></span><br><span class="line">                    <span class="keyword">while</span> (cache[sub] &gt; dict[sub]) &#123;</span><br><span class="line">                        string remove_str = s.<span class="built_in">substr</span>(start, uni_len);</span><br><span class="line">                        cache[remove_str]--;</span><br><span class="line">                        start += uni_len;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 若cache和dict中内容一致，说明找到了一个符合要求的子串</span></span><br><span class="line">                    <span class="keyword">if</span> (dict == cache) &#123;</span><br><span class="line">                        res.<span class="built_in">push_back</span>(start);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 若当前截取字符串不存在于dict中，跳过这个单词</span></span><br><span class="line">                    start = j + uni_len;</span><br><span class="line">                    cache.<span class="built_in">clear</span>();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>76. 最小覆盖子串</title>
    <url>/posts/9865d86d/</url>
    <content><![CDATA[<blockquote>
<p>难度：Hard</p>
<p>标签：哈希表；滑动窗口；字符串</p>
<p>链接： https://leetcode.cn/problems/minimum-window-substring/description/</p>
</blockquote>
<span id="more"></span>
<p>比较容易想到要用滑动窗口来解决，使用两个哈希表来记录信息：cnt_t用于记录字符串t中每个字符出现过的次数，cnt_s用于滑动窗口中的字符的出现次数。</p>
<p>滑动窗口的区间为<code>[left, right]</code>，并记录最小的左右区间（这里我使用一个res数组）。移动right有区间，直到移动到s字符串结束部分。将<code>s[right]</code>加入cnt_s中，如果cnt_s<strong>包含</strong>cnt_t，则：</p>
<ol>
<li>若当前区间长度小于记录的最小区间长度，更新这个最小区间长度；</li>
<li>将cnt_s中<code>s[left]</code>出现的次数-1；</li>
<li>left右移+1；</li>
<li>重复上面3步，直到cnt_s不包含cnt_t；</li>
</ol>
<p>最后，若<code>res[0] &lt; 0</code>（最小左区间），说明<code>s</code> 中不存在涵盖 <code>t</code> 所有字符的子串，则返回空字符串 <code>&quot;&quot;</code> ；反之，返回最小左右区间中的字符串。</p>
<p>这里的“cnt_s包含cnt_t”是什么意思呢？cnt_t中有的字符cnt_s中都要有，并且cnt_t中字符的次数要<strong>小于等于</strong>cnt_s中字符出现的次数。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">string <span class="title">minWindow</span><span class="params">(string s, string t)</span> </span>&#123;</span><br><span class="line">        unordered_map&lt;<span class="type">char</span>, <span class="type">int</span>&gt; cnt_s, <span class="type">cnt_t</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">char</span> c : t) &#123;</span><br><span class="line">            <span class="type">cnt_t</span>[c]++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> left = <span class="number">0</span>, right = <span class="number">0</span>, n = s.<span class="built_in">length</span>();</span><br><span class="line">        <span class="type">int</span> res[<span class="number">2</span>]&#123;<span class="number">-1</span>, n&#125;;</span><br><span class="line">        <span class="keyword">while</span> (right &lt; n) &#123;</span><br><span class="line">            cnt_s[s[right]]++;</span><br><span class="line">            <span class="keyword">while</span> (<span class="built_in">cover</span>(cnt_s, <span class="type">cnt_t</span>)) &#123;</span><br><span class="line">                <span class="keyword">if</span> (right - left &lt; res[<span class="number">1</span>] - res[<span class="number">0</span>]) &#123;</span><br><span class="line">                    res[<span class="number">0</span>] = left;</span><br><span class="line">                    res[<span class="number">1</span>] = right;</span><br><span class="line">                &#125;</span><br><span class="line">                cnt_s[s[left++]]--;</span><br><span class="line">            &#125;</span><br><span class="line">            right++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res[<span class="number">0</span>] &lt; <span class="number">0</span> ? <span class="string">&quot;&quot;</span> : s.<span class="built_in">substr</span>(res[<span class="number">0</span>], res[<span class="number">1</span>] - res[<span class="number">0</span>] + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">cover</span><span class="params">(unordered_map&lt;<span class="type">char</span>, <span class="type">int</span>&gt;&amp; cnt_s, unordered_map&lt;<span class="type">char</span>, <span class="type">int</span>&gt;&amp; <span class="type">cnt_t</span>)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; [k, v] : <span class="type">cnt_t</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (cnt_s.<span class="built_in">find</span>(k) == <span class="type">cnt_t</span>.<span class="built_in">end</span>() || cnt_s[k] &lt; v) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>【链表】判断回文链表</title>
    <url>/posts/b3399ea8/</url>
    <content><![CDATA[<blockquote>
<p><a href="https://leetcode.cn/problems/palindrome-linked-list/">https://leetcode.cn/problems/palindrome-linked-list/</a></p>
</blockquote>
<h3 id="（1）将链表转化为数组进行比较">（1）将链表转化为数组进行比较<a class="anchor" href="#（1）将链表转化为数组进行比较"></a></h3>
<p>比较呆板的做法，空间复杂度为<code>O(n)</code>​。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isPalindrome</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; arr;</span><br><span class="line">        ListNode* p = head;</span><br><span class="line">        <span class="keyword">while</span> (p) &#123;</span><br><span class="line">            arr.<span class="built_in">push_back</span>(p-&gt;val);</span><br><span class="line">            p = p-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        <span class="type">int</span> n = arr.<span class="built_in">size</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>, j = n - <span class="number">1</span>; i &lt; j; i++, j--) &#123;</span><br><span class="line">            <span class="keyword">if</span> (arr[i] != arr[j])</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="（2）递归">（2）递归<a class="anchor" href="#（2）递归"></a></h3>
<p>链表也具有递归性质，二叉树也不过是链表的衍生。</p>
<p>利用<strong>后序遍历</strong>的思想：</p>
<p>先保存头结点（left，全局变量），然后递归至最后（最深）的结点（right），然后比较<code>left</code>​和<code>right</code>​的值；如果相等，由递归栈返回上一层（也即right向左走），再操作left向右走，这样就实现了left和right的双向奔赴。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    ListNode* left_ = <span class="literal">nullptr</span>;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">Traverse</span><span class="params">(ListNode* right)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!right)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="type">bool</span> res = <span class="built_in">Traverse</span>(right-&gt;next);</span><br><span class="line">        res = res &amp;&amp; (left_-&gt;val == right-&gt;val);</span><br><span class="line">        left_ = left_-&gt;next;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isPalindrome</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">        left_ = head;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Traverse</span>(head-&gt;next);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="（3）优化递归">（3）优化递归<a class="anchor" href="#（3）优化递归"></a></h3>
<p>利用方法二，看似是没有使用到额外空间了，但实际上还有递归所带来的函数调用栈的开销，其空间复杂度也为<code>O(n)</code>​。</p>
<p>因此可以利用<strong>双指针</strong>的思想，找到链表的中间结点后，将其后面的结点反转。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> ListNodePtr = ListNode*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function">ListNode* <span class="title">Reverse</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">        ListNodePtr cur = head, pre = <span class="literal">nullptr</span>;</span><br><span class="line">        <span class="keyword">while</span> (cur) &#123;</span><br><span class="line">            ListNodePtr ne = cur-&gt;next;</span><br><span class="line">            cur-&gt;next = pre;</span><br><span class="line">            pre = cur;</span><br><span class="line">            cur = ne;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> pre;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isPalindrome</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">        ListNodePtr fast = head, slow = head;</span><br><span class="line">        <span class="keyword">while</span> (fast &amp;&amp; fast-&gt;next) &#123;</span><br><span class="line">            slow = slow-&gt;next;</span><br><span class="line">            fast = fast-&gt;next-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (fast)</span><br><span class="line">            slow = slow-&gt;next;</span><br><span class="line">    </span><br><span class="line">        ListNodePtr left = head;</span><br><span class="line">        ListNodePtr right = <span class="built_in">Reverse</span>(slow);</span><br><span class="line">        <span class="keyword">while</span> (right) &#123;</span><br><span class="line">            <span class="keyword">if</span> (left-&gt;val != right-&gt;val)</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            left = left-&gt;next;</span><br><span class="line">            right = right-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>股票问题与状态机dp</title>
    <url>/posts/d925b9f8/</url>
    <content><![CDATA[<blockquote>
<p>本篇文章思路来源于 @bilibili/<a href="https://space.bilibili.com/206214">灵茶山艾府</a></p>
</blockquote>
<p>题目描述：<a href="https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-ii">https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-ii</a></p>
<p>相对于买卖股票的最佳时机I，该问题可以多次买入和卖出股票以获取最大利益</p>
<span id="more"></span>
<h2 id="启发思路">启发思路<a class="anchor" href="#启发思路"></a></h2>
<p>以<code>prices = [7,1,5,3,6,4]</code>为例，直到最后一天，我们能获取到的最大利润是什么？</p>
<p>最后一天，也就是第五天的利润（下标从0开始） = 第0天到第5天结束的利润 = <strong>第0天到第四天结束的利润 + 第五天的利润</strong></p>
<p>将利润分为两部分：</p>
<ol>
<li>第五天的利润
<ol>
<li>什么都不做</li>
<li>买入股票（从 未持有股票 -&gt; 持有股票）</li>
<li>卖出股票（从 持有股票 -&gt; 未持有股票）</li>
</ol>
</li>
<li>第零天到第四天的利润</li>
</ol>
<p>由此可以清晰的感受到这样一个大问题可以分割为相同的子问题：</p>
<p>问题：第i天结束，持有/未持有股票的最大利润子问题：第i - 1天结束，持有/未持有股票的最大利润</p>
<h2 id="状态机">状态机<a class="anchor" href="#状态机"></a></h2>
<p>简单的理解就是状态的转换，如下图就是本题的状态机</p>
<p><img src="../../images/Pasted%20image%2020240411111533.png" alt=""></p>
<p>那么如何将状态机与思路结合起来呢？</p>
<p>我们可以这么想：</p>
<ul>
<li>假设<code>f(i, false)</code>代表第i天结束时<strong>未拥有股票</strong>的利润，<code>f(i, true)</code>代表第i天结束时<strong>拥有股票</strong>的利润</li>
<li>第i天未持有股票的情况为：第i-1天未持有股票或者第i-1天拥有股票但是第i天时<strong>卖出</strong>了股票。这时第i天未持有股票的最大利润为<code>f(i, false) = max(f(i - 1, false), f(i - 1, true) + prices[i])</code></li>
<li>第i天持有股票的情况未：第i-1天持有股票或者第i-1天未拥有股票但是第i天时<strong>买入</strong>了股票。这时第i天持有股票的最大利润为<code>f(i, true) = max(f(i - 1, true), f(i - 1, false) - prices[i])</code></li>
</ul>
<p><img src="../../images/Pasted%20image%2020240411161133.png" alt=""></p>
<h2 id="记忆化搜索">记忆化搜索<a class="anchor" href="#记忆化搜索"></a></h2>
<p>使用上面的思路，采用递归的方法，不难写出下面的算法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProfit</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> n = prices.<span class="built_in">size</span>();</span><br><span class="line">        function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">bool</span>)&gt; dfs = [&amp;](<span class="type">int</span> i , <span class="type">bool</span> hold) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> hold ? INT_MIN : <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (hold) &#123;</span><br><span class="line">                <span class="comment">// 第i天持有</span></span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">true</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">false</span>) - prices[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 第i天未持有</span></span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">false</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">true</span>) + prices[i]);</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">dfs</span>(n - <span class="number">1</span>, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>上面还有几个问题</p>
<ul>
<li>为什么当i不在范围时<code>return hold ? INT_MIN : 0;</code>？当i不在范围内，那么说明此时就算拥有股票也是非法的，那么其返回值一定不能影响到正常的i值，而用于比较返回值的函数为max，那么将该返回值设置成INT_MAX就是最好的选择了</li>
<li>为什么最后只要返回<code>dfs(n - 1, false)</code>？这时因为最后一天卖出去（也就是未持有股票，false）所拥有的利润一定比最后一天不卖出去（持有股票，true）所拥有的利润高，因此也每次要返回<code>max(dfs(n - 1, false), dfs(n - 1, true))</code>了</li>
</ul>
<p>好，这时点击提交！会发现超时了！这时因为我们在递归的过程中重复计算了子问题，造成了不必要的开销</p>
<p><img src="../../images/Pasted%20image%2020240411163713.png" alt=""></p>
<p>如图，以上红色的部分都是重复的，我们应该在计算的时候保存它们，这就是记忆化搜索</p>
<p>需要注意的是，记忆化数组cache初始化应该为-1而不是0,是因为计算的利润有可能是0</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProfit</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> n = prices.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">cache</span>(n, <span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;(<span class="number">2</span>, <span class="number">-1</span>));</span><br><span class="line">        function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">bool</span>)&gt; dfs = [&amp;](<span class="type">int</span> i , <span class="type">bool</span> hold) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> hold ? INT_MIN : <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="type">int</span> res = cache[i][hold];</span><br><span class="line">            <span class="keyword">if</span> (res != <span class="number">-1</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> res;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (hold) &#123;</span><br><span class="line">                <span class="comment">// 第i天持有</span></span><br><span class="line">                res = <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">true</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">false</span>) - prices[i]);</span><br><span class="line">                cache[i][hold] = res;</span><br><span class="line">                <span class="keyword">return</span> res;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 第i天未持有</span></span><br><span class="line">            res = <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">false</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">true</span>) + prices[i]);</span><br><span class="line">            cache[i][hold] = res;</span><br><span class="line">            <span class="keyword">return</span> res;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">dfs</span>(n - <span class="number">1</span>, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="递推为dp">递推为dp<a class="anchor" href="#递推为dp"></a></h2>
<p>由以上的记忆化搜索和状态机思路，就不难将其1：1翻译为递推了</p>
<p>但由于<code>dp[i - 1][hold]</code>中的i - 1可能为一个负数，故我们需要在dp前添加一个哨兵位，且i都变为i + 1</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProfit</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> n = prices.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">dp</span>(n + <span class="number">1</span>, <span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;(<span class="number">2</span>, <span class="number">0</span>));</span><br><span class="line">        dp[<span class="number">0</span>][<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        dp[<span class="number">0</span>][<span class="number">1</span>] = INT_MIN;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            dp[i + <span class="number">1</span>][<span class="number">0</span>] = <span class="built_in">max</span>(dp[i][<span class="number">0</span>], dp[i][<span class="number">1</span>] + prices[i]);</span><br><span class="line">            dp[i + <span class="number">1</span>][<span class="number">1</span>] = <span class="built_in">max</span>(dp[i][<span class="number">1</span>], dp[i][<span class="number">0</span>] - prices[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp[n][<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>股票问题第二波</title>
    <url>/posts/bb522614/</url>
    <content><![CDATA[<p>上一篇文章：<a href="https://kerolt.github.io/2024/04/11/%E7%AE%97%E6%B3%95/%E8%82%A1%E7%A5%A8%E9%97%AE%E9%A2%98%E4%B8%8E%E7%8A%B6%E6%80%81%E6%9C%BAdp/">股票问题与状态机dp</a></p>
<p>本篇文章涉及题目如下：</p>
<ul>
<li><a href="https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iii/">123. 买卖股票的最佳时机 III</a></li>
<li><a href="https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iv/">188. 买卖股票的最佳时机 IV</a></li>
<li><a href="https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-with-cooldown/">309. 买卖股票的最佳时机含冷冻期</a></li>
</ul>
<span id="more"></span>
<h2 id="交易K次问题">交易K次问题<a class="anchor" href="#交易K次问题"></a></h2>
<p>在上篇文章的基础上，我们其实需要做的就是在处理函数上加一个参数k代表还可以交易几次，当k &lt; 0时就说明交易次数达到上限</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProfit</span><span class="params">(<span class="type">int</span> k, vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> n = prices.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&gt; <span class="built_in">cache</span>(n, vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;(k + <span class="number">1</span>, <span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;(<span class="number">2</span>, <span class="number">-1</span>)));</span><br><span class="line">        function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">int</span>, <span class="type">bool</span>)&gt; dfs = [&amp;](<span class="type">int</span> i, <span class="type">int</span> k, <span class="type">bool</span> hold) &#123;</span><br><span class="line">            <span class="keyword">if</span> (k &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> INT_MIN;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (i &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> hold ? INT_MIN : <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="type">int</span>&amp; res = cache[i][k][hold];</span><br><span class="line">            <span class="keyword">if</span> (res != <span class="number">-1</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> res;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (hold) &#123;</span><br><span class="line">                <span class="comment">// 第i天持有</span></span><br><span class="line">                <span class="keyword">return</span> res = <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, k, <span class="literal">true</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, k, <span class="literal">false</span>) - prices[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 第i天未持有</span></span><br><span class="line">            <span class="keyword">return</span> res = <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, k,  <span class="literal">false</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, k - <span class="number">1</span>, <span class="literal">true</span>) + prices[i]);</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">dfs</span>(n - <span class="number">1</span>, k, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>值得注意的是，该问题的记忆化搜索实现中cache数组应该为三维，<code>cache[i][k][hold]</code>表示第i天，剩余交易次数k次、是否拥有股票的结果的缓存</p>
<p>使用记忆化搜索是无法通过<a href="https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iii/">123. 买卖股票的最佳时机 III</a>的，因此可以将其改成dp解决</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProfit</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">	    <span class="type">int</span> k = <span class="number">2</span>;</span><br><span class="line">        <span class="type">int</span> n = prices.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&gt; <span class="built_in">dp</span>(n + <span class="number">1</span>, vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;(k + <span class="number">2</span>, <span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;(<span class="number">2</span>, INT_MIN)));</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; k + <span class="number">2</span>; j++) &#123;</span><br><span class="line">            dp[<span class="number">0</span>][j][<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= k + <span class="number">1</span>; j++) &#123;</span><br><span class="line">                dp[i + <span class="number">1</span>][j][<span class="number">0</span>] = <span class="built_in">max</span>(dp[i][j][<span class="number">0</span>], dp[i][j - <span class="number">1</span>][<span class="number">1</span>] + prices[i]);</span><br><span class="line">                dp[i + <span class="number">1</span>][j][<span class="number">1</span>] = <span class="built_in">max</span>(dp[i][j][<span class="number">1</span>], dp[i][j][<span class="number">0</span>] - prices[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp[n][k + <span class="number">1</span>][<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="冷冻期问题">冷冻期问题<a class="anchor" href="#冷冻期问题"></a></h2>
<p>这个问题有点类似与打家劫舍—不能连续偷相邻的房屋。那么在本问题中，是不是将<strong>比较前一天</strong>的代码改成<strong>比较前前一天</strong>的代码就行了？差不多！但是只有在买入股票<strong>或</strong>卖出股票的时候需要修改，这是因为买入卖出才算一次交易，所以在这两个时间段选一个进行修改即可</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProfit</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> n = prices.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">cache</span>(n, <span class="built_in">vector</span>(<span class="number">2</span>, <span class="number">-1</span>));</span><br><span class="line">        function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">bool</span>)&gt; dfs = [&amp;](<span class="type">int</span> i, <span class="type">bool</span> hold) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> hold ? INT_MIN : <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="type">int</span>&amp; res = cache[i][hold];</span><br><span class="line">            <span class="keyword">if</span> (res != <span class="number">-1</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> res;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (hold) &#123;</span><br><span class="line">                <span class="keyword">return</span> res = <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">true</span>), <span class="built_in">dfs</span>(i - <span class="number">2</span>, <span class="literal">false</span>) - prices[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> res = <span class="built_in">max</span>(<span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">false</span>), <span class="built_in">dfs</span>(i - <span class="number">1</span>, <span class="literal">true</span>) + prices[i]);</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">dfs</span>(n - <span class="number">1</span>, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>【动手写协程库 1】协程定义</title>
    <url>/posts/24411cf0/</url>
    <content><![CDATA[<blockquote>
<p>【动手写协程库】系列笔记是学习sylar的协程库时的记录，参考了<a href="https://www.midlane.top/wiki/pages/viewpage.action?pageId=10060952">从零开始重写sylar C++高性能分布式服务器框架</a>和代码随想录中的<a href="https://www.programmercarl.com/other/project_coroutine.html#%E4%B8%8B%E8%BD%BD%E6%96%B9%E5%BC%8F">文档</a>。文章并不是对所有代码的详细解释，而是为了自己理解一些片段所做的笔记。</p>
<p><code>Coroutine</code>类中其他函数的定义可以在这里查看：<a href="https://github.com/kerolt/coroutine-lib/blob/master/src/coroutine.cpp">Github: src/coroutine.cpp</a></p>
</blockquote>
<p>对于什么是协程，为什么要使用协程，可以看看之前的笔记：<a href="https://kerolt.github.io/posts/f663291a/">【协程】C++20协程初体验</a>。</p>
<p>对于我们自己来实现协程，其实在之前Xv6的Lab中就有做过：<a href="https://kerolt.github.io/posts/cb4e63bc/#Uthread-switching-between-threads">【MIT6.S081】Lab6 multithreading</a>，当初做这个lab的时候没有意识到这就是协程。协程的切换最重要的就是要保存和恢复上下文，在这个lab中，我们通过保存每个协程在切换之前的寄存器的值，以此可用来恢复原来的执行流。</p>
<span id="more"></span>
<p>在sylar的协程库实现中，使用的是Linux原生提供的<code>ucontext</code>来保存协程的上下文和切换。对于协程切换，最重要的两个API就是<code>yield</code>和<code>resume</code>，分别对应协程让出执行权和恢复协程。利用 ucontext 提供的四个函数<code>getcontext()</code>、<code>setcontext()</code>、<code>makecontext()</code>、<code>swapcontext()</code>可以在一个进程中实现协程切换。（这里就不介绍这几个函数的用法来，详细文档可使用man命令查看）</p>
<p>Coroutine的相关API如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> COROUTINE_H_</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> COROUTINE_H_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/ucontext.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;ucontext.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdint&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Coroutine</span> : <span class="keyword">public</span> std::enable_shared_from_this&lt;Coroutine&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">using</span> Ptr = std::shared_ptr&lt;Coroutine&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">enum</span> <span class="title class_">State</span> &#123;</span><br><span class="line">        READY,</span><br><span class="line">        RUNNING,</span><br><span class="line">        FINISH</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Coroutine</span>(std::function&lt;<span class="built_in">void</span>()&gt; callback, <span class="type">size_t</span> stack_size = <span class="number">0</span>, <span class="type">bool</span> run_in_scheduler = <span class="literal">true</span>);</span><br><span class="line">    ~<span class="built_in">Coroutine</span>();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Yield</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Resume</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Reset</span><span class="params">(std::function&lt;<span class="type">void</span>()&gt; callback)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">uint64_t</span> <span class="title">GetId</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id_;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">State <span class="title">GetState</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> state_;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">SetNowCoroutine</span><span class="params">(Coroutine* co)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> Coroutine::Ptr <span class="title">GetNowCoroutine</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">uint64_t</span> <span class="title">TotalCoNums</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">Task</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">uint64_t</span> <span class="title">GetCurrentId</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">Coroutine</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">uint64_t</span> id_ = <span class="number">0</span>;</span><br><span class="line">    <span class="type">uint32_t</span> stack_size_ = <span class="number">0</span>;</span><br><span class="line">    State state_ = READY;</span><br><span class="line">    <span class="type">bool</span> is_run_in_sched_;</span><br><span class="line"></span><br><span class="line">    <span class="type">ucontext_t</span> ctx_;         <span class="comment">// 协程上下文</span></span><br><span class="line">    <span class="type">void</span>* pstack_ = <span class="literal">nullptr</span>; <span class="comment">// 协程的栈地址</span></span><br><span class="line"></span><br><span class="line">    std::function&lt;<span class="type">void</span>()&gt; callback_;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">/* COROUTINE_H_ */</span></span></span><br></pre></td></tr></table></figure>
<p>一个线程可以运行多个协程，但是在某一个时刻只能运行一个协程。我们需要为每个线程设置当前运行的协程的指针和表示线程的主协程的指针。这里用到了C++中的<code>thread_local</code>关键字：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">thread_local</span> Coroutine* cur_coroutine = <span class="literal">nullptr</span>;</span><br><span class="line"><span class="type">static</span> <span class="keyword">thread_local</span> Coroutine::Ptr main_coroutine = <span class="literal">nullptr</span>;</span><br></pre></td></tr></table></figure>
<p>每当我们需要创建一个协程来执行任务时，我们必须要传入的参数为要执行的函数，而协程的栈大小有默认值，默认使用调度器进行调度：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Coroutine::<span class="built_in">Coroutine</span>(std::function&lt;<span class="built_in">void</span>()&gt; callback, <span class="type">size_t</span> stack_size, <span class="type">bool</span> run_in_scheduler)</span><br><span class="line">    : <span class="built_in">callback_</span>(callback)</span><br><span class="line">    , <span class="built_in">is_run_in_sched_</span>(run_in_scheduler) &#123;</span><br><span class="line">    co_count++;</span><br><span class="line">    stack_size_ = stack_size &gt; <span class="number">0</span> ? stack_size : CO_STACK_SIZE;</span><br><span class="line">    pstack_ = <span class="built_in">malloc</span>(stack_size_);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// getcontext()用于保存当前上下文，以便将来可以从这个点恢复执行</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">getcontext</span>(&amp;ctx_) != <span class="number">0</span>) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;err: Coroutine::getcontext\n&quot;</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 初始化协程上下文</span></span><br><span class="line">    ctx_.uc_link = <span class="literal">nullptr</span>;</span><br><span class="line">    ctx_.uc_stack.ss_sp = pstack_;</span><br><span class="line">    ctx_.uc_stack.ss_size = stack_size_;</span><br><span class="line">    <span class="comment">// 为已经初始化的上下文设置一个将在该上下文被激活时执行的函数，并为该函数传递参数。</span></span><br><span class="line">    <span class="comment">// 为什么这里的第二个参数不直接设置成callback呢？是因为我们自己写协程的话不仅仅只要将任务函数执行完成就行了，执行完成后还要设置协程的状态</span></span><br><span class="line">    <span class="built_in">makecontext</span>(&amp;ctx_, &amp;Coroutine::Task, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 每个协程会运行它所绑定的callback，并且在执行完成后将重置该协程的状态，并让出执行权</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Coroutine::Task</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> cur = <span class="built_in">GetNowCoroutine</span>();</span><br><span class="line">    <span class="built_in">assert</span>(cur);</span><br><span class="line"></span><br><span class="line">    cur-&gt;<span class="built_in">callback_</span>();</span><br><span class="line">    cur-&gt;callback_ = <span class="literal">nullptr</span>;</span><br><span class="line">    cur-&gt;state_ = FINISH;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> raw_ptr = cur.<span class="built_in">get</span>();</span><br><span class="line">    cur.<span class="built_in">reset</span>();</span><br><span class="line">    raw_ptr-&gt;<span class="built_in">Yield</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>Yield</code>和<code>Resume</code>函数利用<code>swapcontext</code>来进行协程的切换。由于我们在之后会需要将协程添加到调度器中而不是手动调度，所以要注意协程有使用调度器标志时要与调度器协程进行切换：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 让出该协程的执行权，转交到主协程</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Coroutine::Yield</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">assert</span>(state_ == FINISH || state_ == RUNNING);</span><br><span class="line">    <span class="built_in">SetNowCoroutine</span>(main_coroutine.<span class="built_in">get</span>());</span><br><span class="line">    <span class="keyword">if</span> (state_ != FINISH) &#123;</span><br><span class="line">        state_ = READY;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (is_run_in_sched_) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">swapcontext</span>(&amp;ctx_, &amp;(Scheduler::<span class="built_in">GetSchedCoroutine</span>()-&gt;ctx_)) != <span class="number">0</span>) &#123;</span><br><span class="line">            std::cout &lt;&lt; <span class="string">&quot;err: Yield::swapcontext\n&quot;</span>;</span><br><span class="line">            <span class="built_in">assert</span>(<span class="literal">false</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">swapcontext</span>(&amp;ctx_, &amp;(main_coroutine-&gt;ctx_)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            std::cout &lt;&lt; <span class="string">&quot;err: Yield::swapcontext\n&quot;</span>;</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从当前运行的协程恢复到该协程</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Coroutine::Resume</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">assert</span>(state_ != FINISH &amp;&amp; state_ != RUNNING);</span><br><span class="line">    <span class="comment">// 每次恢复时需要将当前运行的协程设置为自身</span></span><br><span class="line">    <span class="built_in">SetNowCoroutine</span>(<span class="keyword">this</span>);</span><br><span class="line">    state_ = RUNNING;</span><br><span class="line">    <span class="keyword">if</span> (is_run_in_sched_) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">swapcontext</span>(&amp;(Scheduler::<span class="built_in">GetSchedCoroutine</span>()-&gt;ctx_), &amp;ctx_) != <span class="number">0</span>) &#123;</span><br><span class="line">            std::cout &lt;&lt; <span class="string">&quot;err: Resume::swapcontext\n&quot;</span>;</span><br><span class="line">            <span class="built_in">assert</span>(<span class="literal">false</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">swapcontext</span>(&amp;(main_coroutine-&gt;ctx_), &amp;ctx_) != <span class="number">0</span>) &#123;</span><br><span class="line">            std::cout &lt;&lt; <span class="string">&quot;err: Resume::swapcontext\n&quot;</span>;</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>【动手写协程库 2】协程调度器</title>
    <url>/posts/4e1ce3da/</url>
    <content><![CDATA[<blockquote>
<p>【动手写协程库】系列笔记是学习sylar的协程库时的记录，参考了<a href="https://www.midlane.top/wiki/pages/viewpage.action?pageId=10060952">从零开始重写sylar C++高性能分布式服务器框架</a>和代码随想录中的<a href="https://www.programmercarl.com/other/project_coroutine.html#%E4%B8%8B%E8%BD%BD%E6%96%B9%E5%BC%8F">文档</a>。文章并不是对所有代码的详细解释，而是为了自己理解一些片段所做的笔记。</p>
<p><code>Scheduler</code>类中其他函数的定义可以在这里查看：<a href="https://github.com/kerolt/coroutine-lib/blob/master/src/scheduler.cpp">Github: src/scheduler.cpp</a></p>
</blockquote>
<p>Sylar的协程调度器是一个N-M模型，意味着N个线程可以运行M个协程，协程能够在线程之间进行切换，也可以被绑定到特定的线程上执行。</p>
<p>调度器可以由应用程序中的任何线程创建，但创建它的线程（称为caller线程）可以选择是否参与协程的调度。如果caller线程参与调度，那么调度器的线程数会相应减少一个，因为caller线程本身也会作为一个调度线程。</p>
<span id="more"></span>
<p>Scheduler相关API如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> SCHEDULER_H_</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SCHEDULER_H_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;list&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;coroutine.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;util.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 协程调度器</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Scheduler</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 调度任务，任务类型可以是协程/函数二选一，并且可指定调度线程</span></span><br><span class="line">    <span class="keyword">using</span> ThreadIdPtr = std::shared_ptr&lt;std::thread::id&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">SchedulerTask</span> &#123;</span><br><span class="line">        ThreadIdPtr thread_id_;</span><br><span class="line">        Coroutine::Ptr coroutine_;</span><br><span class="line">        std::function&lt;<span class="type">void</span>()&gt; callback_;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">SchedulerTask</span>() &#123;&#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">SchedulerTask</span>(Coroutine::Ptr co, ThreadIdPtr id)</span><br><span class="line">            : <span class="built_in">coroutine_</span>(co)</span><br><span class="line">            , <span class="built_in">thread_id_</span>(std::<span class="built_in">move</span>(id)) &#123;&#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">SchedulerTask</span>(std::function&lt;<span class="built_in">void</span>()&gt; callback, ThreadIdPtr id)</span><br><span class="line">            : <span class="built_in">callback_</span>(callback)</span><br><span class="line">            , <span class="built_in">thread_id_</span>(std::<span class="built_in">move</span>(id)) &#123;&#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="type">void</span> <span class="title">Reset</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            thread_id_.<span class="built_in">reset</span>();</span><br><span class="line">            callback_ = <span class="literal">nullptr</span>;</span><br><span class="line">            coroutine_ = <span class="literal">nullptr</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Scheduler</span>(<span class="type">size_t</span> threads = <span class="number">1</span>, <span class="type">bool</span> use_caller = <span class="literal">true</span>, <span class="type">const</span> std::string&amp; name = <span class="string">&quot;scheduler&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Scheduler</span>();</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">GetName</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name_;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Start</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Stop</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">typename</span> TaskType&gt;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Sched</span><span class="params">(TaskType t, ThreadIdPtr id = <span class="literal">nullptr</span>)</span> <span class="title">requires</span><span class="params">(std::invocable&lt;TaskType&gt; || std::same_as&lt;TaskType, Coroutine::Ptr&gt;)</span> </span>&#123;</span><br><span class="line">        <span class="type">bool</span> is_need_tick = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">            is_need_tick = tasks_.<span class="built_in">empty</span>();</span><br><span class="line">            <span class="function">SchedulerTask <span class="title">task</span><span class="params">(t, id)</span></span>;</span><br><span class="line">            <span class="keyword">if</span> (task.callback_ || task.coroutine_) &#123;</span><br><span class="line">                tasks_.<span class="built_in">push_back</span>(task);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (is_need_tick) &#123;</span><br><span class="line">            <span class="built_in">Tickle</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> Scheduler* <span class="title">GetScheduler</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> Coroutine* <span class="title">GetSchedCoroutine</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Tickle</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Run</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">SetThisAsScheduler</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Idle</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">bool</span> <span class="title">IsStop</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">HasIdleThreads</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> idle_threads_ &gt; <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::string name_;</span><br><span class="line">    std::mutex mutex_;</span><br><span class="line">    std::vector&lt;std::thread&gt; thread_pool_;</span><br><span class="line">    std::vector&lt;std::thread::id&gt; thread_ids_;</span><br><span class="line">    std::list&lt;SchedulerTask&gt; tasks_;</span><br><span class="line"></span><br><span class="line">    <span class="type">size_t</span> threads_size_;</span><br><span class="line">    std::<span class="type">atomic_size_t</span> active_threads_&#123;<span class="number">0</span>&#125;;</span><br><span class="line">    std::<span class="type">atomic_size_t</span> idle_threads_&#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line">    std::thread::id sched_id_; <span class="comment">// use_caller为true时，调度器所在的线程id</span></span><br><span class="line">    Coroutine::Ptr sched_co_;  <span class="comment">// use_caller为true时调度器所在线程的调度协程</span></span><br><span class="line">    <span class="type">bool</span> is_stop_;</span><br><span class="line">    <span class="type">bool</span> is_use_caller_;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">/* SCHEDULER_H_ */</span></span></span><br></pre></td></tr></table></figure>
<p>调度器的工作流大致为：</p>
<ul>
<li><strong>协程调度器在初始化时</strong>可传入线程数和一个布尔型的<code>use_caller</code>参数，表示是否使用caller线程。在使用caller线程的情况下，线程数自动减一，并且调度器内部会初始化一个属于caller线程的调度协程并保存起来（比如，在main函数中创建的调度器，如果use_caller为true，那调度器会初始化一个属于main函数线程的调度协程）。</li>
<li><strong>调度器创建好后</strong> ，即可调用调度器的<code>Sched</code>函数向调度器添加调度任务，但此时调度器并不会立刻执行这些任务，而是将它们保存到内部的一个任务队列中。</li>
<li><strong>调用<code>Scheduler::Start()</code>函数启动调度</strong> 。调用Start会创建调度线程池，线程数量由初始化时的线程数和use_caller确定。调度线程一旦创建，就会<strong>立刻</strong>从任务队列里取任务执行。比较特殊的一点是，如果初始化时指定线程数为1且use_caller为true，那么Start方法什么也不做，因为不需要创建新线程用于调度。并且，由于没有创建新的调度线程，那只能由caller线程的调度协程来负责调度协程（这里有点绕），而caller线程的调度协程的执行时机与Start函数并不在同一个地方。caller线程的调度协程的执行时机在Stop函数中。</li>
<li><strong>接下来是调度协程，对应<code>Scheduler::Run()</code></strong> 。调度协程负责从调度器的任务队列中取任务执行。取出的任务即子协程，每个子协程执行完后都必须返回调度协程，由调度协程重新从任务队列中取新的协程并执行。如果任务队列空了，那么调度协程会切换到一个idle协程，等有新任务进来时，idle协程才会退出并回到调度协程，重新开始下一轮调度。（在Scheduler中，idle函数的定义十分简单粗暴，因为实际使用协程库时并不是直接使用Scheduler类，而是使用它的派生类，在派生类中将会实现更为完善的调度）</li>
<li><strong>接下来是添加调度任务，对应<code>Scheduler::Sched()</code></strong> ，这个方法支持传入协程或函数，并且支持一个线程id参数，表示是否将这个协程或函数绑定到一个具体的线程上执行。如果任务队列为空，那么在添加任务之后，要调用一次tickle方法以通知各调度线程的调度协程有新任务来了。在执行调度任务时，还可以通过调度器的<code>GetScheduler()</code>获取到当前调度器，再通过<code>Sched</code>函数继续添加新的任务，这就变相实现了在子协程中创建并运行新的子协程的功能。</li>
<li><strong>接下来是调度器的停止</strong>。调度器的停止行为要分两种情况讨论，首先是use_caller为false的情况，这种情况下，由于没有使用caller线程进行调度，那么只需要简单地等各个调度线程的调度协程退出就行了。如果use_caller为true，表示caller线程也要参于调度，这时，调度器初始化时记录的属于caller线程的调度协程就要起作用了，在调度器停止前，应该让这个caller线程的调度协程也运行一次，让caller线程完成调度工作后再退出。如果调度器只使用了caller线程进行调度，那么所有的调度任务要在调度器停止时才会被调度。</li>
</ul>
<p>调度器中最重要的一个函数我认为就是<code>Run()</code>函数了，这个函数用于协程的调度，或者，你可以将他理解为是一个调度协程（名词）。</p>
<p>创建Scheduler时会为每一个内部线程池中的每一个线程都绑定一个调度协程，线程数量默认为1，此时也默认会使用caller线程，也就是使用的主线程。调度协程<code>Scheduler::Run()</code>会从任务队列Task Queue中不断去取任务去执行。如果有任务可执行，那就切换至任务协程执行，任务协程执行完毕后又切换回调度协程；无任务执行时，调度协程切换至Idle协程进行等待。</p>
<p><img src="../../images/Pasted%20image%2020240911153700.png" alt=""></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 用于协程的调度</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Scheduler::Run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LOG &lt;&lt; <span class="string">&quot;Scheduler running...\n&quot;</span>;</span><br><span class="line">    <span class="built_in">SetThisAsScheduler</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果当前线程不是调度器所在线程，设置调度的协程为当前线程运行的协程</span></span><br><span class="line">    <span class="keyword">if</span> (std::this_thread::<span class="built_in">get_id</span>() != sched_id_) &#123;</span><br><span class="line">        sched_coroutine = Coroutine::<span class="built_in">GetNowCoroutine</span>().<span class="built_in">get</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Coroutine::Ptr idle_co = std::<span class="built_in">make_shared</span>&lt;Coroutine&gt;([<span class="keyword">this</span>] &#123; <span class="keyword">this</span>-&gt;<span class="built_in">Idle</span>(); &#125;);</span><br><span class="line">    Coroutine::Ptr callback_co;</span><br><span class="line"></span><br><span class="line">    SchedulerTask task;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        task.<span class="built_in">Reset</span>();</span><br><span class="line">        <span class="type">bool</span> tickle = <span class="literal">false</span>;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">            <span class="keyword">auto</span> iter = tasks_.<span class="built_in">begin</span>();</span><br><span class="line">            <span class="keyword">while</span> (iter != tasks_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">                <span class="comment">// 当前遍历的task已经分配了线程去执行且这个线程不是当前线程，则不用管</span></span><br><span class="line">                <span class="keyword">if</span> (iter-&gt;thread_id_ &amp;&amp; *iter-&gt;thread_id_ != std::this_thread::<span class="built_in">get_id</span>()) &#123;</span><br><span class="line">                    ++iter;</span><br><span class="line">                    tickle = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (iter-&gt;coroutine_ &amp;&amp; iter-&gt;coroutine_-&gt;<span class="built_in">GetState</span>() != Coroutine::READY) &#123;</span><br><span class="line">                    LOG &lt;&lt; <span class="string">&quot;Coroutine task&#x27;s state should be READY!\n&quot;</span>;</span><br><span class="line">                    <span class="built_in">assert</span>(<span class="literal">false</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                task = *iter;</span><br><span class="line">                tasks_.<span class="built_in">erase</span>(iter++);</span><br><span class="line">                active_threads_++;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 有任务可以去执行，需要tickle一下</span></span><br><span class="line">            tickle |= (iter != tasks_.<span class="built_in">end</span>());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (tickle) &#123;</span><br><span class="line">            <span class="built_in">Tickle</span>();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 子协程执行完毕后yield会回到Run()中</span></span><br><span class="line">        <span class="keyword">if</span> (task.coroutine_) &#123;</span><br><span class="line">            <span class="comment">// 任务类型为协程</span></span><br><span class="line">            task.coroutine_-&gt;<span class="built_in">Resume</span>();</span><br><span class="line">            active_threads_--;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (task.callback_) &#123;</span><br><span class="line">            <span class="comment">// 任务类型为回调函数</span></span><br><span class="line">            <span class="keyword">if</span> (callback_co) &#123;</span><br><span class="line">                callback_co-&gt;<span class="built_in">Reset</span>(task.callback_);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                callback_co = std::<span class="built_in">make_shared</span>&lt;Coroutine&gt;(task.callback_);</span><br><span class="line">            &#125;</span><br><span class="line">            callback_co-&gt;<span class="built_in">Resume</span>();</span><br><span class="line">            active_threads_--;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 无任务，任务队列为空</span></span><br><span class="line">            <span class="keyword">if</span> (idle_co-&gt;<span class="built_in">GetState</span>() == Coroutine::FINISH) &#123;</span><br><span class="line">                LOG &lt;&lt; <span class="string">&quot;Idle coroutine finish\n&quot;</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            idle_threads_++;</span><br><span class="line">            idle_co-&gt;<span class="built_in">Resume</span>(); <span class="comment">// Idle最后Yeild时回到这里</span></span><br><span class="line">            idle_threads_--;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    LOG &lt;&lt; <span class="string">&quot;Scheduler Run() exit\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个Scheduler是一个很简单的调度器，要对任务做更好的调度，少不了Idle协程的帮助。Idle协程的具体实现要在之后的IOManager中，其继承自Scheduler，重写了<code>Tickle()</code>、<code>Idle()</code>等函数，并且使用epoll来实现在不同的 I/O 事件发生时，触发相应的处理逻辑。这使得程序可以以非阻塞的方式处理多个 I/O 操作，而不必等待每个操作完成后再进行下一个操作。</p>
]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>【动手写协程库 3】定时器</title>
    <url>/posts/22b42d4b/</url>
    <content><![CDATA[<blockquote>
<p>【动手写协程库】系列笔记是学习sylar的协程库时的记录，参考了<a href="https://www.midlane.top/wiki/pages/viewpage.action?pageId=10060952">从零开始重写sylar C++高性能分布式服务器框架</a>和代码随想录中的<a href="https://www.programmercarl.com/other/project_coroutine.html#%E4%B8%8B%E8%BD%BD%E6%96%B9%E5%BC%8F">文档</a>。文章并不是对所有代码的详细解释，而是为了自己理解一些片段所做的笔记。</p>
<p><code>TimerManager</code>类中具体定义实现可以在这里查看：<a href="https://github.com/kerolt/coroutine-lib/blob/master/src/timer.cpp">Github: src/timer.cpp</a></p>
</blockquote>
<p>通过定时器，我们可以实现给服务器注册定时事件。sylar的定时器采用最小堆设计，所有定时器根据绝对的超时时间点（也就是超时到期的具体时间戳）进行排序，每次取出离当前时间<strong>最近的一个超时时间点</strong>，计算出超时需要等待的时间，然后等待超时。超时时间到后，获取当前的绝对时间点，然后<strong>把最小堆里超时时间点小于这个时间点的定时器都收集起来</strong>，执行它们的回调函数。</p>
<span id="more"></span>
<p>定时器相关API如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Timer</span> : <span class="keyword">public</span> std::enable_shared_from_this&lt;Timer&gt; &#123;</span><br><span class="line">    <span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">TimerManager</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">using</span> Ptr = std::shared_ptr&lt;Timer&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">Cancel</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">Refresh</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">Reset</span><span class="params">(<span class="type">uint64_t</span> ms, <span class="type">bool</span> from_now)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">Timer</span>(<span class="type">uint64_t</span> ms, std::function&lt;<span class="built_in">void</span>()&gt; cb, <span class="type">bool</span> recur, TimerManager* manager);</span><br><span class="line">    <span class="built_in">Timer</span>(<span class="type">uint64_t</span> next);</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">bool</span> is_recur_;       <span class="comment">// 是否循环定时器</span></span><br><span class="line">    <span class="type">uint64_t</span> exec_cycle_; <span class="comment">// 执行周期</span></span><br><span class="line">    <span class="type">uint64_t</span> next_;       <span class="comment">// 下一次的到期时间</span></span><br><span class="line">    std::function&lt;<span class="type">void</span>()&gt; callback_;</span><br><span class="line">    TimerManager* manager_;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">Comp</span> &#123;</span><br><span class="line">        <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> Timer::Ptr&amp; lt, <span class="type">const</span> Timer::Ptr&amp; rt)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (!lt || !rt) &#123;</span><br><span class="line">                <span class="keyword">return</span> !lt &amp;&amp; rt;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> lt-&gt;next_ &lt; rt-&gt;next_;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TimerManager</span> &#123;</span><br><span class="line">    <span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">Timer</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">TimerManager</span>();</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">TimerManager</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// public 添加定时器</span></span><br><span class="line">    <span class="function">Timer::Ptr <span class="title">AddTimer</span><span class="params">(<span class="type">uint64_t</span> ms, std::function&lt;<span class="type">void</span>()&gt; cb, <span class="type">bool</span> is_recur = <span class="literal">false</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 添加条件定时器，如果条件成立则定时器才有效</span></span><br><span class="line">    <span class="function">Timer::Ptr <span class="title">AddConditionTimer</span><span class="params">(<span class="type">uint64_t</span> ms, std::function&lt;<span class="type">void</span>()&gt; cb, std::weak_ptr&lt;<span class="type">void</span>&gt; cond, <span class="type">bool</span> is_recur = <span class="literal">false</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取下一个定时器到现在的执行间隔时间</span></span><br><span class="line">    <span class="comment">// 如果没有定时器了，就返回uint64_t的最大值</span></span><br><span class="line">    <span class="function"><span class="type">uint64_t</span> <span class="title">GetNextTimerInterval</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取需要执行的定时器的回调函数列表</span></span><br><span class="line">    std::vector&lt;std::function&lt;<span class="type">void</span>()&gt;&gt; <span class="built_in">GetExpiredCbList</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 是否还有定时器</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">HasTimer</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">OnTimerInsertAtFront</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">AddTimer</span><span class="params">(Timer::Ptr timer, std::shared_lock&lt;std::shared_mutex&gt;&amp; lock)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 系统时钟是否出现了回绕（rollover）现象，即当前时间比之前记录的时间要小很多</span></span><br><span class="line">    <span class="comment">// 用于检测服务器时间是否被调后了</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">DetectClockRollover</span><span class="params">(<span class="type">uint64_t</span> now_ms)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::shared_mutex rw_mutex_;</span><br><span class="line">    std::set&lt;Timer::Ptr, Timer::Comp&gt; timer_heap_;</span><br><span class="line">    <span class="type">bool</span> is_tickled_;</span><br><span class="line">    <span class="type">uint64_t</span> pre_exec_time_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>个人感觉最重要的API是<code>AddTimer</code>、<code>GetNextTimerInterval</code>和<code>GetExpiredCbList</code>。</p>
<ul>
<li><code>AddTimer</code>向时间堆中添加超时超时时间到了后的回调函数（利用<code>Timer</code>类来封装）。</li>
<li><code>GetNextTimerInterval</code>用于获取下一个定时器到现在的执行间隔时间，这会用于<code>IOManager::Idle()</code>中用于与规定的最大超时时间进行比较，用较小者作为<code>epoll_wait</code>的超时时间参数。</li>
<li><code>GetExpiredCbList</code>获取的是所有超时定时器的回调函数。在<code>IOManager::Idle()</code>会调用这个函数将所有超时定时器的回调函数作为调度任务加入任务队列进行处理。</li>
</ul>
<p>在定时器中，使用<code>GetElapsedMS()</code>来获取系统自启动来经过的时间，其内部使用<code>clock_gettime</code>来获取时间，这相比于一些传统时间获取函数（如<code>time</code>或<code>gettimeofday</code>）有更高的精度：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取系统自启动来经过的时间</span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">uint64_t</span> <span class="title">GetElapsedMS</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">timespec</span> ts = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="built_in">clock_gettime</span>(CLOCK_MONOTONIC_RAW, &amp;ts);</span><br><span class="line">    <span class="keyword">return</span> ts.tv_sec * <span class="number">1000</span> + ts.tv_nsec / <span class="number">1000000</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>【动手写协程库 4】IO协程调度器</title>
    <url>/posts/d24ea83c/</url>
    <content><![CDATA[<blockquote>
<p>【动手写协程库】系列笔记是学习sylar的协程库时的记录，参考了<a href="https://www.midlane.top/wiki/pages/viewpage.action?pageId=10060952">从零开始重写sylar C++高性能分布式服务器框架</a>和代码随想录中的<a href="https://www.programmercarl.com/other/project_coroutine.html#%E4%B8%8B%E8%BD%BD%E6%96%B9%E5%BC%8F">文档</a>。文章并不是对所有代码的详细解释，而是为了自己理解一些片段所做的笔记。</p>
<p><code>IOManager</code>类中具体定义实现可以在这里查看：<a href="https://github.com/kerolt/coroutine-lib/blob/master/src/iomanager.cpp">Github: src/iomanager.cpp</a></p>
</blockquote>
<p>之前实现的协程调度器的功能其实非常简单，当添加任务后调度器只是单纯的从任务队列中取出任务交给协程去执行。sylar的协程库的关注对象是网络IO，如果采用这么简单的调度就根本没有用到协程的精髓。</p>
<p>sylar的IO协程调度解决了之前调度器在idle状态下忙等待导致CPU占用率高的问题。IO协程调度器使用一对管道fd来tickle调度协程，当调度器空闲时，idle协程通过epoll_wait阻塞在管道的读描述符上，等管道的可读事件。添加新任务时，tickle方法写管道，idle协程检测到管道可读后退出，调度器执行调度。</p>
<span id="more"></span>
<h2 id="IOManager-API">IOManager API<a class="anchor" href="#IOManager-API"></a></h2>
<p><code>IOManager</code>的API如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> IOMANAGER_H_</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> IOMANAGER_H_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/epoll.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;shared_mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;coroutine.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;scheduler.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;timer.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 事件：无、读、写</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Event</span> &#123;</span><br><span class="line">    NONE = <span class="number">0x0</span>,</span><br><span class="line">    READ = EPOLLIN,</span><br><span class="line">    WRITE = EPOLLOUT,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// IO协程调度</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">IOManager</span> : <span class="keyword">public</span> Scheduler, <span class="keyword">public</span> TimerManager &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">IOManager</span>(<span class="type">size_t</span> threads = <span class="number">1</span>, <span class="type">bool</span> use_caller = <span class="literal">true</span>, <span class="type">const</span> std::string&amp; name = <span class="string">&quot;IOManager&quot;</span>);</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">IOManager</span>();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">AddEvent</span><span class="params">(<span class="type">int</span> fd, Event event, std::function&lt;<span class="type">void</span>()&gt; cb = <span class="literal">nullptr</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">DelEvent</span><span class="params">(<span class="type">int</span> fd, Event event)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">CancelEvent</span><span class="params">(<span class="type">int</span> fd, Event event)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">CancelAllEvent</span><span class="params">(<span class="type">int</span> fd)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> IOManager* <span class="title">GetIOManager</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Idle</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Tickle</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">OnTimerInsertAtFront</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">IsStop</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">ResizeContexts</span><span class="params">(<span class="type">size_t</span> size)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// socket fd 上下文</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">FdContext</span> &#123;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">EventContext</span> &#123;</span><br><span class="line">            Scheduler* scheduler = <span class="literal">nullptr</span>;</span><br><span class="line">            Coroutine::Ptr coroutine;</span><br><span class="line">            std::function&lt;<span class="type">void</span>()&gt; callback;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 根据类型获取对应的上下文</span></span><br><span class="line">        <span class="function">EventContext&amp; <span class="title">GetEventContext</span><span class="params">(Event&amp; e)</span></span>;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="type">void</span> <span class="title">ResetEventContext</span><span class="params">(EventContext&amp; ectx)</span></span>;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="type">void</span> <span class="title">TriggerEvent</span><span class="params">(Event e)</span></span>;</span><br><span class="line"></span><br><span class="line">        EventContext read_ctx, write_ctx;</span><br><span class="line">        <span class="type">int</span> fd;</span><br><span class="line">        Event events = Event::NONE;</span><br><span class="line">        std::mutex mutex;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> epfd_;</span><br><span class="line">    <span class="type">int</span> tickle_fd_[<span class="number">2</span>];</span><br><span class="line">    std::<span class="type">atomic_size_t</span> pending_evt_cnt_;</span><br><span class="line">    std::mutex mutex_;</span><br><span class="line">    std::shared_mutex rw_mutex_;</span><br><span class="line">    <span class="comment">// 利用fd作为下标来获取对应的FdContext*，也可以使用哈希表代替</span></span><br><span class="line">    std::vector&lt;FdContext*&gt; fd_contexts_;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">/* IOMANAGER_H_ */</span></span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="Scheduler-Run">Scheduler::Run()<a class="anchor" href="#Scheduler-Run"></a></h2>
<p>我们可以先回顾一下<code>Scheduler::Run()</code>这个函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">Scheduler::Run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LOG &lt;&lt; <span class="string">&quot;Scheduler running...\n&quot;</span>;</span><br><span class="line">    <span class="built_in">SetHookFlag</span>(<span class="literal">true</span>);</span><br><span class="line">    <span class="built_in">SetThisAsScheduler</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果当前线程不是调度器所在线程，设置调度的协程为当前线程运行的协程</span></span><br><span class="line">    <span class="keyword">if</span> (std::this_thread::<span class="built_in">get_id</span>() != sched_id_) &#123;</span><br><span class="line">        sched_coroutine = Coroutine::<span class="built_in">GetNowCoroutine</span>().<span class="built_in">get</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Coroutine::Ptr idle_co = std::<span class="built_in">make_shared</span>&lt;Coroutine&gt;([<span class="keyword">this</span>] &#123; <span class="keyword">this</span>-&gt;<span class="built_in">Idle</span>(); &#125;);</span><br><span class="line">    Coroutine::Ptr callback_co;</span><br><span class="line"></span><br><span class="line">    SchedulerTask task;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        task.<span class="built_in">Reset</span>();</span><br><span class="line">        <span class="type">bool</span> tickle = <span class="literal">false</span>;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">            <span class="keyword">auto</span> iter = tasks_.<span class="built_in">begin</span>();</span><br><span class="line">            <span class="keyword">while</span> (iter != tasks_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">                <span class="comment">// 当前遍历的task已经分配了线程去执行且这个线程不是当前线程，则不用管</span></span><br><span class="line">                <span class="keyword">if</span> (iter-&gt;thread_id_ &amp;&amp; *iter-&gt;thread_id_ != std::this_thread::<span class="built_in">get_id</span>()) &#123;</span><br><span class="line">                    ++iter;</span><br><span class="line">                    tickle = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (iter-&gt;coroutine_ &amp;&amp; iter-&gt;coroutine_-&gt;<span class="built_in">GetState</span>() != Coroutine::READY) &#123;</span><br><span class="line">                    LOG &lt;&lt; <span class="string">&quot;Coroutine task&#x27;s state should be READY!\n&quot;</span>;</span><br><span class="line">                    <span class="built_in">assert</span>(<span class="literal">false</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                task = *iter;</span><br><span class="line">                tasks_.<span class="built_in">erase</span>(iter++);</span><br><span class="line">                active_threads_++;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 有任务可以去执行，需要tickle一下</span></span><br><span class="line">            tickle |= (iter != tasks_.<span class="built_in">end</span>());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (tickle) &#123;</span><br><span class="line">            <span class="built_in">Tickle</span>();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 子协程执行完毕后yield会回到Run()中</span></span><br><span class="line">        <span class="comment">// 注意，每次运行了一个task后需要Reset一下</span></span><br><span class="line">        <span class="keyword">if</span> (task.coroutine_) &#123;</span><br><span class="line">            <span class="comment">// 任务类型为协程</span></span><br><span class="line">            task.coroutine_-&gt;<span class="built_in">Resume</span>();</span><br><span class="line">            active_threads_--;</span><br><span class="line">            task.<span class="built_in">Reset</span>();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (task.callback_) &#123;</span><br><span class="line">            <span class="comment">// 任务类型为回调函数，将其包装为协程</span></span><br><span class="line">            <span class="keyword">if</span> (callback_co) &#123;</span><br><span class="line">                callback_co-&gt;<span class="built_in">Reset</span>(task.callback_);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                callback_co = std::<span class="built_in">make_shared</span>&lt;Coroutine&gt;(task.callback_);</span><br><span class="line">            &#125;</span><br><span class="line">            callback_co-&gt;<span class="built_in">Resume</span>();</span><br><span class="line">            active_threads_--;</span><br><span class="line">            callback_co.<span class="built_in">reset</span>();</span><br><span class="line">            task.<span class="built_in">Reset</span>();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 无任务，任务队列为空</span></span><br><span class="line">            <span class="keyword">if</span> (idle_co-&gt;<span class="built_in">GetState</span>() == Coroutine::FINISH) &#123;</span><br><span class="line">                LOG &lt;&lt; <span class="string">&quot;Idle coroutine finish\n&quot;</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            idle_threads_++;</span><br><span class="line">            idle_co-&gt;<span class="built_in">Resume</span>(); <span class="comment">// Idle最后Yeild时回到这里</span></span><br><span class="line">            idle_threads_--;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    LOG &lt;&lt; <span class="string">&quot;Scheduler Run() exit\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个函数就是每个线程会启动的协程调度函数，负责管理和执行任务队列中的任务，包括协程和回调函数两种类型的任务，如果任务队列为空，则执行Idle协程。</p>
<p>在<code>Scheduler::Idle()</code>函数中，仅仅只是做了一个简单的处理：调度器没有停止就让出当前正在执行的协程，我们要做的增强后的IOManager需要重写Idle函数，让它不断等待事件、处理事件、然后再次等待事件的循环过程，它在没有其他协程运行时保持系统的活跃度，并在有事件发生时进行相应的处理。</p>
<h2 id="重写Idle函数">重写Idle函数<a class="anchor" href="#重写Idle函数"></a></h2>
<p>在<code>IOManager</code>中，我们就需要重写Idle函数，我们需要它是一个不断等待事件、处理事件、然后再次等待事件的循环过程，它在没有其他协程运行时保持系统的活跃度，并在有事件发生时进行相应的处理：</p>
<ol>
<li>我们会先找到最近一个定时器的超时时间，并将其与自定义最长超时时间（源码中是5s）进行比较取最小者作为<code>epoll_wait</code>的超时时间</li>
<li>将超时的定时器的回调函数加入调度器</li>
<li>处理<code>epoll_wait</code>送来的事件</li>
<li>将当前线程运行的协程暂停（也就是暂停Idle协程），并将执行权交给调度协程（<code>Scheduler::Run()</code>）</li>
<li>从1.又开始重复执行</li>
</ol>
<p>具体操作可看代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">IOManager::Idle</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LOG &lt;&lt; <span class="string">&quot;idle coroutine start up\n&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> MAX_EVENTS = <span class="number">256</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> MAX_TIMEOUT = <span class="number">5000</span>;</span><br><span class="line">    epoll_event events[MAX_EVENTS]&#123;&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        <span class="comment">// LOG &lt;&lt; &quot;in idle now\n&quot;;</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">IsStop</span>()) &#123;</span><br><span class="line">            LOG &lt;&lt; <span class="built_in">GetName</span>() &lt;&lt; <span class="string">&quot;idle stop now\n&quot;</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">uint64_t</span> next_timeout = <span class="built_in">GetNextTimerInterval</span>();</span><br><span class="line">        <span class="type">int</span> triggered_events;</span><br><span class="line">        <span class="keyword">do</span> &#123;</span><br><span class="line">            <span class="comment">// 如果时间堆中有超时的定时器，则比较这个超时定时器的下一次触发的时间与MAX_TIMEOUT（5s），选取最小值作为超时时间</span></span><br><span class="line">            next_timeout = next_timeout != ~<span class="number">0ull</span> ? std::<span class="built_in">min</span>(<span class="built_in">static_cast</span>&lt;<span class="type">int</span>&gt;(next_timeout), MAX_TIMEOUT) : MAX_TIMEOUT;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 没有事件到来时会阻塞在epoll_wait上，除非到了超时时间</span></span><br><span class="line">            triggered_events = <span class="built_in">epoll_wait</span>(epfd_, events, MAX_EVENTS, <span class="built_in">static_cast</span>&lt;<span class="type">int</span>&gt;(next_timeout));</span><br><span class="line">            <span class="keyword">if</span> (triggered_events &lt; <span class="number">0</span> &amp;&amp; errno == EINTR) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">while</span> (<span class="literal">true</span>); <span class="comment">// 用while(true)的目的是确保在出现特定错误情况时能够重新尝试执行 epoll_wait</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 将超时的定时器的回调函数加入调度器</span></span><br><span class="line">        <span class="comment">// 这些回调函数的作用可能是关闭连接等操作</span></span><br><span class="line">        std::vector&lt;std::function&lt;<span class="type">void</span>()&gt;&gt; cbs = <span class="built_in">GetExpiredCbList</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; cb : cbs) &#123;</span><br><span class="line">            <span class="built_in">Sched</span>(cb);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 处理事件</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; triggered_events; i++) &#123;</span><br><span class="line">            epoll_event&amp; event = events[i];</span><br><span class="line">            <span class="comment">// 是一个用于通知协程调度的事件</span></span><br><span class="line">            <span class="comment">// epoll中监听了用于通知的管道读端fd，当有数据到时即会触发</span></span><br><span class="line">            <span class="keyword">if</span> (event.data.fd == tickle_fd_[<span class="number">0</span>]) &#123;</span><br><span class="line">                <span class="type">char</span> buf[<span class="number">256</span>]&#123;&#125;;</span><br><span class="line">                <span class="comment">// 将管道内的数据读完</span></span><br><span class="line">                <span class="keyword">while</span> (<span class="built_in">read</span>(tickle_fd_[<span class="number">0</span>], buf, <span class="built_in">sizeof</span>(buf)) &gt; <span class="number">0</span>)</span><br><span class="line">                    ;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// FdContext* fd_ctx = (FdContext*) event.data.ptr;</span></span><br><span class="line">            FdContext* fd_ctx = <span class="built_in">static_cast</span>&lt;FdContext*&gt;(event.data.ptr);</span><br><span class="line">            <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(fd_ctx-&gt;mutex)</span></span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 发生错误时，如果原来的文件描述符上下文（fd_ctx）中有可读或可写事件标志被设置，那么现在将重新触发这些事件</span></span><br><span class="line">            <span class="keyword">if</span> (event.events &amp; (EPOLLERR | EPOLLHUP)) &#123;</span><br><span class="line">                event.events |= (EPOLLIN | EPOLLOUT) &amp; fd_ctx-&gt;events;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 获取fd_ctx对应的事件</span></span><br><span class="line">            <span class="type">int</span> real_event = Event::NONE;</span><br><span class="line">            <span class="keyword">if</span> (event.events &amp; EPOLLIN) &#123;</span><br><span class="line">                real_event |= Event::READ;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (event.events &amp; EPOLLOUT) &#123;</span><br><span class="line">                real_event |= Event::WRITE;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> ((fd_ctx-&gt;events &amp; real_event) == Event::NONE) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 如果还有剩余事件，则修改；否则将其从epoll中删除</span></span><br><span class="line">            <span class="comment">// 注意获取rest_events时不是使用的event.events &amp; ~real_event，因为是要去除fd_ctx-&gt;fd中本次触发的事件</span></span><br><span class="line">            <span class="type">int</span> rest_events = fd_ctx-&gt;events &amp; ~real_event;</span><br><span class="line">            <span class="type">int</span> op = rest_events ? EPOLL_CTL_MOD : EPOLL_CTL_DEL;</span><br><span class="line">            event.events = EPOLLET | rest_events;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">epoll_ctl</span>(epfd_, op, fd_ctx-&gt;fd, &amp;event) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                LOG_ERROR &lt;&lt; <span class="built_in">strerror</span>(errno) &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (real_event &amp; Event::READ) &#123;</span><br><span class="line">                fd_ctx-&gt;<span class="built_in">TriggerEvent</span>(Event::READ);</span><br><span class="line">                --pending_evt_cnt_;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (real_event &amp; Event::WRITE) &#123;</span><br><span class="line">                fd_ctx-&gt;<span class="built_in">TriggerEvent</span>(Event::WRITE);</span><br><span class="line">                --pending_evt_cnt_;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 将当前线程运行的协程暂停（也就是暂停Idle协程），并将执行权交给调度协程</span></span><br><span class="line">        Coroutine::Ptr co_ptr = Coroutine::<span class="built_in">GetNowCoroutine</span>();</span><br><span class="line">        <span class="keyword">auto</span> co = co_ptr.<span class="built_in">get</span>();</span><br><span class="line">        co_ptr.<span class="built_in">reset</span>();</span><br><span class="line">        co-&gt;<span class="built_in">Yield</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="添加事件">添加事件<a class="anchor" href="#添加事件"></a></h2>
<p>IOManager除了重写Idle函数这个重要点外，还有个重要点就是为指定文件描述符添加事件。</p>
<p>IOManager内部有一个[<code>FdContext</code>](#IOManger API)结构体用来封装socket fd的上下文（需要绑定的回调函数，对应的事件、协程），并使用一个vector保存这些FdContext。</p>
<p>我们在添加fd的事件时，需要将其加入vector中，并且需要通过<code>epoll_ctl</code>注册fd的对应事件。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">IOManager::AddEvent</span><span class="params">(<span class="type">int</span> fd, Event event, std::function&lt;<span class="type">void</span>()&gt; cb)</span> </span>&#123;</span><br><span class="line">    FdContext* fd_ctx = <span class="literal">nullptr</span>;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">std::shared_lock <span class="title">rw_lock</span><span class="params">(rw_mutex_)</span></span>;</span><br><span class="line">        <span class="keyword">if</span> (fd_contexts_.<span class="built_in">size</span>() &gt; fd) &#123;</span><br><span class="line">            fd_ctx = fd_contexts_[fd];</span><br><span class="line">            rw_lock.<span class="built_in">unlock</span>();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            rw_lock.<span class="built_in">unlock</span>();</span><br><span class="line">            <span class="function">std::unique_lock <span class="title">rw_lock2</span><span class="params">(rw_mutex_)</span></span>;</span><br><span class="line">            <span class="built_in">ResizeContexts</span>(fd * <span class="number">1.5</span>);</span><br><span class="line">            fd_ctx = fd_contexts_[fd];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (fd_ctx-&gt;events &amp; event) &#123;</span><br><span class="line">        LOG_ERROR &lt;&lt; <span class="string">&quot;A fd can&#x27;t add same event\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> op = fd_ctx-&gt;events ? EPOLL_CTL_MOD : EPOLL_CTL_ADD;</span><br><span class="line">    epoll_event ep_evt&#123;&#125;;</span><br><span class="line">    ep_evt.events = <span class="built_in">static_cast</span>&lt;<span class="type">int</span>&gt;(fd_ctx-&gt;events) | EPOLLET | event;</span><br><span class="line">    ep_evt.data.ptr = fd_ctx; <span class="comment">// 在Idle()中将使用fd对应的这个ep_evt</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> ret = <span class="built_in">epoll_ctl</span>(epfd_, op, fd, &amp;ep_evt);</span><br><span class="line">    <span class="keyword">if</span> (ret) &#123;</span><br><span class="line">        LOG_ERROR &lt;&lt; <span class="string">&quot;epoll_ctl &quot;</span> &lt;&lt; <span class="built_in">strerror</span>(errno);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ++pending_evt_cnt_;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置fd对应事件的EventContext</span></span><br><span class="line">    fd_ctx-&gt;events = <span class="built_in">static_cast</span>&lt;Event&gt;(fd_ctx-&gt;events | event);</span><br><span class="line">    <span class="comment">// 使用event_ctx相当于使用fd_ctx-&gt;read_ctx or fd_ctx-&gt;write_ctx（注意是auto&amp;而不是auto）</span></span><br><span class="line">    <span class="keyword">auto</span>&amp; event_ctx = fd_ctx-&gt;<span class="built_in">GetEventContext</span>(event);</span><br><span class="line">    <span class="built_in">assert</span>(!event_ctx.scheduler &amp;&amp; !event_ctx.callback &amp;&amp; !event_ctx.coroutine);</span><br><span class="line">    event_ctx.scheduler = Scheduler::<span class="built_in">GetScheduler</span>();</span><br><span class="line">    <span class="keyword">if</span> (cb) &#123;</span><br><span class="line">        event_ctx.callback = cb;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 设置fd相关事件触发时使用的协程为当前</span></span><br><span class="line">        event_ctx.coroutine = Coroutine::<span class="built_in">GetNowCoroutine</span>();</span><br><span class="line">        <span class="built_in">assert</span>(event_ctx.coroutine-&gt;<span class="built_in">GetState</span>() == Coroutine::RUNNING);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>【动手写协程库 5】常用IO函数的HOOK功能</title>
    <url>/posts/9b914411/</url>
    <content><![CDATA[<blockquote>
<p>【动手写协程库】系列笔记是学习sylar的协程库时的记录，参考了<a href="https://www.midlane.top/wiki/pages/viewpage.action?pageId=10060952">从零开始重写sylar C++高性能分布式服务器框架</a>和代码随想录中的<a href="https://www.programmercarl.com/other/project_coroutine.html#%E4%B8%8B%E8%BD%BD%E6%96%B9%E5%BC%8F">文档</a>。文章并不是对所有代码的详细解释，而是为了自己理解一些片段所做的笔记。</p>
<p>hook函数的具体定义实现可以在这里查看：<a href="https://github.com/kerolt/coroutine-lib/blob/master/src/hook.cpp">Github: src/hook.cpp</a></p>
</blockquote>
<p>该协程库框架的目标并不是做成类似goroutine那样，而是希望能够通过协程来提高IO处理的效率。因此，对于每个文件描述符fd，我们都希望它有一个读写IO的超时时间。</p>
<p>hook的目的是在不重新编写代码的情况下，把老代码中的socket IO相关的API都<strong>转成异步</strong>，以提高性能。</p>
<span id="more"></span>
<h2 id="需要Hook的几类函数">需要Hook的几类函数<a class="anchor" href="#需要Hook的几类函数"></a></h2>
<p>在sylar的设计中，只针对socket fd进行hook（因为我们更关心的是网络IO），也就是如果我们操作的不是socket fd，那么就会使用原来的API。</p>
<p>sylar对如下三类函数进行了hook：</p>
<ol>
<li><strong>sleep延时系列接口</strong>：包括sleep/usleep/nanosleep。对于这些接口的hook，只需要给IO协程调度器注册一个定时事件，在定时事件触发后再继续执行当前协程即可。当前协程在注册完定时事件后即可yield让出执行权</li>
<li><strong>socket IO系列接口</strong>：包括read/write/recv/send…等，connect及accept也可以归到这类接口中。这类接口的hook首先需要判断操作的fd是否是socket fd，以及用户是否显式地对该fd设置过非阻塞模式，如果不是socket fd或是用户显式设置过非阻塞模式，那么就不需要hook了，直接调用操作系统的IO接口即可。如果需要hook，那么首先在IO协程调度器上注册对应的读写事件，等事件发生后再继续执行当前协程。当前协程在注册完IO事件即可yield让出执行权。</li>
<li><strong>socket/fcntl/ioctl/close等接口</strong>：这类接口主要处理的是边缘情况，比如分配fd上下文，处理超时及用户显式设置非阻塞问题。</li>
</ol>
<h2 id="Hook的实现">Hook的实现<a class="anchor" href="#Hook的实现"></a></h2>
<blockquote>
<p>我们hook的所有函数，都要与原来的API的行为保持一致（使用这些hook api的时候就好像使用的原来的api）。例如原来的API的返回值通常用0表示成功，-1表示失败</p>
</blockquote>
<p>在 Sylar 中，Hook 的实现通常涉及以下几个关键方面：</p>
<p><strong>一、函数指针替换</strong></p>
<ol>
<li>保存原始函数指针：首先，需要保存被 Hook 函数的原始实现的函数指针。这可以通过在程序启动时或者在首次需要 Hook 的时候，获取原始函数的地址并存储起来。例如，可以定义一个与被 Hook 函数具有相同签名的函数指针变量，并将其初始化为指向原始函数的地址。</li>
<li>替换函数指针：然后，将被 Hook 函数的入口地址替换为自定义的 Hook 函数的地址。这样，当程序调用被 Hook 函数时，实际上会执行 Hook 函数。</li>
</ol>
<p><strong>二、参数传递和返回值处理</strong></p>
<ol>
<li>参数传递：在 Hook 函数中，需要接收与被 Hook 函数相同的参数。这可以通过将参数直接传递给 Hook 函数，或者使用一些技术（如函数调用栈的分析）来获取参数的值。如果被 Hook 函数是 <code>int func(int a, char* b)</code>，那么 Hook 函数也应该具有相同的参数列表 <code>int hook_func(int a, char* b)</code>。</li>
<li>返回值处理：Hook 函数需要根据需要处理被 Hook 函数的返回值。可以选择直接返回被 Hook 函数的原始返回值，或者根据特定的逻辑修改返回值后再返回。</li>
</ol>
<p><strong>三、条件判断和控制</strong></p>
<ol>
<li>Hook 启用 / 禁用：通常会提供一种机制来启用或禁用 Hook 功能。这可以通过一个全局变量、配置文件或者运行时参数来控制。我们可以在代码中定义一个布尔变量，如 <code>bool hook_enable</code>，当它为真时启用 Hook 功能，为假时直接调用原始函数而不执行 Hook 函数。</li>
<li>特定条件下的 Hook：可以根据特定的条件来决定是否执行 Hook 函数。例如，可以检查参数的值、函数的调用者、当前的运行环境等条件，只有在满足特定条件时才执行 Hook 函数。</li>
</ol>
<h2 id="FdManager">FdManager<a class="anchor" href="#FdManager"></a></h2>
<p>我们会通过<code>FdContext</code>类（注意与IOManager中的FdContext进行区分）来保存fd的一些状态，例如fd是否关闭了，是否设置为非阻塞，其读写事件超时时间是多少等。</p>
<h2 id="sleep-API">sleep API<a class="anchor" href="#sleep-API"></a></h2>
<p>对<code>sleep</code>，<code>usleep</code>，<code>nanosleep</code>三个函数进行hook操作，其逻辑一致：sleep类函数会阻塞当前线程，那么我们的改造方法就是用一个定时器来代替sleep的休眠阻塞，获取当前运行的协程，然后通过IOManager添加一个定时器，规定时间后再将这个协程加入调度，之后yield这个协程。</p>
<h2 id="socket-API">socket API<a class="anchor" href="#socket-API"></a></h2>
<ul>
<li>socket：当使用socket创建套接字fd时，我们需要将它加入到FdManager中。</li>
<li>connect：对于原始的<code>connect</code>，它是一个阻塞调用，直到连接成功或发生错误，如果网络延迟较高或目标主机不可达，可能会导致程序长时间挂起。我们需要将其改造为与异步或非阻塞操作结合。对应的实现方法就是通过设置一个超时时间，到时间后取消文件描述符的写事件。为socket fd添加写事件后，如果添加成功，则yield当前协程，并取消定时器</li>
<li>setsockopt：对于<code>optname</code>为<code>SO_RCVTIMEO</code>和<code>SO_RCVTIMEO</code>的情况，我们需要设置sockfd对应的超时时间。</li>
</ul>
<h2 id="socket-IO-API">socket IO API<a class="anchor" href="#socket-IO-API"></a></h2>
<p>accept、read、readv、recv、recvfrom、recvmsg、write、writev、send、sendto、sendmsg这些函数所要作的hook操作都很类似，不同的地方无非就是读写事件的不同，其处理逻辑和connect相似，所以利用了模板来减少冗余代码。</p>
<h2 id="other-API">other API<a class="anchor" href="#other-API"></a></h2>
<p>还有类似close、ioctl、fcntl的函数，由于我们在之前hook api时处理了文件描述符，因此在这些函数中我们需要对文件描述符进行清理或其他操作。</p>
]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>CPlusPlus</tag>
        <tag>Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>【MIT6.S081】Lab5 copy-on-write fork</title>
    <url>/posts/4ece7d1a/</url>
    <content><![CDATA[<h2 id="为什么我们需要copy-on-write">为什么我们需要copy on write<a class="anchor" href="#为什么我们需要copy-on-write"></a></h2>
<p>通过xv6的实验指导书，我们可以知道：</p>
<p>xv6中的<code>fork()</code>系统调用将父进程的所有用户空间内存复制到子进程中。如果父进程所使用的页数很大，复制可能需要很长时间，而这样的复制操作经常是没有用的：<code>fork()</code>之后通常是子进程中的<code>exec()</code>，这会丢弃复制的内存而不是使用它们。</p>
<p>那么，我们是不是可以在子进程刚创建时将其页表的映射到父进程的物理页，而在其需要对内存进行写操作时再重新分配内存呢？没错，这就是通过copy on write（写时复制）技术来进行优化。</p>
<blockquote>
<p>该实验的代码实现见：<a href="https://github.com/kerolt/xv6-labs-2023/commit/68fd833dc03a681c04a647ed6aae2108a1e43fbb">仓库commit</a></p>
</blockquote>
<span id="more"></span>
<h2 id="大致思路">大致思路<a class="anchor" href="#大致思路"></a></h2>
<p>首先说一下大致思路：</p>
<p>在使用fork系统调用创建子进程时，我们不用去额外拷贝父进程的内存，而是将子进程的虚拟内存映射到和父进程相同的物理内存，并且此时应该将父子进程对这块内存的<strong>访问权限设置为只读，并且添加一个用于识别<code>COW（copy on write）</code>的标志</strong>：</p>
<p><img src="../../images/Pasted%20image%2020240712153722.png" alt=""></p>
<p>当子进程需要对内存进行写操作时，<strong>RISC-V会检测到这一块物理地址的权限为只写，触发Page Fault</strong>，这时我们可以为子进程重新分配一块内存空间（取消之前的映射，分配内存后重新映射至新内存），并将父子进程的对应物理页的识别标志位进行修改，<strong>去除cow标志<code>PTE_COW</code>，添加写权限标志<code>PTE_W</code></strong>：</p>
<p><img src="../../images/Pasted%20image%2020240712154121.png" alt=""></p>
<h2 id="具体实现">具体实现<a class="anchor" href="#具体实现"></a></h2>
<h3 id="COW标志位">COW标志位<a class="anchor" href="#COW标志位"></a></h3>
<p><img src="../../images/Pasted%20image%2020240712161309.png" alt=""></p>
<p>首先我们需要为PTE添加一个用于识别COW的标志位，上图展示了PTE的后10位也就是其flags的情况，可以看到第8、9位是保留没有被使用的，那么我们可以用第8位来作为COW的标志位：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// riscv.h</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PTE_COW (1L &lt;&lt; 8)</span></span><br></pre></td></tr></table></figure>
<h3 id="修改uvmcopy">修改uvmcopy<a class="anchor" href="#修改uvmcopy"></a></h3>
<p>在xv6的fork系统调用中，子进程拷贝父进程的物理页使用了<code>uvmcopy()</code>函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">fork</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">int</span> i, pid;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">np</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Allocate process.</span></span><br><span class="line">  <span class="keyword">if</span>((np = allocproc()) == <span class="number">0</span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Copy user memory from parent to child.</span></span><br><span class="line">  <span class="keyword">if</span>(uvmcopy(p-&gt;pagetable, np-&gt;pagetable, p-&gt;sz) &lt; <span class="number">0</span>)&#123;</span><br><span class="line">    freeproc(np);</span><br><span class="line">    release(&amp;np-&gt;lock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么我们需要修改uvmcopy中的代码，将原先的拷贝内存的操作去掉，并将PTE的只读标志位去除、添加cow标记位：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">uvmcopy</span><span class="params">(<span class="type">pagetable_t</span> old, <span class="type">pagetable_t</span> new, uint64 sz)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">pte_t</span> *pte;</span><br><span class="line">  uint64 pa, i;</span><br><span class="line">  uint flags;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; sz; i += PGSIZE)&#123;</span><br><span class="line">    <span class="keyword">if</span>((pte = walk(old, i, <span class="number">0</span>)) == <span class="number">0</span>)</span><br><span class="line">      panic(<span class="string">&quot;uvmcopy: pte should exist&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span>((*pte &amp; PTE_V) == <span class="number">0</span>)</span><br><span class="line">      panic(<span class="string">&quot;uvmcopy: page not present&quot;</span>);</span><br><span class="line">    pa = PTE2PA(*pte);</span><br><span class="line">    flags = PTE_FLAGS(*pte);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 这里移除了拷贝的代码，并设置了父进程相应的标志位</span></span><br><span class="line">    <span class="keyword">if</span> (flags &amp; PTE_W) &#123;</span><br><span class="line">      *pte = (*pte &amp; ~PTE_W) | PTE_COW;</span><br><span class="line">      flags = (flags &amp; ~PTE_W) | PTE_COW;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 将子进程的虚拟页映射至父进程的物理页，同时设置了子进程相应的标志位</span></span><br><span class="line">    <span class="keyword">if</span>(mappages(new, i, PGSIZE, (uint64)pa, flags) != <span class="number">0</span>)&#123;</span><br><span class="line">      <span class="keyword">goto</span> err;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 这里是对物理页的引用计数进行+1，后文会说明</span></span><br><span class="line">    kref_inc((<span class="type">void</span>*)pa);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"> err:</span><br><span class="line">  uvmunmap(new, <span class="number">0</span>, i / PGSIZE, <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="引用计数">引用计数<a class="anchor" href="#引用计数"></a></h3>
<blockquote>
<p>Ensure that each physical page is freed when the last PTE reference to it goes away – but not before. A good way to do this is to keep, for each physical page, a “reference count” of the number of user page tables that refer to that page. Set a page’s reference count to one when kalloc() allocates it. Increment a page’s reference count when fork causes a child to share the page, and decrement a page’s count each time any process drops the page from its page table. kfree() should only place a page back on the free list if its reference count is zero. It’s OK to to keep these counts in a fixed-size array of integers.</p>
</blockquote>
<p>当我们采取了cow后，只有当所有虚拟页都没有引用某一物理页时这个物理页才能被释放，那么我们可以使用一个数组来对每一个物理页进行引用计数，当fork导致子进程共享物理页时，对应的物理页的引用计数+1，当有进程不再使用物理页时，对应的物理页的引用计数-1，回收物理页的<code>kfree()</code>函数只有当物理页的引用计数为0时才会将其放回空闲列表。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kalloc.c</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">spinlock</span> <span class="title">lock</span>;</span> <span class="comment">// 保证操作的原子性</span></span><br><span class="line">  <span class="type">int</span> ref_count[(PGROUNDUP(PHYSTOP)) / PGSIZE]; <span class="comment">// KERNBASE～PHYSTOP是物理内存的大小，因为xv6的内核地址采用了直接映射，为了方便，这里直接使用PHYSTOP。PHYSTOP / PGSIZE则表示有多少个物理页</span></span><br><span class="line">&#125; kref;</span><br></pre></td></tr></table></figure>
<p>内核在使用<code>kinit()</code>进行初始化时，需要初始化kref的锁，并设置引用计数数组的值：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">kinit</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">  initlock(&amp;kmem.lock, <span class="string">&quot;kmem&quot;</span>);</span><br><span class="line">  initlock(&amp;kmem.lock, <span class="string">&quot;kref&quot;</span>);</span><br><span class="line">  <span class="comment">// 这里初始化时置为1是为了接下来的freerange在调用kfree时不会触发panic</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; PGROUNDUP(PHYSTOP) / PGSIZE; i++) &#123;</span><br><span class="line">    kref.ref_count[i] = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  freerange(end, (<span class="type">void</span>*)PHYSTOP);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在<code>kfree()</code>中对物理页的引用计数来判断是否应该释放物理内存：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">kfree</span><span class="params">(<span class="type">void</span> *pa)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">r</span>;</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(((uint64)pa % PGSIZE) != <span class="number">0</span> || (<span class="type">char</span>*)pa &lt; end || (uint64)pa &gt;= PHYSTOP)</span><br><span class="line">    panic(<span class="string">&quot;kfree&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 如果在还未对ref count操作前其值已经小于等于0，说明已有问题</span></span><br><span class="line">  <span class="keyword">if</span> (kref.ref_count[(uint64)pa / PGSIZE] &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">    panic(<span class="string">&quot;kref&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 每次free一个page时，先将这个page的引用计数-1</span></span><br><span class="line">  kref_dec(pa);</span><br><span class="line">  <span class="comment">// ref count - 1，如果结果还大于0，说明这个物理页还被其他进程引用，暂时不需要释放</span></span><br><span class="line">  <span class="keyword">if</span> (kref.ref_count[(uint64)pa / PGSIZE] &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Fill with junk to catch dangling refs.</span></span><br><span class="line">  <span class="built_in">memset</span>(pa, <span class="number">1</span>, PGSIZE);</span><br><span class="line"></span><br><span class="line">  r = (<span class="keyword">struct</span> run*)pa;</span><br><span class="line"></span><br><span class="line">  acquire(&amp;kmem.lock);</span><br><span class="line">  r-&gt;next = kmem.freelist;</span><br><span class="line">  kmem.freelist = r;</span><br><span class="line">  release(&amp;kmem.lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每分配一个物理页时，该物理页的引用计数初始为1：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> *</span><br><span class="line"><span class="title function_">kalloc</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">r</span>;</span></span><br><span class="line"></span><br><span class="line">  acquire(&amp;kmem.lock);</span><br><span class="line">  r = kmem.freelist;</span><br><span class="line">  <span class="keyword">if</span>(r)</span><br><span class="line">    kmem.freelist = r-&gt;next;</span><br><span class="line">  release(&amp;kmem.lock);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (r) &#123;</span><br><span class="line">    <span class="built_in">memset</span>((<span class="type">char</span>*)r, <span class="number">5</span>, PGSIZE); <span class="comment">// fill with junk</span></span><br><span class="line">    <span class="comment">// 这里赋值为1一定要在设置垃圾数值之后，否则会造成污染</span></span><br><span class="line">    acquire(&amp;kref.lock);</span><br><span class="line">    kref.ref_count[(uint64)r / PGSIZE] = <span class="number">1</span>;</span><br><span class="line">    release(&amp;kref.lock);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> (<span class="type">void</span>*)r;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>将引用计数的操作封装一下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// kalloc.c</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 为pa所在的page的引用+1</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">kref_inc</span><span class="params">(<span class="type">void</span>* pa)</span> &#123;</span><br><span class="line">  acquire(&amp;kref.lock);</span><br><span class="line">  ++kref.ref_count[(uint64)pa / PGSIZE];</span><br><span class="line">  release(&amp;kref.lock);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为pa所在的page的引用-1</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">kref_dec</span><span class="params">(<span class="type">void</span>* pa)</span> &#123;</span><br><span class="line">  acquire(&amp;kref.lock);</span><br><span class="line">  --kref.ref_count[(uint64)pa / PGSIZE];</span><br><span class="line">  release(&amp;kref.lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="page-fault时分配新内存">page fault时分配新内存<a class="anchor" href="#page-fault时分配新内存"></a></h3>
<p>当需要分配新内存时，要检测虚拟页的cow标志位是否有效。在分配了内存后，拷贝原来的物理页中的数据到新的物理页，并将虚拟页的标志位中的cow去除、添加写标志，最后取消虚拟页与原来物理页的映射关系，同时在解除时对引用计数-1，然后将虚拟页映射至新的物理页：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 当出现page fault时，进行cow操作</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">cow_alloc</span><span class="params">(<span class="type">pagetable_t</span> pagetable, uint64 va)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (va &gt;= MAXVA) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> ((va % PGSIZE) != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">pte_t</span> *pte = walk(pagetable, va, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (pte == <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">  uint64 pa = PTE2PA(*pte);</span><br><span class="line">  <span class="keyword">if</span> (pa == <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 当page的cow标志位有效时才会重新分配内存</span></span><br><span class="line">  <span class="keyword">if</span> ((*pte &amp; PTE_COW) &amp;&amp; (*pte &amp; PTE_V)) &#123;</span><br><span class="line">    <span class="type">char</span>* mem = kalloc();</span><br><span class="line">    <span class="keyword">if</span> (mem == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    uint64 flags = PTE_FLAGS(*pte);</span><br><span class="line">    flags = (flags &amp; ~PTE_COW) | PTE_W; <span class="comment">// 去除COW标记，加上写权限标记</span></span><br><span class="line">    memmove(mem, (<span class="type">char</span> *)pa, PGSIZE);</span><br><span class="line">    uvmunmap(pagetable, PGROUNDDOWN(va), <span class="number">1</span>, <span class="number">1</span>); <span class="comment">// 解除之前的映射，并设置do_free为1，这样在kfree中可来将引用计数-1</span></span><br><span class="line">    <span class="keyword">if</span> (mappages(pagetable, va, PGSIZE, (uint64)mem, flags) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      panic(<span class="string">&quot;cow_alloc&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>实验指导书提醒我们在<code>copyout()</code>中也要执行cow操作，但这里为何要这么做呢？</p>
<p>这是因为需要内核将数据通过copyout拷贝到用户态时，<strong>如果需要拷贝的目标位置是用户进程与其父进程共享的，那么这时应该会有page fault产生，但是copyout中是通过walk遍历页表来获取地址的，不会触发page fault，因此需要我们手动执行cow</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">copyout</span><span class="params">(<span class="type">pagetable_t</span> pagetable, uint64 dstva, <span class="type">char</span> *src, uint64 len)</span></span><br><span class="line">&#123;</span><br><span class="line">  uint64 n, va0, pa0;</span><br><span class="line">  <span class="type">pte_t</span> *pte;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span>(len &gt; <span class="number">0</span>)&#123;</span><br><span class="line">    va0 = PGROUNDDOWN(dstva);</span><br><span class="line">    <span class="keyword">if</span>(va0 &gt;= MAXVA)</span><br><span class="line">      <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (cow_alloc(pagetable, va0) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="usertrap中触发page-fault">usertrap中触发page fault<a class="anchor" href="#usertrap中触发page-fault"></a></h3>
<p>在之前fork操作时我们去除了物理页的write操作，那么在之后需要对该物理页进行写操作时，risc-v就会触发page fault了。</p>
<p>查看risc-v的手册可以发现：</p>
<p><img src="../../images/Pasted%20image%2020240707222619.png" alt=""></p>
<p>我们需要使用excepton code 13和15（读写），当发生page fault时，这个code会保存在<code>scause</code>寄存器中，那么我们只需要在usertrap中对scause进行相应的处理即可：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">usertrap</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (r_scause() == <span class="number">13</span> || r_scause() == <span class="number">15</span>) &#123;</span><br><span class="line">    <span class="comment">// 这里的stval寄存器，我的理解是保存了触发page fault时的虚拟地址</span></span><br><span class="line">    uint64 fault_va = r_stval();</span><br><span class="line">    <span class="comment">// 判断地址是否不合法</span></span><br><span class="line">    <span class="keyword">if</span> (fault_va &gt;= MAXVA ||</span><br><span class="line">        (fault_va &lt; p-&gt;trapframe-&gt;sp &amp;&amp;</span><br><span class="line">         fault_va &gt;= (p-&gt;trapframe-&gt;sp - PGSIZE)) ||</span><br><span class="line">        fault_va &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">      p-&gt;killed = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 尝试进行cow操作</span></span><br><span class="line">    <span class="keyword">if</span> (cow_alloc(p-&gt;pagetable, PGROUNDDOWN(fault_va)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      p-&gt;killed = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;usertrap(): unexpected scause %p pid=%d\n&quot;</span>, r_scause(), p-&gt;pid);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;            sepc=%p stval=%p\n&quot;</span>, r_sepc(), r_stval());</span><br><span class="line">    setkilled(p);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>值得注意的是，在进行cow操作之前，一定要对地址的范围进行合法性判断，因为<code>usertests</code>中会有非常多的测试函数，其中有一部分会检测地址合法性，不合法的地址应该直接让进程死亡。</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>MIT6-S081</tag>
        <tag>OS</tag>
        <tag>xv6</tag>
      </tags>
  </entry>
</search>
